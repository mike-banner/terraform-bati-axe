---
phase: 04.5-conversion-qualification
plan: 08
type: execute
wave: 4
depends_on: ["04.5-05"]
files_modified:
  - server/utils/maskLead.ts
  - app/pages/espace/leads/index.vue
  - app/pages/espace/leads/[id].vue
  - server/api/v1/leads/[id]/status.patch.ts
autonomous: true
requirements: [CNV-08]
must_haves:
  truths:
    - "Unlocked leads display actionable buttons (call, email) instead of standard link."
    - "Unlocked leads display a 2-line truncated preview of the project description."
    - "A pro can track the status of an unlocked lead using a dropdown selector (new, contacted, won, lost)."
    - "The CRM status is synced to the database via PATCH /api/v1/leads/:id/status."
  artifacts:
    - path: "server/utils/maskLead.ts"
      provides: "Returns 'db_status' along with 'status: unlocked'"
    - path: "server/api/v1/leads/[id]/status.patch.ts"
      provides: "PATCH endpoint validating and updating the CRM status of a lead owned by the pro."
    - path: "app/pages/espace/leads/index.vue"
      provides: "Leads listing with direct actions and mini-CRM features for unlocked leads."
    - path: "app/pages/espace/leads/[id].vue"
      provides: "Lead details page with mini-CRM status tracker and copy-to-clipboard tools."
---

<objective>
Enhance the user experience for unlocked leads by introducing a minimalist CRM. Remove the friction of "Voir le contact" by providing direct call/email buttons on the lead card. Provide context through a description snippet. Allow pros to organize their workflow by tracking the status of their leads (new, contacted, won, lost).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@server/utils/maskLead.ts
@app/pages/espace/leads/index.vue
@app/pages/espace/leads/[id].vue
</context>

<tasks>
<task type="auto" tdd="false">
  <name>Task 1: Expose CRM status and Create Status API</name>
  <files>server/utils/maskLead.ts, server/api/v1/leads/[id]/status.patch.ts</files>
  <behavior>
    Test 1: maskLead returns `db_status: lead.status` when isUnlocked is true.
    Test 2: PATCH /api/v1/leads/:id/status updates lead status in DB. Requires pro ownership.
  </behavior>
  <action>
    - Update `maskLead` to add `db_status: lead.status` in the unlocked return object.
    - Create the API route using `serverSupabaseClient(event)` and `auth.getUser()`. Validate body against Zod enum. Update DB.
  </action>
</task>

<task type="auto" tdd="false">
  <name>Task 2: UI Implementation (Index and Detail Pages)</name>
  <files>app/pages/espace/leads/index.vue, app/pages/espace/leads/[id].vue</files>
  <action>
    - **index.vue**: Add a `<select>` for CRM tracking. Add 2 lines of `description` preview (`line-clamp-2`). Replace "Voir le contact" link with Appeler/Email buttons.
    - **[id].vue**: Add the same `<select>` for CRM tracking. Add a copy-to-clipboard icon next to the phone number.
    - Wire up the `<select>` `@change` events to trigger `$fetch('/api/v1/leads/:id/status')` to update the DB.
  </action>
</task>
</tasks>
