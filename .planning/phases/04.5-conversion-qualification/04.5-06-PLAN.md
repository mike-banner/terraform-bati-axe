---
phase: 04.5-conversion-qualification
plan: 06
type: execute
wave: 4
depends_on: ["04.5-03", "04.5-04"]
files_modified:
  - server/utils/categoryLabels.ts
  - server/api/v1/market-local.get.ts
  - app/pages/espace/leads/index.vue
autonomous: true
requirements: [CNV-01, CNV-03, CNV-04, CNV-07]
must_haves:
  truths:
    - "Basic pro with free_leads_used<3 sees the free-leads-remaining banner (neutral, non-amber) per UI-SPEC"
    - "Basic pro with free_leads_used>=3 AND a locked-unfree lead present sees the amber paywall banner per UI-SPEC"
    - "Every lead card shows 4 qualification icons + score X/4 per D-12 / UI-SPEC"
    - "Marché local widget renders when API returns data; silently absent when not"
    - "Loading a locked-not-free lead triggers POST /api/v1/paywall-events (paywall_view) for analytics"
    - "Clicking 'Passer Premium' triggers POST /api/v1/paywall-events (checkout_started)"
  artifacts:
    - path: "server/api/v1/market-local.get.ts"
      provides: "Pro-zone projects count + dominant categories aggregation"
    - path: "app/pages/espace/leads/index.vue"
      provides: "Extended dashboard with banners, qualif badges, Marché local widget, paywall instrumentation"
  key_links:
    - from: "market-local API"
      to: "professionals.zone_id"
      via: "JOIN projects WHERE zone_id = pro.zone_id AND created_at > now() - interval '30 days'"
      pattern: "market-local"
    - from: "leads page"
      to: "paywall-events POST"
      via: "$fetch on mount + on Passer Premium click"
      pattern: "paywall-events"
---

<objective>
Extend `/espace/leads` with all Phase 4.5 UI additions per UI-SPEC §"Page 1":
1. Replace the current binary Premium banner with the two-state banner (free-leads-remaining vs paywall-triggered).
2. Show 4 qualification ✓/✗ icons + "X/4" score on every lead card (locked + unlocked variants).
3. Render the Marché local widget above the leads grid when API data is available.
4. Instrument paywall analytics: log `paywall_view` once per locked-not-free lead card visible on mount; log `checkout_started` when the Passer Premium button is clicked.

Implement the `GET /api/v1/market-local` endpoint that returns project count and top categories for the calling pro's zone over the last 30 days.

Purpose: Surface the new verrou + qualification + market signals to the pro.
Output: 1 new endpoint + 1 page rewrite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04.5-conversion-qualification/04.5-CONTEXT.md
@.planning/phases/04.5-conversion-qualification/04.5-UI-SPEC.md
@.planning/phases/04-le-verrou-stripe-billing/04-UI-SPEC.md
@app/pages/espace/leads/index.vue
@server/api/v1/leads/index.get.ts
@server/api/v1/paywall-events.post.ts

<interfaces>
After Plan 03, GET /api/v1/leads now returns each lead with the masked-or-unmasked fields PLUS the new project-level qualify_* booleans (note: only if the leads/index.get.ts select includes them). Plan 06 must verify the select list includes the four `projects.qualify_*` columns + `projects.qualify_score` and PATCH them in if missing.

UI-SPEC for /espace/leads Phase 4.5 declares:
- Free leads counter banner (neutral border-border, no amber) — shown when isBasic AND free_leads_used < 3.
- Paywall banner (amber border-amber-300 bg-amber-50) — shown when isBasic AND hasLockedUnfreeLead AND free_leads_used >= 3.
- 4 qualification icons strip per card with text-foreground (pass) vs text-muted-foreground/40 (fail).
- "Marché local" widget rendered ONLY when marketData is non-null.

Existing GET /api/v1/leads returns `{ leads: masked[] }` and the page calls it via useAsyncData('pro-leads'). The page must additionally fetch `/api/v1/market-local` and the pro's own profile (to know subscription_status and free_leads_used). Reuse `/api/v1/pro/profile/me` (Plan 05) for the latter.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GET /api/v1/market-local + expose qualify_* in leads list select</name>
  <files>server/api/v1/market-local.get.ts, server/api/v1/leads/index.get.ts</files>
  <read_first>
    - server/api/v1/leads/index.get.ts (current .select column list — must add qualify_score, qualify_budget, qualify_phone, qualify_description, qualify_returning to the projects sub-select)
    - .planning/phases/04.5-conversion-qualification/04.5-CONTEXT.md (Claude's discretion for Marché local SQL)
    - supabase/migrations/20260603000000_schema_initial.sql (zone_id on professionals; projects has zone_id + category)
  </read_first>
  <action>
    File 1 — server/api/v1/leads/index.get.ts:
    Modify the existing `.select(...)` call so the projects sub-select includes the 5 new qualify_* columns. Current pattern:
      `'id, status, unlocked_at, created_at, projects(id, category, budget_range, timeline_range, description, customer_name, customer_email, customer_phone, postal_code)'`
    Add to the projects(...) tuple: `qualify_score, qualify_budget, qualify_phone, qualify_description, qualify_returning`.

    Then update maskLead (in Plan 03 file already) and the page so the qualify_* booleans are returned at the lead level (not nested under projects) — to align with the UI-SPEC template that references `lead.qualify_budget` directly. To achieve this, after the maskLead call in this endpoint, augment the masked object with the qualify_* fields:
      `const enriched = masked.map((m, i) => ({ ...m, qualify_score: leads[i].projects.qualify_score, qualify_budget: leads[i].projects.qualify_budget, qualify_phone: leads[i].projects.qualify_phone, qualify_description: leads[i].projects.qualify_description, qualify_returning: leads[i].projects.qualify_returning }))`. Return `{ leads: enriched }`. Apply the same flat-projection in server/api/v1/leads/[id].get.ts for consistency (single-row case).

    File 2 — server/api/v1/market-local.get.ts:
    New endpoint. Auth via serverSupabaseUser; 401 if missing. Service-role client.

    Step A — fetch the calling pro's zone_id from professionals (select id, zone_id). If zone_id is null, return `{ data: null }` (widget will not render).

    Step B — count projects in the zone over the last 30 days:
      `const { count: projectCount } = await supabase.from('projects').select('id', { count: 'exact', head: true }).eq('zone_id', zone_id).gte('created_at', new Date(Date.now() - 30*24*3600*1000).toISOString())`.

    Step C — fetch top categories:
      Fetch the category column for projects in the zone over the last 30 days (cap at 500 rows): `supabase.from('projects').select('category').eq('zone_id', zone_id).gte('created_at', cutoff).limit(500)`. In JS, build a frequency map and return the top 3 category strings (sorted desc by count). Map category codes to display labels using CATEGORY_LABELS imported from `server/utils/categoryLabels.ts` (create this file — extract the dict from espace/leads/index.vue into it so both the Nitro endpoint and the Vue page can import from one source). The Vue page import in Task 2 should be updated to import from `~/server/utils/categoryLabels` instead of defining it inline.

    Return shape: `{ data: { projectCount: number, topCategories: string[] } }` where topCategories are the human-readable French labels.

    Edge case: if projectCount is 0, return `{ data: { projectCount: 0, topCategories: [] } }` — the page logic will still render the widget per UI-SPEC; the empty-state copy "Aucun projet dans votre zone ce mois." applies only when projectCount is 0.
  </action>
  <verify>
    <automated>test -f server/api/v1/market-local.get.ts && grep -v '^#' server/api/v1/leads/index.get.ts | grep -cE "qualify_score|qualify_budget"</automated>
  </verify>
  <acceptance_criteria>
    - File server/api/v1/market-local.get.ts exists.
    - Source assertion: market-local.get.ts contains the literal string `zone_id` and references `.eq('zone_id'` for the projects query.
    - Source assertion: market-local.get.ts caps the top categories at 3 (use .slice(0,3) or equivalent).
    - Source assertion: server/api/v1/leads/index.get.ts .select projects(...) tuple now includes all 5 qualify_* columns.
    - Source assertion: leads index endpoint flattens qualify_* onto the lead object so `lead.qualify_score` is reachable client-side.
    - Manual curl: GET /api/v1/market-local as authenticated pro with zone_id set returns 200 + projectCount integer + topCategories array.
    - Manual curl: GET /api/v1/market-local as pro with null zone_id returns `{ data: null }`.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>Both endpoints return shape required by the leads page; qualify fields exposed at top level of each lead.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite app/pages/espace/leads/index.vue per UI-SPEC §"Page 1"</name>
  <files>app/pages/espace/leads/index.vue</files>
  <read_first>
    - app/pages/espace/leads/index.vue (current page — keep CATEGORY_LABELS dict, useAsyncData('pro-leads') pattern, success banner logic; replace banners + add new sections)
    - .planning/phases/04.5-conversion-qualification/04.5-UI-SPEC.md §"Page 1 — /espace/leads (Extensions to existing page)" (exact HTML templates for free-leads banner, paywall banner, qualification badge strip, Marché local widget — copy verbatim including class names and SVG paths)
    - .planning/phases/04-le-verrou-stripe-billing/04-UI-SPEC.md (typography rules: text-3xl font-semibold tracking-tight — NOT font-black; the existing leads page uses font-black on the H1, MUST be updated to font-semibold per Phase 4.5 typography section "2 operative weights 400 + 600")
    - server/api/v1/market-local.get.ts (from Task 1)
    - server/api/v1/paywall-events.post.ts (from Plan 04)
  </read_first>
  <action>
    Open app/pages/espace/leads/index.vue and apply the following changes; preserve existing CATEGORY_LABELS, useAsyncData('pro-leads'), and the success banner from Phase 4.

    Step 1 — Add new reactive state in `<script setup>`:
      - Fetch profile via useAsyncData('pro-profile', () => $fetch('/api/v1/pro/profile/me').then(r => r.profile)).
      - Fetch market data via useAsyncData('market-local', () => $fetch('/api/v1/market-local').then(r => r.data)).
      - Computed: `const isPremium = computed(() => profile.value?.subscription_status === 'active')`.
      - Computed: `const freeLeadsUsed = computed(() => profile.value?.free_leads_used ?? 0)`.
      - Computed: `const freeRemaining = computed(() => Math.max(0, 3 - freeLeadsUsed.value))`.
      - Computed: `const hasLockedUnfreeLead = computed(() => leads.value?.some(l => l.status === 'locked') ?? false)`.
      - Computed: `const showFreeLeadsBanner = computed(() => !isPremium.value && freeLeadsUsed.value < 3)`.
      - Computed: `const showPaywallBanner = computed(() => !isPremium.value && freeLeadsUsed.value >= 3 && hasLockedUnfreeLead.value)`.

    Step 2 — Replace the existing `<div v-if="hasLockedLeads" ...>` amber Premium banner block (lines ~55-68) with TWO consecutive blocks, both rendered above the Skeleton/Error/Empty/Grid section:
      - `<div v-if="showFreeLeadsBanner" ...>` using EXACTLY the HTML in UI-SPEC §"Free leads counter banner" (neutral border-border, no amber). Replace the `{{ 3 - freeLeadsUsed }}` interpolation with `{{ freeRemaining }}`. Fix the singular/plural template strings so `1 lead gratuit restant` vs `2 leads gratuits restants`.
      - `<div v-if="showPaywallBanner" ...>` using EXACTLY the HTML in UI-SPEC §"Paywall banner" (amber border-amber-300 bg-amber-50). The "Passer Premium" link must call a `logCheckoutStarted()` handler before navigation — replace the NuxtLink with a button that performs `await logCheckoutStarted(); await navigateTo('/espace/premium')` OR add `@click="logCheckoutStarted"` to a NuxtLink (NuxtLink supports @click).

    Step 3 — Update the page H1 from `text-3xl font-black tracking-tight` to `text-3xl font-semibold tracking-tight` per Phase 4.5 UI-SPEC typography (font-black is NOT in the allowed 2-weight system).

    Step 4 — Insert Marché local widget BETWEEN the page header and the banner blocks (so it appears after H1, before the free-leads/paywall banners), wrapped in `v-if="marketData"`. Use EXACTLY the HTML in UI-SPEC §"Marché local widget". Bind `{{ marketData.projectCount }}` and `v-for="cat in marketData.topCategories"`.

    Step 5 — Inside the lead card body, add the qualification badge strip per UI-SPEC §"Qualification badge strip". For each card variant (Locked, Unlocked, Claimed):
      - For Locked + Unlocked: insert the 4-icon strip ABOVE the existing CTA footer (after the contact info rows). Bind each badge state to lead.qualify_budget, lead.qualify_phone, lead.qualify_description, lead.qualify_returning. Score text: `{{ lead.qualify_score }}/4`.
      - For Claimed variant: skip the qualification strip (no actionable info for a claimed lead).
      - Pass class for icons: `text-foreground` if true, `text-muted-foreground/40` if false (per D-12 + UI-SPEC).
      - `title` attribute per UI-SPEC tooltip-labels table.

    Step 6 — Instrument analytics:
      Function `logPaywallView(leadId, qualifyScore)` calls `$fetch('/api/v1/paywall-events', { method: 'POST', body: { event_type: 'paywall_view', lead_id: leadId, qualify_score: qualifyScore } })`. Wrap in try/catch (silent — analytics failure must not break UI).
      Function `logCheckoutStarted()` calls $fetch with event_type='checkout_started'.

      In onMounted, after leads load: for every locked lead (status === 'locked'), call logPaywallView(lead.id, lead.qualify_score). Use a Set to dedupe per session (do not log twice for the same lead in one page render).

    Step 7 — Update the success banner H1 typography in the existing block from font-semibold (already correct).

    Do NOT change CATEGORY_LABELS keys, do NOT change the existing claimed-variant rendering, do NOT remove the success banner.
  </action>
  <verify>
    <automated>grep -v '^#' app/pages/espace/leads/index.vue | grep -cE "showFreeLeadsBanner|showPaywallBanner|marketData|paywall_view|qualify_score"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: page contains `showFreeLeadsBanner`, `showPaywallBanner`, `marketData` reactive references.
    - Source assertion: page H1 uses `font-semibold` (NOT font-black).
    - Source assertion: page contains the literal string `paywall_view` and `checkout_started`.
    - Source assertion: page renders 4 qualification icons per lead card in Locked AND Unlocked variants (grep finds at least 8 `:class="lead.qualify_` patterns).
    - Manual visual: with mocked profile { free_leads_used: 1 }, neutral free-leads banner renders.
    - Manual visual: with mocked profile { free_leads_used: 3 } AND leads array with at least one status: 'locked', amber paywall banner renders.
    - Manual visual: with marketData = { projectCount: 5, topCategories: ['Toiture','Maçonnerie'] }, Marché local widget renders with the section header "Marché local".
    - Network tab on page load shows POST /api/v1/paywall-events with event_type=paywall_view for each locked lead, fired once.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>Leads page surfaces all Phase 4.5 verrou + qualification + market + analytics signals per UI-SPEC.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/v1/market-local | Authenticated; service-role read scoped to pro's zone_id. |
| client → /api/v1/paywall-events | Authenticated; enum-restricted (T-04.5-11 from Plan 04). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.5-20 | Information Disclosure | Marché local leaks customer addresses via topCategories | accept | Categories are intentionally aggregate; no PII is returned. |
| T-04.5-21 | Tampering | Pro spams paywall_view events (e.g., refresh loop) | mitigate | Dedupe Set per render. Server-side rate-limit deferred (T-04.5-13 from Plan 04). |
| T-04.5-22 | Information Disclosure | Qualification badges expose customer_email existence indirectly via qualify_returning | accept | Same as T-04.5-09; admin already aware. |
| T-04.5-SC | Tampering | No new npm packages | accept | No installs. |
</threat_model>

<verification>
- vitest passes (page rendering test or Vue Test Utils if present).
- `npm run typecheck` exits 0.
- Manual visual smoke: dev server + login as Basic pro at various free_leads_used values (0, 2, 3).
</verification>

<success_criteria>
- All UI-SPEC §"Page 1" extensions implemented faithfully.
- Analytics fire on the right triggers.
- Phase 4 success banner still functions.
</success_criteria>

<output>
Create `.planning/phases/04.5-conversion-qualification/04.5-06-SUMMARY.md`.
</output>
</content>
</invoke>