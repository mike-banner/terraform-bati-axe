---
phase: 04.5-conversion-qualification
plan: 04
type: execute
wave: 3
depends_on: ["04.5-02"]
files_modified:
  - server/api/v1/stripe/checkout.post.ts
  - server/utils/handleStripeEvent.ts
  - server/api/v1/paywall-events.post.ts
  - server/api/v1/admin/paywall-analytics.get.ts
autonomous: true
requirements: [CNV-02, CNV-07]
must_haves:
  truths:
    - "Stripe Checkout session is created with trial_period_days: 14 for first-time subscribers"
    - "customer.subscription.trial_will_end webhook event is acknowledged (no DB write needed — Stripe sends email per D-08)"
    - "POST /api/v1/paywall-events records pro_id, lead_id (nullable), qualify_score, event_type"
    - "GET /api/v1/admin/paywall-analytics returns aggregated counts grouped by event_type and qualify_score bucket"
  artifacts:
    - path: "server/api/v1/stripe/checkout.post.ts"
      provides: "trial_period_days=14 in session params"
    - path: "server/utils/handleStripeEvent.ts"
      provides: "trial_will_end handler (no-op DB; logs)"
    - path: "server/api/v1/paywall-events.post.ts"
      provides: "Authenticated pro logs paywall view + checkout intent"
    - path: "server/api/v1/admin/paywall-analytics.get.ts"
      provides: "Admin-only aggregate query over paywall_events"
  key_links:
    - from: "checkout endpoint"
      to: "Stripe subscription"
      via: "subscription_data.trial_period_days"
      pattern: "trial_period_days.*14"
    - from: "/espace/leads paywall trigger"
      to: "paywall_events table"
      via: "fetch POST /api/v1/paywall-events"
      pattern: "paywall_events"
---

<objective>
Enable Stripe-managed 14-day free trial on Premium subscription checkout (CNV-02) and instrument paywall-hit analytics for qualified pros (CNV-07).

Per D-06: monthly-only (no annual toggle, single STRIPE_PRICE_ID).
Per D-07: CB required at trial start (Stripe default behavior — no extra config needed).
Per D-08: J-2 warning email is sent by Stripe Dashboard configuration — application code receives `customer.subscription.trial_will_end` but does not act on it beyond logging.
Per D-09: trial-end payment failure already handled by existing `invoice.payment_failed` case (sets subscription_status='unpaid'); no new code needed for failure path.

Analytics (CNV-07): pro UI logs a `paywall_view` event when a Basic pro lands on a card flagged as locked-and-not-free-granted; logs `checkout_started` when the Stripe checkout button is clicked; webhook logs `checkout_completed` on `checkout.session.completed`. Admin can query aggregates filtered by qualify_score ≥ 3 (the "qualified pro hits paywall" funnel question).

Purpose: Trial = friction-removal for conversion. Analytics = measure conversion.
Output: Three API endpoints + handler extension.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04.5-conversion-qualification/04.5-CONTEXT.md
@server/api/v1/stripe/checkout.post.ts
@server/utils/handleStripeEvent.ts
@server/api/v1/admin/queue.get.ts
@server/api/v1/admin/projects.get.ts

<interfaces>
Existing Stripe session params (checkout.post.ts):
```typescript
const sessionParams: Stripe.Checkout.SessionCreateParams = {
  mode: 'subscription',
  line_items: [{ price: config.stripePriceId, quantity: 1 }],
  success_url, cancel_url, metadata: { pro_id: user.id },
}
```

To add a trial, Stripe API takes `subscription_data: { trial_period_days: 14 }` on the session create — NOT `trial_period_days` at the top level (that is for the older subscriptions.create endpoint). Source: Stripe Checkout API reference (verify via Context7 mcp `resolve-library-id stripe` + `query-docs subscription_data trial_period_days`).

Existing handleStripeEvent switch handles three cases: checkout.session.completed, customer.subscription.deleted, invoice.payment_failed. Default branch is a no-op break.

Admin role check pattern: existing endpoints in server/api/v1/admin/ check user role via service-role lookup or app_metadata. Reuse the existing pattern verbatim (see queue.get.ts).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 14-day trial to Stripe Checkout + extend webhook handler</name>
  <files>server/api/v1/stripe/checkout.post.ts, server/utils/handleStripeEvent.ts</files>
  <read_first>
    - server/api/v1/stripe/checkout.post.ts (existing sessionParams construction)
    - server/utils/handleStripeEvent.ts (existing switch — 3 cases + default)
    - .planning/phases/04.5-conversion-qualification/04.5-CONTEXT.md (D-06, D-07, D-08, D-09)
  </read_first>
  <action>
    File 1 — server/api/v1/stripe/checkout.post.ts:
    Before `stripe.checkout.sessions.create(sessionParams)`, augment sessionParams with:
      `sessionParams.subscription_data = { trial_period_days: 14 }`
    Place this assignment after the existing customer/customer_email branch so it applies to BOTH new and returning customers.

    Add a guard: if the calling pro's existing `subscription_status === 'active'` or there's a non-null `stripe_customer_id` AND they had a prior trial (this cannot be checked from local DB; defer to Stripe), the session can still be created — Stripe will refuse to grant a second trial automatically based on the customer's payment history. No client-side guard needed.

    Update the inline comment at the top of the handler: "Phase 4.5: trial_period_days=14 per CNV-02 / D-06 / D-07".

    File 2 — server/utils/handleStripeEvent.ts:
    Add one new case to the switch BEFORE the default branch:
      `case 'customer.subscription.trial_will_end': { /* J-2 warning email sent by Stripe Dashboard per D-08 — application no-op. Log event_type for traceability. */ console.log('[stripe] trial_will_end received for subscription', event.data.object.id); break }`

    Also extend `case 'checkout.session.completed'` to additionally insert a `paywall_events` row with `event_type='checkout_completed'`:
      Resolve proId from session.metadata.pro_id (already extracted).
      `await supabase.from('paywall_events').insert({ pro_id: proId, event_type: 'checkout_completed', metadata: { session_id: session.id, mode: session.mode } })` — wrap in try/catch + console.error so a failed analytics write does not break subscription activation.

    Do NOT add new failure-path code for invoice.payment_failed — existing handler already sets subscription_status='unpaid' (D-09).

    Add inline comments referencing D-06, D-07, D-08, D-09.
  </action>
  <verify>
    <automated>grep -v '^#' server/api/v1/stripe/checkout.post.ts | grep -c "trial_period_days" && grep -v '^#' server/utils/handleStripeEvent.ts | grep -c "trial_will_end\|paywall_events"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: checkout.post.ts contains `subscription_data` and `trial_period_days: 14`.
    - Source assertion: handleStripeEvent.ts switch contains a case branch matching string `customer.subscription.trial_will_end`.
    - Source assertion: handleStripeEvent.ts checkout.session.completed branch inserts into `paywall_events` with event_type `checkout_completed`.
    - `npm run typecheck` exits 0.
    - Manual Stripe CLI smoke (if available): `stripe trigger customer.subscription.trial_will_end` against local webhook URL receives the event and returns 200.
  </acceptance_criteria>
  <done>Stripe Checkout sessions start with 14-day trial; trial_will_end is acknowledged; checkout_completed events tracked.</done>
</task>

<task type="auto">
  <name>Task 2: Create paywall-events POST endpoint + admin analytics GET endpoint</name>
  <files>server/api/v1/paywall-events.post.ts, server/api/v1/admin/paywall-analytics.get.ts</files>
  <read_first>
    - server/api/v1/admin/queue.get.ts (admin role-check pattern + service-role usage)
    - server/api/v1/leads/index.get.ts (auth + service-role boilerplate pattern)
    - supabase/migrations/20260609000000_phase45_schema.sql (paywall_events columns)
    - .planning/phases/04.5-conversion-qualification/04.5-CONTEXT.md (CNV-07 + Claude's Discretion: metrics)
  </read_first>
  <action>
    File 1 — server/api/v1/paywall-events.post.ts:
    Create a new Nitro POST endpoint following the existing leads/index.get.ts skeleton (serverSupabaseUser auth + service-role client).

    Validate body with Zod:
      `const eventSchema = z.object({ event_type: z.enum(['paywall_view', 'checkout_started']), lead_id: z.string().uuid().optional(), qualify_score: z.number().int().min(0).max(4).optional() })`.
    Note: 'checkout_completed' is webhook-only — reject if a client tries to submit it.

    Insert one row into `paywall_events` with: pro_id=user.id, event_type, lead_id (nullable), qualify_score (nullable), metadata={ source: 'client', user_agent: getHeader(event,'user-agent') }, occurred_at default (clock_timestamp from DB).

    Return `{ logged: true }`.

    Rate-limit consideration: cap to ~1 event/sec per pro using a simple in-memory throttle or rely on existing global middleware (INFRA-04). If neither exists, accept duplicates — admin query can dedupe by 1-minute window.

    File 2 — server/api/v1/admin/paywall-analytics.get.ts:
    Create a new GET endpoint. Auth: serverSupabaseUser + check `(user as any)?.app_metadata?.role === 'admin'` — mirror the pattern used in app/pages/admin/index.vue `isAdmin` computed and server endpoint admin guards if present (read server/api/v1/admin/queue.get.ts for the canonical server-side admin check). If unauthorized, throw 403.

    Run three aggregation queries via service-role client:
      Query A — count of events by event_type for the last 30 days: `select event_type, count(*) from paywall_events where occurred_at > now() - interval '30 days' group by event_type`. Use Supabase JS `.rpc` OR a raw select with grouping via PostgREST — if PostgREST grouping is unavailable, fetch the rows and group in JS (acceptable for v1 volume).
      Query B — funnel for qualified pros: count paywall_view, checkout_started, checkout_completed events where qualify_score >= 3, last 30 days.
      Query C — recent 50 events with pro_id + qualify_score + event_type for the activity feed.

    Return `{ totals: { paywall_view, checkout_started, checkout_completed }, qualified_funnel: { paywall_view, checkout_started, checkout_completed, conversion_rate }, recent: Array<{ pro_id, qualify_score, event_type, occurred_at }> }`.

    conversion_rate = qualified_funnel.checkout_completed / qualified_funnel.paywall_view (round to 2 decimals; return 0 if denominator is 0).

    Inline comments cite CNV-07 + the discretion note from CONTEXT.md (metrics chosen).
  </action>
  <verify>
    <automated>test -f server/api/v1/paywall-events.post.ts && test -f server/api/v1/admin/paywall-analytics.get.ts && grep -c "paywall_view\|checkout_started\|checkout_completed" server/api/v1/admin/paywall-analytics.get.ts | awk '{ if ($1 >= 3) print "PASS"; else print "FAIL" }'</automated>
  </verify>
  <acceptance_criteria>
    - File server/api/v1/paywall-events.post.ts exists.
    - File server/api/v1/admin/paywall-analytics.get.ts exists.
    - Source assertion: paywall-events.post.ts uses Zod enum restricting to 'paywall_view' and 'checkout_started' only (NOT 'checkout_completed').
    - Source assertion: paywall-analytics.get.ts contains a 403 throw when admin check fails.
    - Source assertion: paywall-analytics.get.ts returns at minimum `totals`, `qualified_funnel`, `recent` keys.
    - Manual curl: POST /api/v1/paywall-events with `{"event_type":"checkout_completed"}` returns 400 (Zod rejects).
    - Manual curl: POST /api/v1/paywall-events with `{"event_type":"paywall_view","qualify_score":4}` as authenticated pro returns 200; row appears in paywall_events table.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>Pro can log paywall views; admin can query aggregates filtered by qualify_score.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → POST /paywall-events | Authenticated pro; service-role insert; Zod-restricted enum prevents client-forged checkout_completed inflation. |
| client → GET /admin/paywall-analytics | Admin role-checked; service-role read. |
| Stripe → webhook | Signature-verified (existing Phase 4 pattern via constructEventAsync). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.5-11 | Tampering | Client forges checkout_completed to inflate conversion metrics | mitigate | Zod enum excludes 'checkout_completed' on POST /paywall-events; only webhook writes that event_type. |
| T-04.5-12 | Information Disclosure | Non-admin reads aggregate analytics | mitigate | 403 on admin role check failure. |
| T-04.5-13 | Denial of Service | Pro spams paywall_view events to flood DB | mitigate | Rate limit per pro via global middleware (INFRA-04); if absent, accept (admin query dedupes by window). Risk is low — volume is sub-pro. |
| T-04.5-14 | Tampering | Trial granted twice to same Stripe customer | accept | Stripe Checkout enforces single-trial-per-customer based on payment-method history; application does not need to gate. |
| T-04.5-SC | Tampering | No new npm packages — uses existing stripe SDK (Phase 4 audit) | accept | No package installs. |
</threat_model>

<verification>
- All four files compile (typecheck 0).
- Stripe CLI smoke (optional): `stripe trigger customer.subscription.trial_will_end` returns 200.
- Manual: webhook on `checkout.session.completed` writes a paywall_events row.
</verification>

<success_criteria>
- Premium checkout offers 14-day trial.
- Webhook acknowledges trial_will_end and tracks checkout_completed.
- Paywall analytics endpoints functional end-to-end.
</success_criteria>

<output>
Create `.planning/phases/04.5-conversion-qualification/04.5-04-SUMMARY.md`.
</output>
</content>
</invoke>