---
phase: 04.5-conversion-qualification
plan: "01"
subsystem: database
tags: [supabase, postgres, migration, rls, sql]

requires:
  - phase: 04-le-verrou-stripe-billing
    provides: professionals table, leads table, projects table with initial schema

provides:
  - free_leads_used INTEGER column on professionals (cumulative free lead counter)
  - free_lead_grants junction table (pro_id + lead_id with unique constraint, RLS service-role-only)
  - qualify_score INTEGER (0-4 CHECK) + 4 boolean criterion columns on projects
  - bio, zone, logo_url TEXT columns on professionals (editable profile fields)
  - paywall_events table with event_type CHECK constraint + RLS service-role-only

affects:
  - 04.5-02 (db push applies this migration)
  - 04.5-03 (maskLead extension uses free_leads_used + free_lead_grants)
  - 04.5-04 (projects POST endpoint computes qualify_score into new columns)
  - 04.5-05 (profile API reads/writes bio, zone, logo_url)
  - 04.5-06 (paywall analytics writes to paywall_events)

tech-stack:
  added: []
  patterns:
    - "Idempotent migrations: IF NOT EXISTS on all CREATE TABLE, CREATE INDEX, ADD COLUMN"
    - "Service-role-only tables: ENABLE ROW LEVEL SECURITY with zero policies"

key-files:
  created:
    - supabase/migrations/20260609000000_phase45_schema.sql
  modified: []

key-decisions:
  - "qualify_score stored in DB as computed-once at project submission (D-10) — no trigger, computed in Plan 04 endpoint"
  - "free_lead_grants as separate junction table rather than JSONB array for referential integrity and FK cascades (D-02, D-03)"
  - "paywall_events.lead_id nullable (ON DELETE SET NULL) so paywall view events without a specific lead still persist"
  - "bio/zone/logo_url columns have no SQL-level length constraint — Zod enforces at API layer (D-17)"

patterns-established:
  - "New analytics tables: ENABLE ROW LEVEL SECURITY with no CREATE POLICY (service_role only)"

requirements-completed: [CNV-01, CNV-03, CNV-05, CNV-07]

duration: 1min
completed: "2026-06-09"
---

# Phase 4.5 Plan 01: Phase 4.5 Schema Migration Summary

**Idempotent SQL migration adding free_leads_used counter, free_lead_grants junction table, qualify_score with 4 criterion booleans, editable profile fields (bio/zone/logo_url), and paywall_events analytics table — all with RLS enabled.**

## Performance

- **Duration:** 1 min
- **Started:** 2026-06-09T18:01:14Z
- **Completed:** 2026-06-09T18:02:26Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments

- Created idempotent migration covering all 4 schema sections required by Phase 4.5 downstream plans
- All new tables (free_lead_grants, paywall_events) secured with RLS and zero policies (service-role-only access), satisfying T-04.5-01 and T-04.5-03 threat mitigations
- qualify_score CHECK constraint (0-4) enforced at DB layer; criterion booleans stored as separate columns for efficient reads on lead cards

## Task Commits

1. **Task 1: Write Phase 4.5 schema migration** - `d51e671` (feat)

**Plan metadata:** (docs commit below)

## Files Created/Modified

- `supabase/migrations/20260609000000_phase45_schema.sql` - Full Phase 4.5 schema migration (4 sections, 70 lines)

## Decisions Made

- `qualify_score` gets a DB-level `CHECK (qualify_score BETWEEN 0 AND 4)` to enforce the 0–4 range even if API validation is bypassed via service_role
- `paywall_events.lead_id` is nullable with `ON DELETE SET NULL` (not CASCADE) so a `paywall_view` event recorded before a lead is resolved does not orphan the row
- No length constraints in SQL for bio/zone/logo_url — consistent with existing schema pattern (no SQL-level text lengths on other free-form fields); Zod max-length validation belongs at the API layer

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required. Plan 02 handles `supabase db push`.

## Threat Surface Scan

No new network endpoints or auth paths introduced. Two new tables (free_lead_grants, paywall_events) added with RLS enabled and zero policies — consistent with threat model T-04.5-01 and T-04.5-03 dispositions of `mitigate`. No additional threat flags.

## Self-Check: PASSED

- `[ -f supabase/migrations/20260609000000_phase45_schema.sql ]` → FOUND
- `git log --oneline | grep d51e671` → FOUND: feat(04.5-01): add Phase 4.5 schema migration
- All 10 acceptance criteria: PASS (verified by grep checks above)

## Next Phase Readiness

- Ready for Plan 02 (`supabase db push`) — migration file is idempotent and syntactically valid
- Plans 03–07 can proceed once migration is applied to DB

---
*Phase: 04.5-conversion-qualification*
*Completed: 2026-06-09*
