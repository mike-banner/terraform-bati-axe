---
phase: 04.5-conversion-qualification
plan: 02
type: execute
wave: 2
depends_on: ["04.5-01"]
files_modified: []
autonomous: false
requirements: [CNV-01, CNV-03, CNV-05, CNV-07]
user_setup:
  - service: supabase
    why: "Apply Phase 4.5 schema migration to local + linked remote DB"
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard → Account → Access Tokens (required for non-TTY db push)"
must_haves:
  truths:
    - "Local Supabase DB has columns free_leads_used, qualify_score, qualify_budget, qualify_phone, qualify_description, qualify_returning, bio, zone, logo_url"
    - "Tables free_lead_grants and paywall_events exist in local DB"
    - "supabase db push exits 0"
  artifacts:
    - path: "supabase/migrations/20260609000000_phase45_schema.sql"
      provides: "Migration applied to DB"
  key_links:
    - from: "migration file"
      to: "live DB"
      via: "supabase db push"
      pattern: "applied"
---

<objective>
[BLOCKING] Apply the Phase 4.5 schema migration to the database. Downstream API plans (03, 04, 05) cannot compile or run without the new columns existing — Nuxt + Supabase typegen reads the live schema, and any reference to free_leads_used / qualify_score before this push will fail at runtime or in TypeScript validation.

Purpose: Bridge schema definition (Plan 01) with implementation (Plans 03+).
Output: Migration applied; verification queries confirm columns + tables exist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04.5-conversion-qualification/04.5-01-SUMMARY.md
@supabase/migrations/20260609000000_phase45_schema.sql

<interfaces>
Command reference:
- Local apply (preferred): `supabase db push --local` OR `supabase migration up` (Postgres local container).
- Remote apply (linked project): `supabase db push` requires SUPABASE_ACCESS_TOKEN env var for non-TTY contexts.
- Verification query: `supabase db remote sql "SELECT column_name FROM information_schema.columns WHERE table_name='professionals' AND column_name IN ('free_leads_used','bio','zone','logo_url')"`.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [BLOCKING] Run supabase db push and verify columns</name>
  <read_first>
    - supabase/migrations/20260609000000_phase45_schema.sql
    - .planning/STATE.md (note: Supabase CLI + Docker is the local-dev pattern per INFRA-02)
  </read_first>
  <what-built>
    Migration file 20260609000000_phase45_schema.sql exists from Plan 01 but is NOT yet applied.
  </what-built>
  <how-to-verify>
    Step 1 — Confirm local Supabase is running:
      `supabase status` should show containers running. If not, run `supabase start`.

    Step 2 — Apply the migration to local DB:
      `supabase db push --local` (preferred). If that flag is unavailable on the installed CLI version, run `supabase migration up` instead. If the command requires interactive confirmation and you are in a non-TTY environment, set `SUPABASE_ACCESS_TOKEN` env var first.

    Step 3 — Verify columns exist via psql against local Supabase:
      `supabase db remote sql "SELECT column_name FROM information_schema.columns WHERE table_name='professionals' AND column_name IN ('free_leads_used','bio','zone','logo_url') ORDER BY column_name"` — expect 4 rows.

      `supabase db remote sql "SELECT column_name FROM information_schema.columns WHERE table_name='projects' AND column_name LIKE 'qualify_%' ORDER BY column_name"` — expect 5 rows (qualify_budget, qualify_description, qualify_phone, qualify_returning, qualify_score).

      `supabase db remote sql "SELECT table_name FROM information_schema.tables WHERE table_name IN ('free_lead_grants','paywall_events') ORDER BY table_name"` — expect 2 rows.

    Step 4 — Regenerate Supabase types if the project uses generated types (check for `types/supabase.ts` or similar). If `nuxt-supabase` is set to type-generate, run `supabase gen types typescript --local > types/supabase.ts` or the project's existing equivalent script.

    Expected outcome: all three verification queries return the expected row counts; downstream plans can safely reference the new columns and tables.

    If `supabase db push` fails with a syntax error: report the exact error message — Plan 01 must be revised.
    If push fails due to auth: confirm SUPABASE_ACCESS_TOKEN is set, retry.
  </how-to-verify>
  <acceptance_criteria>
    - `supabase db push` (or `supabase migration up`) exited 0.
    - Verification query against `information_schema.columns` returns exactly the expected column lists shown above.
    - Verification query against `information_schema.tables` returns rows for both `free_lead_grants` and `paywall_events`.
    - If types are auto-generated, regenerated types file contains a `free_leads_used: number` and `qualify_score: number` line.
  </acceptance_criteria>
  <resume-signal>Type "applied" once verification queries pass, or paste the error to revise Plan 01.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer machine → DB | Migration is applied with elevated privileges; nothing else changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.5-05 | Tampering | Migration applied against wrong DB (e.g., prod instead of local) | mitigate | Plan instructs `--local` flag explicitly. Remote push deferred to user-controlled deploy. |
| T-04.5-SC | Tampering | No npm/pip/cargo installs | accept | CLI invocation only. |
</threat_model>

<verification>
- All four information_schema queries pass.
- Generated types (if used) regenerated.
</verification>

<success_criteria>
DB schema reflects Plan 01 migration; downstream plans can run without "column does not exist" errors.
</success_criteria>

<output>
Create `.planning/phases/04.5-conversion-qualification/04.5-02-SUMMARY.md` capturing the exact CLI commands used (versions vary) and the verification query outputs.
</output>
</content>
</invoke>