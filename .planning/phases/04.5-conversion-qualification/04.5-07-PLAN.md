---
phase: 04.5-conversion-qualification
plan: 07
type: execute
wave: 4
depends_on: ["04.5-04", "04.5-05"]
files_modified:
  - app/pages/espace/premium.vue
  - app/pages/espace/profil.vue
  - app/pages/app/dashboard.vue
autonomous: false
requirements: [CNV-02, CNV-05, CNV-06, CNV-08]
must_haves:
  truths:
    - "/espace/premium hero shows 'Premier contact exclusif.' and trial callout '14 jours gratuits' per UI-SPEC + D-06/D-07"
    - "/espace/premium ROI section shows '1 chantier signé rembourse 6 mois d abonnement.' per SPECIFICS"
    - "/espace/premium checkout button reads 'Démarrer votre essai gratuit — 14 jours offerts' per UI-SPEC"
    - "/espace/profil renders bio (textarea, 500 max with counter), category select, zone input, slug read-only, logo upload (drag-or-click), submit button"
    - "Logo upload performs presign → PUT R2 → PATCH /me logo_url flow"
    - "Dashboard navigation surfaces a 'Mon profil public' link to /espace/profil"
    - "Phase 4.5 typography rules enforced: only font-semibold (600) and regular (400); no font-black"
  artifacts:
    - path: "app/pages/espace/premium.vue"
      provides: "Refactored hero + trial callout + ROI section + updated CTA"
    - path: "app/pages/espace/profil.vue"
      provides: "New editable profile page"
    - path: "app/pages/app/dashboard.vue"
      provides: "Nav link to /espace/profil"
  key_links:
    - from: "espace/profil.vue logo upload"
      to: "logo-presign endpoint"
      via: "$fetch POST then PUT to signedUrl then PATCH /me"
      pattern: "logo-presign"
    - from: "dashboard nav"
      to: "/espace/profil"
      via: "NuxtLink"
      pattern: "/espace/profil"
---

<objective>
Deliver the remaining UI surfaces of Phase 4.5:
1. Refactor `/espace/premium` per UI-SPEC §"Page 2" — new hero, trial callout, refreshed pricing card features, ROI section, FAQ addition, updated comparison table row.
2. Build the new `/espace/profil` page per UI-SPEC §"Page 3" — header, view-public-profile link, logo upload, bio/category/zone/slug form, save handling, skeleton + error states.
3. Add nav link to dashboard.

Purpose: Close the loop on CNV-02 (free trial copy + CTA), CNV-05 (profile UI), CNV-06 (ROI copy). CNV-08 is the phase milestone — verified by all plans completing.
Output: 2 pages refactored/created + 1 dashboard nav update.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04.5-conversion-qualification/04.5-CONTEXT.md
@.planning/phases/04.5-conversion-qualification/04.5-UI-SPEC.md
@.planning/phases/04-le-verrou-stripe-billing/04-UI-SPEC.md
@app/pages/espace/premium.vue
@app/pages/app/dashboard.vue

<interfaces>
After Plan 05, API endpoints available:
- GET /api/v1/pro/profile/me → { profile: { id, canonical_slug, short_id, postal_code, dept, category, bio, zone, logo_url, company_name, full_name, is_verified, subscription_status } }.
- PATCH /api/v1/pro/profile/me → { updated: true, profile: <same shape> }.
- POST /api/v1/pro/profile/logo-presign with { content_type, filename } → { status: 'SUCCESS', signedUrl, fileKey, publicUrl }.

Phase 4 espace/premium.vue uses:
- watchEffect auth guard.
- supabase.from('professionals').select('id, subscription_status') via useAsyncData.
- $fetch('/api/v1/stripe/checkout', { method: 'POST' }) returning { url } for window redirect.

Phase 4 dashboard.vue has steps[] array; insert a new nav element either in steps OR add a new "Mon profil public" CTA section between header and progress checklist.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor /espace/premium per UI-SPEC §"Page 2"</name>
  <files>app/pages/espace/premium.vue</files>
  <read_first>
    - app/pages/espace/premium.vue (preserve auth guard, isAlreadyPremium logic, startCheckout, success banner, accordion FAQ; replace hero + price card features + add trial callout + add ROI section + add FAQ item)
    - .planning/phases/04.5-conversion-qualification/04.5-UI-SPEC.md §"Page 2 — /espace/premium (Refactored)" (copy hero, trial callout, pricing card, ROI section, FAQ addition VERBATIM including class names)
    - .planning/phases/04.5-conversion-qualification/04.5-CONTEXT.md (D-06 monthly only, D-07 CB required, SPECIFICS exact wording)
  </read_first>
  <action>
    Open app/pages/espace/premium.vue. Apply the following changes preserving the existing script setup auth + checkout flow:

    Step 1 — Hero section: replace the existing `<div class="mb-10">` containing the H1 "Débloquez vos leads instantanément." with the UI-SPEC §"Hero section" block exactly:
      Eyebrow "Abonnement", H1 "Premier contact exclusif." (text-3xl font-semibold tracking-tight — NOT font-black), subline "Accédez aux coordonnées du prospect dès la qualification — 72h avant les autres pros de votre métier."

    Step 2 — Insert trial callout block BEFORE the pricing card div, exactly per UI-SPEC §"Free trial callout".

    Step 3 — Pricing card: keep the amber border-amber-300 bg-amber-50 structure. Replace the 3 feature rows with the 3 from UI-SPEC §"Pricing card" (Accès immédiat / 3 leads gratuits inclus en BASIC — illimités en Premium / Exclusivité 72h). Replace the H4-equivalent `text-4xl font-black` with `text-4xl font-semibold` (typography rule).

    Replace the checkout button text from "Démarrer Premium — 39€/mois" with "Démarrer votre essai gratuit — 14 jours offerts" (CONTEXT D-06/D-07 + UI-SPEC copywriting table).

    Replace the existing fine-print line "Paiement sécurisé par Stripe" with "Paiement sécurisé par Stripe · CB requise · Facturation après 14 jours" per UI-SPEC.

    Step 4 — Add ROI section AFTER the comparison table div and BEFORE the FAQ div, exactly per UI-SPEC §"ROI section" block:
      Eyebrow "Retour sur investissement", headline "1 chantier signé rembourse 6 mois d'abonnement." (literal — SPECIFICS), supporting copy "Un chantier moyen en rénovation représente 8 000 € de chiffre d'affaires. Votre abonnement : 39€/mois.".

    Step 5 — Comparison table: ADD a new row after the existing rows: label "3 leads gratuits" with BASIC checkmark, Premium checkmark (illimité variant per CONTEXT D-04). Use the existing SVG check pattern.

    Step 6 — FAQ accordion: add one new <details> item with summary "Que se passe-t-il après mes 14 jours d'essai ?" and answer "Si vous n'annulez pas pendant la période d'essai, votre abonnement est automatiquement activé à 39€/mois. Vous pouvez annuler à tout moment depuis votre espace." Insert it as the LAST FAQ item.

    Step 7 — Typography sweep: replace ALL `font-black` occurrences in this file with `font-semibold` per Phase 4.5 UI-SPEC typography rule (2 weights: 400, 600). Audit price display, hero, success banner.

    Do NOT change the auth guard, the success banner, the isAlreadyPremium computed, the startCheckout function, or any logic.
  </action>
  <verify>
    <automated>grep -v '^#' app/pages/espace/premium.vue | grep -cE "Premier contact exclusif|14 jours gratuits|14 jours offerts|1 chantier signé rembourse 6 mois|font-semibold" && grep -v '^#' app/pages/espace/premium.vue | grep -c "font-black" | awk '{ if ($1 == 0) print "PASS"; else print "FAIL: font-black still present" }'</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: file contains literal strings "Premier contact exclusif." and "14 jours gratuits" and "Démarrer votre essai gratuit — 14 jours offerts" and "1 chantier signé rembourse 6 mois d'abonnement.".
    - Source assertion: file contains "CB requise · Facturation après 14 jours".
    - Source assertion: file contains zero `font-black` occurrences (after the sweep).
    - Source assertion: comparison table has a row containing "3 leads gratuits".
    - Source assertion: FAQ accordion has a <summary> containing "Que se passe-t-il après mes 14 jours".
    - Manual visual: dev server `/espace/premium` shows the new hero/trial/ROI/FAQ at correct positions.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>Premium page reflects Phase 4.5 copy + trial CTA; comparison table updated; FAQ extended.</done>
</task>

<task type="auto">
  <name>Task 2: Create /espace/profil page + dashboard nav link</name>
  <files>app/pages/espace/profil.vue, app/pages/app/dashboard.vue</files>
  <read_first>
    - app/pages/app/dashboard.vue (auth guard pattern: watchEffect + watch(user) → loadProData; reuse for /espace/profil)
    - app/pages/espace/premium.vue (auth guard + $fetch usage pattern)
    - .planning/phases/04.5-conversion-qualification/04.5-UI-SPEC.md §"Page 3 — /espace/profil (New Page)" (page header, public-profile link, logo upload block, form fields, skeleton, error state — copy ALL HTML verbatim)
    - .planning/phases/04.5-conversion-qualification/04.5-CONTEXT.md (D-14 fields, D-15 slug immutable, D-16 immediate, D-17 5MB)
  </read_first>
  <action>
    File 1 — app/pages/espace/profil.vue (NEW):

    `<script setup lang="ts">` block:
      - useHead({ title: 'Mon profil public — BÂTI-AXE' }).
      - const user = useSupabaseUser().
      - watchEffect(() => { if (user.value === null) navigateTo('/pro/claim') }) (same pattern as Phase 4 espace pages, per STATE.md known patterns).
      - reactive `profile` object with bio, category, zone, logo_url, canonical_slug, short_id, dept, company_name.
      - State: `loading`, `saving`, `saveSuccess`, `saveError`, `logoError`, `logoUploading`.
      - useAsyncData('pro-profile', () => $fetch('/api/v1/pro/profile/me').then(r => r.profile)) — populate profile on mount.
      - async function `handleLogoUpload(event)`:
        Step A — validate file size client-side (≤ 5_242_880 bytes); else set logoError "Logo trop volumineux (5 Mo max)." and return.
        Step B — validate MIME against the 5 allowed types client-side; else set logoError "Format non supporté.".
        Step C — POST /api/v1/pro/profile/logo-presign with { content_type, filename } → { signedUrl, publicUrl }.
        Step D — fetch(signedUrl, { method: 'PUT', headers: { 'Content-Type': file.type }, body: file }) — same pattern as dashboard.vue uploadDoc.
        Step E — PATCH /api/v1/pro/profile/me with { logo_url: publicUrl }; on success set profile.logo_url to publicUrl.
        Step F — silent catch with logoError set to a human message.
      - async function `saveProfile()`:
        PATCH /api/v1/pro/profile/me with { bio, category, zone } (slug NOT sent — D-15). On success set saveSuccess true for 3s then false. On error set saveError. Set saving true during the call.

    `<template>`:
      Wrap in `<div class="max-w-2xl mx-auto px-6 py-16">` per Phase 4 container rule.
      Page header per UI-SPEC §"Page header" (H1 text-3xl font-semibold — NOT font-black).
      View public profile link per UI-SPEC §"View public profile link" — only render if profile loaded; `:to` computed from profile.dept + profile.canonical_slug + profile.short_id (slug already includes short_id per Phase 3; verify with claim.post.ts canonicalSlug format — it's `${slugify(company_name)}-${slugify(cityName)}-${shortId}`, so the link is `/pro/${dept}/${canonical_slug}` since the slug already contains the short_id).
      Logo upload section per UI-SPEC verbatim; bind preview img to profile.logo_url; building SVG fallback when null.
      Form per UI-SPEC §"Profile form fields" verbatim including character counter `{{ profile.bio?.length ?? 0 }}/500`.
      Slug field read-only per UI-SPEC — `:value="\`batixe.fr/pro/${profile.dept}/${profile.canonical_slug}\`"`.
      Submit button per UI-SPEC.
      Skeleton loading state per UI-SPEC §"Skeleton loading".
      API error state per UI-SPEC §"API error" with @click="refresh".

    File 2 — app/pages/app/dashboard.vue:
    Add a navigation entry that points to /espace/profil. Two patterns acceptable:
      Option A — extend the `steps` computed array with a new entry { label: 'Mon profil public', done: !!pro.value?.bio || !!pro.value?.logo_url, desc: 'Bio, logo, zone d intervention', action: { label: 'Éditer mon profil', to: '/espace/profil' } } AFTER the existing 'Profil vérifié' step.
      Option B — add a standalone card/link block above the progress checklist with NuxtLink to /espace/profil.

    Pick Option A — it integrates with the existing checklist UX. Reuse existing step template; no new component.

    Typography sweep on dashboard.vue: replace ALL `font-black` occurrences with `font-semibold`. Phase 4 UI-SPEC also declared exactly 2 weights (400 regular + 600 font-semibold), so font-black was already non-compliant. The sweep is unconditional.
  </action>
  <verify>
    <automated>test -f app/pages/espace/profil.vue && grep -v '^#' app/pages/espace/profil.vue | grep -cE "logo-presign|profile/me|Choisir un logo|Mon profil public" && grep -v '^#' app/pages/app/dashboard.vue | grep -c "/espace/profil"</automated>
  </verify>
  <acceptance_criteria>
    - File app/pages/espace/profil.vue exists.
    - Source assertion: file contains literal strings "Mon profil public", "Choisir un logo d'entreprise", "Enregistrer les modifications", "L'URL de votre profil est fixe et ne peut pas être modifiée.", "5 Mo max".
    - Source assertion: file uses watchEffect auth guard pattern.
    - Source assertion: file performs $fetch on `/api/v1/pro/profile/logo-presign` followed by PUT to signedUrl followed by PATCH to `/api/v1/pro/profile/me`.
    - Source assertion: file does NOT include canonical_slug in any PATCH body (slug stays read-only per D-15).
    - Source assertion: dashboard.vue contains `/espace/profil` link string at least once.
    - Manual visual smoke: navigate to /espace/profil → form loads with existing values, editing bio + saving updates the row, uploading a logo persists logo_url, slug field is visibly disabled.
    - Manual visual: dashboard shows "Mon profil public" entry.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>New profile editing page live; nav link added; entire Phase 4.5 user-facing surface complete.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: End-of-phase visual verification across all Phase 4.5 surfaces</name>
  <what-built>
    All Phase 4.5 features merged: free leads counter banner, paywall banner, qualification badge strip, Marché local widget, refactored Premium page (hero + trial + ROI + FAQ + new comparison row), new Profile editing page, dashboard nav link.
  </what-built>
  <how-to-verify>
    Run `npm run dev` and verify the following manually:

    1. Log in as a Basic pro with free_leads_used=0 and at least 1 locked lead. Visit /espace/leads:
       a. Header H1 uses font-semibold (not font-black).
       b. Marché local widget renders below H1 (or hidden if zone has no projects).
       c. Neutral free-leads banner says "3 leads gratuits restants. Passez Premium pour un accès illimité." with right-side "Voir l'offre" link.
       d. Each lead card shows 4 qualification icons + X/4 score.
       e. Open browser devtools Network tab → on page load, observe one POST /api/v1/paywall-events with `event_type=paywall_view` per locked lead.

    2. Manually update DB: `update professionals set free_leads_used=3 where id='<your test pro>'`. Refresh /espace/leads:
       a. Amber paywall banner renders "Vous avez utilisé vos 3 leads gratuits. Passez Premium pour continuer à accéder aux coordonnées."
       b. Locked cards still flouté.

    3. Click "Passer Premium" — verify Network shows POST /api/v1/paywall-events `event_type=checkout_started`, then navigates to /espace/premium.

    4. On /espace/premium:
       a. Hero shows "Premier contact exclusif." (H1) and the new subline.
       b. Trial callout block ("14 jours gratuits") above the price card.
       c. Price card features include "3 leads gratuits inclus en BASIC".
       d. Checkout button says "Démarrer votre essai gratuit — 14 jours offerts".
       e. Below the comparison table, the ROI section says "1 chantier signé rembourse 6 mois d'abonnement.".
       f. FAQ has the new "Que se passe-t-il après mes 14 jours d'essai ?" item.
       g. Click checkout button → Stripe Checkout opens showing "14 days free trial" badge or equivalent.

    5. Cancel back to /espace, navigate to /espace/profil:
       a. Page loads with current bio/zone/category values.
       b. Slug field visually disabled (bg-muted).
       c. Try editing slug → confirm field is read-only and submit excludes it.
       d. Upload a logo file (PNG, ≤5MB) → verify preview updates after upload completes.
       e. Try uploading a 6MB file → verify error "Logo trop volumineux".
       f. Save bio change → green confirmation "Profil mis à jour".
       g. Visit /pro/{dept}/{slug} in incognito → confirm new bio appears.

    6. Visit /app/dashboard → confirm "Mon profil public" entry visible in checklist with link to /espace/profil.

    7. Type/visual sweep on all three pages: no `font-black` class active, no amber color usage on /espace/profil page (per UI-SPEC §"Global Consistency Rules" #7).

    Expected: all 7 sections produce the expected behavior.

    If any step fails: report the failure inline.
  </how-to-verify>
  <acceptance_criteria>
    - Each of the 7 verification sections passes.
    - No console errors during the flows.
    - Stripe Checkout displays trial period.
  </acceptance_criteria>
  <resume-signal>Type "approved" once all 7 sections verified; otherwise paste failure details for revision.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → API | All POST/PATCH requests authenticated; client-side validations are non-authoritative. |
| browser → R2 (presigned PUT) | URL-scoped to one key under pro-logos/${user.id}/. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.5-23 | Tampering | Client bypasses 5MB check by editing JS | mitigate | Server presign policy enforces 5MB Content-Length cap (Plan 05, T-04.5-16). Client check is UX only. |
| T-04.5-24 | Information Disclosure | Profile page logs PII to console.error in catch | mitigate | catch blocks emit only generic French messages — no raw error payload. |
| T-04.5-25 | Information Disclosure | XSS via bio HTML injection | mitigate | Vue templates auto-escape `{{ profile.bio }}` interpolation. Do NOT use v-html anywhere on profile fields. |
| T-04.5-26 | Tampering | Pro reads profile.subscription_status from /me and forges UI to bypass paywall | accept | UI flag is non-authoritative — server-side maskLead still enforces masking. |
| T-04.5-SC | Tampering | No new npm packages | accept | No installs. |
</threat_model>

<verification>
- `npm run typecheck` exits 0 across all three files.
- Manual end-of-phase checkpoint completes all 7 sections.
- vitest passes (no new failing tests).
</verification>

<success_criteria>
- All UI-SPEC Page 2 and Page 3 contracts implemented faithfully.
- Phase 4.5 end-to-end flow verified.
- Phase 4.5 done.
</success_criteria>

<output>
Create `.planning/phases/04.5-conversion-qualification/04.5-07-SUMMARY.md` noting visual-checkpoint outcome and any deferred polish items.
</output>
</content>
</invoke>