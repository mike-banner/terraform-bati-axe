---
plan: 05.12-01
phase: 05.12-front-polish-branding
status: complete
wave: 1
---

# Plan 05.12-01 — Landing page Partenaires dédiée + tunnel allégé

## What was done
- **`app/pages/partenaires/index.vue`** (nouveau) — landing page dédiée en 9 sections :
  1. Hero (gradient slate-900, badge « Espace Professionnels & Prescripteurs », H1 + 2 CTAs)
  2. Barre de confiance (4 chiffres : rappel 4h, pré-chiffrage 48h, décennales 100% API, dépôt 24/7)
  3. Problème → Solution (douleurs rouges vs ce que BÂTI-AXE change en vert)
  4. Services détaillés (6 cards : chiffrage réactif, audit continu, respect des plans, chargé d'affaires, suivi chantier, partenariat régulier)
  5. Comment ça marche (4 étapes numérotées, ancre `#comment-ca-marche`)
  6. Pour qui (5 profils `APPORTEUR_LABELS` réutilisés)
  7. Conformité (badge audit API + suspension auto)
  8. FAQ (5 questions en accordéon)
  9. CTA final (fond sombre + contact email)
- **`app/pages/b2b/partenaires.vue`** — section landing retirée (hero + 4 promesses + badge conformité) ; le tunnel démarre directement sur le choix de profil ; lien retour « ← Retour à la page Partenaires » ajouté en tête.
- **`app/layouts/default.vue`** — les 3 liens « Partenaires » (header connecté/déconnecté + footer) pointent sur `/partenaires` ; `isPartenairesPage` couvre `['/partenaires', '/b2b/partenaires']`.
- **`app/assets/css/tailwind.css`** — `scroll-behavior: smooth` global sur `html` + `scroll-behavior: auto` dans le bloc `prefers-reduced-motion`.
- Copy CTA : « Voir comment ça marche » → **« Découvrir le processus »** (verbe + objet).

## Addendum 2 (2026-08-23) — anti glassmorphism / anti AI slop (même PR #49)
- Landing nettoyée selon les règles `impeccable` : blobs flous `blur-3xl` supprimés (hero + CTA final), chips/buttons translucides `bg-white/5` → **fonds solides** slate-800/700, eyebrows uppercase sur chaque section **supprimés** (les h2 se suffisent), icônes émoji des services → **SVG line** (horloge, bouclier, fichier, user, clipboard, chaîne), barre « gros chiffre + petite étiquette » → **promesses concrètes** avec checks orange.
- Header : `bg-page/95 backdrop-blur-sm` → **`bg-page` plein** (plus de translucidité).

## Addendum (2026-08-23) — header raffiné (même PR #49)
- Logo réduit `h-16` → `h-12` + centré (respiration en haut, plus de contact avec la barre de favoris du navigateur) + `shrink-0`.
- « Se connecter » : lien texte → **bouton outline** `h-9` (cohérent avec « Déconnexion ») — hiérarchie impeccable : lien texte < outline < CTA plein.
- **CTA contextuel** : « Déposer un projet » (→ simulateur) par défaut ; sur `/partenaires` → « Déposer un dossier » (orange `safety`, → tunnel) ; masqué dans le tunnel `/b2b/partenaires`.

## Addendum 3 (2026-08-23) — icônes Phosphor, FAQ éditoriale, univers copper, radius unifié (même PR #49)
- **Icônes : `@phosphor-icons/vue` installé** (v2.2.1). Les SVG line faits main des services et les émojis des profils sont remplacés par des icônes Phosphor **duotone** (services : Clock, ShieldCheck, FileText, Headset, ClipboardText, LinkSimple ; profils : Ruler, Blueprint, HouseSimple, Buildings, Wrench). Les icônes UI (checks, flèches, plus/moins, warning) sont aussi en Phosphor.
- **Section « Pour qui »** : fini la card grid identique (interdit impeccable) → **rangées éditoriales** avec filets séparateurs, icône duotone dans un carré `bg-copper/5`, crainte (icône warning destructive) puis promesse (check copper).
- **FAQ repensée** : 2 colonnes (intro sticky + contact à gauche, accordéon à droite), filets horizontaux, question qui passe en copper à l'ouverture, icône `+` qui pivote dans un rond copper, CTA mailto « Écrire à partenaires@batiaxe.fr ».
- **Retour site particuliers** : lien « ← Retour au site des particuliers » en haut du hero + lien « Particuliers » ajouté dans le footer (le logo pointait déjà vers `/`).
- **Univers partenaire = token `copper` (#C2410C)** — orange brûlé, même famille que le `safety` : les CTA, étapes numérotées, sélections du tunnel, radio, checkbox, barre de progression passent en copper. Le reste du site garde le `safety`. Zéro `bg-safety` restant sur `/partenaires` et `/b2b/partenaires`.
- **Radius unifié** : les boutons du header (Se connecter, Déposer un projet, Déposer un dossier, Déconnexion) et du tunnel (Retour, Continuer, Envoyer, Retour accueil) passent de `rounded-sm` à **`rounded-full`** — le langage pill des CTA du site s'applique partout ; les cartes gardent leur `rounded-sm` industriel (contraste volontaire cartes carrées / actions pill).
- Copy impeccable : tirets cadratins du corps de texte remplacés par virgules/deux-points (les `useHead` gardent le `—` de convention).

## Verification
- Typecheck ✅ 0 erreur | navigation vérifiée ✅ | PR #49 (branch `fix/tweaks-front`) ✅
