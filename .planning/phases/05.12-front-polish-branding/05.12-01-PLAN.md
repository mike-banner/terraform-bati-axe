---
phase: 05.12-front-polish-branding
plan: 01
type: execute
wave: 1
files_modified:
  - app/pages/partenaires/index.vue
  - app/pages/b2b/partenaires.vue
  - app/layouts/default.vue
  - app/assets/css/tailwind.css
autonomous: true
requirements: []

must_haves:
  truths:
    - "Nouvelle route /partenaires avec landing dédiée (hero, barre de confiance, problème/solution, services, 4 étapes, profils, conformité, FAQ, CTA final)"
    - "CTAs de la landing lancent le tunnel /b2b/partenaires"
    - "Tunnel allégé : formulaire seul + lien retour vers la landing"
    - "Header et footer pointent sur /partenaires ; lien masqué sur les deux pages"
    - "Scroll fluide vers l'ancre (#comment-ca-marche) avec fallback prefers-reduced-motion"
---

# Plan 05.12-01 — Landing page Partenaires dédiée + tunnel allégé

## Objective
Transformer l'entrée « Partenaires » en une vraie landing page de conversion qui explique en détail l'offre aux apporteurs d'affaires, avec CTAs vers le tunnel existant.

## Deliverables
- `app/pages/partenaires/index.vue` (nouveau) : landing 9 sections (voir CONTEXT).
- `app/pages/b2b/partenaires.vue` : suppression de la section landing (hero + 4 promesses + badge conformité), le tunnel démarre directement ; ajout d'un lien retour « ← Retour à la page Partenaires ».
- `app/layouts/default.vue` : les 3 liens « Partenaires » (header ×2, footer) pointent sur `/partenaires` ; `isPartenairesPage` couvre `/partenaires` et `/b2b/partenaires`.
- `app/assets/css/tailwind.css` : `scroll-behavior: smooth` sur `html` + désactivation dans le bloc `prefers-reduced-motion`.

## Notes
- Copy CTA secondaire : « Découvrir le processus » (verbe + objet, règle UX copy).
- Réutilisation de `APPORTEUR_LABELS` (types/b2b) pour la section « Pour qui ».

## Verification
- [x] Typecheck 0 erreur
- [x] Navigation : header/footer → /partenaires, lien masqué sur les 2 pages
- [x] Scroll fluide actif, désactivé en prefers-reduced-motion
