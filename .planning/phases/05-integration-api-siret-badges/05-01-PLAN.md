---
phase: "05"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260624000000_phase5_siret_badges.sql
autonomous: true
requirements:
  - API-01
  - API-02
  - TRST-01

must_haves:
  truths:
    - "Les colonnes siret_verified_at, siret_company_name, siret_address, siret_status existent sur professionals"
    - "supabase db push passe sans erreur"
    - "La contrainte CHECK sur siret_status interdit toute valeur hors du set autorisé"
  artifacts:
    - path: "supabase/migrations/20260624000000_phase5_siret_badges.sql"
      provides: "Ajout colonnes siret_* sur professionals"
      contains: "ALTER TABLE professionals"
  key_links:
    - from: "supabase/migrations/20260624000000_phase5_siret_badges.sql"
      to: "professionals table"
      via: "supabase db push"
      pattern: "siret_status.*CHECK"
---

<objective>
Ajouter les 4 colonnes siret_* sur la table professionals via une migration Supabase, puis pousser le schéma sur la base locale.

Purpose: Les plans Wave 2 (lookup claim, badges) ont besoin de ces colonnes avant toute écriture ou lecture. Ce plan est [BLOCKING] pour la suite de la phase.
Output: Fichier de migration SQL committé + schéma local à jour.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-integration-api-siret-badges/05-RESEARCH.md

<interfaces>
<!-- Schéma actuel de professionals (colonnes existantes pertinentes) -->
-- Depuis supabase/migrations/20260603000000_schema_initial.sql
-- siret       TEXT UNIQUE NOT NULL
-- decennal_status decennal_status DEFAULT 'none'  (ENUM: pending/valid/expired/none)
-- labels      JSONB DEFAULT '[]'
-- is_verified BOOLEAN DEFAULT false

-- Nouvelles colonnes à ajouter (per RESEARCH.md § Nouvelles colonnes nécessaires) :
-- siret_verified_at   TIMESTAMPTZ         -- null = pas encore vérifié
-- siret_company_name  TEXT                -- raison sociale retournée par l'API
-- siret_address       TEXT                -- adresse complète retournée
-- siret_status        TEXT CHECK (siret_status IN ('active', 'closed', 'not_found', 'error'))
-- Choix TEXT+CHECK (pas ENUM) : évite ALTER TYPE si nouveaux statuts émergent
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tâche 1 : Créer la migration SQL siret_*</name>
  <files>supabase/migrations/20260624000000_phase5_siret_badges.sql</files>
  <read_first>
    - supabase/migrations/20260603000000_schema_initial.sql (colonnes existantes professionals)
    - supabase/migrations/20260616000000_phase5_lead_feedback.sql (format migration récent à reproduire)
  </read_first>
  <action>
    Créer le fichier de migration avec le contenu suivant :

    1. ALTER TABLE professionals ADD COLUMN IF NOT EXISTS siret_verified_at TIMESTAMPTZ
    2. ALTER TABLE professionals ADD COLUMN IF NOT EXISTS siret_company_name TEXT
    3. ALTER TABLE professionals ADD COLUMN IF NOT EXISTS siret_address TEXT
    4. ALTER TABLE professionals ADD COLUMN IF NOT EXISTS siret_status TEXT CHECK (siret_status IN ('active', 'closed', 'not_found', 'error'))

    Chaque ALTER utilise IF NOT EXISTS pour idempotence.
    Ajouter un commentaire SQL en tête : -- Phase 5 : colonnes vérification SIRET via API Recherche Entreprises
    Ne pas modifier les colonnes existantes (siret TEXT, labels JSONB, decennal_status).
    Ne pas créer d'ENUM — utiliser TEXT + CHECK constraint comme spécifié dans RESEARCH.md.
  </action>
  <verify>
    <automated>grep -c "siret_status.*CHECK" /home/mike/projects/bati-axe/supabase/migrations/20260624000000_phase5_siret_badges.sql</automated>
  </verify>
  <done>Le fichier SQL existe et contient les 4 ADD COLUMN avec IF NOT EXISTS et la CHECK constraint sur siret_status.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Tâche 2 : [BLOCKING] supabase db push + vérification colonnes</name>
  <read_first>
    - supabase/migrations/20260624000000_phase5_siret_badges.sql (produit par tâche 1)
  </read_first>
  <what-built>Migration SQL créant les 4 colonnes siret_* sur professionals.</what-built>
  <how-to-verify>
    1. Depuis la racine du projet, lancer : supabase db push
    2. Vérifier qu'aucune erreur n'est retournée
    3. Contrôler les colonnes en DB :
       supabase db query "SELECT column_name, data_type FROM information_schema.columns WHERE table_name = 'professionals' AND column_name LIKE 'siret_%' ORDER BY column_name;"
    4. Résultat attendu : 4 lignes (siret_address text, siret_company_name text, siret_status text, siret_verified_at timestamp with time zone)
  </how-to-verify>
  <resume-signal>Taper "ok" si les 4 colonnes sont présentes, ou décrire l'erreur rencontrée.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration SQL → Supabase local | DDL exécuté via CLI avec droits superadmin sur la base locale |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | CHECK constraint siret_status | mitigate | Contrainte CHECK en DB empêche l'insertion de valeurs arbitraires hors du set autorisé ; enforced côté serveur indépendamment du code applicatif |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | Aucun package installé dans ce plan — section non applicable |
</threat_model>

<verification>
- supabase db push retourne exit code 0
- SELECT sur information_schema.columns retourne exactement 4 lignes siret_*
- La CHECK constraint est visible dans \d professionals (psql) ou dans la réponse de la query
</verification>

<success_criteria>
Les 4 colonnes siret_verified_at (TIMESTAMPTZ), siret_company_name (TEXT), siret_address (TEXT), siret_status (TEXT avec CHECK) existent sur professionals en base locale. La migration est idempotente (IF NOT EXISTS). Les plans Wave 2 peuvent démarrer.
</success_criteria>

<output>
Créer `.planning/phases/05-integration-api-siret-badges/05-01-SUMMARY.md` une fois terminé.
</output>
