---
phase: "05"
plan: "02"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - server/api/v1/pro/claim.post.ts
  - tests/siret-lookup.test.ts
autonomous: true
requirements:
  - API-01

must_haves:
  truths:
    - "Le claim POST appelle l'API Recherche Entreprises et stocke le résultat dans siret_*"
    - "Un SIRET d'entreprise fermée (etat_administratif F) bloque l'inscription avec un message explicite"
    - "Un SIRET non diffusible (total_results 0) retourne siret_status not_found et ne bloque pas l'inscription"
    - "Une API down (timeout ou erreur réseau) retourne siret_status error et ne bloque pas l'inscription"
    - "Les 4 cas de la fonction lookupSiret sont couverts par des tests Vitest avec fetch mocké"
  artifacts:
    - path: "server/api/v1/pro/claim.post.ts"
      provides: "Lookup SIRET inline après validation Zod"
      contains: "lookupSiret"
    - path: "tests/siret-lookup.test.ts"
      provides: "Tests unitaires couvrant les 4 cas API-01"
      exports: ["active", "closed", "not_found", "error"]
  key_links:
    - from: "server/api/v1/pro/claim.post.ts"
      to: "recherche-entreprises.api.gouv.fr"
      via: "fetch avec AbortSignal.timeout(5000)"
      pattern: "recherche-entreprises\\.api\\.gouv\\.fr"
    - from: "server/api/v1/pro/claim.post.ts"
      to: "professionals table"
      via: "upsert avec colonnes siret_*"
      pattern: "siret_status.*siret_verified_at"
---

<objective>
Intégrer le lookup SIRET inline dans claim.post.ts : après la validation Zod, appeler l'API d'État, mapper le résultat, et inclure les colonnes siret_* dans l'upsert professionals. Couvrir les 4 cas par des tests Vitest.

Purpose: Automatise la vérification légale au moment du Claim sans ajouter de latence perceptible (timeout 5s, non bloquant sauf entreprise fermée). Implémente API-01.
Output: claim.post.ts modifié + tests/siret-lookup.test.ts créé.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-integration-api-siret-badges/05-RESEARCH.md

<interfaces>
<!-- Interface lookupSiret à implémenter (per RESEARCH.md Pattern 1) -->
interface SiretLookupResult {
  status: 'active' | 'closed' | 'not_found' | 'error'
  company_name?: string
  address?: string
  verified_at: string  // ISO string, NOW()
}

async function lookupSiret(siret: string): Promise<SiretLookupResult>

<!-- Appel API (vérifié par test direct dans RESEARCH.md) -->
URL: https://recherche-entreprises.api.gouv.fr/search?q={SIRET_14}&page=1&per_page=1
Champs utilisés: results[0].nom_complet, results[0].adresse, results[0].etat_administratif
etat_administratif: 'A' = actif, 'F' = fermé
Cas total_results === 0 : entreprise non diffusible → siret_status = 'not_found'

<!-- Upsert professionals à compléter (extrait de claim.post.ts lignes 188-205) -->
// Ajouter dans le payload upsert :
siret_status: lookup.status,
siret_verified_at: lookup.verified_at,
siret_company_name: lookup.company_name ?? null,
siret_address: lookup.address ?? null,

<!-- Blocage entreprise fermée -->
// Après le lookupSiret, si status === 'closed' :
throw createError({ statusCode: 422, statusMessage: 'Votre entreprise apparaît comme fermée dans l\'annuaire officiel. Contactez-nous si c\'est une erreur : contact@bati-axe.fr' })
// status 'not_found' et 'error' : continuer normalement (non bloquant)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tâche 1 : Tests Vitest pour lookupSiret (4 cas)</name>
  <files>tests/siret-lookup.test.ts</files>
  <read_first>
    - server/api/v1/pro/claim.post.ts (pour comprendre l'interface existante avant modification)
    - .planning/phases/05-integration-api-siret-badges/05-RESEARCH.md (§ Architecture de validation)
  </read_first>
  <behavior>
    - Cas 1 (active) : fetch retourne total_results=1 + etat_administratif='A' → status='active', company_name et address peuplés
    - Cas 2 (closed) : fetch retourne total_results=1 + etat_administratif='F' → status='closed', company_name peuplé
    - Cas 3 (not_found) : fetch retourne total_results=0 → status='not_found', company_name undefined
    - Cas 4 (error) : fetch throw (timeout/réseau) → status='error', company_name undefined
    Les tests mockent globalThis.fetch via vi.stubGlobal('fetch', ...).
    La fonction lookupSiret est extraite dans un helper importable depuis claim.post.ts ou un fichier utilitaire séparé.
  </behavior>
  <action>
    Créer tests/siret-lookup.test.ts avec Vitest.
    Importer lookupSiret depuis server/utils/siretLookup.ts (à créer dans la tâche 2).
    Utiliser vi.stubGlobal('fetch', vi.fn()) pour mocker les 4 réponses.
    Structure du mock pour cas actif :
      fetch retourne { ok: true, json: async () => ({ results: [{ nom_complet: 'DUPONT SARL', adresse: '12 RUE TEST 78000', etat_administratif: 'A' }], total_results: 1 }) }
    Structure du mock pour cas error :
      fetch throw new Error('AbortError')
    Chaque cas vérifie uniquement les champs status, company_name (présence/absence).
    Ne pas importer depuis claim.post.ts directement (le fichier utilise des globals Nitro non disponibles en test).
  </action>
  <verify>
    <automated>cd /home/mike/projects/bati-axe && npx vitest run tests/siret-lookup.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>4 tests passent en vert. Le test "closed" et "not_found" et "error" ne throw pas (ils retournent un objet avec status).</done>
</task>

<task type="auto">
  <name>Tâche 2 : Extraire lookupSiret + modifier claim.post.ts</name>
  <files>server/utils/siretLookup.ts, server/api/v1/pro/claim.post.ts</files>
  <read_first>
    - server/api/v1/pro/claim.post.ts (fichier entier — implémenter après lecture complète)
    - tests/siret-lookup.test.ts (produit par tâche 1 — respecter l'interface attendue)
  </read_first>
  <action>
    1. Créer server/utils/siretLookup.ts :
       - Exporter l'interface SiretLookupResult (status | company_name? | address? | verified_at)
       - Exporter la fonction async lookupSiret(siret: string): Promise<SiretLookupResult>
       - Implémentation : fetch avec AbortSignal.timeout(5000), mapper etat_administratif
       - Valider la longueur de company_name avant retour : tronquer à 255 chars si nécessaire (protection injection)
       - Ne jamais throw : toujours retourner { status: 'error', verified_at: now } en cas d'exception

    2. Modifier server/api/v1/pro/claim.post.ts :
       - Ajouter import { lookupSiret } from '~/server/utils/siretLookup'
       - Après le log 'SIRET check passed' (ligne ~118) et avant la zone lookup, ajouter :
           const siretDigits = data.siret  // déjà normalisé sans espaces par le body
           const siretLookup = await lookupSiret(siretDigits)
           if (siretLookup.status === 'closed') {
             throw createError({ statusCode: 422, statusMessage: "Votre entreprise apparaît comme fermée dans l'annuaire officiel. Contactez-nous si c'est une erreur : contact@bati-axe.fr" })
           }
       - Compléter le payload upsert professionals (étape 6 du fichier existant) avec les 4 champs :
           siret_status: siretLookup.status,
           siret_verified_at: siretLookup.verified_at,
           siret_company_name: siretLookup.company_name ?? null,
           siret_address: siretLookup.address ?? null,
       - NE PAS modifier le champ company_name existant dans le payload (per RESEARCH.md Pattern 3 — silencieux pour v1)
       - NE PAS modifier la validation Zod (déjà correcte)
       - Conserver tous les logs existants ([claim.post] prefix)
       - Ajouter log : log('SIRET lookup: ' + siretLookup.status)
  </action>
  <verify>
    <automated>cd /home/mike/projects/bati-axe && grep -c "lookupSiret" server/api/v1/pro/claim.post.ts && grep -c "siret_status" server/api/v1/pro/claim.post.ts && npx vitest run tests/siret-lookup.test.ts --reporter=verbose 2>&1 | grep -E "PASS|FAIL|Tests"</automated>
  </verify>
  <done>server/utils/siretLookup.ts exportant lookupSiret. claim.post.ts appellent lookupSiret, bloquent sur 'closed', incluent siret_* dans l'upsert. Les 4 tests Vitest passent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| claim.post.ts → API Recherche Entreprises | Appel fetch sortant server-side depuis Nitro/Worker Cloudflare |
| Body client → Zod validation → siret string | SIRET déjà validé /^\d{14}$/ avant d'entrer dans lookupSiret |
| API response → DB INSERT | company_name tronqué à 255 chars avant écriture |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | Spoofing | SIRET lookup par artisan | mitigate | Unicité SIRET enforced par UNIQUE constraint en DB ; lookup API ajoute vérification d'état légal |
| T-05-03 | DoS | Quota API 7 req/s par IP Cloudflare | accept | Phase pilote ~dizaines inscriptions/jour — quota non atteint ; AbortSignal.timeout(5000) évite le blocage indéfini |
| T-05-04 | Tampering | Injection via siret_company_name venant de l'API | mitigate | Troncature à 255 chars dans lookupSiret ; Vue escape automatiquement au rendu |
| T-05-05 | Tampering | Entreprise fermée contourne via retry | mitigate | Check sur siret_status='closed' au niveau endpoint ; UNIQUE constraint SIRET en DB empêche re-claim |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | Aucun package installé dans ce plan |
</threat_model>

<verification>
- grep -c "lookupSiret" server/api/v1/pro/claim.post.ts retourne >= 2
- grep -c "siret_status" server/api/v1/pro/claim.post.ts retourne >= 1
- npx vitest run tests/siret-lookup.test.ts : 4 tests verts
- server/utils/siretLookup.ts existe et exporte SiretLookupResult + lookupSiret
</verification>

<success_criteria>
Le claim POST appelle l'API Recherche Entreprises inline, bloque sur etat_administratif='F' avec message clair, continue gracieusement sur not_found et error, et stocke les colonnes siret_* en DB. Les 4 cas sont couverts par des tests Vitest passants.
</success_criteria>

<output>
Créer `.planning/phases/05-integration-api-siret-badges/05-02-SUMMARY.md` une fois terminé.
</output>
