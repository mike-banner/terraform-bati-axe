---
phase: "05"
plan: "04"
type: execute
wave: 3
depends_on:
  - "05-02"
  - "05-03"
files_modified:
  - app/pages/app/dashboard.vue
  - app/pages/pro/[dept]/[slug].vue
autonomous: true
requirements:
  - API-02
  - TRST-01

must_haves:
  truths:
    - "Le dashboard affiche BadgeEntrepriseVerifiee si siret_status === 'active'"
    - "Le dashboard affiche BadgeDecennaleCertifiee si decennal_status === 'valid'"
    - "Le profil public /pro/[dept]/[slug] affiche les mêmes badges sous les mêmes conditions"
    - "Le select Supabase dans dashboard.vue inclut siret_status et decennal_status"
    - "Si aucun badge ne s'applique, rien n'est affiché (pas de badge vide ou placeholder)"
  artifacts:
    - path: "app/pages/app/dashboard.vue"
      provides: "Badges confiance dans le header profil"
      contains: "BadgeEntrepriseVerifiee"
    - path: "app/pages/pro/[dept]/[slug].vue"
      provides: "Badges confiance sur profil public"
      contains: "BadgeEntrepriseVerifiee"
  key_links:
    - from: "app/pages/app/dashboard.vue"
      to: "professionals table"
      via: "select incluant siret_status, decennal_status"
      pattern: "siret_status"
    - from: "app/pages/pro/[dept]/[slug].vue"
      to: "professionals table"
      via: "select incluant siret_status, decennal_status"
      pattern: "siret_status"
---

<objective>
Câbler les badges de confiance sur le dashboard pro privé et le profil public. Étendre les selects Supabase pour inclure siret_status et decennal_status. Afficher BadgeEntrepriseVerifiee et BadgeDecennaleCertifiee sous conditions.

Purpose: Rend les badges visibles aux pros (dashboard) et aux particuliers (profil public) — valeur B2B immédiate. Implémente la partie affichage de API-02 et TRST-01.
Output: dashboard.vue et [slug].vue modifiés.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-integration-api-siret-badges/05-RESEARCH.md

<interfaces>
<!-- dashboard.vue — select actuel à étendre (ligne 44) -->
// Actuellement :
.select('id, company_name, full_name, phone, postal_code, canonical_slug, short_id, is_verified, is_claimed, decennal_status, created_at, categories, subscription_status, bio, logo_url')
// À remplacer par (ajouter siret_status, siret_company_name) :
.select('id, company_name, full_name, phone, postal_code, canonical_slug, short_id, is_verified, is_claimed, decennal_status, siret_status, siret_company_name, created_at, categories, subscription_status, bio, logo_url')

<!-- Interface Pro dans dashboard.vue (lignes 4-9) à étendre -->
interface Pro {
  id: string; company_name: string; full_name: string; phone: string
  postal_code: string; canonical_slug: string; short_id: string
  is_verified: boolean; is_claimed: boolean; decennal_status: string; created_at: string
  categories: string[]; subscription_status: string; bio?: string; logo_url?: string
  // Ajouter :
  siret_status?: string | null
  siret_company_name?: string | null
}

<!-- Emplacement wiring badges dans dashboard.vue — header (lignes 193-215) -->
// Dans la div class="mb-4 space-y-2" (ligne 193), après les badges existants :
// Ajouter dans la div class="div" qui contient PremiumBadge :
<BadgeEntrepriseVerifiee v-if="pro.siret_status === 'active'" />
<BadgeDecennaleCertifiee v-if="pro.decennal_status === 'valid'" />

<!-- Règles d'affichage (per RESEARCH.md § Diagramme de flux) -->
siret_status === 'active'      → BadgeEntrepriseVerifiee
decennal_status === 'valid'    → BadgeDecennaleCertifiee
siret_status === 'not_found'   → aucun badge (silencieux)
siret_status === 'error'       → aucun badge (silencieux)
siret_status null              → aucun badge (pro inscrit avant cette phase)

<!-- Profil public [slug].vue — à lire entièrement avant modification -->
<!-- Ajouter siret_status, decennal_status dans le select de la page -->
<!-- Même logique v-if pour les badges dans la section header/hero du profil -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tâche 1 : Badges dans dashboard.vue</name>
  <files>app/pages/app/dashboard.vue</files>
  <read_first>
    - app/pages/app/dashboard.vue (lire entièrement — fichier déjà en contexte partiellement, relire pour les numéros de ligne exacts)
    - app/components/ui/BadgeEntrepriseVerifiee.vue (produit par plan 03)
    - app/components/ui/BadgeDecennaleCertifiee.vue (produit par plan 03)
  </read_first>
  <action>
    1. Étendre l'interface Pro (début de script setup) avec siret_status?: string | null et siret_company_name?: string | null

    2. Étendre le select Supabase dans loadProData() :
       Ajouter siret_status et siret_company_name à la chaîne de select (ne pas casser la virgule-séparée existante)

    3. Dans le template, section header (div class="mb-4 space-y-2") :
       - Après la div contenant PremiumBadge (ligne ~195), ajouter dans la même div space-y-2 :
         <BadgeEntrepriseVerifiee v-if="pro.siret_status === 'active'" />
         <BadgeDecennaleCertifiee v-if="pro.decennal_status === 'valid'" />
       - PremiumBadge reste affiché (condition is_verified inchangée)
       - La div "Vérification en cours" (badge amber) reste inchangée

    4. NE PAS modifier la logique de chargement, les documents, les steps, ni aucune autre section.
    5. Les composants sont auto-importés par Nuxt (aucun import manuel nécessaire).
  </action>
  <verify>
    <automated>grep -c "BadgeEntrepriseVerifiee" /home/mike/projects/bati-axe/app/pages/app/dashboard.vue && grep -c "siret_status" /home/mike/projects/bati-axe/app/pages/app/dashboard.vue</automated>
  </verify>
  <done>dashboard.vue contient BadgeEntrepriseVerifiee avec v-if siret_status active, BadgeDecennaleCertifiee avec v-if decennal_status valid, et siret_status dans le select Supabase.</done>
</task>

<task type="auto">
  <name>Tâche 2 : Badges sur profil public [slug].vue</name>
  <files>app/pages/pro/[dept]/[slug].vue</files>
  <read_first>
    - app/pages/pro/[dept]/[slug].vue (lire entièrement avant toute modification)
    - app/components/ui/BadgeEntrepriseVerifiee.vue (produit par plan 03)
    - app/components/ui/BadgeDecennaleCertifiee.vue (produit par plan 03)
  </read_first>
  <action>
    1. Lire le fichier [slug].vue entièrement pour identifier :
       - Le select Supabase (ou fetch) qui charge les données du profil public
       - L'interface/type local pour les données du pro
       - L'emplacement dans le template où le nom/header du pro est affiché

    2. Étendre le select/fetch pour inclure siret_status et decennal_status si pas déjà présents.
       (decennal_status peut déjà être présent — vérifier avant d'ajouter)

    3. Étendre l'interface TypeScript locale avec siret_status?: string | null si elle existe.

    4. Dans le template, dans la section hero/header du profil public, ajouter après le nom de l'entreprise ou après les badges existants :
       <div class="flex flex-wrap gap-2 mt-2">
         <BadgeEntrepriseVerifiee v-if="pro.siret_status === 'active'" />
         <BadgeDecennaleCertifiee v-if="pro.decennal_status === 'valid'" />
       </div>
       Adapter la variable (peut s'appeler pro, professional, data, etc. — utiliser le nom réel du fichier).

    5. NE PAS modifier la logique de routing, de redirect 301, ni les meta tags SEO.
    6. NE PAS afficher les badges si siret_status est null (pro inscrit avant cette phase) — v-if strict.
  </action>
  <verify>
    <automated>grep -c "BadgeEntrepriseVerifiee" /home/mike/projects/bati-axe/app/pages/pro/\[dept\]/\[slug\].vue && grep -c "siret_status" /home/mike/projects/bati-axe/app/pages/pro/\[dept\]/\[slug\].vue</automated>
  </verify>
  <done>La page profil public affiche BadgeEntrepriseVerifiee et BadgeDecennaleCertifiee sous les mêmes conditions que le dashboard. siret_status est dans le select.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB → Vue template | siret_status et decennal_status lus depuis DB, affichés via v-if — pas de logique utilisateur |
| Profil public → visiteur anonyme | Badges visibles sans authentification sur /pro/[dept]/[slug] |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Information Disclosure | Badges sur profil public | accept | Les badges reflètent un statut vérifiable publiquement (annuaire État) — pas de fuite d'information sensible |
| T-05-08 | Spoofing | Badge affiché pour un pro non vérifié | mitigate | Condition v-if stricte sur siret_status === 'active' (string exact) ; null et autres valeurs ne déclenchent pas le badge |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | Aucun package installé dans ce plan |
</threat_model>

<verification>
- grep -c "BadgeEntrepriseVerifiee" dashboard.vue retourne >= 1
- grep -c "siret_status" dashboard.vue retourne >= 2 (select + v-if)
- grep -c "BadgeEntrepriseVerifiee" [slug].vue retourne >= 1
- grep -c "siret_status" [slug].vue retourne >= 1
- npm run build (ou nuxt build) passe sans erreur TypeScript
</verification>

<success_criteria>
BadgeEntrepriseVerifiee s'affiche sur dashboard ET profil public quand siret_status === 'active'. BadgeDecennaleCertifiee s'affiche sur dashboard ET profil public quand decennal_status === 'valid'. Aucun badge n'apparaît si les conditions ne sont pas remplies.
</success_criteria>

<output>
Créer `.planning/phases/05-integration-api-siret-badges/05-04-SUMMARY.md` une fois terminé.
</output>
