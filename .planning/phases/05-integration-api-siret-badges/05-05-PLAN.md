---
phase: "05"
plan: "05"
type: execute
wave: 3
depends_on:
  - "05-03"
files_modified:
  - server/api/v1/admin/approve-pro.post.ts
  - tests/admin-approve.test.ts
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - API-01
  - API-02
  - TRST-01

must_haves:
  truths:
    - "approve-pro.post.ts met à jour decennal_status à 'valid' lors d'une approbation"
    - "approve-pro.post.ts pousse 'decennale_certified' dans labels JSONB lors d'une approbation"
    - "Un rejet admin ne modifie pas labels"
    - "Les requirements API-01, API-02, TRST-01 existent dans REQUIREMENTS.md"
    - "Les tests Vitest couvrent approve → decennal_status=valid + labels update, et reject → inchangé"
  artifacts:
    - path: "server/api/v1/admin/approve-pro.post.ts"
      provides: "Mise à jour decennal_status + labels au moment de l'approbation"
      contains: "decennale_certified"
    - path: "tests/admin-approve.test.ts"
      provides: "Tests unitaires approve-pro avec Supabase mocké"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Requirements API-01, API-02, TRST-01 formalisés"
      contains: "API-01"
  key_links:
    - from: "server/api/v1/admin/approve-pro.post.ts"
      to: "professionals.labels JSONB"
      via: "UPDATE SET labels = labels || '[\"decennale_certified\"]'"
      pattern: "decennale_certified"
    - from: "server/api/v1/admin/approve-pro.post.ts"
      to: "professionals.decennal_status"
      via: "UPDATE SET decennal_status = 'valid'"
      pattern: "decennal_status.*valid"
---

<objective>
Modifier approve-pro.post.ts pour pousser le badge décennale dans labels JSONB et basculer decennal_status à 'valid' lors d'une approbation admin. Couvrir par des tests. Formaliser les requirements de la phase dans REQUIREMENTS.md.

Purpose: Complète la boucle admin → badge. Un rejet de dossier ne déclenche pas de badge. Implémente TRST-01. La tâche REQUIREMENTS.md crée les IDs référencés dans tous les plans de la phase.
Output: approve-pro.post.ts modifié + tests + REQUIREMENTS.md enrichi.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-integration-api-siret-badges/05-RESEARCH.md

<interfaces>
<!-- approve-pro.post.ts — état actuel à modifier -->
// Ligne 42 (UPDATE existant) — actuellement :
await supabase
  .from('professionals')
  .update({ is_verified: approved })
  .eq('id', pro_id)

// À remplacer par (per RESEARCH.md § Piège 4) :
if (approved) {
  await supabase
    .from('professionals')
    .update({
      is_verified: true,
      decennal_status: 'valid',
      // Concaténation JSONB : ajouter 'decennale_certified' sans écraser les labels existants
      // Utiliser la syntaxe Supabase RPC ou raw update
    })
    .eq('id', pro_id)
} else {
  await supabase
    .from('professionals')
    .update({ is_verified: false })
    .eq('id', pro_id)
}

// Note sur la concaténation JSONB labels :
// Supabase JS ne supporte pas les expressions PostgreSQL dans .update()
// Utiliser supabase.rpc('append_label', { pro_id, label: 'decennale_certified' })
// OU utiliser une raw query via rpc ou un UPDATE SQL manuel
// Solution recommandée : deux requêtes séquentielles
//   1. update({ is_verified: true, decennal_status: 'valid' })
//   2. supabase.rpc('jsonb_array_append', ...) ou récupérer labels + concat + update

// Alternative la plus simple (sans RPC custom) :
//   1. SELECT labels FROM professionals WHERE id = pro_id
//   2. Concat en JS : const newLabels = [...(currentLabels || []), 'decennale_certified']
//   3. UPDATE labels = newLabels (pas de doublon : vérifier includes avant concat)

// Utiliser l'alternative 2 étapes (SELECT puis UPDATE) pour éviter une RPC custom.

<!-- decennal_status ENUM existant : 'pending' | 'valid' | 'expired' | 'none' -->
<!-- 'valid' est déjà dans l'ENUM — pas de migration nécessaire -->

<!-- REQUIREMENTS.md — format à respecter -->
// Lire l'état actuel de .planning/REQUIREMENTS.md avant d'ajouter
// Ajouter une section Phase 5 avec les IDs API-01, API-02, TRST-01
// Format inspiré des sections existantes (IDs, descriptions courtes)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tâche 1 : Modifier approve-pro.post.ts + tests admin</name>
  <files>server/api/v1/admin/approve-pro.post.ts, tests/admin-approve.test.ts</files>
  <read_first>
    - server/api/v1/admin/approve-pro.post.ts (lire entièrement avant modification)
    - app/components/ui/BadgeDecennaleCertifiee.vue (pour confirmer le label string exact attendu)
  </read_first>
  <behavior>
    Test 1 : approve(true) → professionals.update appelé avec is_verified=true, decennal_status='valid'
    Test 2 : approve(true) → labels mis à jour avec 'decennale_certified' ajouté (pas de doublon si déjà présent)
    Test 3 : approve(false) → professionals.update appelé avec is_verified=false uniquement (decennal_status inchangé, labels inchangés)
    Test 4 : approve(true) sans KBIS approuvé → createError 422 levé (comportement existant conservé)
  </behavior>
  <action>
    Modifier server/api/v1/admin/approve-pro.post.ts :

    1. Remplacer le UPDATE monolithique ligne 42 par une logique conditionnelle :
       - Si approved === true :
         a. Récupérer les labels actuels du pro : SELECT labels FROM professionals WHERE id = pro_id → currentLabels (array)
         b. Construire newLabels : si 'decennale_certified' absent → [...currentLabels, 'decennale_certified'], sinon currentLabels inchangé (idempotent)
         c. UPDATE professionals SET is_verified = true, decennal_status = 'valid', labels = newLabels WHERE id = pro_id
       - Si approved === false :
         UPDATE professionals SET is_verified = false WHERE id = pro_id
         (NE PAS modifier decennal_status ni labels sur un rejet)

    2. Mettre à jour le log audit existant pour inclure decennal_status dans metadata si approved=true :
       metadata: { manual_approval: true, approved, ...(approved ? { decennal_status: 'valid', badge: 'decennale_certified' } : {}) }

    3. NE PAS modifier la vérification KBIS/décennale existante (lignes 24-40) — la conserver intacte.

    Créer tests/admin-approve.test.ts avec Vitest :
    - Mocker serverSupabaseUser (retourne un admin)
    - Mocker serverSupabaseServiceRole avec un client Supabase factice
    - Le mock du client supabase doit supporter les chaînes .from().select(), .from().update(), .from().insert()
    - Tester les 4 comportements listés dans <behavior>
    - Utiliser vi.fn() pour capturer les arguments du UPDATE et vérifier is_verified, decennal_status, labels
  </action>
  <verify>
    <automated>grep -c "decennale_certified" /home/mike/projects/bati-axe/server/api/v1/admin/approve-pro.post.ts && grep -c "decennal_status.*valid" /home/mike/projects/bati-axe/server/api/v1/admin/approve-pro.post.ts && cd /home/mike/projects/bati-axe && npx vitest run tests/admin-approve.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>approve-pro.post.ts contient 'decennale_certified' et 'decennal_status: valid'. Sur approbation, labels est mis à jour (idempotent). Sur rejet, seul is_verified=false est modifié. 4 tests Vitest verts.</done>
</task>

<task type="auto">
  <name>Tâche 2 : Ajouter API-01, API-02, TRST-01 dans REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (lire entièrement pour respecter le format existant)
    - .planning/ROADMAP.md (section Phase 5 pour le contexte)
  </read_first>
  <action>
    Lire .planning/REQUIREMENTS.md pour identifier le format exact des requirements existants (IDs, descriptions, section headers).

    Ajouter une section "## Phase 5 : Intégration API État (SIRET) & Badges de Confiance" avec les trois requirements suivants, en respectant le format des autres sections :

    API-01 : Lookup SIRET automatique au Claim
    - Description : Au POST /api/v1/pro/claim, appeler l'API Recherche Entreprises (recherche-entreprises.api.gouv.fr) avec le SIRET saisi. Stocker raison sociale, adresse et statut dans siret_company_name, siret_address, siret_status. Bloquer si etat_administratif='F' (fermé). Ne pas bloquer sur not_found (non diffusible) ou error (API indisponible).

    API-02 : Badge Entreprise Vérifiée
    - Description : Afficher le badge BadgeEntrepriseVerifiee sur le dashboard pro et le profil public /pro/[dept]/[slug] quand siret_status === 'active'. Ne pas afficher si null, not_found ou error.

    TRST-01 : Badge Décennale Certifiée BÂTI-AXE
    - Description : L'approbation admin du dossier décennale met à jour decennal_status à 'valid' et ajoute 'decennale_certified' dans labels JSONB. Le badge BadgeDecennaleCertifiee s'affiche sur dashboard et profil public quand decennal_status === 'valid'.

    Conserver tout le contenu existant de REQUIREMENTS.md — ajouter uniquement, ne rien supprimer.
  </action>
  <verify>
    <automated>grep -c "API-01" /home/mike/projects/bati-axe/.planning/REQUIREMENTS.md && grep -c "TRST-01" /home/mike/projects/bati-axe/.planning/REQUIREMENTS.md</automated>
  </verify>
  <done>REQUIREMENTS.md contient les sections API-01, API-02, TRST-01 avec descriptions. Le reste du fichier est intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin → approve-pro endpoint | Protégé par app_metadata.role === 'admin' + serverSupabaseUser |
| labels JSONB → badge affiché | Lecture côté client — valeur vient de la DB après UPDATE admin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09 | Spoofing | Approbation admin par un pro non-admin | mitigate | Endpoint vérifie app_metadata.role === 'admin' via serverSupabaseUser (lignes 10-14 existantes, conservées intactes) |
| T-05-10 | Tampering | Doublon 'decennale_certified' dans labels | mitigate | Vérification includes() avant concat — idempotent même si endpoint appelé deux fois |
| T-05-11 | Tampering | decennal_status non resetté sur rejet | accept | Sur reject, is_verified=false mais decennal_status reste 'valid' si précédemment approuvé — comportement voulu (l'assurance reste valide, seul le compte est désactivé) |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | Aucun package installé dans ce plan |
</threat_model>

<verification>
- grep -c "decennale_certified" approve-pro.post.ts retourne >= 1
- grep -c "decennal_status.*valid" approve-pro.post.ts retourne >= 1
- npx vitest run tests/admin-approve.test.ts : 4 tests verts
- grep -c "API-01" .planning/REQUIREMENTS.md retourne >= 1
- grep -c "TRST-01" .planning/REQUIREMENTS.md retourne >= 1
</verification>

<success_criteria>
L'approbation admin déclenche decennal_status='valid' + labels=['decennale_certified'] de façon idempotente. Le rejet ne touche pas ces champs. REQUIREMENTS.md contient les 3 IDs de la phase. 4 tests Vitest passent.
</success_criteria>

<output>
Créer `.planning/phases/05-integration-api-siret-badges/05-05-SUMMARY.md` une fois terminé.
</output>
