---
phase: "05"
plan: "06"
type: execute
wave: 4
depends_on:
  - "05-04"
files_modified:
  - app/pages/app/dashboard.vue
  - server/api/v1/pro/documents/upload.post.ts
  - tests/auto-approve.test.ts
autonomous: true
requirements:
  - TRST-01
must_haves:
  truths:
    - "Le pro saisit son numéro de police et sa date d'expiration lors de l'upload de la décennale"
    - "L'upload valide automatiquement le statut (auto-approbation) car le pro engage sa responsabilité"
    - "Le dashboard affiche une alerte bloquante si la date d'expiration est dépassée"
  artifacts: []
  key_links: []
---

<objective>
Remplacer la validation manuelle de la décennale par une auto-approbation sous la responsabilité du pro.
Ajouter des champs "Numéro de police" et "Date d'expiration" dans le formulaire d'upload du dashboard. Mettre à jour l'API d'upload pour sauvegarder ces champs et basculer automatiquement `decennal_status` à 'valid'. Mettre en place la logique de blocage du badge si la date est expirée.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-integration-api-siret-badges/05-VALIDATION.md
@app/pages/app/dashboard.vue
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tâche 1 : Modifier le formulaire d'upload (dashboard.vue)</name>
  <files>app/pages/app/dashboard.vue</files>
  <action>
    Dans `app/pages/app/dashboard.vue`, modifier la section d'upload de la décennale :
    1. Avant le bouton "Envoyer la décennale", ajouter deux champs (input text pour le Numéro de police, input date pour la Date d'expiration).
    2. Rendre ces champs obligatoires pour pouvoir cliquer sur "Envoyer".
    3. Passer ces deux nouvelles valeurs dans l'appel API `uploadDoc('decennale')` via `FormData` ou dans le body JSON de la requête de metadata (selon la façon dont l'upload R2 est implémenté).
    4. Ajouter une note claire sous le formulaire (déjà partiellement présente) sur l'engagement de responsabilité vis-à-vis des CGU.
  </action>
  <verify>
    <automated>grep -q "Numéro de police" app/pages/app/dashboard.vue && grep -q "Date d'expiration" app/pages/app/dashboard.vue || echo "Champs absents"</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Tâche 2 : API d'upload et Auto-approbation</name>
  <files>server/api/v1/pro/documents/upload.post.ts</files>
  <action>
    Modifier l'endpoint qui reçoit les documents :
    1. Récupérer `policy_number` et `expiration_date` depuis la requête.
    2. Lors de l'enregistrement dans la table `verifications`, insérer ces données avec le status = 'approved' (auto-approbation).
    3. Mettre à jour la table `professionals` du pro connecté :
       - `decennal_status = 'valid'`
       - Ajouter `'decennale_certified'` aux `labels` JSONB.
  </action>
  <verify>
    <automated>grep -q "decennal_status.*valid" server/api/v1/pro/documents/upload.post.ts || echo "Auto-approbation absente"</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Tâche 3 : Test de la logique d'auto-approbation</name>
  <files>tests/auto-approve.test.ts</files>
  <action>
    Créer `tests/auto-approve.test.ts` :
    1. Simuler un appel à l'endpoint d'upload de la décennale avec fichier + numéro + date.
    2. Vérifier que la DB est appelée avec update sur `professionals` (`decennal_status: 'valid'`).
    3. Vérifier que la table `verifications` reçoit bien la date et le numéro de police.
  </action>
  <verify>
    <automated>grep -q "auto-approve" tests/auto-approve.test.ts || echo "Test manquant"</automated>
  </verify>
  <done>Le workflow d'auto-approbation responsable est complet.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06-1 | Repudiation | Auto-approbation décennale | accept | La case à cocher / note engageant la responsabilité légale du pro est affichée. C'est prévu par la gouvernance de BÂTI-AXE pour s'éviter le travail manuel. |
</threat_model>

<verification>
- [ ] Le pro doit saisir sa date d'expiration et numéro de police pour uploader la décennale.
- [ ] L'upload auto-approuve le statut décennale.
- [ ] Les tests Vitest `auto-approve.test.ts` passent.
</verification>

<success_criteria>
Le processus de validation admin est bypassé. Le pro devient autonome et le système stocke sa date d'expiration pour automatiser le retrait du badge quand la date est dépassée.
</success_criteria>

<output>
Créer `.planning/phases/05-integration-api-siret-badges/05-06-SUMMARY.md` une fois terminé.
</output>
