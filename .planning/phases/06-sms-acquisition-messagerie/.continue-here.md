---
context: phase
phase: 06-sms-acquisition-messagerie
plan: 1
status: in_progress
last_updated: 2026-06-15T12:24:36.791Z
branch: fix/messaging-unlock-guard
---

# Reprise — Phase 6 (Espace Client & Messagerie)

<current_state>
Vérification du plan 06-01 (Magic Link & Messagerie In-App), déjà codé hors flux GSD.
Vérif terminée (4/5 PASS), 1 bug majeur trouvé ET corrigé (REQ-03). Email réel testé.
Reste : passe visuelle Playwright, merge, dette typecheck, puis near-term messagerie, puis planif Phase 6, puis design final.
On bosse sur la branche `fix/messaging-unlock-guard` (2 commits).
</current_state>

<completed_work>
- UAT Phase 6 plan 01 → `.planning/phases/06-sms-acquisition-messagerie/06-UAT.md` (status: complete, 4/5 PASS)
- Fix REQ-03 (verrou déblocage messagerie) → commit `1700a83`
- Bascule `EMAIL_LIVE` pour vrais emails en dev → commit `883fc18`
- Email réel envoyé via Resend vers mike.webfree@gmail.com (id 9d811364) → câblage prouvé
</completed_work>

<remaining_work>
1. **Passe visuelle Playwright** sur `/mon-projet/[token]` et `/espace/messages` (BLOQUÉ tant que CC pas redémarré — MCP Chromium).
2. **Merge** `fix/messaging-unlock-guard` → `development`.
3. **Corriger 10 erreurs typecheck préexistantes** dans `server/api/v1/leads/index.get.ts` (typage Supabase, `{}`).
4. **Design minimum messagerie + test réel envoi/réception** d'un message (branche `feat/messagerie-min`).
5. **Planifier le reste de Phase 6** (SMS, cold outreach, email prod) — EN DERNIER.
6. **Phase design TOTALE finale** (ux-pro-max → impeccable → taste-skill), design email inclus — toute fin de projet.
</remaining_work>

<decisions_made>
- Phase design totale repoussée à la toute fin ; pipeline ux-pro-max → impeccable → taste-skill. Voir mémoire `project_design_strategy`.
- Pas de branche séparée pour le design email maintenant (intégré à la phase design finale).
- Planif du reste de Phase 6 en dernier (après le near-term messagerie).
- Fix REQ-03 = réplication de `maskLead.isUnlocked` (Premium OU free_grant OU 72h), pas de nouveau modèle.
</decisions_made>

<blockers>
- Playwright : MCP reconfiguré sur Chromium (`~/.claude.json` args `--browser chromium`) → nécessite **restart CC**.
- Resend : domaine `bati-axe.fr` non acheté/vérifié → envoi limité à mike.webfree@gmail.com (expéditeur onboarding@resend.dev). Multi-destinataires (yopmail) débloqué à l'achat+vérif.
- Auth locale : gotrue rejette la création/login d'un user de test (bad_jwt HS256) → endpoints authentifiés non curl-ables ; passer par cookie mocké (E2E) ou revue de code.
</blockers>

## Required Reading (in order)
1. `.planning/phases/06-sms-acquisition-messagerie/06-UAT.md` — résultats de vérif + détail du fix REQ-03
2. `.planning/phases/06-sms-acquisition-messagerie/06-SPEC.md` — périmètre RÉEL (SMS hors scope, repoussé)
3. Mémoire `project_design_strategy` + `project_deploiement_cloudflare` — design final & déploiement (prod = `development`)

## Critical Anti-Patterns (do NOT repeat these)
- [SERVEUR DEV] : un seul serveur sur :3000 (CLAUDE.md). `pkill -f "nuxt dev"` ne tue PAS les process lancés via `@nuxt/cli` directement → vérifier `ss -ltnp | grep :3000` et kill par PID si besoin.
- [EMAIL DEV] : `import.meta.dev` mocke les emails par défaut → pour de vrais envois en dev, lancer avec `EMAIL_LIVE=true`.

## Infrastructure State
- Serveur dev : sur `:3000` (relancé à froid cette session). Health OK. Peut être arrêté à la reprise → relancer `npm run dev`.
- Supabase local : `127.0.0.1:54321` (Postgres `:54322`, user/pwd postgres). Migrations Phase 6 appliquées (table `messages`, colonne `projects.access_token`).
- Prod : `https://bati-axe.pages.dev` (branche `development`). Smoke OK.
- Token de projet local pour test magic link : `f43c8e3c-2555-404e-86d5-9391f43bfd3d`.

<context>
Le plan 06-01 a été codé sans SUMMARY ni flux GSD ; on l'a vérifié rétroactivement. Le backend est solide.
Le SPEC a restreint la Phase 6 à « Espace Client & Messagerie » — le ROADMAP (SMS, etc.) est plus large et reste à planifier en dernier.
Priorité utilisateur : visuel d'abord (après restart), puis merge, puis dette, puis messagerie min, puis planif, puis design final.
</context>

<next_action>
Après restart CC : si le serveur dev est arrêté, `npm run dev` (vérifier :3000 libre).
Puis passe visuelle Playwright : naviguer http://localhost:3000/mon-projet/f43c8e3c-2555-404e-86d5-9391f43bfd3d et /espace/messages, screenshots + audit rendu.
</next_action>
