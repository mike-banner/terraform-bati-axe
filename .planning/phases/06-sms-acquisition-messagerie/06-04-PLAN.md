# Phase 6 — Plan 04 : SMS différencié — ⏸ DIFFÉRÉ (à exécuter en TOUTE FIN)

> **Ne pas exécuter maintenant.** Décision utilisateur (2026-06-16) : aucune dépense fournisseur SMS, **même pour tester**, tant que le feu vert explicite n'est pas donné. Ce plan est posé pour mémoire ; il passe en dernier, après le reste de Phase 6 et potentiellement après la phase design. Couvre REQ-08.

## Pré-requis avant exécution
- **Feu vert utilisateur** + budget SMS accepté.
- Choix du fournisseur tranché. Reco : **Brevo** (🇫🇷/RGPD, API REST en `fetch` comme Resend, peut unifier email+SMS) ; alternative la moins chère **OVHcloud SMS** ; fallback fiable **Twilio** (US, plus cher). Voir mémoire `project_pre_launch_checklist`.

## Existant réutilisé
- `sms_opt_in` déjà collecté dans `server/api/v1/pro/claim.post.ts`.
- `server/utils/email.ts` = patron à copier pour le `fetch` direct (contrainte Workers : pas de SDK lourd).

## 1. Utilitaire d'envoi
- **Fichier** : `server/utils/sms.ts`.
- **Action** : `sendSms({to, body})` symétrique de `sendEmail` — mock en dev (console.log), appel REST fournisseur en prod via `fetch`. Clé API en runtimeConfig (`NUXT_*`). Sender alphanumérique « BÂTI-AXE » (autorisé en transactionnel France).

## 2. Logique différenciée
- **Action** : sur un événement « nouveau lead correspondant » :
  - Pro **Basic** → SMS d'**incitation à l'upgrade** (« un chantier correspond à votre zone, passez Premium pour le débloquer »).
  - Pro **Premium** → SMS **lead direct** (notification actionnable).
- Respecter `sms_opt_in` ; ne jamais envoyer sans opt-in. Prévoir opt-out (STOP).

## 3. Garde-fous coût
- Plafond d'envois / throttling.
- Mock par défaut ; envoi réel derrière un flag explicite (`SMS_LIVE`), jamais activé en CI.

## Vérification (DoD — à l'exécution seulement)
- [ ] `sendSms` mocke en dev, n'appelle le fournisseur qu'en prod avec flag.
- [ ] Différenciation Basic/Premium correcte.
- [ ] `sms_opt_in` et opt-out respectés.
- [ ] Aucune dépense engagée sans validation explicite.
