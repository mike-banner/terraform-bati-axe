---
plan: 05.10-08
phase: 05.10-espace-partenaires-apporteurs-affaires
status: complete
wave: 4
---

# Plan 05.10-08 — Workflow DirCo (human-in-the-loop)

## What was done

**Migration** — `supabase/migrations/20260822000003_b2b_dirco.sql`
- `qualifications_requises TEXT[]` (Qualibat/RGE/exigences)
- `planning_start` / `planning_end DATE`
- `recommended_pros UUID[]` (2-3 sous-traitants sélectionnés)
- ✅ appliquée en prod (`supabase db push`)

**Endpoint GET** — `b2b-requests.get.ts` étendu
- Retourne aussi la liste des pros **vérifiés** (`is_verified = true`, ≤ 500) pour le picker sous-traitants

**Endpoint PATCH** — `b2b-requests/[id].patch.ts` étendu
- Champs DirCo (Zod) : `qualifications_requises` (≤ 20 items ≤ 100 chars), dates ISO `YYYY-MM-DD` (nullables), `recommended_pros` (uuid[], **max 3**)
- Retour enrichi + audit log (qualifications, planning, pros)

**Endpoint restitution** — `b2b-requests/[id]/restitution.post.ts`
- Email au donneur d'ordres (`contact_email`) : cartes des pros recommandés (nom, catégorie, lien profil public `/pro/{dept}/{canonical_slug}`), exigences retenues, planning, budget
- Statut → `qualifie` (sauf si déjà `converti`/`perdu`), note horodatée ajoutée, audit log `b2b_restitution_sent`
- 422 si aucun pro recommandé

**UI** — `AdminB2bTab.vue`
- Section « Qualification DirCo » : qualifications (input virgules), planning (2 date inputs), picker sous-traitants (checkboxes, max 3, pros vérifiés uniquement, désactivation au-delà de 3)
- Bouton « Envoyer les propositions au donneur d'ordres » (désactivé sans pro sélectionné), refresh après envoi

## Verification
- Typecheck `nuxi typecheck` : ✅ 0 erreur
- Tests : 63/64 (échec préexistant badges, hors périmètre)
- Migration 20260822000003 appliquée en prod ✅
- Import email corrigé (chemin `../../../../../utils/email`)

## Decisions
- `cctp_files` (draft CONTEXT) écarté : redondant avec `b2b_requests.files` (toutes les pièces y sont déjà, consultables dans l'onglet)
- V1 « sous-traitants validés » = `is_verified = true` (Phase 05.11 filtrera sur `documents_artisan`)

## Not done (not_yet du plan)
- Phase 05.11 : filtrage des pros par documents légaux valides
- Compte Prescripteur (P8) : restitution en libre-service côté apporteur
- Workflow auto (IA) : analyse CCTP automatique
