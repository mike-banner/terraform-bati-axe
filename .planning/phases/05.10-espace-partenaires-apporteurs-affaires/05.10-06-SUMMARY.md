---
plan: 05.10-06
phase: 05.10-espace-partenaires-apporteurs-affaires
status: complete
wave: 3
---

# Plan 05.10-06 — Back-office admin b2b_requests

## What was done

**Endpoint liste** — `server/api/v1/admin/b2b-requests.get.ts`
- Liste des demandes triées `created_at DESC` (limit 200) avec jointure `assigned:auth.users(email)`
- Filtre optionnel `?status=`
- Retourne aussi la liste des admins (via `auth.admin.listUsers`, filtre `role === 'admin'`) pour le dropdown d'assignation

**Endpoint mise à jour** — `server/api/v1/admin/b2b-requests/[id].patch.ts`
- PATCH statut pipeline / `assigned_to` (uuid ou null) / `notes` (Zod, notes max 5000)
- Audit log `b2b_request_updated` (statut, assignation, présence de notes)

**Endpoint lecture pièces** — `server/api/v1/admin/b2b-file.view.post.ts`
- Lien signé GET (5 min) sur le bucket dédié B2B (`R2_BUCKET` / `bati-axe-uploads` — distinct de `R2_BUCKET_NAME` des documents de vérification)

**Composant** — `app/components/admin/AdminB2bTab.vue`
- File d'attente : cartes avec header cliquable (type apporteur, besoin, localisation, budget, âge)
- Filtres par statut (Tous + 6 statuts pipeline)
- Pièces jointes cliquables (ouverture du lien signé dans un nouvel onglet) + taille formatée
- Gestion pipeline : select statut (nouveau → en_cours → rappele → qualifie → converti → perdu), select « Chargé d'affaires » (admins), textarea notes, bouton Enregistrer (PATCH différentiel)
- Première carte (la plus récente) dépliée par défaut

**Intégration** — `app/components/admin/AdminLayout.vue` (nav item « Dossiers B2B », icône briefcase) + `app/pages/admin/index.vue` (onglet `b2b`)

## Verification
- Typecheck `nuxi typecheck` : ✅ 0 erreur (projet entier)
- Tests vitest : 63/64 pass — le seul échec (`tests/badges.test.ts`, palette `bg-[#F8FAFC]` vs `bg-green-100`) est **préexistant et hors périmètre** (badges, pas B2B)
- RLS : lecture/écriture via service role admin, politique `admin_all_b2b_requests` déjà en place

## Not done (not_yet du plan)
- Workflow DirCo (05.10-08) : qualification CCTP, sélection 2-3 sous-traitants, restitution
- Intégration HubSpot/Pipedrive (CRM externe) — hors phase
- Notif Slack webhook — optionnel (Resend équipe déjà branché sur POST /requests)
