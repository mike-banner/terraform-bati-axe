---
phase: 07-formulaire-ao-mod-le-multi-lots
plan: 03
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - app/pages/b2b/partenaires.vue
  - app/components/admin/AdminB2bTab.vue
autonomous: false
requirements: [TEND-01, TEND-02, TEND-05, SYNDIC-01]
must_haves:
  truths:
    - "Un partenaire ne peut pas passer l'etape 3 du tunnel sans saisir 20 caracteres de description"
    - "Un syndic voit un selecteur de corps de metier a l'etape 3 et peut en cocher plusieurs"
    - "Un persona non syndic ne voit pas le selecteur de corps de metier"
    - "Le DirCo choisit « confirme » ou « en attente de decision » et saisit un code postal 5 chiffres dans la fiche B2B"
    - "Le DirCo lit la description saisie par le partenaire dans la fiche B2B"
    - "Le persona syndic est selectionnable a l'etape 1 du tunnel (SYNDIC-01, deja livre — a verifier, pas a reconstruire)"
  artifacts:
    - path: "app/pages/b2b/partenaires.vue"
      provides: "Description obligatoire + selecteur multi-lots syndic"
      contains: "LOT_CATEGORY_OPTIONS"
    - path: "app/components/admin/AdminB2bTab.vue"
      provides: "Statut de decision + code postal + affichage description"
      contains: "decision_status"
  key_links:
    - from: "app/pages/b2b/partenaires.vue"
      to: "POST /api/v1/b2b/requests"
      via: "payload description + lots_categories"
      pattern: "lots_categories"
    - from: "app/components/admin/AdminB2bTab.vue"
      to: "PATCH /api/v1/admin/b2b-requests/[id]"
      via: "payload decision_status + project_postal_code"
      pattern: "payload.project_postal_code"
---

<objective>
Exposer dans les deux écrans existants les champs posés par 07-01/07-02 : description
obligatoire et sélecteur multi-lots syndic dans le tunnel public, statut de décision
et code postal structuré dans la fiche de qualification DirCo. Vérifier au passage que
SYNDIC-01 est bien déjà satisfait par le code existant.

Purpose: sans cette couche, les colonnes restent vides en usage réel — c'est le plan
qui rend les 4 exigences observables par un humain.
Output: 2 fichiers modifiés, 1 vérification humaine du parcours complet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-formulaire-ao-mod-le-multi-lots/07-UI-SPEC.md
@.planning/phases/07-formulaire-ao-mod-le-multi-lots/07-CONTEXT.md
@app/pages/b2b/partenaires.vue
@app/components/admin/AdminB2bTab.vue

<interfaces>
Contrat serveur consommé par cette UI (posé en 07-02, `server/utils/b2bTender.ts`) :
```ts
// payload POST /api/v1/b2b/requests
description?: string          // requis (>= 20 car.) si need_type === 'projet_immediat'
lots_categories?: ('maconnerie'|'toiture'|'electricite'|'plomberie'|'peinture'|'isolation')[]  // max 6

// payload PATCH /api/v1/admin/b2b-requests/[id]
decision_status?: 'confirme' | 'en_attente'
project_postal_code?: string | null   // /^\d{5}$/
```

Types front disponibles (posés en 07-01, `app/types/b2b.ts`) :
```ts
export type B2bDecisionStatus = 'confirme' | 'en_attente'
export type B2bLotCategory = 'maconnerie' | 'toiture' | 'electricite' | 'plomberie' | 'peinture' | 'isolation'
export const LOT_CATEGORY_OPTIONS: { value: B2bLotCategory; label: string }[]
export const DECISION_STATUS_LABELS: Record<B2bDecisionStatus, string>
```

Règle projet (CLAUDE.md) : tout nouvel input porte des contraintes HTML
(`maxlength`/`pattern`/`type`) cohérentes avec la contrainte Zod serveur, posées dès
la création du champ.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tunnel public — description obligatoire + sélecteur multi-lots syndic</name>
  <files>app/pages/b2b/partenaires.vue</files>
  <read_first>
    - app/pages/b2b/partenaires.vue (fichier modifié : state lignes 9-40, canNextStep lignes 42-50, bloc étape 3 lignes 275-332 incluant le pattern TRAVAUX_OPTIONS lignes 303-319, submit ~ligne 160)
    - .planning/phases/07-formulaire-ao-mod-le-multi-lots/07-UI-SPEC.md (copy exacte des labels/placeholders/erreurs + classes Tailwind imposées)
    - app/types/b2b.ts (LOT_CATEGORY_OPTIONS livré en 07-01)
    - app/components/admin/AdminB2bTab.vue (lignes 393-402 : markup textarea de référence à adapter)
  </read_first>
  <action>
1. Import : ajouter `LOT_CATEGORY_OPTIONS` à l'import existant depuis `~/types/b2b`,
   et `B2bLotCategory` à l'import de types.
2. State, après `const travauxSuggeres = ...` :
   ```ts
   const description = ref('')
   const lotsCategories = ref<B2bLotCategory[]>([])
   const isSyndic = computed(() => apporteurType.value === 'syndic')
   function toggleLot(c: B2bLotCategory) {
     const idx = lotsCategories.value.indexOf(c)
     if (idx === -1) lotsCategories.value.push(c)
     else lotsCategories.value.splice(idx, 1)
   }
   ```
   (copie exacte du pattern `toggleTravail`.)
3. `canNextStep` : remplacer `case 3: return true // files optional` par
   ```ts
   case 3: return description.value.trim().length >= 20
   ```
4. Bloc étape 3 (`<div v-if="step === 3">`), insérer un champ description
   IMMÉDIATEMENT APRÈS le bloc « Adresse ou département du projet » (avant le
   `<template v-if="isDiagnostiqueur">`) :
   ```html
   <div class="mb-4">
     <label class="block text-sm font-medium text-foreground mb-1.5">Décrivez votre besoin *</label>
     <textarea
       v-model="description"
       rows="4"
       maxlength="5000"
       placeholder="Ex : Ravalement de façade + réfection toiture sur bâtiment collectif, 1200 m², parties communes uniquement."
       class="w-full px-3 py-2 border border-border rounded-sm text-sm bg-card text-foreground placeholder:text-muted-foreground focus:outline-none focus:ring-2 focus:ring-ring resize-y"
     />
     <p v-if="description.trim().length > 0 && description.trim().length < 20" class="mt-1 text-xs text-destructive">
       Description trop courte (20 caractères minimum, {{ description.trim().length }} actuellement).
     </p>
     <p v-else-if="description.trim().length === 0" class="mt-1 text-xs text-muted-foreground">
       20 caractères minimum — plus l'artisan comprend l'ampleur du chantier, plus vite il vous répond.
     </p>
   </div>
   ```
5. Sélecteur multi-lots, inséré JUSTE APRÈS le bloc description, réservé au syndic —
   copie verbatim des classes du bloc `TRAVAUX_OPTIONS` (lignes 303-319) :
   ```html
   <div v-if="isSyndic" class="mb-4">
     <label class="block text-sm font-medium text-foreground mb-1.5">Corps de métier concernés (parties communes)</label>
     <div class="flex flex-wrap gap-2">
       <button
         v-for="opt in LOT_CATEGORY_OPTIONS"
         :key="opt.value"
         type="button"
         @click="toggleLot(opt.value)"
         class="px-3 py-1.5 rounded-full border text-xs font-medium transition-colors"
         :class="lotsCategories.includes(opt.value)
           ? 'border-copper bg-copper/10 text-copper'
           : 'border-border text-muted-foreground hover:border-muted-foreground/50'"
       >
         {{ opt.label }}
       </button>
     </div>
     <p class="mt-1.5 text-xs text-muted-foreground">
       Sélectionnez tous les corps de métier nécessaires — chacun sera traité comme un lot indépendant.
     </p>
   </div>
   ```
6. Dans la fonction de soumission (près de `project_location: projectLocation.value || null,`),
   ajouter au body :
   ```ts
   description: description.value.trim() || null,
   lots_categories: lotsCategories.value.length ? lotsCategories.value : null,
   ```

Ne PAS introduire de composant shadcn (`<Input>`, `<Select>`, `<Textarea>`) : le
fichier est en HTML natif + Tailwind, cf. 07-UI-SPEC.md « Scope note ».
Ne PAS toucher au parcours `partenariat_regulier` (l'étape 3 lui reste sautée, donc
sans description — comportement voulu, aligné sur le `superRefine` serveur).
Ne PAS ajouter de sélecteur de statut ici : le statut est saisi par le DirCo (TEND-02).
  </action>
  <verify>
    <automated>grep -q "LOT_CATEGORY_OPTIONS" app/pages/b2b/partenaires.vue &amp;&amp; grep -q "lots_categories" app/pages/b2b/partenaires.vue &amp;&amp; grep -q "description.trim().length >= 20" app/pages/b2b/partenaires.vue &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "case 3: return description.trim().length >= 20" app/pages/b2b/partenaires.vue` passe
    - `grep -q "Décrivez votre besoin \*" app/pages/b2b/partenaires.vue` passe
    - `grep -q "Corps de métier concernés (parties communes)" app/pages/b2b/partenaires.vue` passe
    - `grep -q "v-if=\"isSyndic\"" app/pages/b2b/partenaires.vue` passe
    - `grep -q "border-copper bg-copper/10 text-copper" app/pages/b2b/partenaires.vue` passe (pill réutilisée, pas de nouvelle couleur)
    - `grep -q "maxlength=\"5000\"" app/pages/b2b/partenaires.vue` passe (miroir HTML du `.max(5000)` Zod)
    - `grep -c "shadcn\|<Textarea\|<Input " app/pages/b2b/partenaires.vue` retourne 0
    - SYNDIC-01 (déjà livré) : `grep -q "Object.entries(APPORTEUR_LABELS)" app/pages/b2b/partenaires.vue` passe et `grep -q "syndic:" app/types/b2b.ts` passe — si l'un des deux échoue, la carte syndic n'est plus rendue, le signaler au lieu de l'ignorer
    - `npm run build` réussit
  </acceptance_criteria>
  <done>L'étape 3 exige 20 caractères de description avant de continuer, et affiche le sélecteur de corps de métier pour le seul persona syndic.</done>
</task>

<task type="auto">
  <name>Task 2: Fiche DirCo — statut de décision, code postal, description en lecture</name>
  <files>app/components/admin/AdminB2bTab.vue</files>
  <read_first>
    - app/components/admin/AdminB2bTab.vue (fichier modifié : interface B2bRequest lignes 20-30, B2bDraft lignes 96-104, ensureDraft 109-125, fetchRequests 141-151, saveChanges 168-200, bloc Qualification DirCo 404-436)
    - .planning/phases/07-formulaire-ao-mod-le-multi-lots/07-UI-SPEC.md (labels/options/erreurs exacts + classes `focus:border-safety` imposées)
  </read_first>
  <action>
Dans `app/components/admin/AdminB2bTab.vue` :

1. Interface locale `B2bRequest` : ajouter
   ```ts
   description: string | null
   decision_status: 'confirme' | 'en_attente'
   project_postal_code: string | null
   ```
2. Ajouter à côté de `STATUS_LABELS` :
   ```ts
   const DECISION_LABELS: Record<string, string> = {
     confirme: 'Confirmé — travaux décidés et budgétés',
     en_attente: 'En attente de décision — devis à comparer avant validation (ex. avant AG)',
   }
   ```
3. Interface `B2bDraft` : ajouter `decision_status: string` et `project_postal_code: string`.
4. `ensureDraft` ET `fetchRequests` (les deux endroits qui construisent un draft) :
   ajouter `decision_status: r?.decision_status || 'en_attente'` et
   `project_postal_code: r?.project_postal_code || ''` (sans `?.` dans `fetchRequests`
   qui itère `data.requests`).
5. `saveChanges`, après la ligne `recommended_pros` :
   ```ts
   if (d.decision_status !== r.decision_status) payload.decision_status = d.decision_status
   if (d.project_postal_code !== (r.project_postal_code || '')) payload.project_postal_code = d.project_postal_code || null
   ```
6. Template — dans le bloc « Qualification DirCo », ajouter une seconde grille
   `grid sm:grid-cols-2 gap-3 mt-3` sous la grille existante :
   ```html
   <div class="grid sm:grid-cols-2 gap-3 mt-3">
     <div>
       <label class="block text-[10px] uppercase tracking-wide text-muted-foreground mb-1.5">Statut de la décision</label>
       <select
         v-model="ensureDraft(r.id).decision_status"
         class="w-full h-9 px-2.5 text-sm rounded-sm border border-border bg-background text-foreground focus:outline-none focus:border-safety"
       >
         <option v-for="(label, value) in DECISION_LABELS" :key="value" :value="value">{{ label }}</option>
       </select>
       <p class="mt-1.5 text-[10px] text-muted-foreground">
         Visible par l'artisan avant qu'il ne réponde. Un AO confirmé se ferme au premier artisan intéressé ; un AO en attente accepte plusieurs devis.
       </p>
     </div>
     <div>
       <label class="block text-[10px] uppercase tracking-wide text-muted-foreground mb-1.5">Code postal du projet</label>
       <input
         v-model="ensureDraft(r.id).project_postal_code"
         type="text"
         inputmode="numeric"
         maxlength="5"
         pattern="\d{5}"
         placeholder="78000"
         class="w-full h-9 px-2.5 text-sm rounded-sm border border-border bg-background text-foreground placeholder:text-muted-foreground/50 focus:outline-none focus:border-safety"
       />
       <p v-if="ensureDraft(r.id).project_postal_code && !/^\d{5}$/.test(ensureDraft(r.id).project_postal_code)" class="mt-1.5 text-[10px] text-destructive">
         Code postal invalide (5 chiffres attendus).
       </p>
     </div>
   </div>
   ```
7. Affichage de la description du partenaire (lecture seule) : au-dessus du bloc
   « Gestion pipeline », ajouter
   ```html
   <div v-if="r.description" class="mb-3">
     <p class="text-[10px] uppercase tracking-wide text-muted-foreground mb-1.5">Besoin décrit par le partenaire</p>
     <p class="text-sm text-foreground whitespace-pre-line">{{ r.description }}</p>
   </div>
   ```

Utiliser `focus:border-safety` (accent admin), jamais `copper` dans ce fichier.
Ne PAS toucher au picker `recommended_pros` (son remplacement par la diffusion
automatique est la Phase 8).
  </action>
  <verify>
    <automated>grep -q "decision_status" app/components/admin/AdminB2bTab.vue &amp;&amp; grep -q "project_postal_code" app/components/admin/AdminB2bTab.vue &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "Statut de la décision" app/components/admin/AdminB2bTab.vue` passe
    - `grep -q "Code postal du projet" app/components/admin/AdminB2bTab.vue` passe
    - `grep -q "Besoin décrit par le partenaire" app/components/admin/AdminB2bTab.vue` passe
    - `grep -c "decision_status" app/components/admin/AdminB2bTab.vue` est >= 5 (interface, draft x2, saveChanges, template)
    - `grep -q "pattern=\"\\\\d{5}\"" app/components/admin/AdminB2bTab.vue` — l'input porte `pattern="\d{5}"` et `maxlength="5"`
    - `grep -q "payload.project_postal_code" app/components/admin/AdminB2bTab.vue` passe
    - `grep -c "copper" app/components/admin/AdminB2bTab.vue` retourne 0
    - `npm run build` réussit
  </acceptance_criteria>
  <done>La fiche B2B affiche la description du partenaire et permet d'enregistrer le statut de décision et le code postal, avec erreur inline hors format.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Vérification humaine du parcours AO complet</name>
  <read_first>
    - .planning/phases/07-formulaire-ao-mod-le-multi-lots/07-UI-SPEC.md
  </read_first>
  <files>aucun (verification uniquement)</files>
  <action>
    Lancer `npm run dev`, dérouler le scénario de `<how-to-verify>` avec l'utilisateur,
    puis attendre son signal explicite avant de poursuivre. Ne modifier aucun fichier
    dans cette tâche : si un écart est constaté, le corriger dans la tâche concernée
    (Task 1 ou Task 2) puis rejouer la vérification.
  </action>
  <what-built>
    Tunnel `/b2b/partenaires` : description obligatoire (>= 20 car.) et sélecteur de
    corps de métier réservé au syndic. Fiche admin B2B : statut de décision, code
    postal 5 chiffres, description du partenaire en lecture.
    Backend : lots `b2b_tender_lots` créés au dépôt syndic.
  </what-built>
  <how-to-verify>
    1. `npm run dev` puis ouvrir http://localhost:3000/b2b/partenaires
    2. Étape 1 : vérifier que la carte « Syndic de Copropriété / Gestionnaire » est
       proposée (SYNDIC-01) et la sélectionner. Étape 2 : « Projet immédiat ».
    3. Étape 3 : le bouton « Continuer » doit rester désactivé tant que la description
       fait moins de 20 caractères ; le message « Description trop courte (20 caractères
       minimum, N actuellement) » doit s'afficher dès la première frappe.
    4. Cocher au moins 3 corps de métier (ex. Charpente & Toiture, Peinture & Finitions,
       Électricité) — les pastilles sélectionnées passent en copper.
    5. Étape 4 : compléter les coordonnées, cocher le consentement, envoyer. Attendu :
       écran de remerciement avec référence.
    6. En base : `select category from b2b_tender_lots where request_id = '<id>'` doit
       retourner exactement les 3 catégories cochées (1 ligne chacune).
    7. Refaire un dépôt avec le persona « Architecte » : le sélecteur de corps de métier
       NE doit PAS apparaître, et aucun lot ne doit être créé.
    8. Ouvrir http://localhost:3000/admin (onglet « Dossiers B2B »), déplier le dossier
       syndic : la description doit être lisible, choisir « Confirmé — travaux décidés
       et budgétés », saisir `78000`, cliquer « Enregistrer », puis recharger la page —
       les deux valeurs doivent être conservées.
    9. Saisir `780` dans le code postal : le message « Code postal invalide (5 chiffres
       attendus) » doit s'afficher et l'enregistrement doit être refusé par le serveur (400).
  </how-to-verify>
  <acceptance_criteria>
    - Étapes 3, 6, 7, 8 et 9 se comportent exactement comme décrit
    - Aucune erreur console navigateur sur les deux écrans
    - Le parcours « Partenariat régulier » (qui saute l'étape 3) reste soumissible sans description
  </acceptance_criteria>
  <verify>
    <automated>npm run build &amp;&amp; npx vitest run</automated>
  </verify>
  <done>L'utilisateur a répondu « approuvé » sur les 9 étapes du scénario.</done>
  <resume-signal>Répondre « approuvé » ou décrire les écarts constatés</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Navigateur public -> tunnel `/b2b/partenaires` | Saisie non authentifiée, envoyée à l'intake public |
| Navigateur admin -> `AdminB2bTab.vue` | Console admin, mutations de dossiers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-12 | Tampering | contraintes de saisie côté client | mitigate | `maxlength="5000"` (description) et `maxlength="5"` + `pattern="\d{5}"` (CP) posés dès la création du champ, en miroir des règles Zod serveur (`.max(5000)`, `/^\d{5}$/`) — règle CLAUDE.md, la validation serveur reste la seule autoritaire |
| T-07-13 | Information disclosure | description du partenaire affichée dans l'admin | accept | Écran déjà réservé au rôle admin (`app_metadata.role === 'admin'`), même surface que les coordonnées partenaire déjà affichées ; ADR-004 ne s'applique pas au B2B (cf. `.planning/research/ARCHITECTURE.md`) |
| T-07-14 | Spoofing | dépôt d'AO frauduleux par un faux partenaire | transfer | Filtre humain DirCo à la qualification (workflow existant 05.10) + Turnstile déjà en place sur les formulaires publics ; le rate-limit dédié aux AO est explicitement TEND-11, Phase 8 |
| T-07-15 | Injection (XSS) | rendu de la description | mitigate | Rendu via interpolation Vue `{{ r.description }}` (échappée), jamais `v-html` — critère `grep -c "v-html"` à 0 dans le bloc ajouté |
</threat_model>

<verification>
- `npm run build` réussit
- `npx vitest run` reste vert
- `npx playwright test` : aucune régression des specs existantes du tunnel B2B
- Checkpoint humain approuvé
</verification>

<success_criteria>
- Impossible de dépasser l'étape 3 du tunnel sans 20 caractères de description
- Le sélecteur de corps de métier n'apparaît que pour le persona syndic et crée 1 lot par catégorie cochée
- Le DirCo enregistre un statut de décision et un code postal 5 chiffres persistés en base
- La description du partenaire est lisible dans la fiche admin
- Le persona syndic reste sélectionnable à l'étape 1 (SYNDIC-01 vérifié, non reconstruit)
</success_criteria>

<output>
Après complétion, créer `.planning/phases/07-formulaire-ao-mod-le-multi-lots/07-03-SUMMARY.md`
</output>
