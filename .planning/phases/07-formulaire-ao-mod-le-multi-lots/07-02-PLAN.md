---
phase: 07-formulaire-ao-mod-le-multi-lots
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - server/utils/b2bTender.ts
  - server/api/v1/b2b/requests.post.ts
  - server/api/v1/admin/b2b-requests/[id].patch.ts
  - tests/unit/b2b-tender.test.ts
autonomous: true
requirements: [TEND-01, TEND-02, TEND-05]
must_haves:
  truths:
    - "Un POST /api/v1/b2b/requests sans description (ou < 20 caracteres) sur un projet immediat est rejete en 400"
    - "Un depot syndic avec plusieurs corps de metier cree une ligne b2b_tender_lots par corps de metier"
    - "Le DirCo peut enregistrer decision_status et project_postal_code via PATCH /api/v1/admin/b2b-requests/[id]"
    - "Un code postal non conforme a 5 chiffres est rejete en 400 par l'endpoint admin"
  artifacts:
    - path: "server/utils/b2bTender.ts"
      provides: "Schema Zod d'intake B2B + construction des lots, testables unitairement"
      exports: ["b2bRequestSchema", "buildTenderLots", "B2B_LOT_CATEGORIES"]
    - path: "tests/unit/b2b-tender.test.ts"
      provides: "Tests de la regle description >= 20 et de la construction des lots"
      min_lines: 40
    - path: "server/api/v1/b2b/requests.post.ts"
      provides: "Intake ecrivant description + lots"
      contains: "b2b_tender_lots"
    - path: "server/api/v1/admin/b2b-requests/[id].patch.ts"
      provides: "Qualification DirCo : statut decision + code postal"
      contains: "project_postal_code"
  key_links:
    - from: "server/api/v1/b2b/requests.post.ts"
      to: "server/utils/b2bTender.ts"
      via: "import du schema Zod et de buildTenderLots"
      pattern: "utils/b2bTender"
    - from: "server/api/v1/b2b/requests.post.ts"
      to: "table b2b_tender_lots"
      via: "supabase.from('b2b_tender_lots').insert(lots)"
      pattern: "from\\('b2b_tender_lots'\\)"
---

<objective>
Câbler le backend sur le socle posé par 07-01 : description obligatoire à l'intake
(TEND-01), création des lots multi-métiers au dépôt syndic (TEND-05), et saisie du
statut de décision + code postal structuré à la qualification DirCo (TEND-02).

Purpose: c'est la couche qui remplit réellement les colonnes — sans elle la
migration reste du schéma mort.
Output: 1 util testé, 2 endpoints étendus, 1 fichier de tests unitaires.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-formulaire-ao-mod-le-multi-lots/07-CONTEXT.md
@server/api/v1/b2b/requests.post.ts
@app/types/b2b.ts

<interfaces>
Référence exacte de la contrainte description à répliquer
(`server/api/v1/projects.post.ts:36`) :
```ts
description: z.string().min(20, 'La description doit faire au moins 20 caractères.'),
```
Référence exacte de la contrainte code postal (`server/api/v1/projects.post.ts:38`) :
```ts
postal_code: z.string().regex(/^\d{5}$/, 'Le code postal doit comporter 5 chiffres.'),
```

Piège du tunnel à connaître avant d'écrire le schéma : `app/pages/b2b/partenaires.vue`
saute l'étape 3 quand `need_type === 'partenariat_regulier'`
(`nextStep()` : `step 2 -> step 4`). L'étape 3 porte la description et le sélecteur
de lots. Rendre `description` inconditionnellement obligatoire casserait donc le
parcours « partenariat régulier » existant.

Style de test attendu (import d'un util pur, pas de handler Nitro) :
voir `tests/unit/qualify-score.test.ts`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Util b2bTender — schéma Zod d'intake + construction des lots</name>
  <files>server/utils/b2bTender.ts, tests/unit/b2b-tender.test.ts</files>
  <read_first>
    - server/api/v1/b2b/requests.post.ts (schéma Zod actuel, lignes 5-23 — à déplacer tel quel puis étendre)
    - server/api/v1/projects.post.ts (lignes 30-45 : formulation exacte des messages Zod description/postal_code)
    - tests/unit/qualify-score.test.ts (style de test unitaire du repo)
    - server/utils/qualifyScore.ts (style d'un util serveur pur du repo)
  </read_first>
  <behavior>
    - `b2bRequestSchema.safeParse({...need_type:'projet_immediat', description:'court'})` -> `success === false`
    - `b2bRequestSchema.safeParse({...need_type:'projet_immediat', description: 20+ caracteres})` -> `success === true`
    - `b2bRequestSchema.safeParse({...need_type:'partenariat_regulier'})` sans description -> `success === true`
    - `b2bRequestSchema.safeParse({...lots_categories:['plomberie','inexistant']})` -> `success === false`
    - `buildTenderLots('req-1','syndic',['toiture','toiture','electricite'])` -> 2 lignes dedupliquees, dans l'ordre `toiture` puis `electricite`
    - `buildTenderLots('req-1','architecte',['toiture'])` -> `[]` (selecteur multi-lots reserve au syndic en Phase 7)
    - `buildTenderLots('req-1','syndic',null)` -> `[]`
  </behavior>
  <action>
Créer `server/utils/b2bTender.ts` :

```ts
import { z } from 'zod'

// Phase 7 — TEND-01 / TEND-05.
// Schéma extrait de server/api/v1/b2b/requests.post.ts pour être testable sans
// charger le handler Nitro (#supabase/server n'est pas résolvable sous vitest).

export const B2B_LOT_CATEGORIES = ['maconnerie', 'toiture', 'electricite', 'plomberie', 'peinture', 'isolation'] as const
export type B2bLotCategory = typeof B2B_LOT_CATEGORIES[number]

export const b2bRequestSchema = z.object({
  apporteur_type: z.enum(['architecte', 'bet', 'agence_immo', 'syndic', 'diagnostiqueur', 'autre']),
  need_type: z.enum(['projet_immediat', 'partenariat_regulier']),
  project_location: z.string().max(200).optional(),
  // TEND-01 — même contrainte que le formulaire particulier (projects.post.ts).
  description: z.string().max(5000).optional(),
  budget_range: z.enum(['<30k', '30-100k', '100-300k', '>300k']).optional(),
  certification_number: z.string().max(50).optional().nullable(),
  travaux_suggeres: z.array(z.enum(['isolation', 'chauffage', 'electricite', 'toiture'])).max(4).optional().nullable(),
  // TEND-05 — corps de métier demandés, 1 lot chacun.
  lots_categories: z.array(z.enum(B2B_LOT_CATEGORIES)).max(6).optional().nullable(),
  files: z.array(z.object({
    file_key: z.string(),
    filename: z.string().max(255),
    content_type: z.string(),
    size: z.number().max(52428800), // 50 Mo
  })).max(10).optional().default([]),
  contact_name: z.string().min(2).max(100),
  contact_company: z.string().max(200).optional(),
  contact_phone: z.string().min(8).max(20),
  contact_email: z.string().email(),
  consent_accepted: z.boolean().refine(v => v === true, { message: 'Le consentement est obligatoire.' }),
}).superRefine((data, ctx) => {
  // L'étape 3 du tunnel (description + lots) est sautée pour un partenariat
  // régulier — la description n'est exigée que sur un projet immédiat.
  if (data.need_type !== 'projet_immediat') return
  if (!data.description || data.description.trim().length < 20) {
    ctx.addIssue({
      code: z.ZodIssueCode.custom,
      path: ['description'],
      message: 'La description doit faire au moins 20 caractères.',
    })
  }
})

export type B2bRequestInput = z.infer<typeof b2bRequestSchema>

/**
 * TEND-05 — 1 lot par corps de métier. Réservé au persona syndic en Phase 7
 * (seul persona qui expose le sélecteur multi-lots dans le tunnel).
 * ponytail: dédoublonnage ici plutôt que de compter sur l'erreur
 * UNIQUE (request_id, category) qui ferait échouer l'insert en bloc.
 */
export function buildTenderLots(
  requestId: string,
  apporteurType: string,
  categories: string[] | null | undefined,
): { request_id: string; category: B2bLotCategory }[] {
  if (apporteurType !== 'syndic' || !categories?.length) return []
  const seen = new Set<string>()
  const lots: { request_id: string; category: B2bLotCategory }[] = []
  for (const c of categories) {
    if (!B2B_LOT_CATEGORIES.includes(c as B2bLotCategory) || seen.has(c)) continue
    seen.add(c)
    lots.push({ request_id: requestId, category: c as B2bLotCategory })
  }
  return lots
}
```

Créer `tests/unit/b2b-tender.test.ts` couvrant les 7 comportements listés dans
`<behavior>` : import relatif `../../server/utils/b2bTender`, `describe`/`it`/`expect`
de vitest, un objet `base` valide réutilisé (comme `qualify-score.test.ts`) contenant
au minimum `apporteur_type`, `need_type`, `contact_name`, `contact_phone`,
`contact_email`, `consent_accepted: true`.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/b2b-tender.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/unit/b2b-tender.test.ts` : tous les tests passent, au moins 7 blocs `it(`
    - `grep -q "superRefine" server/utils/b2bTender.ts` passe
    - `grep -q "export function buildTenderLots" server/utils/b2bTender.ts` passe
    - `grep -q "export const B2B_LOT_CATEGORIES" server/utils/b2bTender.ts` passe
    - `grep -c "#supabase/server" server/utils/b2bTender.ts` retourne 0 (util pur, testable)
    - `npx vitest run` (suite complète) reste vert
  </acceptance_criteria>
  <done>`b2bTender.ts` valide la description >= 20 sur projet immédiat uniquement et construit les lots syndic dédoublonnés ; les tests passent.</done>
</task>

<task type="auto">
  <name>Task 2: Intake public — persister description + lots multi-métiers</name>
  <files>server/api/v1/b2b/requests.post.ts</files>
  <read_first>
    - server/api/v1/b2b/requests.post.ts (fichier modifié — schéma inline à retirer, insert à étendre, email interne à compléter)
    - server/utils/b2bTender.ts (créé en Task 1 : schéma + buildTenderLots)
  </read_first>
  <action>
Dans `server/api/v1/b2b/requests.post.ts` :

1. Supprimer le bloc `const schema = z.object({...})` (lignes 5-23) et l'import `zod`
   devenu inutile. Ajouter à la place :
   ```ts
   import { b2bRequestSchema, buildTenderLots } from '../../../utils/b2bTender'
   ```
   (même profondeur relative que l'import existant `from '../../../utils/email'`).
2. Remplacer `schema.safeParse(body)` par `b2bRequestSchema.safeParse(body)`.
3. Dans l'`insert` sur `b2b_requests`, ajouter juste après `project_location` :
   ```ts
   description: data.description?.trim() || null,
   ```
   Ne PAS écrire `decision_status` ici : il vaut `en_attente` par défaut en base et
   n'est saisi qu'à la qualification DirCo (TEND-02, décision 07-CONTEXT.md).
4. Juste après le `if (error) { throw ... }` qui suit l'insert, ajouter :
   ```ts
   // TEND-05 — 1 lot par corps de métier (syndic). Non bloquant : la demande est
   // déjà enregistrée, une erreur ici ne doit pas faire perdre le dossier au
   // partenaire. ponytail: log console suffisant au pilote, à remonter dans
   // audit_logs si le volume B2B le justifie.
   const lots = buildTenderLots(request.id, data.apporteur_type, data.lots_categories)
   if (lots.length > 0) {
     const { error: lotsError } = await supabase.from('b2b_tender_lots').insert(lots)
     if (lotsError) console.error('[b2b] échec insertion des lots', request.id, lotsError.message)
   }
   ```
5. Dans le HTML de l'email interne (bloc `if (notifyEmails.length > 0)`), ajouter deux
   lignes de tableau dans le style exact des lignes existantes, juste avant la ligne
   « Contact » :
   ```
   ${data.description ? `<tr><td style="padding: 8px 0; color: #64748B;">Description</td><td style="padding: 8px 0; color: #0F172A;">${data.description}</td></tr>` : ''}
   ${lots.length ? `<tr><td style="padding: 8px 0; color: #64748B;">Lots (corps de métier)</td><td style="padding: 8px 0; color: #0F172A;">${lots.map(l => l.category).join(', ')}</td></tr>` : ''}
   ```

Ne PAS toucher au bloc `consents.insert` (bug corrigé en 05.17, laisser tel quel).
Ne PAS déclencher de diffusion/matching/`matchZone` ici — c'est la Phase 8.
  </action>
  <verify>
    <automated>grep -q "b2bRequestSchema" server/api/v1/b2b/requests.post.ts &amp;&amp; grep -q "b2b_tender_lots" server/api/v1/b2b/requests.post.ts &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "const schema = z.object" server/api/v1/b2b/requests.post.ts` retourne 0
    - `grep -q "import { b2bRequestSchema, buildTenderLots }" server/api/v1/b2b/requests.post.ts` passe
    - `grep -q "description: data.description" server/api/v1/b2b/requests.post.ts` passe
    - `grep -q "from('b2b_tender_lots')" server/api/v1/b2b/requests.post.ts` passe
    - `grep -c "decision_status" server/api/v1/b2b/requests.post.ts` retourne 0
    - `grep -c "matchZone\|broadcast" server/api/v1/b2b/requests.post.ts` retourne 0 (scope Phase 8)
    - `npm run build` réussit
  </acceptance_criteria>
  <done>L'intake persiste la description et crée 1 ligne `b2b_tender_lots` par corps de métier sélectionné par un syndic.</done>
</task>

<task type="auto">
  <name>Task 3: Qualification DirCo — statut de décision + code postal structuré</name>
  <files>server/api/v1/admin/b2b-requests/[id].patch.ts</files>
  <read_first>
    - server/api/v1/admin/b2b-requests/[id].patch.ts (fichier modifié — schéma Zod, select de retour, auditMeta)
    - server/api/v1/projects.post.ts (ligne 38 : regex code postal de référence)
  </read_first>
  <action>
Dans `server/api/v1/admin/b2b-requests/[id].patch.ts` :

1. Ajouter sous `const DATE_RE = /^\d{4}-\d{2}-\d{2}$/` :
   ```ts
   const POSTAL_RE = /^\d{5}$/
   const DECISION_STATUS = ['confirme', 'en_attente'] as const
   ```
2. Ajouter dans `const schema = z.object({ ... })`, après `recommended_pros` :
   ```ts
   // Phase 7 — TEND-02 / support TEND-04
   decision_status: z.enum(DECISION_STATUS).optional(),
   project_postal_code: z.string().regex(POSTAL_RE, 'Le code postal doit comporter 5 chiffres.').nullable().optional(),
   ```
3. Remplacer la liste `.select(...)` de l'update par exactement :
   ```ts
   .select('id, status, assigned_to, notes, description, decision_status, project_postal_code, qualifications_requises, planning_start, planning_end, recommended_pros, updated_at')
   ```
   (`description` est renvoyée en lecture seule pour que l'UI admin la réaffiche après
   sauvegarde ; elle n'est pas modifiable par cet endpoint.)
4. Ajouter dans `auditMeta`, après la ligne `recommended_pros` :
   ```ts
   if (changes.decision_status) auditMeta.decision_status = changes.decision_status
   if ('project_postal_code' in changes) auditMeta.postal_code = true
   ```

Ne PAS ajouter `description` au schéma Zod d'update : la description est la parole du
partenaire, elle n'est pas réécrite par le DirCo.
  </action>
  <verify>
    <automated>grep -q "decision_status: z.enum(DECISION_STATUS)" "server/api/v1/admin/b2b-requests/[id].patch.ts" &amp;&amp; grep -q "POSTAL_RE" "server/api/v1/admin/b2b-requests/[id].patch.ts" &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "const POSTAL_RE = /\^\\\\d{5}\$/" "server/api/v1/admin/b2b-requests/[id].patch.ts"` — la regex `/^\d{5}$/` est présente
    - `grep -q "decision_status: z.enum(DECISION_STATUS).optional()" "server/api/v1/admin/b2b-requests/[id].patch.ts"` passe
    - Le `.select(...)` de l'update contient `description, decision_status, project_postal_code`
    - `grep -q "auditMeta.decision_status" "server/api/v1/admin/b2b-requests/[id].patch.ts"` passe
    - `grep -c "description: z" "server/api/v1/admin/b2b-requests/[id].patch.ts"` retourne 0 (description non modifiable)
    - `npm run build` réussit
  </acceptance_criteria>
  <done>Le PATCH admin accepte et persiste `decision_status` (2 valeurs) et `project_postal_code` (5 chiffres), et les renvoie dans sa réponse.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet anonyme -> `POST /api/v1/b2b/requests` | Intake public sans authentification, écrit en base via service role |
| Admin authentifié -> `PATCH /api/v1/admin/b2b-requests/[id]` | Mutation de dossier, gate `app_metadata.role === 'admin'` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06 | Tampering | `lots_categories` (intake public) | mitigate | `z.enum(B2B_LOT_CATEGORIES)` + `.max(6)` côté Zod, `buildTenderLots` refiltre sur la même liste, et CHECK SQL en base (07-01) — 3 couches, aucune catégorie arbitraire ne peut créer un lot |
| T-07-07 | Elevation of privilege | création de lots pour un persona non syndic | mitigate | `buildTenderLots` retourne `[]` si `apporteurType !== 'syndic'` — le champ est ignoré, pas rejeté (pas de fuite d'information sur la règle) |
| T-07-08 | Denial of service | payload description géant | mitigate | `z.string().max(5000)` sur `description` (le tunnel pose aussi `maxlength="5000"`, cf. plan 03) |
| T-07-09 | Elevation of privilege | PATCH admin appelé par un non-admin | accept | Gate existante inchangée (`serverSupabaseUser` + `app_metadata.role === 'admin'`, lignes 22-26) — auditée en 05.10-06, aucune modification apportée ici |
| T-07-10 | Information disclosure | `description` exposée à un artisan non habilité | accept | Aucun endpoint de lecture pro n'est créé dans cette phase ; la lecture côté artisan et son gate arrivent en Phase 9 (TEND-07/TEND-08) |
| T-07-11 | Tampering | `decision_status` forcé par le partenaire à l'intake | mitigate | Le champ est absent du schéma d'intake ; la valeur vient du défaut SQL `en_attente` et n'est modifiable que par l'endpoint admin |
</threat_model>

<verification>
- `npx vitest run` : suite complète verte, dont `tests/unit/b2b-tender.test.ts`
- `npm run build` réussit
- `grep -q "from('b2b_tender_lots')" server/api/v1/b2b/requests.post.ts`
</verification>

<success_criteria>
- Un projet immédiat sans description >= 20 caractères est refusé en 400
- Un partenariat régulier reste acceptable sans description (aucune régression du tunnel existant)
- Un dépôt syndic avec N corps de métier crée N lignes `b2b_tender_lots`
- Le PATCH admin persiste `decision_status` et `project_postal_code` et refuse un CP hors format
</success_criteria>

<output>
Après complétion, créer `.planning/phases/07-formulaire-ao-mod-le-multi-lots/07-02-SUMMARY.md`
</output>
