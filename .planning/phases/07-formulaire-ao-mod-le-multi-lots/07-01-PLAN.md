---
phase: 07-formulaire-ao-mod-le-multi-lots
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260904000000_phase7_tender_lots.sql
  - app/types/b2b.ts
  - app/types/database.types.ts
autonomous: true
requirements: [TEND-01, TEND-02, TEND-05]
must_haves:
  truths:
    - "La table b2b_tender_lots existe en base et accepte 1 ligne par (request_id, category)"
    - "b2b_requests porte une colonne description, une colonne decision_status et une colonne project_postal_code"
    - "Le front dispose des types et de la liste des corps de métier pour construire le sélecteur multi-lots"
  artifacts:
    - path: "supabase/migrations/20260904000000_phase7_tender_lots.sql"
      provides: "Migration multi-lots + colonnes description/statut/CP"
      contains: "CREATE TABLE IF NOT EXISTS b2b_tender_lots"
    - path: "app/types/b2b.ts"
      provides: "Types B2bDecisionStatus, B2bTenderLot, LOT_CATEGORY_OPTIONS"
      contains: "LOT_CATEGORY_OPTIONS"
    - path: "app/types/database.types.ts"
      provides: "Types Supabase régénérés incluant b2b_tender_lots"
      contains: "b2b_tender_lots"
  key_links:
    - from: "b2b_tender_lots.request_id"
      to: "b2b_requests.id"
      via: "FOREIGN KEY ON DELETE CASCADE"
      pattern: "REFERENCES b2b_requests\\(id\\) ON DELETE CASCADE"
    - from: "b2b_tender_lots.zone_id"
      to: "zones.id"
      via: "FOREIGN KEY (utilisé par matchZone en Phase 8)"
      pattern: "REFERENCES zones\\(id\\)"
---

<objective>
Poser le socle de données de la Phase 7 : la table `b2b_tender_lots` (1 lot par
corps de métier sur un appel d'offres) et les 3 nouvelles colonnes de
`b2b_requests` (`description`, `decision_status`, `project_postal_code`), puis
exposer les types correspondants côté front.

Purpose: tout le reste de la phase (intake, qualification DirCo, UI) écrit ou lit
ces colonnes. Sans elles, aucun autre plan ne peut être vérifié.
Output: 1 migration appliquée, `app/types/b2b.ts` étendu, `database.types.ts` régénéré.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-formulaire-ao-mod-le-multi-lots/07-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@supabase/migrations/20260822000002_b2b_requests.sql
@supabase/migrations/20260830000001_diagnostiqueur_apporteur.sql
@app/types/b2b.ts

<interfaces>
Vocabulaire de catégories (source de vérité, identique partout dans le repo —
`server/api/v1/pro/claim.post.ts:10`, `server/api/v1/pro/profile/me.patch.ts:5`,
`app/types/admin.ts:59` `CATEGORY_LABELS`) :

```
maconnerie   → 'Maçonnerie & Gros Œuvre'
toiture      → 'Charpente & Toiture'
electricite  → 'Électricité'
plomberie    → 'Plomberie & Chauffage'
peinture     → 'Peinture & Finitions'
isolation    → 'Isolation & Cloisons'
```

`app/types/admin.ts` exporte déjà `CATEGORY_LABELS: Record<string, string>` avec
exactement ces 6 entrées — ne PAS re-dupliquer les libellés, les réutiliser.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration b2b_tender_lots + colonnes description/decision_status/project_postal_code</name>
  <files>supabase/migrations/20260904000000_phase7_tender_lots.sql</files>
  <read_first>
    - supabase/migrations/20260822000002_b2b_requests.sql (schéma actuel de b2b_requests, style des enums/RLS/index)
    - supabase/migrations/20260830000001_diagnostiqueur_apporteur.sql (pattern d'ajout de colonnes persona-spécifiques)
    - supabase/migrations/20260828000002_zones_78_packs.sql (confirmer le nom exact de la table `zones` et de sa PK `id`)
  </read_first>
  <action>
Créer `supabase/migrations/20260904000000_phase7_tender_lots.sql` avec exactement
ce contenu (entête de commentaire dans le style des migrations existantes) :

```sql
-- =====================================================
-- Phase 7 — Formulaire AO & Modèle Multi-Lots
-- TEND-01 (description), TEND-02 (statut décision), TEND-05 (lots)
-- =====================================================

-- TEND-01 : description obligatoire du besoin (contrainte min 20 caractères
-- appliquée côté serveur/Zod, pas en CHECK SQL : les lignes b2b_requests
-- existantes n'ont pas de description et un CHECK les invaliderait).
ALTER TABLE b2b_requests ADD COLUMN IF NOT EXISTS description TEXT;

-- TEND-02 : statut de la décision, saisi par le DirCo à la qualification.
-- TEXT + CHECK plutôt qu'un ENUM : un ENUM Postgres ne peut pas voir ses
-- valeurs retirées/renommées sans recréer le type (cf. friction rencontrée en
-- 05.17 avec b2b_apporteur_type).
ALTER TABLE b2b_requests
    ADD COLUMN IF NOT EXISTS decision_status TEXT NOT NULL DEFAULT 'en_attente';

ALTER TABLE b2b_requests DROP CONSTRAINT IF EXISTS b2b_requests_decision_status_check;
ALTER TABLE b2b_requests
    ADD CONSTRAINT b2b_requests_decision_status_check
    CHECK (decision_status IN ('confirme', 'en_attente'));

-- Support TEND-04 (Phase 8) : code postal structuré exploitable par matchZone().
ALTER TABLE b2b_requests ADD COLUMN IF NOT EXISTS project_postal_code TEXT;

ALTER TABLE b2b_requests DROP CONSTRAINT IF EXISTS b2b_requests_project_postal_code_check;
ALTER TABLE b2b_requests
    ADD CONSTRAINT b2b_requests_project_postal_code_check
    CHECK (project_postal_code IS NULL OR project_postal_code ~ '^\d{5}$');

-- TEND-05 : 1 ligne par corps de métier requis sur un AO.
CREATE TABLE IF NOT EXISTS b2b_tender_lots (
    id         UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    request_id UUID NOT NULL REFERENCES b2b_requests(id) ON DELETE CASCADE,
    category   TEXT NOT NULL,               -- vocabulaire professionals.categories
    zone_id    UUID REFERENCES zones(id),   -- résolu via matchZone() en Phase 8
    status     TEXT NOT NULL DEFAULT 'open',
    created_at TIMESTAMPTZ DEFAULT clock_timestamp(),
    CONSTRAINT b2b_tender_lots_category_check
        CHECK (category IN ('maconnerie', 'toiture', 'electricite', 'plomberie', 'peinture', 'isolation')),
    CONSTRAINT b2b_tender_lots_status_check
        CHECK (status IN ('open', 'claimed', 'closed')),
    CONSTRAINT b2b_tender_lots_request_category_key UNIQUE (request_id, category)
);

CREATE INDEX IF NOT EXISTS idx_b2b_tender_lots_zone_category
    ON b2b_tender_lots(zone_id, category) WHERE status = 'open';

-- RLS : même politique que b2b_requests — aucun accès anonyme/authentifié,
-- tout passe par le service role (intake public + endpoints admin).
ALTER TABLE b2b_tender_lots ENABLE ROW LEVEL SECURITY;

DROP POLICY IF EXISTS "admin_all_b2b_tender_lots" ON b2b_tender_lots;
CREATE POLICY "admin_all_b2b_tender_lots" ON b2b_tender_lots
    FOR ALL
    USING (auth.jwt()->>'role' = 'admin');
```

NE PAS créer `b2b_tender_claims` ni `b2b_tender_notifications` (Phase 8/9,
hors scope — cf. 07-CONTEXT.md).
NE PAS modifier une migration déjà appliquée en remote.
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260904000000_phase7_tender_lots.sql &amp;&amp; grep -q "CREATE TABLE IF NOT EXISTS b2b_tender_lots" supabase/migrations/20260904000000_phase7_tender_lots.sql &amp;&amp; grep -q "b2b_requests_decision_status_check" supabase/migrations/20260904000000_phase7_tender_lots.sql</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ADD COLUMN IF NOT EXISTS" supabase/migrations/20260904000000_phase7_tender_lots.sql` retourne 3
    - Le fichier contient `REFERENCES b2b_requests(id) ON DELETE CASCADE`
    - Le fichier contient `REFERENCES zones(id)`
    - Le fichier contient `UNIQUE (request_id, category)`
    - Le fichier contient `CHECK (decision_status IN ('confirme', 'en_attente'))`
    - Le fichier contient `project_postal_code IS NULL OR project_postal_code ~ '^\d{5}$'`
    - Le fichier contient `ALTER TABLE b2b_tender_lots ENABLE ROW LEVEL SECURITY;`
    - `grep -c "b2b_tender_claims\|b2b_tender_notifications" supabase/migrations/20260904000000_phase7_tender_lots.sql` retourne 0
  </acceptance_criteria>
  <done>La migration existe, crée `b2b_tender_lots` avec RLS activé et ajoute les 3 colonnes à `b2b_requests`.</done>
</task>

<task type="auto">
  <name>Task 2: Types front — statut décision, lot, options corps de métier</name>
  <files>app/types/b2b.ts</files>
  <read_first>
    - app/types/b2b.ts (fichier modifié — voir le style de `TRAVAUX_OPTIONS` et `BUDGET_OPTIONS`)
    - app/types/admin.ts (lignes 59-66, `CATEGORY_LABELS` à réutiliser tel quel)
  </read_first>
  <action>
Dans `app/types/b2b.ts` :

1. Ajouter en haut du fichier : `import { CATEGORY_LABELS } from '~/types/admin'`
2. Ajouter les types, après la ligne `export type B2bRequestStatus = ...` :
```ts
export type B2bDecisionStatus = 'confirme' | 'en_attente'
export type B2bLotCategory = 'maconnerie' | 'toiture' | 'electricite' | 'plomberie' | 'peinture' | 'isolation'
export type B2bTenderLotStatus = 'open' | 'claimed' | 'closed'

export interface B2bTenderLot {
  id: string
  request_id: string
  category: B2bLotCategory
  zone_id: string | null
  status: B2bTenderLotStatus
  created_at: string
}
```
3. Ajouter dans l'interface `B2bRequest` (après `travaux_suggeres`) :
```ts
  description: string | null
  decision_status: B2bDecisionStatus
  project_postal_code: string | null
```
4. Ajouter en bas du fichier, à côté de `TRAVAUX_OPTIONS` :
```ts
// Corps de métier sélectionnables comme lots (TEND-05) — réutilise le
// vocabulaire unique de professionals.categories, pas de libellés dupliqués.
export const LOT_CATEGORY_OPTIONS: { value: B2bLotCategory; label: string }[] =
  (Object.keys(CATEGORY_LABELS) as B2bLotCategory[]).map(value => ({
    value,
    label: CATEGORY_LABELS[value] as string,
  }))

export const DECISION_STATUS_LABELS: Record<B2bDecisionStatus, string> = {
  confirme: 'Confirmé — travaux décidés et budgétés',
  en_attente: 'En attente de décision — devis à comparer avant validation (ex. avant AG)',
}
```
NE PAS re-déclarer les 6 libellés de catégories en dur.
  </action>
  <verify>
    <automated>grep -q "LOT_CATEGORY_OPTIONS" app/types/b2b.ts &amp;&amp; grep -q "CATEGORY_LABELS" app/types/b2b.ts &amp;&amp; grep -q "B2bDecisionStatus" app/types/b2b.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "import { CATEGORY_LABELS } from '~/types/admin'" app/types/b2b.ts` passe
    - `grep -c "Maçonnerie & Gros Œuvre" app/types/b2b.ts` retourne 0 (aucune duplication de libellé)
    - `app/types/b2b.ts` contient `export type B2bDecisionStatus = 'confirme' | 'en_attente'`
    - `app/types/b2b.ts` contient `export interface B2bTenderLot`
    - `app/types/b2b.ts` contient les 3 champs `description`, `decision_status`, `project_postal_code` dans `interface B2bRequest`
    - `app/types/b2b.ts` contient `DECISION_STATUS_LABELS`
  </acceptance_criteria>
  <done>`LOT_CATEGORY_OPTIONS` expose les 6 corps de métier dérivés de `CATEGORY_LABELS` ; `B2bRequest` porte les 3 nouveaux champs.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Appliquer la migration et régénérer les types Supabase</name>
  <files>app/types/database.types.ts</files>
  <read_first>
    - supabase/migrations/20260904000000_phase7_tender_lots.sql (la migration à pousser)
    - .planning/STATE.md (section « Known Patterns » → commandes exactes push + gen types, projet lié xpwoczcbyamnjknloxgz)
  </read_first>
  <action>
[BLOCKING] Sans cette tâche, le build et les tests passent alors que la base ne
porte pas les colonnes — faux positif de vérification.

1. Appliquer la migration :
```bash
npx supabase db push --yes
```
Si la CLI réclame une authentification en non-TTY, exporter `SUPABASE_ACCESS_TOKEN`
avant de relancer.

2. Régénérer les types (fichier committé) :
```bash
npx supabase gen types typescript --project-id xpwoczcbyamnjknloxgz --schema public > app/types/database.types.ts
```

3. Vérifier que `b2b_tender_lots` et les 3 colonnes apparaissent dans le fichier généré.

Ne jamais rééditer à la main `app/types/database.types.ts`.
  </action>
  <verify>
    <automated>grep -q "b2b_tender_lots" app/types/database.types.ts &amp;&amp; grep -q "decision_status" app/types/database.types.ts &amp;&amp; grep -q "project_postal_code" app/types/database.types.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx supabase db push --yes` se termine sans erreur (ou signale « no changes » si déjà appliquée)
    - `grep -c "b2b_tender_lots" app/types/database.types.ts` est ≥ 3 (Row / Insert / Update)
    - `grep -q "project_postal_code" app/types/database.types.ts` passe
    - `grep -q "decision_status" app/types/database.types.ts` passe
    - `git diff --name-only` liste `app/types/database.types.ts`
  </acceptance_criteria>
  <done>La base porte `b2b_tender_lots` + les 3 colonnes, et `database.types.ts` régénéré le reflète.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tunnel public anonyme → `b2b_requests` / `b2b_tender_lots` | Insert non authentifié via service role |
| client authentifié → Postgres (PostgREST) | Un pro connecté ne doit pas lire les AO avant la Phase 9 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information disclosure | `b2b_tender_lots` (nouvelle table) | mitigate | `ENABLE ROW LEVEL SECURITY` + unique policy `auth.jwt()->>'role' = 'admin'` — aucun rôle `anon`/`authenticated` ne lit la table ; le seul accès est le service role côté Nitro (miroir exact de `b2b_requests`) |
| T-07-02 | Tampering | `b2b_requests.decision_status` | mitigate | CHECK SQL `IN ('confirme','en_attente')` — une valeur arbitraire est rejetée en base même si un appelant contourne Zod |
| T-07-03 | Tampering | `b2b_requests.project_postal_code` | mitigate | CHECK SQL `~ '^\d{5}$'` — aligné sur la règle Zod (double couche, cf. CLAUDE.md) |
| T-07-04 | Denial of service | insertion massive de lots sur un même AO | mitigate | `UNIQUE (request_id, category)` + `CHECK category IN (...)` plafonnent structurellement à 6 lots par AO |
| T-07-05 | Repudiation | traçabilité des lots | accept | `created_at` suffit au pilote ; l'audit log des AO existe déjà au niveau `b2b_requests` (`b2b_request_updated`) |
</threat_model>

<verification>
- `npx supabase db push --yes` appliqué sans erreur
- `grep -q "b2b_tender_lots" app/types/database.types.ts`
- `npx vitest run` reste vert (aucun test existant cassé)
</verification>

<success_criteria>
- `b2b_tender_lots` existe en base avec RLS activé et `UNIQUE (request_id, category)`
- `b2b_requests` porte `description`, `decision_status` (défaut `en_attente`), `project_postal_code`
- `app/types/b2b.ts` expose `LOT_CATEGORY_OPTIONS` (6 entrées), `B2bDecisionStatus`, `B2bTenderLot`
- `app/types/database.types.ts` régénéré et committé
</success_criteria>

<output>
Après complétion, créer `.planning/phases/07-formulaire-ao-mod-le-multi-lots/07-01-SUMMARY.md`
</output>
