---
phase: 06.3-notifications-email-transactionnelles
plan: 01
subsystem: notifications
tags: [email, resend, nuxt-runtime-config, vitest]

requires: []
provides:
  - "renderEmail() : layout HTML unique brandé copper/navy avec footer légal LCEN, pur (testable node)"
  - "resolveSender() : choix explicite de l'expéditeur no-reply@/notifications@/contact@, rétrocompatible"
  - "sendEmail() étendu : sender + replyTo (reply_to Resend), mock dev logue le from"
  - "notifyAdmin() : alerte admin centralisée vers NUXT_ADMIN_EMAIL, jamais bloquante"
  - "adminDetailsTable() : helper de rendu tableau clé/valeur pour les alertes admin"
  - "runtimeConfig : emailFromNoReply/Notifications/Contact + cronSecret (pour plan 06.3-02)"
affects: [06.3-02, 06.3-03, 06.3-04]

tech-stack:
  added: []
  patterns:
    - "Fonctions de rendu HTML e-mail pures (aucun import Nuxt) pour rester testables en vitest node"
    - "vi.hoisted + vi.mock('#imports', ...) pour tester les utils server qui appellent useRuntimeConfig()"

key-files:
  created:
    - server/utils/emailLayout.ts
    - server/utils/notifyAdmin.ts
    - tests/unit/email-engine.test.ts
  modified:
    - server/utils/email.ts
    - nuxt.config.ts
    - .env.example

key-decisions:
  - "sendEmail sans sender explicite continue de partir depuis contact@ (rétrocompatibilité stricte, aucun appelant existant modifié dans ce plan)"
  - "renderEmail reste une fonction pure sans accès runtimeConfig pour permettre des tests unitaires rapides en environnement node"

patterns-established:
  - "Tester un util server qui importe #imports : vi.mock('#imports', () => ({ useRuntimeConfig: () => mockConfig })) + vi.hoisted pour les mocks de fonctions référencées dans un factory vi.mock"

requirements-completed: [EML-02, EML-10]

duration: 25min
completed: 2026-08-31
---

# Phase 06.3 Plan 01: Moteur d'e-mailing commun Summary

**Layout HTML e-mail brandé copper/navy avec footer LCEN, expéditeur Resend dynamique (no-reply@/notifications@/contact@) et alerte admin centralisée, tous couverts par tests unitaires.**

## Performance

- **Duration:** ~25 min
- **Started:** 2026-08-31T17:03:00Z (estimation)
- **Completed:** 2026-08-31T17:28:58Z
- **Tasks:** 3
- **Files modified:** 6 (3 créés, 3 modifiés)

## Accomplishments
- Layout HTML unique (`renderEmail`) : bandeau marque copper, titre, intro, CTA optionnel, preheader masqué, footer légal LCEN (art. L34-5) — fonction pure, aucune dépendance Nuxt
- `sendEmail` accepte désormais un `sender` explicite (`no-reply` / `notifications` / `contact`) et un `replyTo`, sans casser aucun appelant existant (défaut `contact@` inchangé)
- `notifyAdmin` centralisé : une alerte = un appel, jamais bloquant, no-op silencieux si `NUXT_ADMIN_EMAIL` absent
- `runtimeConfig` et `.env.example` étendus (3 expéditeurs dédiés + `cronSecret` pour le plan 06.3-02)

## Task Commits

Each task was committed atomically:

1. **Task 1: Layout HTML unique + footer LCEN** - `76de473` (feat)
2. **Task 2: sendEmail multi-expéditeurs + runtimeConfig + .env.example** - `77c37a6` (feat)
3. **Task 3: notifyAdmin centralisé** - `eb595e8` (feat)

**Plan metadata:** (commit à suivre — SUMMARY + planning)

_Note: tâches TDD exécutées en un seul commit par tâche (tests + implémentation écrits ensemble, vérifiés avant commit)._

## Files Created/Modified
- `server/utils/emailLayout.ts` - `renderEmail()` : layout HTML unique, pur, brandé copper/navy, footer LCEN
- `server/utils/notifyAdmin.ts` - `notifyAdmin()` + `adminDetailsTable()` : alerte admin centralisée
- `server/utils/email.ts` - `resolveSender()` + `sendEmail()` étendu (sender, replyTo, reply_to Resend)
- `nuxt.config.ts` - `emailFromNoReply`, `emailFromNotifications`, `emailFromContact`, `cronSecret`
- `.env.example` - documentation des 3 expéditeurs dédiés + `CRON_SECRET`
- `tests/unit/email-engine.test.ts` - 15 tests (renderEmail, resolveSender, notifyAdmin, adminDetailsTable)

## Decisions Made
- Aucun appelant existant de `sendEmail` n'a été touché dans ce plan (migration prévue aux plans 02/03/04) — vérifié par `grep -rn "sendEmail(" server/` (11 appels, inchangé).
- Le mock `#imports` dans les tests réutilise `vi.hoisted` pour éviter le piège classique de hoisting `vi.mock` (variable référencée avant déclaration).

## Deviations from Plan

None - plan exécuté tel qu'écrit.

## Issues Encountered
- `npx vitest run` complet a révélé 3 échecs pré-existants dans `tests/auto-approve.test.ts` (`supabase.from(...).select is not a function`), confirmés indépendants de ce plan par `git stash` (échouent aussi sur la base avant les changements) — hors périmètre, non corrigés.
- `npx nuxt build` échoue dans cet environnement d'exécution (`trustedFunctions.difference is not a function`) : incompatibilité Node 20 vs Node 22 requis par ce projet (`Set.prototype.difference`, disponible seulement depuis Node 22) — environnement local, pas une régression du code de ce plan.

## User Setup Required

**Configuration DNS/Resend nécessaire avant activation en production** (non bloquant pour le code, cf. frontmatter `user_setup` du plan) :
- Cloudflare DNS `bati-axe.com` : DKIM (CNAME/TXT fournis par Resend), SPF (`v=spf1 include:amazonses.com ~all`), DMARC (`p=none`), Email Routing `contact@` → Gmail admin
- `RESEND_API_KEY` (Resend Dashboard → API Keys)

## Next Phase Readiness
- `renderEmail`, `resolveSender`, `notifyAdmin`, `adminDetailsTable` disponibles pour les plans 06.3-02 (Pros), 06.3-03 (Particuliers/B2B), 06.3-04 (Stripe/Admin) — aucune duplication attendue dans les endpoints.
- Aucun risque de régression : rétrocompatibilité stricte vérifiée par grep + tests.

---
*Phase: 06.3-notifications-email-transactionnelles*
*Completed: 2026-08-31*

## Self-Check: PASSED
All created files verified present; all 3 task commits (76de473, 77c37a6, eb595e8) verified in `git log`.
