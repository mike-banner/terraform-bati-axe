---
phase: 06.3-notifications-email-transactionnelles
plan: 04
subsystem: notifications
tags: [email, stripe-webhook, notifyAdmin, vitest]

requires: [06.3-01]
provides:
  - "handleStripeEvent.ts envoie une confirmation e-mail sur souscription/retrait de zone/changement de billing (jamais bloquant)"
  - "claim.post.ts : alerte SIRET migrée vers notifyAdmin (moteur commun 06.3-01)"
  - "claim.post.ts : alerte admin systématique à chaque nouvelle inscription pro"
affects: []

tech-stack:
  added: []
  patterns:
    - "sendBillingEmail : wrapper try/catch dédié pour tout envoi déclenché depuis un webhook Stripe, garantit une réponse 200 même en cas d'échec Resend"
    - "Mock Supabase chaînable réutilisable en test : chain thenable {eq, neq, then} pour update(), select().eq().maybeSingle() paramétrable par table"

key-files:
  created: []
  modified:
    - server/utils/handleStripeEvent.ts
    - server/api/v1/pro/claim.post.ts
    - tests/unit/stripe-webhook.test.ts

key-decisions:
  - "customer.subscription.deleted et invoice.payment_failed restent sans e-mail applicatif (Stripe envoie déjà ses relances Dashboard, même logique que trial_will_end/D-08)"
  - "Alerte 'nouvelle inscription' placée après l'upsert professionals (données complètes disponibles), distincte et non fusionnée avec l'alerte SIRET conditionnelle placée plus haut dans le flux"

requirements-completed: [EML-06, EML-10]

duration: 35min
completed: 2026-08-31
---

# Phase 06.3 Plan 04: Confirmations Stripe & Alertes Admin Summary

**handleStripeEvent envoie désormais 3 confirmations e-mail de facturation (souscription active, retrait de zone effectif, changement mensuel/annuel) sans jamais bloquer le webhook Stripe ; l'alerte admin SIRET du claim pro migre vers notifyAdmin et une nouvelle alerte systématique couvre toute inscription.**

## Performance

- **Duration:** ~35 min
- **Started:** 2026-08-31T17:02:00Z (estimation)
- **Completed:** 2026-08-31T17:36:51Z
- **Tasks:** 3
- **Files modified:** 3

## Accomplishments
- `checkout.session.completed` avec `zone_id`/`billing` déclenche un e-mail « abonnement actif » depuis `notifications@`
- `customer.subscription.updated` déclenche « retrait de zone effectif » (via `removalApplied`) ou « changement de facturation effectif » (via `newBilling !== previousBilling`), jamais les deux en même temps, jamais au rejeu d'un même événement
- Tout envoi encapsulé dans `sendBillingEmail` : une exception Resend est loguée et avalée, le webhook Stripe répond toujours 200
- Alerte admin SIRET du claim pro (05.15) migrée du HTML inline + try/catch local vers `notifyAdmin`/`adminDetailsTable`
- Nouvelle alerte admin systématique à chaque inscription pro (indépendante du statut SIRET), comblant le trou identifié à l'audit du 2026-08-30

## Task Commits

Each task was committed atomically:

1. **Task 1: Confirmations e-mail de facturation dans handleStripeEvent** - `d5e0b8f` (feat)
2. **Task 2: Migrer l'alerte admin SIRET du claim vers notifyAdmin** - `b42a68a` (feat)
3. **Task 3: Alerte admin — nouvelle inscription pro** - `e4a33b8` (feat)

## Files Created/Modified
- `server/utils/handleStripeEvent.ts` - `sendBillingEmail`, `resolveProEmail`, `resolveZoneName` + 3 déclenchements + import `calculateZonePrice` (util déjà appelé dans le fichier mais jamais importé — bug latent non détecté faute de test exerçant ce chemin, corrigé au passage)
- `server/api/v1/pro/claim.post.ts` - alerte SIRET via `notifyAdmin` (remplace HTML inline), nouvelle alerte inscription systématique après l'upsert `professionals`
- `tests/unit/stripe-webhook.test.ts` - mock Supabase chaînable réécrit (`select().eq().maybeSingle()` paramétrable, `update()/eq()/neq()` thenable, `upsert()`), 10 tests (4 existants + 6 nouveaux couvrant les 6 cas du plan)

## Decisions Made
- Le mock `#imports` du test webhook reprend le pattern établi par `email-engine.test.ts` (plan 06.3-01) : `vi.mock('#imports', () => ({ useRuntimeConfig: () => ({...}) }))`.
- Le mock Supabase original (`eq` unique, non chaînable) ne supportait ni `.eq().eq()` ni `.eq().neq()` ni `.upsert()` — nécessaire dès qu'on exerce le chemin `removalApplied` ou `checkout.session.completed` avec `zone_id`. Reconstruit en objet chaînable thenable partagé par table.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking issue] Import manquant de `calculateZonePrice` dans `handleStripeEvent.ts`**
- **Found during:** Task 1, écriture du test « checkout.session.completed avec zone_id + billing »
- **Issue:** `calculateZonePrice` était appelé sans import (comptait sur l'auto-import Nitro, absent en environnement vitest). Aucun test existant n'exerçait ce chemin (`zone_id`/`billing` absents des metadata testées), donc le bug était invisible jusqu'ici.
- **Fix:** `import { calculateZonePrice } from './zoneMatcher'` ajouté en tête de fichier.
- **Files modified:** `server/utils/handleStripeEvent.ts`
- **Commit:** `d5e0b8f`

**2. [Rule 3 - Blocking issue] Rebase de worktree sur le mauvais commit de base**
- **Found during:** Vérification de branche initiale
- **Issue:** Le worktree était basé sur une chaîne de commits CI/Terraform sans rapport avec la phase 06.3 (`merge-base` != commit cible fourni). `git rebase --onto` a échoué silencieusement (`|| true`).
- **Fix:** `git reset --hard` vers le commit cible `685cdf9` (working tree vérifié propre avant l'opération).
- **Files modified:** aucun (opération git uniquement)
- **Commit:** n/a (pré-exécution)

## Issues Encountered
- `npx vitest run` complet révèle toujours les 3 échecs pré-existants dans `tests/auto-approve.test.ts` (`supabase.from(...).select is not a function`), déjà documentés comme hors périmètre dans `06.3-01-SUMMARY.md` — non corrigés, aucun lien avec ce plan.

## Next Phase Readiness
- Les 4 plans de la phase 06.3 sont exécutés (01 moteur, 02/03 non lus dans ce contexte car dépendance uniquement sur 01, 04 Stripe/Admin).
- `handleStripeEvent.ts` couvre désormais les 3 transitions de facturation de la Phase 05.16 avec confirmations e-mail ; `claim.post.ts` n'a plus de HTML inline pour les alertes admin.

---
*Phase: 06.3-notifications-email-transactionnelles*
*Completed: 2026-08-31*

## Self-Check: PASSED
All modified files verified present (`server/utils/handleStripeEvent.ts`, `server/api/v1/pro/claim.post.ts`, `tests/unit/stripe-webhook.test.ts`); all 3 task commits (`d5e0b8f`, `b42a68a`, `e4a33b8`) verified in `git log`.
