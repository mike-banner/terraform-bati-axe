---
phase: 06.3-notifications-email-transactionnelles
plan: 02
type: execute
wave: 2
depends_on: ["06.3-01"]
files_modified:
  - server/api/v1/admin/verify.post.ts
  - server/utils/notifyProLead.ts
  - server/api/v1/cron/decennale-alerts.get.ts
  - supabase/migrations/20260830000000_decennale_alerts.sql
  - .github/workflows/cron-decennale-alerts.yml
  - tests/unit/decennale-alerts.test.ts
autonomous: true
requirements: [EML-03, EML-04, EML-05]

must_haves:
  truths:
    - "Un pro dont l'attestation décennale est validée par un admin reçoit un e-mail de confirmation"
    - "Un pro dont un document est rejeté reçoit un e-mail expliquant le motif du rejet"
    - "Un pro reçoit une alerte 30 jours puis 7 jours avant l'expiration de sa décennale"
    - "Une même alerte J-30 ou J-7 n'est jamais envoyée deux fois pour le même document"
    - "Les alertes leads partent de notifications@ et gardent leur idempotence lead_notifications"
  artifacts:
    - path: "server/api/v1/cron/decennale-alerts.get.ts"
      provides: "Endpoint cron J-30/J-7 protégé par CRON_SECRET"
      exports: ["default"]
    - path: "supabase/migrations/20260830000000_decennale_alerts.sql"
      provides: "Colonnes d'idempotence alert_j30_sent_at / alert_j7_sent_at"
      contains: "alert_j30_sent_at"
    - path: ".github/workflows/cron-decennale-alerts.yml"
      provides: "Déclencheur quotidien du cron HTTP"
    - path: "server/api/v1/admin/verify.post.ts"
      provides: "E-mail validation/rejet document au pro"
  key_links:
    - from: "server/api/v1/admin/verify.post.ts"
      to: "server/utils/email.ts"
      via: "sendEmail sender 'contact'"
      pattern: "sender: 'contact'"
    - from: "server/api/v1/cron/decennale-alerts.get.ts"
      to: "documents_artisan"
      via: "requête Supabase service role"
      pattern: "from\\('documents_artisan'\\)"
    - from: "server/utils/notifyProLead.ts"
      to: "server/utils/email.ts"
      via: "sendEmail sender 'notifications'"
      pattern: "sender: 'notifications'"
---

<objective>
Couvrir les notifications e-mail du parcours Pro : validation/rejet de document par
l'admin (`contact@`), alerte leads passée sur `notifications@` avec le layout commun,
et nouveau cron d'alerte J-30 / J-7 avant expiration de l'attestation décennale.

Purpose: un pro ne doit plus découvrir par surprise qu'il est bloqué (document rejeté
ou décennale expirée) — la suspension auto existe déjà (05.11-03) mais silencieuse.
Output: 2 endpoints modifiés, 1 endpoint cron + migration + workflow GitHub Actions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06.3-notifications-email-transactionnelles/06.3-CONTEXT.md

<interfaces>
Moteur livré par le plan 06.3-01 (à consommer, ne pas redéfinir) :

```ts
// server/utils/email.ts
export type EmailSender = 'no-reply' | 'notifications' | 'contact'
export async function sendEmail(options: { to: string; subject: string; html: string; sender?: EmailSender; replyTo?: string })

// server/utils/emailLayout.ts
export interface EmailCta { label: string; href: string }
export function renderEmail(opts: { title: string; preheader?: string; intro?: string; bodyHtml?: string; cta?: EmailCta; footerNote?: string }): string
```

Table `documents_artisan` (migration 20260823000000) :
```sql
id UUID, professional_id UUID REFERENCES professionals(id),
doc_type artisan_doc_type ('kbis'|'urssaf'|'decennale'),
status artisan_doc_status ('pending'|'valid'|'expired'|'suspended'),
file_key TEXT, expires_at TIMESTAMPTZ, activities_subscribed TEXT[],
validated_by_api BOOLEAN, last_reviewed_at TIMESTAMPTZ, created_at, updated_at
```

Table `professionals` : colonnes utiles `id`, `email`, `company_name`, `full_name`,
`decennal_status`, `is_verified`, `lead_alerts_email`.

Cron existant (pattern pg_cron, migration 20260823000001) : `expire-artisan-documents`
horaire → passe `status='expired'` quand `expires_at < NOW()`. Le nouveau cron d'alerte
est HTTP (il envoie des e-mails, pg_cron ne peut pas) et tourne AVANT expiration.

Pattern GitHub Actions planifié existant (.github/workflows/supabase-keep-alive.yml) :
```yaml
on:
  schedule:
    - cron: '0 12 * * 1,4'
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: ...
        run: curl -X GET "..." -H "..."
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: E-mail validation / rejet de document au pro (verify.post.ts)</name>
  <files>server/api/v1/admin/verify.post.ts</files>
  <read_first>
    - server/api/v1/admin/verify.post.ts (fichier à modifier — noter l'étape 6 audit_logs, l'e-mail se place juste après)
    - server/utils/emailLayout.ts (renderEmail, livré par 06.3-01)
    - server/utils/email.ts (signature sendEmail avec sender)
  </read_first>
  <action>
1. Ajouter en tête de `server/api/v1/admin/verify.post.ts` :
```ts
import { sendEmail } from '../../../utils/email'
import { renderEmail } from '../../../utils/emailLayout'
```
(vérifier la profondeur relative réelle depuis `server/api/v1/admin/` → `../../../utils/`).

2. Élargir le schéma Zod `verifySchema` avec le motif de rejet :
```ts
  rejection_reason: z.string().min(5, 'Le motif doit faire au moins 5 caractères.').max(500, 'Le motif ne peut dépasser 500 caractères.').optional(),
```
et refuser un rejet sans motif : après `validation.success`, si
`validation.data.status === 'rejected' && !validation.data.rejection_reason`
→ `throw createError({ statusCode: 400, statusMessage: 'Un motif est requis pour rejeter un document.' })`.
Stocker le motif dans le record `verifications` mis à jour/inséré via le champ existant
`metadata` de `audit_logs` (étape 6 : ajouter `rejection_reason` dans l'objet `metadata`).

3. Juste après l'insertion `audit_logs` (étape 6) et avant le `return`, ajouter un bloc
non bloquant (try/catch qui log et avale — la validation admin ne doit jamais échouer
à cause d'un e-mail) :

```ts
    // 06.3 — informer le pro du résultat de la revue documentaire (contact@ : il peut répondre)
    try {
      const { data: pro } = await supabase
        .from('professionals')
        .select('email, company_name, full_name')
        .eq('id', pro_id)
        .maybeSingle()

      if (pro?.email) {
        const siteUrl = useRuntimeConfig().public.siteUrl || 'https://bati-axe.com'
        const docLabel = document_type === 'decennale' ? 'attestation décennale' : 'extrait Kbis'
        const approved = status === 'approved'
        await sendEmail({
          to: pro.email,
          sender: 'contact',
          subject: approved
            ? `Votre ${docLabel} est validée — BÂTI-AXE`
            : `Votre ${docLabel} n'a pas pu être validée — BÂTI-AXE`,
          html: renderEmail({
            title: approved ? `Votre ${docLabel} est validée` : `Votre ${docLabel} a été refusée`,
            preheader: approved ? 'Document validé par notre équipe.' : 'Un nouvel envoi est nécessaire.',
            intro: approved
              ? `Bonjour ${pro.full_name || pro.company_name || ''}, notre équipe a validé votre ${docLabel}.${expiry_date ? ` Elle est enregistrée comme valide jusqu'au ${new Date(expiry_date).toLocaleDateString('fr-FR')}.` : ''}`
              : `Bonjour ${pro.full_name || pro.company_name || ''}, votre ${docLabel} n'a pas pu être validée par notre équipe.`,
            bodyHtml: approved
              ? ''
              : `<p style="margin:0 0 8px;font-size:14px;color:#334155;"><strong>Motif :</strong></p><p style="margin:0;padding:12px 16px;background:#F8FAFC;border-left:3px solid #EA580C;font-size:14px;color:#334155;">${rejection_reason}</p>`,
            cta: {
              label: approved ? 'Accéder à mon espace' : 'Renvoyer mon document',
              href: approved ? `${siteUrl}/espace/dashboard` : `${siteUrl}/espace/dashboard?doc=${document_type}`,
            },
            footerNote: approved
              ? undefined
              : 'Vous pouvez répondre directement à cet e-mail si le motif ne vous semble pas justifié.',
          }),
        })
      }
    } catch (err) {
      console.error('[06.3] e-mail verify pro échoué:', err)
    }
```
(`rejection_reason` et `expiry_date` viennent du destructuring `validation.data` — l'étendre.)

4. Vérifier que `useRuntimeConfig` est bien disponible (auto-import Nitro) ; sinon
ajouter `import { useRuntimeConfig } from '#imports'`.
  </action>
  <verify>
    <automated>npx vitest run &amp;&amp; npx nuxt typecheck 2>/dev/null || npx vue-tsc --noEmit -p tsconfig.json 2>/dev/null || true</automated>
  </verify>
  <acceptance_criteria>
    - `server/api/v1/admin/verify.post.ts` contient `rejection_reason` (schéma Zod + corps de l'e-mail)
    - `server/api/v1/admin/verify.post.ts` contient `sender: 'contact'` et `renderEmail(`
    - `server/api/v1/admin/verify.post.ts` contient `Un motif est requis pour rejeter un document.`
    - Le bloc e-mail est entouré d'un `try {` / `catch` qui `console.error` (grep `[06.3] e-mail verify pro échoué`)
    - `npx vitest run` sort en code 0 (dont `tests/unit/admin-approve.test.ts` existant)
  </acceptance_criteria>
  <done>Approbation → e-mail de confirmation ; rejet → e-mail avec motif obligatoire. L'endpoint répond 200 même si l'e-mail échoue.</done>
</task>

<task type="auto">
  <name>Task 2: Alerte leads sur notifications@ + layout commun</name>
  <files>server/utils/notifyProLead.ts</files>
  <read_first>
    - server/utils/notifyProLead.ts (fichier à modifier — préserver strictement l'idempotence lead_notifications et le caractère non bloquant)
    - server/utils/emailLayout.ts (renderEmail)
    - supabase/migrations/20260823000002_p4_lead_notifications.sql (contrainte UNIQUE pro_id/project_id/channel)
  </read_first>
  <action>
Dans `server/utils/notifyProLead.ts` :

1. Ajouter `import { renderEmail } from './emailLayout'`.

2. Remplacer la constante `html` (template littéral actuel, lignes ~55-83) par un appel
à `renderEmail`, en conservant EXACTEMENT les mêmes informations :

```ts
    const detailsHtml = `
      <table style="width:100%;border-collapse:collapse;margin:8px 0 0;">
        <tr><td style="padding:8px 0;font-size:13px;color:#64748B;">Budget estimé</td><td style="padding:8px 0;text-align:right;font-size:13px;color:#0F172A;font-weight:600;">${project.budget_range || '—'}</td></tr>
        <tr><td style="padding:8px 0;border-top:1px solid #E2E8F0;font-size:13px;color:#64748B;">Délai souhaité</td><td style="padding:8px 0;border-top:1px solid #E2E8F0;text-align:right;font-size:13px;color:#0F172A;font-weight:600;">${project.timeline_range || 'Flexible'}</td></tr>
        <tr><td style="padding:8px 0;border-top:1px solid #E2E8F0;font-size:13px;color:#64748B;">Localisation</td><td style="padding:8px 0;border-top:1px solid #E2E8F0;text-align:right;font-size:13px;color:#0F172A;font-weight:600;">${project.postal_code || '—'}</td></tr>
      </table>`

    const html = renderEmail({
      title: `Nouveau lead : ${label}`,
      preheader: `Un projet ${label} correspond à vos catégories d'intervention.`,
      intro: "Un projet correspond à vos catégories d'intervention.",
      bodyHtml: detailsHtml,
      cta: { label: 'Voir le lead', href: `${siteUrl}/espace/leads/${project.id}?src=email` },
      footerNote: 'Les coordonnées du client sont débloquées immédiatement si vous êtes Premium. Vous pouvez désactiver ces alertes depuis votre espace.',
    })
```

Note : retirer de `footerNote` la mention « automatiquement après 48 h » présente
aujourd'hui — le cron 48h est confirmé inopérant par construction (cf. STATE.md
§ Blockers), il ne faut plus le promettre par e-mail.

3. Dans l'appel `sendEmail` (~ligne 89), ajouter `sender: 'notifications',`.

4. Ne toucher à RIEN d'autre : la requête `professionals`, le filtre d'idempotence
`lead_notifications`, la boucle séquentielle, l'insert de trace après succès et les
deux niveaux de try/catch restent identiques.
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `server/utils/notifyProLead.ts` contient `sender: 'notifications'` et `renderEmail(`
    - `server/utils/notifyProLead.ts` contient toujours `from('lead_notifications')` et `.insert({ pro_id: pro.id, project_id: project.id, channel: 'email' })`
    - `server/utils/notifyProLead.ts` ne contient plus la chaîne `48 h`
    - `grep -c "try {" server/utils/notifyProLead.ts` ≥ 2 (double filet non bloquant préservé)
    - `npx vitest run` sort en code 0
  </acceptance_criteria>
  <done>L'alerte lead part de notifications@ avec le layout commun ; idempotence et non-blocage intacts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Cron alertes J-30 / J-7 expiration décennale</name>
  <files>supabase/migrations/20260830000000_decennale_alerts.sql, server/api/v1/cron/decennale-alerts.get.ts, .github/workflows/cron-decennale-alerts.yml, tests/unit/decennale-alerts.test.ts</files>
  <read_first>
    - supabase/migrations/20260823000000_documents_artisan.sql (colonnes de documents_artisan)
    - supabase/migrations/20260823000001_documents_artisan_cron.sql (cron pg_cron d'expiration existant — le nouveau cron est HTTP, complémentaire)
    - .github/workflows/supabase-keep-alive.yml (pattern workflow planifié + secrets)
    - server/utils/emailLayout.ts (renderEmail)
    - nuxt.config.ts (clé runtimeConfig `cronSecret` ajoutée par 06.3-01)
  </read_first>
  <behavior>
    - selectAlerts([], now) → { j30: [], j7: [] }
    - Document expirant dans 20 jours, alert_j30_sent_at null → classé j30
    - Document expirant dans 20 jours, alert_j30_sent_at renseigné → ignoré
    - Document expirant dans 5 jours, alert_j7_sent_at null → classé j7 (même si j30 déjà envoyé)
    - Document expirant dans 5 jours, alert_j7_sent_at renseigné → ignoré
    - Document déjà expiré (expires_at &lt; now) → ignoré (le pg_cron d'expiration s'en charge)
    - Document expirant dans 45 jours → ignoré
  </behavior>
  <action>
1. Créer `supabase/migrations/20260830000000_decennale_alerts.sql` :
```sql
-- =====================================================
-- 06.3 — Idempotence des alertes d'expiration décennale (J-30 / J-7)
-- =====================================================

ALTER TABLE public.documents_artisan
    ADD COLUMN IF NOT EXISTS alert_j30_sent_at TIMESTAMPTZ,
    ADD COLUMN IF NOT EXISTS alert_j7_sent_at TIMESTAMPTZ;

COMMENT ON COLUMN public.documents_artisan.alert_j30_sent_at IS '06.3 — horodatage de l''alerte e-mail J-30 avant expiration (NULL = jamais envoyée)';
COMMENT ON COLUMN public.documents_artisan.alert_j7_sent_at IS '06.3 — horodatage de l''alerte e-mail J-7 avant expiration (NULL = jamais envoyée)';
```
Appliquer avec `npx supabase db push --yes`, puis régénérer les types :
`npx supabase gen types typescript --project-id xpwoczcbyamnjknloxgz --schema public > app/types/database.types.ts`
(pattern imposé par STATE.md § Known Patterns).

2. Créer `server/api/v1/cron/decennale-alerts.get.ts`. Extraire la logique de sélection
dans une fonction pure exportée depuis le même fichier pour la tester :

```ts
import { serverSupabaseServiceRole } from '#supabase/server'
import { useRuntimeConfig } from '#imports'
import { sendEmail } from '../../../utils/email'
import { renderEmail } from '../../../utils/emailLayout'

export interface DecennaleDoc {
  id: string
  professional_id: string
  expires_at: string
  alert_j30_sent_at: string | null
  alert_j7_sent_at: string | null
  professionals?: { email: string | null; company_name: string | null; full_name: string | null } | null
}

/** Pure : classe les documents en alertes J-30 / J-7 (testable sans Supabase). */
export function selectAlerts(docs: DecennaleDoc[], now: Date): { j30: DecennaleDoc[]; j7: DecennaleDoc[] } {
  const j30: DecennaleDoc[] = []
  const j7: DecennaleDoc[] = []
  for (const d of docs) {
    const days = (new Date(d.expires_at).getTime() - now.getTime()) / 86_400_000
    if (days < 0 || days > 30) continue
    if (days <= 7) {
      if (!d.alert_j7_sent_at) j7.push(d)
    } else if (!d.alert_j30_sent_at) {
      j30.push(d)
    }
  }
  return { j30, j7 }
}

export default defineEventHandler(async (event) => {
  // Secret partagé : sans lui l'endpoint est publiquement déclenchable.
  const secret = (useRuntimeConfig() as any).cronSecret
  const auth = getHeader(event, 'authorization')
  if (!secret || auth !== `Bearer ${secret}`) {
    throw createError({ statusCode: 401, statusMessage: 'Non autorisé.' })
  }

  const supabase = await serverSupabaseServiceRole(event) as any
  const now = new Date()
  const horizon = new Date(now.getTime() + 30 * 86_400_000).toISOString()

  const { data: docs, error } = await supabase
    .from('documents_artisan')
    .select('id, professional_id, expires_at, alert_j30_sent_at, alert_j7_sent_at, professionals(email, company_name, full_name)')
    .eq('doc_type', 'decennale')
    .eq('status', 'valid')
    .not('expires_at', 'is', null)
    .gte('expires_at', now.toISOString())
    .lte('expires_at', horizon)

  if (error) throw createError({ statusCode: 500, statusMessage: 'Erreur lecture documents.' })

  const { j30, j7 } = selectAlerts(docs || [], now)
  const siteUrl = useRuntimeConfig().public.siteUrl || 'https://bati-axe.com'
  let sent = 0

  for (const [bucket, docsList] of [['j30', j30], ['j7', j7]] as const) {
    for (const doc of docsList) {
      const email = doc.professionals?.email
      if (!email) continue
      const dateFr = new Date(doc.expires_at).toLocaleDateString('fr-FR')
      const urgent = bucket === 'j7'
      try {
        const res = await sendEmail({
          to: email,
          sender: 'notifications',
          subject: urgent
            ? `Votre attestation décennale expire le ${dateFr} — action requise`
            : `Votre attestation décennale expire dans 30 jours (${dateFr})`,
          html: renderEmail({
            title: urgent ? 'Votre décennale expire dans moins de 7 jours' : 'Votre décennale expire dans 30 jours',
            preheader: `Expiration le ${dateFr}.`,
            intro: `Bonjour ${doc.professionals?.full_name || doc.professionals?.company_name || ''}, votre attestation de responsabilité civile décennale enregistrée sur BÂTI-AXE expire le ${dateFr}.`,
            bodyHtml: `<p style="margin:0;padding:12px 16px;background:#F8FAFC;border-left:3px solid #EA580C;font-size:14px;color:#334155;">Sans attestation valide à cette date, votre profil est automatiquement suspendu : vous ne pourrez plus débloquer de nouveaux chantiers ni apparaître comme disponible en sous-traitance.</p>`,
            cta: { label: 'Envoyer ma nouvelle attestation', href: `${siteUrl}/espace/dashboard?doc=decennale` },
          }),
        })
        if (res.success) {
          await supabase
            .from('documents_artisan')
            .update(urgent ? { alert_j7_sent_at: now.toISOString() } : { alert_j30_sent_at: now.toISOString() })
            .eq('id', doc.id)
          sent++
        }
      } catch (err) {
        console.error('[06.3] alerte décennale échouée pour', doc.id, err)
      }
    }
  }

  return { status: 'SUCCESS', checked: docs?.length ?? 0, j30: j30.length, j7: j7.length, sent }
})
```

3. Créer `.github/workflows/cron-decennale-alerts.yml` :
```yaml
name: Alertes expiration décennale (J-30 / J-7)

on:
  schedule:
    # Tous les jours à 07h00 UTC
    - cron: '0 7 * * *'
  workflow_dispatch:

jobs:
  alerts:
    runs-on: ubuntu-latest
    steps:
      - name: Déclencher le cron d'alertes décennale
        run: |
          curl --fail --silent --show-error \
            -X GET "${{ secrets.CRON_BASE_URL }}/api/v1/cron/decennale-alerts" \
            -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
            -w "\nCode HTTP: %{http_code}\n"
```
Documenter dans le SUMMARY que les secrets GitHub `CRON_BASE_URL`
(ex. `https://dev.bati-axe.fr`) et `CRON_SECRET` (identique à la var d'env
serveur `CRON_SECRET`) doivent être créés avant que le workflow ne serve à quoi que ce soit.

4. Créer `tests/unit/decennale-alerts.test.ts` couvrant les 7 cas du bloc `<behavior>`
via `import { selectAlerts } from '../../server/api/v1/cron/decennale-alerts.get'`.
Construire les dates relativement à un `now` fixe (`new Date('2026-09-01T00:00:00Z')`).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/decennale-alerts.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/20260830000000_decennale_alerts.sql` contient `alert_j30_sent_at` et `alert_j7_sent_at`
    - `app/types/database.types.ts` contient `alert_j30_sent_at` (types régénérés après db push)
    - `server/api/v1/cron/decennale-alerts.get.ts` contient `export function selectAlerts(`, `Bearer ${secret}`, `statusCode: 401`, `sender: 'notifications'`
    - `.github/workflows/cron-decennale-alerts.yml` contient `cron: '0 7 * * *'` et `secrets.CRON_SECRET`
    - `npx vitest run tests/unit/decennale-alerts.test.ts` sort en code 0 avec 7 tests passants
    - `curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/api/v1/cron/decennale-alerts` renvoie `401` (sans header Authorization)
  </acceptance_criteria>
  <done>Le cron quotidien envoie une alerte J-30 puis J-7 par document, jamais deux fois la même, et refuse tout appel non authentifié.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → /api/v1/cron/decennale-alerts | Endpoint HTTP public déclenchable par n'importe qui |
| admin authentifié → verify.post.ts | `rejection_reason` saisi par l'admin est réinjecté dans un e-mail |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-063-05 | Elevation of Privilege | server/api/v1/cron/decennale-alerts.get.ts | mitigate | Header `Authorization: Bearer ${cronSecret}` obligatoire ; 401 si le secret est vide ou différent (pas de bypass en l'absence de config) |
| T-063-06 | Denial of Service | même endpoint (spam d'envois e-mail) | mitigate | Idempotence `alert_j30_sent_at` / `alert_j7_sent_at` : un rejeu immédiat n'envoie rien |
| T-063-07 | Information Disclosure | réponse JSON du cron | accept | Ne retourne que des compteurs (`checked`, `j30`, `j7`, `sent`), aucune donnée nominative |
| T-063-08 | Tampering | `rejection_reason` injecté dans le HTML de l'e-mail | mitigate | Borné par Zod `max(500)` et écrit par un admin authentifié (`app_metadata.role === 'admin'`) |
</threat_model>

<verification>
- `npx vitest run` : 0 échec.
- `npx supabase db push --yes` appliqué et `app/types/database.types.ts` régénéré + committé.
- Appel local du cron avec le bon Bearer en mode dev (`EMAIL_LIVE=false`) : les e-mails mock s'affichent en console, la réponse JSON compte correctement.
- Deuxième appel immédiat : `sent: 0` (idempotence prouvée).
</verification>

<success_criteria>
- Validation admin → e-mail au pro ; rejet → e-mail avec motif obligatoire côté API.
- Alertes leads sur `notifications@`, idempotence P4 préservée.
- Cron J-30/J-7 opérationnel, protégé, idempotent, déclenché quotidiennement par GitHub Actions.
</success_criteria>

<output>
Créer `.planning/phases/06.3-notifications-email-transactionnelles/06.3-02-SUMMARY.md`
</output>
