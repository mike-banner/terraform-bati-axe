---
phase: 06.3-notifications-email-transactionnelles
plan: 04
type: execute
wave: 2
depends_on: ["06.3-01"]
files_modified:
  - server/utils/handleStripeEvent.ts
  - server/api/v1/pro/claim.post.ts
  - tests/unit/stripe-webhook.test.ts
autonomous: true
requirements: [EML-06, EML-10]

must_haves:
  truths:
    - "Un pro reçoit une confirmation e-mail quand sa souscription de zone devient active"
    - "Un pro reçoit une confirmation quand son retrait de zone prend effet en fin de période payée"
    - "Un pro reçoit une confirmation quand son passage mensuel↔annuel prend effet"
    - "Un échec d'envoi d'e-mail ne fait jamais échouer le traitement du webhook Stripe"
    - "L'alerte admin SIRET du claim passe par notifyAdmin au lieu d'un HTML inline"
    - "L'admin reçoit une alerte à chaque nouvelle inscription pro, pas seulement en cas de SIRET non confirmé"
  artifacts:
    - path: "server/utils/handleStripeEvent.ts"
      provides: "Confirmations e-mail souscription / changement de facturation / retrait de zone"
      contains: "sender: 'notifications'"
    - path: "server/api/v1/pro/claim.post.ts"
      provides: "Alerte admin SIRET via notifyAdmin"
      contains: "notifyAdmin("
    - path: "tests/unit/stripe-webhook.test.ts"
      provides: "Tests des 3 envois + non-blocage"
  key_links:
    - from: "server/utils/handleStripeEvent.ts"
      to: "server/utils/email.ts"
      via: "import { sendEmail }"
      pattern: "from './email'"
    - from: "server/utils/handleStripeEvent.ts"
      to: "pro_zones / zones"
      via: "résolution du pro et du nom de zone avant envoi"
      pattern: "from\\('zones'\\)"
---

<objective>
Couvrir les confirmations e-mail de facturation (Phase 05.16) : souscription de zone
active, retrait de zone effectif via Subscription Schedule, changement mensuel↔annuel —
tous depuis `notifications@`. Migrer au passage l'alerte admin SIRET du claim pro vers
`notifyAdmin`.

Purpose: le mécanisme de retrait de zone (Subscription Schedule) a été validé
fonctionnellement le 2026-08-29 via Test Clock, mais le pro n'est prévenu de rien —
son changement prend effet silencieusement en fin de période payée.
Output: `handleStripeEvent.ts` enrichi + tests, `claim.post.ts` simplifié.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06.3-notifications-email-transactionnelles/06.3-CONTEXT.md
@.planning/phases/05.16-decoupage-zones-78-pricing-degressif/05.16-STRIPE-SCHEDULES.md

<interfaces>
Moteur livré par le plan 06.3-01 (à consommer, ne pas redéfinir) :

```ts
// server/utils/email.ts
export type EmailSender = 'no-reply' | 'notifications' | 'contact'
export async function sendEmail(options: { to: string; subject: string; html: string; sender?: EmailSender; replyTo?: string })

// server/utils/emailLayout.ts
export function renderEmail(opts: { title: string; preheader?: string; intro?: string; bodyHtml?: string; cta?: { label: string; href: string }; footerNote?: string }): string

// server/utils/notifyAdmin.ts
export async function notifyAdmin(options: { subject: string; title: string; intro?: string; bodyHtml?: string; cta?: { label: string; href: string } }): Promise<void>
export function adminDetailsTable(rows: Array<[string, string]>): string
```

`server/utils/handleStripeEvent.ts` — signature et cas gérés aujourd'hui :
```ts
export async function handleStripeEvent(event: any, supabase: any, stripe: any): Promise<void>
// 'checkout.session.completed'      → professionals.subscription_status='active' + upsert pro_zones + paywall_events
//                                     metadata utiles : pro_id, zone_id, billing ('monthly'|'annual')
// 'customer.subscription.updated'   → statusMap Stripe→pro_zones ; newBilling depuis items[0].price.recurring.interval
//                                     removalApplied si metadata.pending_zone_removal_id && price.id === metadata.pending_zone_removal_price
// 'customer.subscription.deleted'   → pro_zones cancelled + professionals.subscription_status='canceled'
// 'invoice.payment_failed'          → professionals.subscription_status='unpaid'
// 'customer.subscription.trial_will_end' → log seulement (D-08)
```
Le handler ne prend AUCUN paramètre HTTP : il ne peut donc pas appeler `useRuntimeConfig(event)`.
Utiliser `useRuntimeConfig()` sans argument (déjà le pattern de `notifyProLead.ts`).

Tests existants : `tests/unit/stripe-webhook.test.ts`, avec un mock Supabase chaînable
maison (`makeSupabaseMock()` → `{ from, update, insert, eq }`). Il faudra l'étendre pour
supporter `.select().eq().maybeSingle()`.

Table `zones` : colonnes `id`, `name`, `is_active`, `postal_codes`.
Table `pro_zones` : `pro_id`, `zone_id`, `billing`, `price_cents`, `stripe_subscription_id`, `status`.
Table `professionals` : `id`, `email`, `company_name`, `full_name`, `stripe_customer_id`.

`server/api/v1/pro/claim.post.ts` lignes ~130-158 : bloc d'alerte admin SIRET
(05.15) qui lit `useRuntimeConfig().adminEmail`, construit un HTML inline et appelle
`sendEmail` dans un try/catch. C'est exactement ce que `notifyAdmin` fait maintenant.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Confirmations e-mail de facturation dans handleStripeEvent</name>
  <files>server/utils/handleStripeEvent.ts, tests/unit/stripe-webhook.test.ts</files>
  <read_first>
    - server/utils/handleStripeEvent.ts (fichier à modifier — noter que `removalApplied` déclenche déjà un update pro_zones + un nettoyage de metadata)
    - tests/unit/stripe-webhook.test.ts (mock Supabase chaînable à étendre)
    - server/api/v1/pro/zones/unsubscribe.post.ts (comment le retrait de zone pose `pending_zone_removal_id` / `pending_zone_removal_price`)
    - server/utils/emailLayout.ts (renderEmail)
    - .planning/phases/05.16-decoupage-zones-78-pricing-degressif/05.16-STRIPE-SCHEDULES.md (sémantique des transitions de schedule)
  </read_first>
  <behavior>
    - checkout.session.completed avec zone_id + billing + subscription → 1 sendEmail vers l'e-mail du pro, sender 'notifications', sujet contenant le nom de zone
    - checkout.session.completed sans zone_id → aucun sendEmail
    - customer.subscription.updated avec removalApplied → 1 sendEmail « retrait de zone effectif »
    - customer.subscription.updated où newBilling diffère du billing stocké en base → 1 sendEmail « changement de facturation effectif »
    - customer.subscription.updated où newBilling est identique au billing stocké → aucun sendEmail
    - sendEmail qui rejette → handleStripeEvent résout sans lever (le webhook doit rendre 200 à Stripe)
  </behavior>
  <action>
1. Ajouter en tête de `server/utils/handleStripeEvent.ts` :
```ts
import { useRuntimeConfig } from '#imports'
import { sendEmail } from './email'
import { renderEmail } from './emailLayout'
```

2. Ajouter en bas du fichier deux helpers privés (non exportés hors test) :

```ts
async function resolveProEmail(supabase: any, proId: string): Promise<{ email: string | null; name: string } | null> {
  const { data } = await supabase
    .from('professionals')
    .select('email, company_name, full_name')
    .eq('id', proId)
    .maybeSingle()
  if (!data?.email) return null
  return { email: data.email, name: data.company_name || data.full_name || '' }
}

async function resolveZoneName(supabase: any, zoneId: string): Promise<string> {
  const { data } = await supabase.from('zones').select('name').eq('id', zoneId).maybeSingle()
  return data?.name || 'votre zone'
}

/** Envoi non bloquant : un webhook Stripe doit répondre 200 même si l'e-mail échoue. */
async function sendBillingEmail(
  supabase: any,
  proId: string,
  opts: { subject: string; title: string; intro: string; footerNote?: string },
): Promise<void> {
  try {
    const pro = await resolveProEmail(supabase, proId)
    if (!pro?.email) return
    const siteUrl = (useRuntimeConfig().public?.siteUrl as string) || 'https://bati-axe.com'
    await sendEmail({
      to: pro.email,
      sender: 'notifications',
      subject: opts.subject,
      html: renderEmail({
        title: opts.title,
        intro: opts.intro,
        cta: { label: 'Gérer mon abonnement', href: `${siteUrl}/espace/premium` },
        footerNote: opts.footerNote ?? 'Votre facture est disponible dans votre espace, rubrique Abonnement.',
      }),
    })
  } catch (err) {
    console.error('[06.3] e-mail facturation Stripe échoué:', err)
  }
}
```

3. Cas `checkout.session.completed` — après l'upsert `pro_zones` (dans le `if (zoneId && billing && subscriptionId)`), ajouter :
```ts
        const zoneName = await resolveZoneName(supabase, zoneId)
        await sendBillingEmail(supabase, proId, {
          subject: `Votre abonnement ${zoneName} est actif — BÂTI-AXE`,
          title: `Votre abonnement ${zoneName} est actif`,
          intro: `Votre abonnement ${billing === 'annual' ? 'annuel' : 'mensuel'} sur la zone ${zoneName} est désormais actif. Les nouveaux chantiers de cette zone vous sont accessibles immédiatement.`,
        })
```

4. Cas `customer.subscription.updated` — AVANT le `bulkUpdate` (il faut lire l'ancien
`billing` avant de l'écraser), lire l'état courant :
```ts
      const { data: existingZones } = await supabase
        .from('pro_zones')
        .select('pro_id, zone_id, billing')
        .eq('stripe_subscription_id', stripeSubId)
      const proId = existingZones?.[0]?.pro_id as string | undefined
      const previousBilling = existingZones?.[0]?.billing as string | undefined
```

Puis, APRÈS le `await bulkUpdate` :
```ts
      if (proId && removalApplied && removedZoneId) {
        const zoneName = await resolveZoneName(supabase, removedZoneId)
        await sendBillingEmail(supabase, proId, {
          subject: `Retrait de la zone ${zoneName} effectif — BÂTI-AXE`,
          title: `Le retrait de la zone ${zoneName} est effectif`,
          intro: `Votre période payée est arrivée à son terme : la zone ${zoneName} n'est plus incluse dans votre abonnement. Vous ne recevrez plus les chantiers de cette zone. Vos autres zones restent actives et votre tarif dégressif a été recalculé en conséquence.`,
        })
      } else if (proId && newBilling && previousBilling && newBilling !== previousBilling) {
        await sendBillingEmail(supabase, proId, {
          subject: `Votre facturation est passée en ${newBilling === 'annual' ? 'annuel' : 'mensuel'} — BÂTI-AXE`,
          title: `Changement de facturation effectif`,
          intro: `Votre abonnement est désormais facturé en ${newBilling === 'annual' ? 'annuel' : 'mensuel'}. Ce changement prend effet à partir de la période en cours.`,
        })
      }
```

Ne pas envoyer d'e-mail sur `customer.subscription.deleted` ni `invoice.payment_failed`
dans ce plan : Stripe envoie déjà ses propres e-mails de relance de paiement depuis le
Dashboard (même logique que `trial_will_end`/D-08), doubler serait du bruit.

5. Étendre `tests/unit/stripe-webhook.test.ts` :
- ajouter `vi.mock('../../server/utils/email', () => ({ sendEmail: vi.fn().mockResolvedValue({ success: true }) }))`
  et importer le mock pour compter les appels ;
- enrichir `makeSupabaseMock()` pour supporter `.select(...).eq(...).maybeSingle()` et
  `.select(...).eq(...)` renvoyant des données paramétrables (pro `{ email: 'pro@test.fr', company_name: 'SARL Test' }`,
  zone `{ name: 'Versailles' }`, pro_zones `[{ pro_id: 'pro-1', zone_id: 'z1', billing: 'monthly' }]`) ;
- couvrir les 6 cas du bloc `<behavior>`, dont le cas d'échec (`sendEmail` mocké en `mockRejectedValue`)
  qui doit laisser `handleStripeEvent` résoudre sans lever ;
- ne pas casser les tests existants du fichier.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/stripe-webhook.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `server/utils/handleStripeEvent.ts` contient `sendBillingEmail`, `resolveZoneName`, `resolveProEmail`, `sender: 'notifications'`
    - `server/utils/handleStripeEvent.ts` contient `previousBilling` et `newBilling !== previousBilling`
    - `server/utils/handleStripeEvent.ts` contient `console.error('[06.3] e-mail facturation Stripe échoué:'`
    - `server/utils/handleStripeEvent.ts` ne contient AUCUN `throw` ajouté dans les nouveaux blocs
    - `tests/unit/stripe-webhook.test.ts` contient `vi.mock` sur `server/utils/email` et au moins 6 nouveaux `it(`
    - `npx vitest run tests/unit/stripe-webhook.test.ts` sort en code 0
    - `npx vitest run` sort en code 0
  </acceptance_criteria>
  <done>Les 3 transitions de facturation déclenchent une confirmation e-mail au pro ; aucun envoi ne peut faire échouer le webhook.</done>
</task>

<task type="auto">
  <name>Task 2: Migrer l'alerte admin SIRET du claim vers notifyAdmin</name>
  <files>server/api/v1/pro/claim.post.ts</files>
  <read_first>
    - server/api/v1/pro/claim.post.ts (fichier à modifier — bloc alerte admin lignes ~130-158, et le 2ᵉ sendEmail d'onboarding ligne ~332 qui NE doit PAS être touché)
    - server/utils/notifyAdmin.ts (notifyAdmin, adminDetailsTable)
    - .planning/STATE.md (§ 05.15 : raison d'être de cette alerte — Kbis bloqué en revue manuelle si SIRET non actif)
  </read_first>
  <action>
1. Ajouter l'import :
```ts
import { notifyAdmin, adminDetailsTable } from '../../../utils/notifyAdmin'
```
(vérifier la profondeur relative réelle depuis `server/api/v1/pro/`).

2. Remplacer intégralement le bloc `if (siretLookup.status !== 'active') { ... }`
(lecture de `adminEmail`, HTML inline, try/catch, `log('Admin SIRET alert email failed: ...')`)
par :

```ts
    // 05.15 — Le Kbis ne pourra pas s'auto-approuver (upload.post.ts exige
    // siret_status === 'active') : le pro restera bloqué en attente de revue
    // manuelle admin. On alerte tout de suite plutôt que d'attendre qu'il s'impatiente.
    if (siretLookup.status !== 'active') {
      await notifyAdmin({
        subject: `SIRET non confirmé au claim — ${data.company_name}`,
        title: 'Vérification manuelle requise',
        intro: `Le SIRET de ${data.company_name} n'a pas pu être confirmé « actif » via l'API Recherche Entreprises. Le Kbis de ce pro restera en attente de revue manuelle tant que ce point n'est pas tranché.`,
        bodyHtml: adminDetailsTable([
          ['Entreprise', data.company_name],
          ['Contact', data.full_name],
          ['SIRET', data.siret],
          ['Statut API', String(siretLookup.status)],
          ['Téléphone', data.phone],
        ]),
      })
    }
```

`notifyAdmin` gère déjà le cas `adminEmail` vide (warn + no-op) et avale toute erreur :
plus besoin du `if (adminEmail)` ni du try/catch local.

3. **Ne toucher à rien d'autre** dans ce fichier — en particulier le second `sendEmail`
(~ligne 332, e-mail d'onboarding pro conditionné par `onboardingEmails`) reste inchangé
dans ce plan.
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `server/api/v1/pro/claim.post.ts` contient `notifyAdmin({` et `adminDetailsTable([`
    - `server/api/v1/pro/claim.post.ts` ne contient plus `Admin SIRET alert email failed`
    - `server/api/v1/pro/claim.post.ts` ne contient plus `const adminEmail = useRuntimeConfig().adminEmail`
    - `server/api/v1/pro/claim.post.ts` contient toujours `siretLookup.status !== 'active'` et le second appel `sendEmail(` (onboarding)
    - `grep -c "sendEmail(" server/api/v1/pro/claim.post.ts` retourne `1`
    - `npx vitest run` sort en code 0 (dont `tests/siret-lookup.test.ts`)
  </acceptance_criteria>
  <done>L'alerte SIRET passe par notifyAdmin (layout commun, notifications@) ; l'e-mail d'onboarding pro est inchangé.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Alerte admin — nouvelle inscription pro (claim.post.ts)</name>
  <files>server/api/v1/pro/claim.post.ts</files>
  <read_first>
    - server/api/v1/pro/claim.post.ts (fichier à modifier — repérer l'upsert `professionals` ~ligne 196-224, et le bloc `notifyAdmin` SIRET posé par la Task 2 juste au-dessus)
    - server/utils/notifyAdmin.ts (notifyAdmin, adminDetailsTable)
    - .planning/phases/06.3-notifications-email-transactionnelles/06.3-CONTEXT.md (§ Notifications Admin : « Inscription d'un nouveau Pro / Nouveau document soumis à vérification » — trou identifié lors de l'audit du 2026-08-30, absent des plans 01-04 initiaux)
  </read_first>
  <action>
Juste après le bloc `if (proError || !newPro) { ... }` (upsert `professionals` réussi,
`log('Upsert successful')`), et **avant** le bloc `notifyAdmin` de la Task 2 (celui-ci
reste conditionné à `siretLookup.status !== 'active'` — ne pas le fusionner), ajouter
une alerte admin systématique de nouvelle inscription :

```ts
    // 06.3 — Alerte admin à chaque nouvelle inscription pro (jamais bloquante).
    // Distincte de l'alerte SIRET ci-dessous : celle-ci part toujours, l'autre
    // seulement si le SIRET n'a pas pu être confirmé actif.
    await notifyAdmin({
      subject: `Nouvelle inscription pro — ${data.company_name}`,
      title: 'Nouveau professionnel inscrit',
      intro: `${data.company_name} vient de s'inscrire (${data.full_name}).`,
      bodyHtml: adminDetailsTable([
        ['Entreprise', data.company_name],
        ['Contact', data.full_name],
        ['SIRET', data.siret],
        ['Statut SIRET', String(siretLookup.status)],
        ['Catégories', data.categories.join(', ')],
        ['Code postal', data.postal_code],
      ]),
      cta: { label: 'Ouvrir la console admin', href: `${useRuntimeConfig().public.siteUrl || 'https://bati-axe.com'}/admin` },
    })
```

L'import `notifyAdmin, adminDetailsTable` est déjà ajouté par la Task 2 — ne pas le dupliquer.
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `server/api/v1/pro/claim.post.ts` contient `Nouvelle inscription pro`
    - `server/api/v1/pro/claim.post.ts` contient deux appels distincts à `notifyAdmin({` (inscription + SIRET conditionnel)
    - Le nouvel appel `notifyAdmin` n'est pas imbriqué dans `if (siretLookup.status !== 'active')`
    - `npx vitest run` sort en code 0
  </acceptance_criteria>
  <done>L'admin est notifié de toute nouvelle inscription pro, pas seulement des cas SIRET non confirmé.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stripe → /api/v1/stripe/webhook | Événements signés HMAC, traités par handleStripeEvent |
| metadata Stripe → corps d'e-mail | `zone_id`, `billing`, `pending_zone_removal_*` viennent de metadata Stripe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-063-14 | Spoofing | webhook Stripe | mitigate | Signature HMAC vérifiée en amont dans `server/api/v1/stripe/webhook.post.ts` (inchangé, re-testé le 2026-08-29) — `handleStripeEvent` n'est appelé qu'après |
| T-063-15 | Denial of Service | rejeu d'un même `customer.subscription.updated` → e-mails en boucle | mitigate | L'e-mail « retrait » n'est envoyé que si `removalApplied`, condition qui devient fausse dès le nettoyage des metadata déjà en place ; l'e-mail « changement de facturation » n'est envoyé que si `newBilling !== previousBilling`, faux au rejeu |
| T-063-16 | Information Disclosure | e-mail envoyé au mauvais pro | mitigate | Le destinataire est résolu par `pro_zones.stripe_subscription_id` → `professionals.id`, jamais par une donnée de metadata modifiable |
| T-063-17 | Availability | échec d'envoi → webhook en erreur → Stripe rejoue | mitigate | `sendBillingEmail` encapsule tout dans un `try/catch` : le webhook rend toujours 200 |
</threat_model>

<verification>
- `npx vitest run` : 0 échec.
- Rejeu local via Stripe CLI (`stripe trigger checkout.session.completed` avec metadata zone) : 1 e-mail mock en console, réponse 200.
- Test Clock existant du retrait de zone (procédure `05.16-STRIPE-SCHEDULES.md`) rejoué : l'e-mail « retrait effectif » apparaît une seule fois.
</verification>

<success_criteria>
- Les 3 transitions d'abonnement de la Phase 05.16 déclenchent une confirmation e-mail.
- Aucun e-mail dupliqué au rejeu d'un événement Stripe.
- L'alerte admin SIRET utilise le moteur commun.
</success_criteria>

<output>
Créer `.planning/phases/06.3-notifications-email-transactionnelles/06.3-04-SUMMARY.md`
</output>
