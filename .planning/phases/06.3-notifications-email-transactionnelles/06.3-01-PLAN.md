---
phase: 06.3-notifications-email-transactionnelles
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/utils/email.ts
  - server/utils/emailLayout.ts
  - server/utils/notifyAdmin.ts
  - nuxt.config.ts
  - .env.example
  - tests/unit/email-engine.test.ts
autonomous: true
requirements: [EML-02, EML-10]
user_setup:
  - service: cloudflare-dns
    why: "Délivrabilité des 3 expéditeurs sur bati-axe.com (bloquant avant activation prod, pas avant le code)"
    dashboard_config:
      - task: "Ajouter les enregistrements DKIM (CNAME/TXT) fournis par Resend pour le domaine bati-axe.com"
        location: "Cloudflare Dashboard → bati-axe.com → DNS"
      - task: "Ajouter SPF : TXT @ = v=spf1 include:amazonses.com ~all"
        location: "Cloudflare Dashboard → bati-axe.com → DNS"
      - task: "Ajouter DMARC : TXT _dmarc = v=DMARC1; p=none; rua=mailto:admin@bati-axe.com"
        location: "Cloudflare Dashboard → bati-axe.com → DNS"
      - task: "Activer Email Routing sur contact@bati-axe.com → Gmail admin"
        location: "Cloudflare Dashboard → bati-axe.com → Email → Email Routing"
  - service: resend
    why: "Envoi réel des e-mails transactionnels"
    env_vars:
      - name: RESEND_API_KEY
        source: "Resend Dashboard → API Keys"

must_haves:
  truths:
    - "Un appel sendEmail peut choisir explicitement l'expéditeur no-reply@, notifications@ ou contact@"
    - "Les appels sendEmail existants (sans sender) continuent de partir depuis contact@ sans modification"
    - "Tout e-mail rendu via renderEmail porte le branding copper/navy et le footer légal LCEN"
    - "Une alerte admin s'envoie par un seul appel notifyAdmin et ne fait jamais échouer l'appelant"
  artifacts:
    - path: "server/utils/emailLayout.ts"
      provides: "Layout HTML responsive unique + footer LCEN"
      exports: ["renderEmail"]
    - path: "server/utils/notifyAdmin.ts"
      provides: "Alerte admin centralisée vers NUXT_ADMIN_EMAIL"
      exports: ["notifyAdmin"]
    - path: "server/utils/email.ts"
      provides: "sendEmail multi-expéditeurs"
      exports: ["sendEmail", "resolveSender"]
    - path: "tests/unit/email-engine.test.ts"
      provides: "Tests layout + résolution d'expéditeur"
  key_links:
    - from: "server/utils/notifyAdmin.ts"
      to: "server/utils/email.ts"
      via: "import { sendEmail }"
      pattern: "from '\\./email'"
    - from: "server/utils/email.ts"
      to: "nuxt.config.ts runtimeConfig"
      via: "useRuntimeConfig().emailFromNoReply / emailFromNotifications / emailFromContact"
      pattern: "emailFromNotifications"
---

<objective>
Poser le moteur d'e-mailing commun de la phase 06.3 : expéditeur dynamique
(`no-reply@` / `notifications@` / `contact@`), layout HTML unique avec branding
copper/navy et footer légal LCEN, et utilitaire `notifyAdmin` centralisé.

Purpose: les 3 plans suivants (Pros, Particuliers/B2B, Stripe/Admin) consomment
ce moteur. Rien ne doit être dupliqué dans les endpoints.
Output: `email.ts` étendu, `emailLayout.ts` + `notifyAdmin.ts` créés, runtimeConfig
et `.env.example` à jour, tests unitaires verts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06.3-notifications-email-transactionnelles/06.3-CONTEXT.md

<interfaces>
État actuel de `server/utils/email.ts` (à étendre, pas à réécrire) :

```ts
export async function sendEmail(options: { to: string; subject: string; html: string })
// - DEV : mock console sauf EMAIL_LIVE=true
// - PROD : fetch POST https://api.resend.com/emails, Bearer config.resendApiKey
// - from = (config as any).emailFrom || 'BÂTI-AXE <contact@bati-axe.com>'
```

Appelants existants de `sendEmail` (aucun ne passe de `sender` — la valeur par
défaut DOIT rester `contact@` pour ne rien casser) :
- server/api/v1/pro/claim.post.ts (2 appels)
- server/api/v1/b2b/requests.post.ts (2 appels)
- server/api/v1/messages/index.post.ts (2 appels)
- server/api/v1/magic-link/[token]/decision.post.ts
- server/api/v1/admin/b2b-requests/[id]/restitution.post.ts
- server/utils/notifyProLead.ts

runtimeConfig existant (nuxt.config.ts, ~ligne 81-93) :
```ts
resendApiKey: process.env.NUXT_RESEND_API_KEY || process.env.RESEND_API_KEY,
emailFrom: process.env.NUXT_EMAIL_FROM || process.env.EMAIL_FROM || 'BÂTI-AXE <contact@bati-axe.com>',
onboardingEmails: (...) === 'true',
adminEmail: process.env.NUXT_ADMIN_EMAIL || process.env.ADMIN_EMAIL || '',
public: { siteUrl: process.env.NUXT_PUBLIC_SITE_URL || 'http://localhost:3000' },
```

Palette de marque (déjà utilisée dans les e-mails existants) :
copper `#EA580C`, navy `#0F172A`, texte secondaire `#64748B`, bordure `#E2E8F0`,
surface `#F8FAFC`, texte corps `#334155`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Layout HTML unique + footer LCEN</name>
  <files>server/utils/emailLayout.ts, tests/unit/email-engine.test.ts</files>
  <read_first>
    - server/utils/notifyProLead.ts (HTML e-mail existant : styles inline, largeur 560px, bouton navy)
    - server/api/v1/b2b/requests.post.ts (HTML e-mail existant : largeur 600px, bouton copper #EA580C)
    - vitest.config.ts (alias `~` → `.`, environment node, include `tests/**/*.test.ts`)
  </read_first>
  <behavior>
    - renderEmail({ title: 'Titre' }) retourne du HTML contenant `<h1` et 'Titre'
    - renderEmail avec cta retourne un `<a href="...">Label</a>` avec `background:#EA580C`
    - renderEmail sans cta ne contient aucun `<a href` de bouton
    - Le HTML retourné contient toujours la mention LCEN 'L34-5' et 'contact@bati-axe.com'
    - Le HTML retourné contient toujours `max-width:600px`
    - preheader passé → présent dans un div masqué (`display:none`)
  </behavior>
  <action>
Créer `server/utils/emailLayout.ts` exportant une seule fonction pure (aucun import Nuxt,
aucun accès runtimeConfig — elle doit être testable en vitest node) :

```ts
export interface EmailCta { label: string; href: string }

export interface EmailLayoutOptions {
  title: string
  preheader?: string
  intro?: string          // paragraphe d'introduction (texte brut, échappé côté appelant)
  bodyHtml?: string       // bloc HTML libre (tableaux de détails, listes)
  cta?: EmailCta
  footerNote?: string     // note contextuelle ajoutée au-dessus du footer légal
}

export function renderEmail(opts: EmailLayoutOptions): string
```

Le HTML retourné respecte exactement :
- Wrapper : `<div style="margin:0;padding:24px;background:#F8FAFC;font-family:-apple-system,BlinkMacSystemFont,'Segoe UI',Arial,sans-serif;">`
- Carte : `<div style="max-width:600px;margin:0 auto;background:#ffffff;border:1px solid #E2E8F0;border-radius:12px;padding:32px;">`
- Preheader (si fourni) : `<div style="display:none;max-height:0;overflow:hidden;opacity:0;">${preheader}</div>` en toute première ligne du wrapper
- Bandeau marque : `<p style="margin:0 0 24px;font-weight:700;font-size:14px;letter-spacing:0.08em;color:#EA580C;">BÂTI-AXE</p>`
- Titre : `<h1 style="margin:0 0 12px;font-size:20px;line-height:1.3;color:#0F172A;">${title}</h1>`
- Intro (si fourni) : `<p style="margin:0 0 20px;font-size:14px;line-height:1.6;color:#334155;">${intro}</p>`
- bodyHtml inséré tel quel après l'intro
- CTA (si fourni) : `<p style="margin:24px 0 0;"><a href="${cta.href}" style="display:inline-block;background:#EA580C;color:#ffffff;padding:12px 24px;border-radius:8px;text-decoration:none;font-weight:700;font-size:14px;">${cta.label}</a></p>`
- footerNote (si fourni) : `<p style="margin:24px 0 0;font-size:13px;line-height:1.6;color:#64748B;">${footerNote}</p>`
- Séparateur : `<hr style="margin:28px 0 16px;border:none;border-top:1px solid #E2E8F0;">`
- Footer légal LCEN, exactement ce texte dans un `<p style="margin:0;font-size:11px;line-height:1.6;color:#94A3B8;">` :
  `BÂTI-AXE — Mise en relation entre particuliers et artisans certifiés du bâtiment. Ce message transactionnel vous est adressé dans le cadre de l'exécution du service auquel vous avez souscrit (art. L34-5 du Code des postes et des communications électroniques, LCEN). Conformément au RGPD, vous disposez d'un droit d'accès, de rectification et d'effacement de vos données : contact@bati-axe.com.`

Ne pas utiliser de balise `<style>` ni de media query : styles inline uniquement
(Gmail/Outlook strippent le `<head>`). Le responsive vient de `max-width:600px` + padding.

Créer `tests/unit/email-engine.test.ts` avec les assertions listées dans `<behavior>`
(import : `import { renderEmail } from '../../server/utils/emailLayout'`).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/email-engine.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `server/utils/emailLayout.ts` contient `export function renderEmail(`
    - `server/utils/emailLayout.ts` contient `L34-5` et `max-width:600px` et `#EA580C`
    - `server/utils/emailLayout.ts` ne contient aucune occurrence de `useRuntimeConfig`
    - `npx vitest run tests/unit/email-engine.test.ts` sort en code 0 avec au moins 5 tests passants
  </acceptance_criteria>
  <done>renderEmail rend un e-mail complet brandé avec footer LCEN, couvert par tests unitaires.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: sendEmail multi-expéditeurs + runtimeConfig + .env.example</name>
  <files>server/utils/email.ts, nuxt.config.ts, .env.example, tests/unit/email-engine.test.ts</files>
  <read_first>
    - server/utils/email.ts (fichier à modifier — mock dev, fetch Resend, gestion clé manquante à préserver)
    - nuxt.config.ts (bloc runtimeConfig lignes ~70-96)
    - .env.example (section `# Resend Mailing`)
  </read_first>
  <behavior>
    - resolveSender('no-reply', cfg) → cfg.emailFromNoReply
    - resolveSender('notifications', cfg) → cfg.emailFromNotifications
    - resolveSender('contact', cfg) → cfg.emailFromContact
    - resolveSender(undefined, cfg) → cfg.emailFromContact (rétrocompat : défaut inchangé)
    - resolveSender('notifications', {}) → 'BÂTI-AXE &lt;notifications@bati-axe.com&gt;' (fallback en dur)
  </behavior>
  <action>
1. Dans `server/utils/email.ts`, ajouter au-dessus de `sendEmail` :

```ts
export type EmailSender = 'no-reply' | 'notifications' | 'contact'

const SENDER_FALLBACKS: Record<EmailSender, string> = {
  'no-reply': 'BÂTI-AXE <no-reply@bati-axe.com>',
  'notifications': 'BÂTI-AXE <notifications@bati-axe.com>',
  'contact': 'BÂTI-AXE <contact@bati-axe.com>',
}

// Exportée pour test unitaire (pure, aucun accès runtimeConfig).
export function resolveSender(sender: EmailSender | undefined, config: Record<string, any>): string {
  const key = sender ?? 'contact'
  const fromConfig = {
    'no-reply': config.emailFromNoReply,
    'notifications': config.emailFromNotifications,
    'contact': config.emailFromContact || config.emailFrom,
  }[key]
  return fromConfig || SENDER_FALLBACKS[key]
}
```

2. Élargir la signature :
```ts
export async function sendEmail(options: {
  to: string
  subject: string
  html: string
  sender?: EmailSender
  replyTo?: string
})
```
- Dans la branche mock dev, logger en plus `[MOCK EMAIL] From: ${options.sender ?? 'contact'}`.
- Dans la branche prod, remplacer la ligne `const from = (config as any).emailFrom || 'BÂTI-AXE <contact@bati-axe.com>'`
  par `const from = resolveSender(options.sender, config as any)`.
- Ajouter `...(options.replyTo ? { reply_to: options.replyTo } : {})` dans le body JSON envoyé à Resend.
- Ne rien changer d'autre : le comportement mock dev / EMAIL_LIVE / clé manquante reste identique.

3. Dans `nuxt.config.ts`, sous la ligne `emailFrom:`, ajouter :
```ts
    // 06.3 — expéditeurs dédiés par nature de message (bati-axe.com)
    emailFromNoReply: process.env.NUXT_EMAIL_FROM_NO_REPLY || process.env.EMAIL_FROM_NO_REPLY || 'BÂTI-AXE <no-reply@bati-axe.com>',
    emailFromNotifications: process.env.NUXT_EMAIL_FROM_NOTIFICATIONS || process.env.EMAIL_FROM_NOTIFICATIONS || 'BÂTI-AXE <notifications@bati-axe.com>',
    emailFromContact: process.env.NUXT_EMAIL_FROM_CONTACT || process.env.EMAIL_FROM_CONTACT || process.env.NUXT_EMAIL_FROM || process.env.EMAIL_FROM || 'BÂTI-AXE <contact@bati-axe.com>',
    // 06.3 — secret partagé du cron HTTP (alertes décennale, cf. plan 06.3-02)
    cronSecret: process.env.NUXT_CRON_SECRET || process.env.CRON_SECRET || '',
```

4. Dans `.env.example`, sous la section `# Resend Mailing`, ajouter :
```
# 06.3 — expéditeurs dédiés (nécessitent DKIM/SPF/DMARC sur bati-axe.com)
EMAIL_FROM_NO_REPLY=BÂTI-AXE <no-reply@bati-axe.com>
EMAIL_FROM_NOTIFICATIONS=BÂTI-AXE <notifications@bati-axe.com>
EMAIL_FROM_CONTACT=BÂTI-AXE <contact@bati-axe.com>
# 06.3 — secret du cron HTTP alertes décennale (Bearer)
CRON_SECRET=change-me
```

5. Ajouter dans `tests/unit/email-engine.test.ts` un `describe('resolveSender')` couvrant les 5 cas
du bloc `<behavior>` (import `{ resolveSender }` depuis `../../server/utils/email`).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/email-engine.test.ts &amp;&amp; npx nuxt build --dotenv .env.example 2>/dev/null || npx vitest run tests/unit/email-engine.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `server/utils/email.ts` contient `export function resolveSender(` et `sender?: EmailSender`
    - `server/utils/email.ts` contient `reply_to`
    - `grep -c "emailFrom" server/utils/email.ts` ne trouve plus l'ancienne ligne `const from = (config as any).emailFrom ||`
    - `nuxt.config.ts` contient `emailFromNoReply`, `emailFromNotifications`, `emailFromContact`, `cronSecret`
    - `.env.example` contient `EMAIL_FROM_NOTIFICATIONS=` et `CRON_SECRET=`
    - `npx vitest run tests/unit/email-engine.test.ts` sort en code 0
    - `npx vitest run` sort en code 0 (aucune régression sur les tests existants)
  </acceptance_criteria>
  <done>sendEmail accepte un expéditeur explicite ; sans argument il part toujours de contact@ (aucun appelant existant modifié).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: notifyAdmin centralisé</name>
  <files>server/utils/notifyAdmin.ts, tests/unit/email-engine.test.ts</files>
  <read_first>
    - server/api/v1/pro/claim.post.ts (lignes 130-158 : alerte admin actuelle, à remplacer au plan 06.3-04 — ne PAS la modifier ici)
    - server/utils/email.ts (signature sendEmail après Task 2)
    - server/utils/emailLayout.ts (renderEmail)
  </read_first>
  <behavior>
    - notifyAdmin appelé sans adminEmail configuré → ne lève pas, ne fait aucun envoi, log un warn
    - notifyAdmin avec adminEmail → un seul sendEmail, sender 'notifications', subject préfixé '[BÂTI-AXE ADMIN] '
    - sendEmail qui rejette → notifyAdmin résout quand même (jamais bloquant)
  </behavior>
  <action>
Créer `server/utils/notifyAdmin.ts` :

```ts
import { useRuntimeConfig } from '#imports'
import { sendEmail } from './email'
import { renderEmail, type EmailCta } from './emailLayout'

/**
 * 06.3 — Alerte admin centralisée (destinataire : NUXT_ADMIN_EMAIL).
 * Jamais bloquant : toute erreur est loggée et avalée, l'appelant répond normalement.
 */
export async function notifyAdmin(options: {
  subject: string
  title: string
  intro?: string
  bodyHtml?: string
  cta?: EmailCta
}): Promise<void> {
  try {
    const config = useRuntimeConfig()
    const to = (config as any).adminEmail
    if (!to) {
      console.warn('[notifyAdmin] NUXT_ADMIN_EMAIL non configuré — alerte ignorée:', options.subject)
      return
    }
    await sendEmail({
      to,
      subject: `[BÂTI-AXE ADMIN] ${options.subject}`,
      sender: 'notifications',
      html: renderEmail({
        title: options.title,
        intro: options.intro,
        bodyHtml: options.bodyHtml,
        cta: options.cta,
      }),
    })
  } catch (err) {
    console.error('[notifyAdmin] échec envoi alerte admin:', err)
  }
}

/** Helper de rendu d'un tableau clé/valeur pour le corps des alertes admin. */
export function adminDetailsTable(rows: Array<[string, string]>): string {
  return `<table style="width:100%;border-collapse:collapse;margin:8px 0 0;">${rows
    .map(
      ([k, v]) =>
        `<tr><td style="padding:8px 0;border-top:1px solid #E2E8F0;font-size:13px;color:#64748B;width:180px;">${k}</td><td style="padding:8px 0;border-top:1px solid #E2E8F0;font-size:13px;color:#0F172A;font-weight:600;">${v}</td></tr>`,
    )
    .join('')}</table>`
}
```

Ajouter dans `tests/unit/email-engine.test.ts` un `describe('notifyAdmin')` avec
`vi.mock('#imports', () => ({ useRuntimeConfig: () => mockConfig }))` et
`vi.mock('../../server/utils/email', () => ({ sendEmail: vi.fn() }))`, couvrant les 3 cas du bloc `<behavior>`.
Ajouter aussi un test `adminDetailsTable([['SIRET','123']])` contient `SIRET` et `123`.
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `server/utils/notifyAdmin.ts` contient `export async function notifyAdmin(` et `export function adminDetailsTable(`
    - `server/utils/notifyAdmin.ts` contient `sender: 'notifications'` et `[BÂTI-AXE ADMIN]`
    - `tests/unit/email-engine.test.ts` contient `describe('notifyAdmin'`
    - `npx vitest run` sort en code 0
  </acceptance_criteria>
  <done>notifyAdmin envoie une alerte brandée vers NUXT_ADMIN_EMAIL, no-op silencieux si non configuré, jamais bloquant.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → Resend API | La clé `RESEND_API_KEY` transite en header ; le HTML sortant peut contenir des données saisies par un tiers |
| données utilisateur → corps HTML | `intro`, `bodyHtml`, `title` peuvent contenir de la saisie utilisateur non échappée |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-063-01 | Information Disclosure | server/utils/email.ts (log mock dev) | mitigate | Le mock ne s'active que sous `import.meta.dev` (déjà en place) — ne jamais logger le HTML complet en prod |
| T-063-02 | Tampering | emailLayout.ts injection HTML via `intro`/`bodyHtml` | accept | E-mails transactionnels sortants uniquement, pas de rendu navigateur privilégié ; les valeurs proviennent de champs déjà validés Zod côté endpoint. Ne jamais passer de HTML tiers brut dans `bodyHtml`. |
| T-063-03 | Spoofing | expéditeurs @bati-axe.com | mitigate | DKIM + SPF (`include:amazonses.com`) + DMARC `p=none` sur bati-axe.com (user_setup, bloquant avant activation prod) |
| T-063-04 | Denial of Service | notifyAdmin appelé en boucle sur un endpoint public | accept | Volume borné par les endpoints appelants (claim/projet/B2B, déjà rate-limités côté Turnstile) |
</threat_model>

<verification>
- `npx vitest run` : 0 échec, tests existants inclus.
- `grep -rn "sendEmail(" server/ | wc -l` : nombre d'appels inchangé (aucun appelant migré dans ce plan).
- `npx nuxt build` réussit (runtimeConfig valide).
</verification>

<success_criteria>
- `renderEmail` et `resolveSender` couverts par `tests/unit/email-engine.test.ts`.
- `notifyAdmin` disponible pour les plans 02/03/04.
- Aucun e-mail existant n'a changé d'expéditeur ni de rendu (rétrocompatibilité stricte).
</success_criteria>

<output>
Créer `.planning/phases/06.3-notifications-email-transactionnelles/06.3-01-SUMMARY.md`
</output>
