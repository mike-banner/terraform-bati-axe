---
phase: 05.9-extension-simulateur-mes-aides-reno
plan: 04
type: execute
wave: 3
depends_on: ["05.9-02"]
files_modified:
  - app/pages/calculateur-aides.vue
  - app/pages/index.vue
  - server/api/v1/magic-link/[token].get.ts
  - app/pages/mon-projet/[token].vue
autonomous: true
requirements: [D-02, D-06, D-07]
must_haves:
  truths:
    - "La route standalone /calculateur-aides réutilise AidesMiniTunnel sans dupliquer sa logique (D-02)"
    - "La home expose un teaser visuel bento pointant vers /calculateur-aides (D-06) — aucun calcul embarqué dans la home"
    - "L'espace client /mon-projet/[token] affiche estimation + aides + reste à charge depuis calculator_data (D-07)"
  artifacts:
    - path: "app/pages/calculateur-aides.vue"
      provides: "Route standalone calculateur d'aides"
    - path: "app/pages/index.vue"
      provides: "Teaser home aides (carte bento + CTA)"
    - path: "app/pages/mon-projet/[token].vue"
      provides: "Bloc bilan financier dans l'espace client"
    - path: "server/api/v1/magic-link/[token].get.ts"
      provides: "calculator_data exposé au client via magic link"
  key_links:
    - from: "app/pages/calculateur-aides.vue"
      to: "app/components/aides/AidesMiniTunnel.vue"
      via: "<AidesMiniTunnel>"
      pattern: "AidesMiniTunnel"
    - from: "app/pages/index.vue"
      to: "/calculateur-aides"
      via: "NuxtLink"
      pattern: "calculateur-aides"
    - from: "app/pages/mon-projet/[token].vue"
      to: "data.project.calculator_data"
      via: "lecture reste_a_charge_min/max"
      pattern: "reste_a_charge_min"
---

<objective>
Compléter la Phase 05.9 : (1) une route standalone `/calculateur-aides` réutilisant `AidesMiniTunnel` ; (2) un teaser visuel sur la home pointant vers cette route (D-06) ; (3) l'affichage du bilan financier (estimation + aides + reste à charge) dans l'espace client `/mon-projet/[token]` (D-07), en exposant `calculator_data` via le magic link.

Purpose: Offrir le second point d'entrée (D-02) + la restitution client, sans nouvelle migration ni calcul côté home.
Output: `app/pages/calculateur-aides.vue` créé ; `app/pages/index.vue`, `server/api/v1/magic-link/[token].get.ts`, `app/pages/mon-projet/[token].vue` modifiés.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/05.9-extension-simulateur-mes-aides-reno/05.9-CONTEXT.md
@app/components/aides/AidesMiniTunnel.vue
@app/pages/index.vue
@app/pages/mon-projet/[token].vue
@server/api/v1/magic-link/[token].get.ts

<interfaces>
Contrat AidesMiniTunnel (plan 05.9-02) :
  Props : `:cout-travaux-min`, `:cout-travaux-max`, `:code-postal-initial?`
  Emits : `@complete` → { aides_estimees, reste_a_charge_min, reste_a_charge_max } ; `@skip` → void.

magic-link endpoint actuel : select `'id, category, description, budget_range, timeline_range, status, created_at'` — NE renvoie PAS calculator_data (à ajouter pour D-07).

mon-projet/[token].vue : affiche `data.project.budget_range` (fourchette formatée) et `data.project.timeline_range` ; aucun helper `formatEuro` encore défini (à ajouter).

Charte 04.7 : sections home existantes `max-w-7xl px-6 py-[clamp(3.5rem,7vw,6rem)]`, CTA `bg-safety rounded-full`, cartes `bento-card border-slate-200 bg-white shadow-lg`. Zéro cyan/vert.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route standalone /calculateur-aides</name>
  <files>app/pages/calculateur-aides.vue</files>
  <read_first>
    - app/components/aides/AidesMiniTunnel.vue
    - app/pages/simulateur.vue
  </read_first>
  <action>
    Créer `app/pages/calculateur-aides.vue` (`.vue`, `<script setup lang="ts">`).

    `useHead({ title: 'Calculateur d'aides — BÂTI-AXE', meta: [{ name:'description', content:'Estimez vos aides MaPrimeRénov, CEE et Éco-PTZ et votre reste à charge réel.' }] })`.

    État : `const cout = ref<number | null>(null)` + `const coutInput = ref<number>(0)`.

    Deux états dans le template :

    1. Si `cout === null` — saisie du coût travaux :
    ```
    <div class="min-h-[calc(100vh-3.5rem)] bg-page flex items-start justify-center px-4 py-12">
      <div class="w-full max-w-xl bg-white rounded-sm border border-slate-200 shadow-sm p-8 md:p-10">
        <h1 class="text-3xl md:text-4xl font-black tracking-tight text-foreground">Estimez vos aides de rénovation</h1>
        <p class="text-sm text-muted-foreground mt-2">MaPrimeRénov' · CEE · Éco-PTZ — quel est le montant estimé de vos travaux ?</p>
        <input v-model.number="coutInput" type="number" min="0" step="500" inputmode="numeric" placeholder="40000"
               class="w-full h-14 px-4 mt-6 border border-border rounded-sm text-xl font-semibold bg-background text-foreground placeholder:text-muted-foreground focus:outline-none focus:ring-2 focus:ring-foreground/20" />
        <button type="button" @click="cout = coutInput" :disabled="coutInput <= 0"
                class="mt-4 inline-flex h-11 px-6 bg-safety text-white text-sm font-semibold rounded-full hover:scale-105 transition-transform disabled:opacity-30">Continuer</button>
      </div>
    </div>
    ```

    2. Sinon — déléguer au composant :
    ```
    <AidesMiniTunnel :cout-travaux-min="cout" :cout-travaux-max="cout" @complete="onComplete" @skip="onSkip" />
    ```

    Handlers :
    - `const onComplete = (_p) => {}` (le composant affiche déjà le résultat ; no-op accepté).
    - `const onSkip = () => {}`.

    NB : `cout` passé à la fois en min et max → le reste à charge est une estimation ponctuelle (pas de fourchette), acceptable pour l'entrée standalone « calculatrice de poche » (PIVOT-B2B §2).
  </action>
  <verify>
    <automated>cd /home/mike/projects/bati-axe && npx nuxi typecheck 2>&1 | grep -i "calculateur-aides" && exit 1 || echo "OK calculateur-aides typecheck"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f app/pages/calculateur-aides.vue`
    - `grep -q "AidesMiniTunnel" app/pages/calculateur-aides.vue`
    - `grep -q "cout-travaux-min" app/pages/calculateur-aides.vue` et `grep -q "cout-travaux-max" app/pages/calculateur-aides.vue`
    - `grep -q "Estimez vos aides de rénovation" app/pages/calculateur-aides.vue`
    - `! grep -q "mesaides.france-renov" app/pages/calculateur-aides.vue` (aucun appel direct)
  </acceptance_criteria>
  <done>La route /calculateur-aides collecte un coût travaux puis réutilise AidesMiniTunnel sans duplication de logique.</done>
</task>

<task type="auto">
  <name>Task 2: Teaser home (carte bento → /calculateur-aides)</name>
  <files>app/pages/index.vue</files>
  <read_first>
    - app/pages/index.vue
  </read_first>
  <action>
    Dans `app/pages/index.vue`, insérer une `<section>` ENTRE la section « Comment ça marche » (celle qui contient `<h2 ...>Comment ça marche</h2>`) et la section « Chantiers Réalisés » (`v-if="showcasedProjects.length"`). Contenu (aucun calcul embarqué, purement visuel + CTA — D-06) :

    ```
    <section class="mx-auto w-full max-w-7xl px-6 py-[clamp(3.5rem,7vw,6rem)]">
      <div class="reveal-item bento-card flex flex-col md:flex-row md:items-center justify-between gap-6 rounded-sm border border-slate-200 bg-white p-8 shadow-lg">
        <div>
          <h2 class="text-3xl font-black tracking-tight text-slate-900">Estimez vos aides en 30 secondes</h2>
          <p class="mt-2 max-w-xl text-slate-500 leading-relaxed">MaPrimeRénov', CEE, Éco-PTZ : découvrez les aides d'État et votre reste à charge réel avant de choisir votre artisan.</p>
        </div>
        <NuxtLink
          to="/calculateur-aides"
          class="inline-flex h-14 items-center gap-2 rounded-full bg-safety px-8 text-lg font-bold text-white shadow-lg shadow-safety/20 transition-transform hover:scale-105"
        >
          Calculer mes aides
          <svg class="h-4 w-4" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M5 12h14M12 5l7 7-7 7"/></svg>
        </NuxtLink>
      </div>
    </section>
    ```
  </action>
  <verify>
    <automated>grep -q "Estimez vos aides en 30 secondes" app/pages/index.vue && grep -q "Calculer mes aides" app/pages/index.vue && grep -q "calculateur-aides" app/pages/index.vue && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "Estimez vos aides en 30 secondes" app/pages/index.vue`
    - `grep -q "Calculer mes aides" app/pages/index.vue`
    - `grep -q "to=\"/calculateur-aides\"" app/pages/index.vue`
    - `! grep -Eq "cyan|emerald|bg-green|text-green" app/pages/index.vue` (aucune régression charte)
  </acceptance_criteria>
  <done>La home expose un teaser visuel bento « Estimez vos aides » pointant vers /calculateur-aides, sans calcul embarqué.</done>
</task>

<task type="auto">
  <name>Task 3: Restitution dans l'espace client (magic-link + /mon-projet)</name>
  <files>server/api/v1/magic-link/[token].get.ts, app/pages/mon-projet/[token].vue</files>
  <read_first>
    - server/api/v1/magic-link/[token].get.ts
    - app/pages/mon-projet/[token].vue
  </read_first>
  <action>
    PARTIE A — `server/api/v1/magic-link/[token].get.ts` :
    Étendre le select de la table `projects` :
    de `'id, category, description, budget_range, timeline_range, status, created_at'`
    à `'id, category, description, budget_range, timeline_range, status, created_at, calculator_data'`.
    Aucun autre changement (calculator_data est un JSONB déjà stocké, aucune migration).

    PARTIE B — `app/pages/mon-projet/[token].vue` :
    1. Dans `<script setup>`, ajouter `const formatEuro = (n) => n.toLocaleString('fr-FR') + ' €'`.
    2. Dans le template, insérer APRÈS la section « Détails projet » (la `<section class="border-b border-border">` qui affiche Budget estimé / Délai / Statut / Protection) un bloc conditionnel :

    ```
    <section v-if="data.project.calculator_data?.reste_a_charge_min != null" class="border-b border-border">
      <div class="max-w-6xl mx-auto px-6 py-12">
        <p class="text-xs font-medium text-muted-foreground tracking-widest uppercase mb-5">Bilan financier estimé</p>
        <div class="grid grid-cols-1 sm:grid-cols-3 gap-6">
          <div>
            <p class="text-xs text-muted-foreground mb-1.5">Estimation travaux</p>
            <p class="text-sm font-semibold text-foreground">{{ data.project.budget_range }}</p>
          </div>
          <div>
            <p class="text-xs text-muted-foreground mb-1.5">Aides estimées</p>
            <p class="text-sm font-semibold text-foreground">{{ formatEuro(data.project.calculator_data.aides_estimees) }}</p>
          </div>
          <div>
            <p class="text-xs text-muted-foreground mb-1.5">Reste à charge</p>
            <p class="text-sm font-semibold text-foreground">{{ formatEuro(data.project.calculator_data.reste_a_charge_min) }} – {{ formatEuro(data.project.calculator_data.reste_a_charge_max) }}</p>
          </div>
        </div>
      </div>
    </section>
    ```
    Le bloc est absent si `calculator_data` n'a pas de champs aides (parcours « Non » ou anciens projets).
  </action>
  <verify>
    <automated>cd /home/mike/projects/bati-axe && grep -q "calculator_data" server/api/v1/magic-link/[token].get.ts && grep -q "formatEuro" app/pages/mon-projet/[token].vue && grep -q "Bilan financier estimé" app/pages/mon-projet/[token].vue && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "calculator_data" server/api/v1/magic-link/[token].get.ts`
    - `grep -q "formatEuro" app/pages/mon-projet/[token].vue`
    - `grep -q "Bilan financier estimé" app/pages/mon-projet/[token].vue`
    - `grep -q "aides_estimees" app/pages/mon-projet/[token].vue`
    - `grep -q "reste_a_charge_min" app/pages/mon-projet/[token].vue`
    - Le bloc est gardé par `calculator_data?.reste_a_charge_min != null` (lecture)
  </acceptance_criteria>
  <done>L'espace client expose calculator_data via le magic link et affiche le bilan financier uniquement quand les champs aides sont présents.</done>
</task>

</tasks>

<verification>
- `npx nuxi typecheck` sans erreur sur les fichiers modifiés.
- Parcours manuel : (a) /calculateur-aides → coût travaux → mini-tunnel → résultat ; (b) home → teaser « Calculer mes aides » → /calculateur-aides ; (c) /mon-projet/[token] d'un projet avec aides → bloc « Bilan financier estimé » visible ; d'un projet sans aides → bloc absent.
</verification>

<success_criteria>
- La route standalone /calculateur-aides fonctionne sans duplication (D-02).
- La home a un teaser visuel vers le calculateur (D-06).
- L'espace client restitue estimation + aides + reste à charge (D-07).
</success_criteria>

<output>
Créer `.planning/phases/05.9-extension-simulateur-mes-aides-reno/05.9-04-SUMMARY.md` quand terminé.
</output>
