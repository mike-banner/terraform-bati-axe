---
phase: 05.9-extension-simulateur-mes-aides-reno
plan: 02
type: execute
wave: 2
depends_on: ["05.9-01"]
files_modified:
  - app/components/aides/AidesMiniTunnel.vue
autonomous: true
requirements: [SC-1, D-01]
must_haves:
  truths:
    - "Un seul composant réutilisable AidesMiniTunnel collecte logement + foyer/revenu et appelle le proxy /api/v1/aides-reno (D-01)"
    - "Le composant n'appelle JAMAIS l'API mesaides en direct — uniquement via le proxy Nitro"
    - "API indisponible → message de fallback court + bouton continuer sans les aides, aucun blocage (D-04)"
    - "Le composant émet le résumé { aides_estimees, reste_a_charge_min, reste_a_charge_max } pour que le parent le persiste"
  artifacts:
    - path: "app/components/aides/AidesMiniTunnel.vue"
      provides: "Mini-tunnel aides réutilisable (2 étapes + résultat/fallback)"
  key_links:
    - from: "app/components/aides/AidesMiniTunnel.vue"
      to: "/api/v1/aides-reno"
      via: "$fetch POST avec situation + cout_travaux_min/max"
      pattern: "aides-reno"
---

<objective>
Créer le composant réutilisable `AidesMiniTunnel.vue` (D-01) qui collecte les informations « logement » (type, période de construction, statut propriétaire, résidence principale) puis « foyer & revenu » (nb personnes, classe de revenu, code postal), appelle le proxy `/api/v1/aides-reno`, et affiche soit le bilan aides + reste à charge, soit un fallback non bloquant.

Purpose: Fournir la brique UI unique branchée ensuite par le fork du simulateur (plan 03) et la route standalone (plan 04). Pas de duplication de logique.
Output: `app/components/aides/AidesMiniTunnel.vue`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/05.9-extension-simulateur-mes-aides-reno/05.9-CONTEXT.md
@.planning/phases/05.9-extension-simulateur-mes-aides-reno/05.9-RESEARCH.md
@.planning/phases/04.7-UI-Refonte/04.7-UI-SPEC.md
@app/assets/css/tailwind.css
@app/pages/simulateur.vue

<interfaces>
Contrat proxy (plan 05.9-01) — `POST /api/v1/aides-reno` :
  body: { situation: { revenu_classe, logement_type, statut_proprietaire, periode_construction, residence_principale, nb_personnes, code_postal }, cout_travaux_min: number, cout_travaux_max: number }
  réponse OK : { ok: true, aides: [{ label, montant }], aides_total: number, reste_a_charge_min: number, reste_a_charge_max: number }
  réponse panne : { ok: false, reason: 'unavailable' }  (jamais de throw 500)

Props du composant :
  coutTravauxMin: number (required) — estimate_min de computeEstimate
  coutTravauxMax: number (required) — estimate_max
  codePostalInitial?: string — pré-rempli depuis l'étape 5 du simulateur (route standalone : absent)

Emits :
  'complete' → { aides_estimees: number; reste_a_charge_min: number; reste_a_charge_max: number }  (succès)
  'skip'     → void  (API indisponible OU l'utilisateur choisit de continuer sans les aides)

Charte 04.7 : .bento-card / .reveal-item, tuile active `border-orange-500 bg-orange-50`, CTA `bg-safety`, tuiles `min-h-11` (44px), zéro token cyan/vert.
Regex/format à réutiliser : `form.postal_code` 5 chiffres (pattern `[0-9]{5}`, inputmode numeric) comme simulateur.vue étape 5.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Script setup + état + appel proxy</name>
  <files>app/components/aides/AidesMiniTunnel.vue</files>
  <read_first>
    - app/pages/simulateur.vue
    - .planning/phases/05.9-extension-simulateur-mes-aides-reno/05.9-RESEARCH.md
    - .planning/phases/04.7-UI-Refonte/04.7-UI-SPEC.md
  </read_first>
  <action>
    Créer `app/components/aides/AidesMiniTunnel.vue` en `<script setup lang="ts">`.

    Props :
    ```
    const props = withDefaults(defineProps<{
      coutTravauxMin: number
      coutTravauxMax: number
      codePostalInitial?: string
    }>(), { codePostalInitial: '' })
    const emit = defineEmits<{
      (e: 'complete', p: { aides_estimees: number; reste_a_charge_min: number; reste_a_charge_max: number }): void
      (e: 'skip'): void
    }>()
    ```

    État :
    - `step = ref(1)` (1 = logement, 2 = foyer/revenu, 3 = résultat).
    - `form = reactive({ logement_type: '', periode_construction: '', statut_proprietaire: '', residence_principale: true, nb_personnes: 2, revenu_classe: '', code_postal: props.codePostalInitial })`.
    - `result = ref<null | { aides_total: number; reste_a_charge_min: number; reste_a_charge_max: number }>(null)`.
    - `unavailable = ref(false)`, `isLoading = ref(false)`.

    Options (label FR + valeur API Publicodes en `value`) :
    - `logementTypes = [{ id:'maison', label:'Maison' }, { id:'appartement', label:'Appartement' }]`.
    - `periodesConstruction = [{ id:'avant_1948', label:'Avant 1948' }, { id:'1948_1974', label:'1948 – 1974' }, { id:'1975_1977', label:'1975 – 1977' }, { id:'1978_1996', label:'1978 – 1996' }, { id:'apres_1996', label:'Après 1996' }]` — `// ponytail: valeurs Publicodes exactes à re-valider (la recherche n'a pas épinglé ce champ)`.
    - `statutsProprietaire = [{ id:'proprietaire', label:'Propriétaire' }, { id:'locataire', label:'Locataire' }]` — `// ponytail: valeur 'locataire' à re-valider`.
    - `revenusClasses = [{ id:'modeste', label:'Modeste' }, { id:'intermediaire', label:'Intermédiaire' }, { id:'superieure', label:'Supérieure' }]` — `'modeste' confirmé par la recherche, les deux autres à re-valider`.

    Validation :
    - `isStepValid = computed(() => step.value === 1 ? !!form.logement_type && !!form.periode_construction && !!form.statut_proprietaire : (form.nb_personnes >= 1 && form.nb_personnes <= 12 && !!form.revenu_classe && /^\d{5}$/.test(form.code_postal)))`.

    `submit()` :
    - `isLoading.value = true`.
    - `$fetch('/api/v1/aides-reno', { method:'POST', body: { situation: { revenu_classe: form.revenu_classe, logement_type: form.logement_type, statut_proprietaire: form.statut_proprietaire, periode_construction: form.periode_construction, residence_principale: form.residence_principale, nb_personnes: form.nb_personnes, code_postal: form.code_postal }, cout_travaux_min: props.coutTravauxMin, cout_travaux_max: props.coutTravauxMax } })`.
    - Réponse `ok === true` → `result.value = { aides_total, reste_a_charge_min, reste_a_charge_max }` ; `step.value = 3` ; émettre `emit('complete', { aides_estimees: aides_total, reste_a_charge_min, reste_a_charge_max })`.
    - Réponse `ok === false` OU erreur `$fetch` → `unavailable.value = true` (afficher le fallback, ne pas throw).
    - `finally { isLoading.value = false }`.

    `skip()` → `emit('skip')`.
  </action>
  <verify>
    <automated>cd /home/mike/projects/bati-axe && npx nuxi typecheck 2>&1 | grep -i "AidesMiniTunnel" && exit 1 || echo "OK AidesMiniTunnel typecheck"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "defineProps" app/components/aides/AidesMiniTunnel.vue` et `grep -q "coutTravauxMin" app/components/aides/AidesMiniTunnel.vue`
    - `grep -q "'complete'" app/components/aides/AidesMiniTunnel.vue` et `grep -q "'skip'" app/components/aides/AidesMiniTunnel.vue`
    - `grep -q "aides-reno" app/components/aides/AidesMiniTunnel.vue` (appel proxy, jamais `mesaides.france-renov` en direct)
    - `grep -q "unavailable" app/components/aides/AidesMiniTunnel.vue`
    - `grep -q "aides_estimees" app/components/aides/AidesMiniTunnel.vue`
    - `! grep -q "mesaides.france-renov" app/components/aides/AidesMiniTunnel.vue`
  </acceptance_criteria>
  <done>Le script collecte la situation, appelle le proxy, émet complete (succès) ou skip (panne/abandon), sans jamais appeler l'API externe en direct.</done>
</task>

<task type="auto">
  <name>Task 2: Template — 2 étapes tuilées + résultat/fallback</name>
  <files>app/components/aides/AidesMiniTunnel.vue</files>
  <read_first>
    - app/components/aides/AidesMiniTunnel.vue
    - app/pages/simulateur.vue
    - .planning/phases/04.7-UI-Refonte/04.7-UI-SPEC.md
  </read_first>
  <action>
    Écrire le `<template>` en 3 états, charte 04.7 (`.bento-card`, tuile active `border-orange-500 bg-orange-50`, CTA `bg-safety`, `min-h-11`, zéro cyan/vert) :

    Étape 1 — « Votre logement » :
    - Titre `Votre logement`.
    - 2 tuiles `logementTypes` (Maison / Appartement) — sélection unique, auto-advance vers la question suivante (pas de saut d'étape).
    - Tuiles `periodesConstruction` (5) et `statutsProprietaire` (2) — sélection unique, actif `border-orange-500 bg-orange-50`.
    - Toggle `residence_principale` : deux boutons Oui/Non (Oui = `true` par défaut).

    Étape 2 — « Votre foyer & revenus » :
    - Titre `Votre foyer & revenus` + sous-texte court « Ces informations servent uniquement à estimer vos aides MaPrimeRénov' / CEE / Éco-PTZ. ».
    - Input `nb_personnes` (type number, min 1, max 12, inputmode numeric).
    - Tuiles `revenusClasses` (3) — sélection unique.
    - Input `code_postal` (maxlength 5, pattern `[0-9]{5}`, inputmode numeric, pré-rempli si `codePostalInitial`) + `normalizePostalCode` (même logique que simulateur.vue : `.replace(/\D/g,'').slice(0,5)`).
    - CTA `Voir mes aides` (bg-safety) déclenchant `submit()`, désactivé si `!isStepValid || isLoading`, spinner `isLoading`.
    - Bouton secondaire `Retour`.

    Étape 3 — Résultat (si `result`) :
    - Titre `Vos aides estimées`.
    - Bloc `formatEuro(aides_total)` + bloc fourchette `formatEuro(reste_a_charge_min) – formatEuro(reste_a_charge_max)` libellé « Reste à charge estimé ».
    - `formatEuro = (n) => n.toLocaleString('fr-FR') + ' €'` (réutiliser le helper du simulateur, ne pas réinventer).
    - Bouton `Continuer` (bg-safety) qui émet `emit('complete', …)` — ou, si le parent a déjà capté l'event au step 3, laisser le parent piloter la suite (le `complete` est émis dès l'arrivée au résultat, cf. Task 1).

    Fallback (si `unavailable`) :
    - Message court « Le calcul des aides est temporairement indisponible. Vous pouvez continuer sans. » + bouton `Continuer sans les aides` émettant `emit('skip')`.

    Navigation globale : header « Étape X / 2 » + barre de progression `bg-safety` (même pattern que simulateur.vue), bouton `Annuler` émettant `emit('skip')`.
  </action>
  <verify>
    <automated>grep -q "Votre logement" app/components/aides/AidesMiniTunnel.vue && grep -q "Votre foyer & revenus" app/components/aides/AidesMiniTunnel.vue && grep -q "Reste à charge estimé" app/components/aides/AidesMiniTunnel.vue && grep -q "border-orange-500" app/components/aides/AidesMiniTunnel.vue && grep -q "min-h-11" app/components/aides/AidesMiniTunnel.vue</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "Votre logement" app/components/aides/AidesMiniTunnel.vue`
    - `grep -q "Votre foyer & revenus" app/components/aides/AidesMiniTunnel.vue`
    - `grep -q "Reste à charge estimé" app/components/aides/AidesMiniTunnel.vue`
    - `grep -q "temporairement indisponible" app/components/aides/AidesMiniTunnel.vue`
    - `grep -q "border-orange-500" app/components/aides/AidesMiniTunnel.vue` et `grep -q "min-h-11" app/components/aides/AidesMiniTunnel.vue`
    - `! grep -Eq "cyan|emerald|bg-green|text-green" app/components/aides/AidesMiniTunnel.vue`
  </acceptance_criteria>
  <done>Le mini-tunnel affiche 2 étapes tuilées (logement puis foyer/revenu), le résultat aides + reste à charge, et le fallback non bloquant en cas de panne.</done>
</task>

</tasks>

<verification>
- `npx nuxi typecheck` sans erreur sur AidesMiniTunnel.vue.
- Les grep d'acceptance passent (proxy seul appelé, zéro appel direct, zéro cyan/vert).
- Parcours manuel (via plan 03/04) : 2 étapes → résultat OU fallback sans blocage.
</verification>

<success_criteria>
- Un seul composant réutilisable collecte logement + foyer/revenu et interroge le proxy (D-01, SC-1).
- Dégradation propre : panne API → message + « Continuer sans les aides », jamais de blocage (D-04).
</success_criteria>

<output>
Créer `.planning/phases/05.9-extension-simulateur-mes-aides-reno/05.9-02-SUMMARY.md` quand terminé.
</output>
