# 05.9-04 Summary — Route standalone + teaser home + restitution espace client

**Status:** ✅ Complete
**Date:** 2026-08-18

## Livré
- `app/pages/calculateur-aides.vue` — route `/calculateur-aides` : saisie coût travaux puis délégation à `AidesMiniTunnel` (aucune duplication).
- `app/pages/index.vue` — teaser bento « Estimez vos aides en 30 secondes » → `/calculateur-aides` (D-06, aucun calcul embarqué).
- `server/api/v1/magic-link/[token].get.ts` — `calculator_data` exposé dans le select (D-07).
- `app/pages/mon-projet/[token].vue` — bloc « Bilan financier estimé » gardé par `calculator_data?.reste_a_charge_min != null`.

## Vérification
- Greps d'acceptance : tous passent (zéro appel direct, zéro régression charte).
- `tsc --noEmit` : 0 erreur. `npm run build` : exit 0.

## Écarts / notes
- `cout` (coût travaux) est passé à la fois en `min` et `max` → reste à charge ponctuel (pas de fourchette) pour l'entrée standalone « calculatrice de poche ».
