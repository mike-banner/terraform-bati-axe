# 05.9-01 Summary — Proxy Nitro aides-reno + resolver CP→INSEE + persistance

**Status:** ✅ Complete
**Date:** 2026-08-18

## Livré
- `server/utils/codePostalToInsee.ts` — résolution code postal → INSEE via `geo.api.gouv.fr`, cache mémoire (`Map<string, string | null>`), timeout 5s, ne throw jamais.
- `server/utils/aidesReno.ts` — `buildSituationQuery` (mapping FERMÉ whitelist, valeurs entre guillemets simples, clés accentuées sans espaces) + `computeAidesSummary` (somme des subventions numériques, prêts exclus, reste à charge local = coût − aides).
- `server/api/v1/aides-reno.post.ts` — endpoint proxy `POST /api/v1/aides-reno` : Zod, résolution INSEE, `$fetch` vers `mesaides.france-renov.gouv.fr/api/v1` (timeout 8s), dégradation `{ ok:false }` sur toute panne.
- `server/api/v1/projects.post.ts` — `calculator_data` étendu de 3 champs optionnels (`aides_estimees`, `reste_a_charge_min`, `reste_a_charge_max`), aucune migration.

## Vérification
- `tsx` smoke test du mapping + calcul : **OK** (reste à charge 38400–43400 sur coût 40000–45000, éco-PTZ exclu).
- `tsc --noEmit` : **0 erreur**.
- `npm run build` : **exit 0**.
- Greps d'acceptance : tous passent.

## Écarts / notes
- `nuxi typecheck` réparé (ajout `vue-tsc@3.3.10` en devDependency) : 0 erreur sur les fichiers 05.9. Le repo comporte 11 erreurs pré-existantes sans rapport avec cette phase.
- Mapping Publicodes **validé par appel réel le 2026-08-18** : `logement.période de construction` ✅ (disparaît des `missingVariables`) ; `logement.résidence principale` et `ménage.personnes` sont des **no-ops** (non consommés par `eligibilite`) → retirés du mapping.
- `logement.surface` est désormais envoyé : récupéré de `surface_m2` dans le simulateur (prop `:surface`), ou demandé par un champ dédié dans `/calculateur-aides` (le mini-tunnel n'affiche le champ surface que si la prop est absente).
- Reste ouvert (noté en deferred dans 05.9-CONTEXT.md) : `DPE.actuel` / `projet.DPE visé` (classes A→G, valeurs validées) et `parcours d'aide` (enum à identifier) — à traiter plus tard pour un montant 100% complet.
