---
phase: 05.9-extension-simulateur-mes-aides-reno
plan: 03
type: execute
wave: 2
depends_on: ["05.9-01", "05.9-02"]
files_modified:
  - app/pages/simulateur.vue
autonomous: true
requirements: [SC-1, SC-2, D-02]
must_haves:
  truths:
    - "Le parcours 'Non' (les 6 étapes actuelles) reste identique — le fork n'ajoute AUCUNE question au flux 'Non'"
    - "Le fork Oui/Non s'affiche entre l'étape 5 (code postal) et l'étape 6 (lead wall), jamais après la révélation"
    - "Le parcours 'Oui' branche AidesMiniTunnel puis retombe sur l'étape 6 → un seul POST /api/v1/projects avec les champs aides"
    - "La révélation devient un 'bilan complet' : estimation + aides + reste à charge quand les aides sont présentes"
  artifacts:
    - path: "app/pages/simulateur.vue"
      provides: "Fork aides optionnel + intégration AidesMiniTunnel + révélation bilan complet"
  key_links:
    - from: "app/pages/simulateur.vue"
      to: "app/components/aides/AidesMiniTunnel.vue"
      via: "<AidesMiniTunnel> avec @complete/@skip"
      pattern: "AidesMiniTunnel"
    - from: "app/pages/simulateur.vue"
      to: "/api/v1/projects"
      via: "calculator_data incluant aides_estimees/reste_a_charge_min/max"
      pattern: "aides_estimees"
---

<objective>
Intégrer le mini-tunnel aides dans `/simulateur.vue` comme **embranchement optionnel** (D-02) : un choix Oui/Non apparaît après l'étape 5 (code postal) ; « Non » mène directement au lead wall (étape 6, comportement actuel inchangé) ; « Oui » ouvre `AidesMiniTunnel` puis retombe sur l'étape 6. Un seul `POST /api/v1/projects` persiste estimation + aides, et la révélation devient un « bilan complet » (estimation + aides + reste à charge).

Purpose: Faire des aides un accélérateur de conversion sans toucher au parcours « Non » (le tunnel 5.6 reste intact).
Output: `app/pages/simulateur.vue` modifié.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/05.9-extension-simulateur-mes-aides-reno/05.9-CONTEXT.md
@.planning/phases/05.9-extension-simulateur-mes-aides-reno/05.9-RESEARCH.md
@app/pages/simulateur.vue
@app/components/aides/AidesMiniTunnel.vue
@.planning/phases/04.7-UI-Refonte/04.7-UI-SPEC.md

<interfaces>
État actuel de simulateur.vue :
- `step = ref(1)`, `totalSteps = 6`, `stepLabels` 1..6, `progress` = ((step-1)/totalSteps)*100.
- Étape 5 = code postal (validation `form.postal_code === '78955'`) ; étape 6 = lead wall.
- `nextStep()` avance `step` de 5 à 6 sans écran intermédiaire.
- `handleSubmit()` envoie `calculator_data: { renovation_type, pieces, surface_m2, gamme, estimate_min, estimate_max }`.
- Bouton « Suivant » (étapes 2/3/5) : label `step === totalSteps - 1 ? 'Voir mon estimation' : 'Suivant'` — à changer car l'étape 5 ne mène plus à l'estimation.
- `revealed = ref(false)` → bloc révélation affiche `estimate.estimate_min`/`estimate_max`.

Contrat AidesMiniTunnel (plan 05.9-02) :
  Props : `:cout-travaux-min`, `:cout-travaux-max`, `:code-postal-initial`
  Emits : `@complete` → `{ aides_estimees, reste_a_charge_min, reste_a_charge_max }` ; `@skip` → void.

Champs aides attendus par le serveur (plan 05.9-01, Task 3) : `aides_estimees`, `reste_a_charge_min`, `reste_a_charge_max` (optionnels dans calculator_data).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Script — état du fork, nextStep, handlers aides, handleSubmit étendu</name>
  <files>app/pages/simulateur.vue</files>
  <read_first>
    - app/pages/simulateur.vue
    - app/components/aides/AidesMiniTunnel.vue
  </read_first>
  <action>
    Dans `<script setup>` de `app/pages/simulateur.vue`, ajouter après `const revealed = ref(false)` :

    ```
    // ─── Fork aides (Phase 05.9) — embranchement optionnel avant le lead wall ───
    const atAidesFork     = ref(false)          // affiche le choix Oui/Non
    const showAidesTunnel = ref(false)          // affiche <AidesMiniTunnel>
    const aidesResult     = ref<null | { aides_estimees: number; reste_a_charge_min: number; reste_a_charge_max: number }>(null)
    ```

    Modifier `nextStep()` : intercepter le passage étape 5 → 6 :
    ```
    const nextStep = () => {
      if (step.value < totalSteps && isStepValid.value) {
        if (step.value === 5) {
          atAidesFork.value = true      // le fork remplace le saut direct vers l'étape 6
          submitError.value = null
          return
        }
        step.value++
        if (step.value === 2 && form.renovation_type === 'totale') step.value = 3
        submitError.value = null
      }
    }
    ```

    Ajouter les handlers :
    ```
    const chooseNoAides = () => { atAidesFork.value = false; step.value = 6 }
    const chooseYesAides = () => { atAidesFork.value = false; showAidesTunnel.value = true }
    const onAidesComplete = (p: { aides_estimees: number; reste_a_charge_min: number; reste_a_charge_max: number }) => {
      aidesResult.value = p
      showAidesTunnel.value = false
      step.value = 6
    }
    const onAidesSkip = () => { showAidesTunnel.value = false; step.value = 6 }
    ```

    Étendre `handleSubmit()` — dans le body POST, `calculator_data` devient :
    ```
    calculator_data: {
      renovation_type: form.renovation_type,
      pieces:          form.pieces,
      surface_m2:      form.surface_m2,
      gamme:           form.gamme,
      estimate_min:    estimate.value.estimate_min,
      estimate_max:    estimate.value.estimate_max,
      ...(aidesResult.value ?? {}),
    },
    ```
    (`aidesResult` porte EXACTEMENT les clés `aides_estimees`, `reste_a_charge_min`, `reste_a_charge_max` émises par le composant ; `?? {}` = aucun champ aides si l'utilisateur a choisi « Non »).
  </action>
  <verify>
    <automated>cd /home/mike/projects/bati-axe && grep -q "atAidesFork" app/pages/simulateur.vue && grep -q "showAidesTunnel" app/pages/simulateur.vue && grep -q "aidesResult" app/pages/simulateur.vue && grep -q "aides_estimees" app/pages/simulateur.vue && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "atAidesFork" app/pages/simulateur.vue` et `grep -q "showAidesTunnel" app/pages/simulateur.vue`
    - `grep -q "aidesResult" app/pages/simulateur.vue`
    - `grep -q "step.value === 5" app/pages/simulateur.vue` (interception du fork dans nextStep)
    - `grep -q "...(aidesResult.value ?? {})" app/pages/simulateur.vue` (ou `...(aidesResult.value ?? {})` sans guillemets)
    - `grep -q "onAidesComplete" app/pages/simulateur.vue` et `grep -q "onAidesSkip" app/pages/simulateur.vue`
  </acceptance_criteria>
  <done>Le fork intercepte le passage 5→6, le tunnel remplit aidesResult, et handleSubmit persiste les champs aides uniquement si présents.</done>
</task>

<task type="auto">
  <name>Task 2: Template — fork Oui/Non + intégration AidesMiniTunnel + navigation</name>
  <files>app/pages/simulateur.vue</files>
  <read_first>
    - app/pages/simulateur.vue
    - app/components/aides/AidesMiniTunnel.vue
  </read_first>
  <action>
    Dans le `<template>`, INSÉRER deux états dans la branche `<template v-else>` (à l'intérieur du `<Transition>`, AVANT les blocs `v-if="step === 1"` etc.), et masquer la barre de navigation du tunnel principal quand le fork/tunnel est actif :

    1. Fork (Oui/Non) — `v-if="atAidesFork"` :
    ```
    <div v-if="atAidesFork" key="aidesFork" class="space-y-4 reveal">
      <h1 class="text-3xl md:text-4xl font-black tracking-tight text-foreground" style="text-wrap: balance">
        Connaissez-vous vos aides au financement ?
      </h1>
      <p class="text-sm text-muted-foreground">
        MaPrimeRénov' · CEE · Éco-PTZ — estimez le reste à charge réel de votre projet.
      </p>
      <div class="grid grid-cols-1 sm:grid-cols-2 gap-2 pt-2">
        <button type="button" @click="chooseYesAides" class="bento-card reveal-item ... bg-safety text-white ... min-h-11">Oui, estimer mes aides</button>
        <button type="button" @click="chooseNoAides" class="bento-card reveal-item ... border-border ... min-h-11">Non, voir mon estimation</button>
      </div>
    </div>
    ```

    2. Mini-tunnel — `v-else-if="showAidesTunnel"` :
    ```
    <div v-else-if="showAidesTunnel" key="aidesTunnel">
      <AidesMiniTunnel
        :cout-travaux-min="estimate.estimate_min"
        :cout-travaux-max="estimate.estimate_max"
        :code-postal-initial="form.postal_code"
        @complete="onAidesComplete"
        @skip="onAidesSkip"
      />
    </div>
    ```

    3. Les étapes existantes passent en `v-else-if="step === N"` (déjà le cas ; vérifier que les blocs step 1..6 suivent bien les deux nouveaux blocs).

    4. Navigation footer : masquer la barre `Retour`/`Suivant` du tunnel principal quand `atAidesFork || showAidesTunnel` — envelopper la `<div class="mt-8 pt-6 border-t ...">` d'un `v-if="!atAidesFork && !showAidesTunnel"` (le fork et le tunnel ont leurs propres contrôles).

    5. Bouton « Suivant » de l'étape 5 : remplacer le label `step === totalSteps - 1 ? 'Voir mon estimation' : 'Suivant'` par `step === 5 ? 'Continuer' : 'Suivant'` (l'étape 5 ouvre désormais le fork, pas l'estimation). Le CTA submit de l'étape 6 (« Recevoir mon estimation gratuite ») reste inchangé.
  </action>
  <verify>
    <automated>grep -q "Connaissez-vous vos aides au financement" app/pages/simulateur.vue && grep -q "AidesMiniTunnel" app/pages/simulateur.vue && grep -q "chooseYesAides" app/pages/simulateur.vue && grep -q "!atAidesFork" app/pages/simulateur.vue && grep -q "step === 5 ? 'Continuer'" app/pages/simulateur.vue && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "Connaissez-vous vos aides au financement" app/pages/simulateur.vue`
    - `grep -q "Oui, estimer mes aides" app/pages/simulateur.vue` et `grep -q "Non, voir mon estimation" app/pages/simulateur.vue`
    - `grep -q "AidesMiniTunnel" app/pages/simulateur.vue`
    - `grep -q "@complete=\"onAidesComplete\"" app/pages/simulateur.vue` et `grep -q "@skip=\"onAidesSkip\"" app/pages/simulateur.vue`
    - `grep -q "!atAidesFork && !showAidesTunnel" app/pages/simulateur.vue`
    - `grep -q "step === 5 ? 'Continuer' : 'Suivant'" app/pages/simulateur.vue`
  </acceptance_criteria>
  <done>Le fork Oui/Non s'affiche entre l'étape 5 et 6, branche le composant aides, et la navigation principale est masquée pendant le fork/tunnel.</done>
</task>

<task type="auto">
  <name>Task 3: Révélation « bilan complet » (estimation + aides + reste à charge)</name>
  <files>app/pages/simulateur.vue</files>
  <read_first>
    - app/pages/simulateur.vue
  </read_first>
  <action>
    Dans le bloc `v-if="revealed"` de `app/pages/simulateur.vue`, APRÈS le `<h1>` affichant la fourchette `estimate_min – estimate_max`, insérer un bloc conditionnel d'aides :

    ```
    <div v-if="aidesResult" class="p-5 border border-slate-200 bg-slate-50 rounded-sm mb-8">
      <p class="text-xs font-semibold text-slate-500 uppercase tracking-wide mb-2">Bilan financier estimé</p>
      <div class="grid grid-cols-2 gap-4">
        <div>
          <p class="text-xs text-muted-foreground mb-1">Aides estimées</p>
          <p class="text-sm font-semibold text-foreground">{{ formatEuro(aidesResult.aides_estimees) }}</p>
        </div>
        <div>
          <p class="text-xs text-muted-foreground mb-1">Reste à charge</p>
          <p class="text-sm font-semibold text-foreground">{{ formatEuro(aidesResult.reste_a_charge_min) }} – {{ formatEuro(aidesResult.reste_a_charge_max) }}</p>
        </div>
      </div>
      <p class="text-xs text-slate-500 mt-3">Estimation indicative basée sur les barèmes d'État en vigueur. Un artisan certifié vous confirmera les montants exacts.</p>
    </div>
    ```

    `formatEuro` existe déjà dans le script (`const formatEuro = (n: number) => n.toLocaleString('fr-FR') + ' €'`). NE PAS le redéfinir. Le bloc doit être ABSENT quand `aidesResult` est null (parcours « Non » — aucune mention d'aides).
  </action>
  <verify>
    <automated>grep -q "Bilan financier estimé" app/pages/simulateur.vue && grep -q "aidesResult.aides_estimees" app/pages/simulateur.vue && grep -q "aidesResult.reste_a_charge_min" app/pages/simulateur.vue && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "Bilan financier estimé" app/pages/simulateur.vue`
    - `grep -q "aidesResult.aides_estimees" app/pages/simulateur.vue`
    - `grep -q "aidesResult.reste_a_charge_min" app/pages/simulateur.vue`
    - Le bloc aides est dans un `v-if="aidesResult"` (lecture : pas d'affichage d'aides hors de ce guard)
    - `npm run build` réussit (exit 0)
  </acceptance_criteria>
  <done>La révélation affiche estimation + aides + reste à charge quand le parcours « Oui » a été suivi, et reste identique pour le parcours « Non ».</done>
</task>

</tasks>

<verification>
- `npm run build` réussit.
- Parcours manuel : (a) « Non » → étape 6 → révélation estimation seule, identique à avant ; (b) « Oui » → mini-tunnel 2 étapes → étape 6 → un seul POST avec aides_estimees/reste_a_charge_* → révélation bilan complet ; (c) panne API simulée → fallback « Continuer sans les aides » puis lead wall sans champs aides.
</verification>

<success_criteria>
- Le parcours « Non » est strictement inchangé (D-02).
- Le parcours « Oui » ajoute les aides via un seul POST et révèle le bilan complet (SC-1, SC-2).
- Aucune question aides imposée au parcours « Non » ; dégradation propre en cas de panne (D-04).
</success_criteria>

<output>
Créer `.planning/phases/05.9-extension-simulateur-mes-aides-reno/05.9-03-SUMMARY.md` quand terminé.
</output>
