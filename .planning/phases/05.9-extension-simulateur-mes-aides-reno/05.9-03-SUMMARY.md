# 05.9-03 Summary — Fork aides dans simulateur.vue + révélation bilan complet

**Status:** ✅ Complete
**Date:** 2026-08-18

## Livré
- `app/pages/simulateur.vue` — fork Oui/Non après l'étape 5 (code postal), branchement `<AidesMiniTunnel>`, navigation principale masquée pendant le fork/tunnel, un seul `POST /api/v1/projects` avec `...(aidesResult.value ?? {})`, révélation « bilan complet » (estimation + aides + reste à charge) gardée par `v-if="aidesResult"`.

## Vérification
- Greps d'acceptance : tous passent.
- `tsc --noEmit` : 0 erreur. `npm run build` : exit 0.

## Écarts / notes
- Parcours « Non » strictement inchangé : `nextStep()` n'intercepte que `step === 5`, les 6 étapes d'origine sont intactes.
- Le label du bouton étape 5 passe de « Voir mon estimation » à « Continuer » (il ouvre désormais le fork, pas l'estimation).
