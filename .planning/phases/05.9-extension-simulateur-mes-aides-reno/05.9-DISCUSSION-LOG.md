# Phase 5.9: Extension Simulateur — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-08-18
**Phase:** 05.9-extension-simulateur-mes-aides-reno
**Areas discussed:** Données revenu, Articulation tunnels, Appel API, Fallback API, Persistance

---

## Données revenu / composition du foyer

| Option | Description | Selected |
|--------|-------------|----------|
| Ajouter une étape revenu/foyer dans le tunnel existant | Nouvelle étape optionnelle avant révélation | |
| Estimation par défaut sans demander | Tranche médiane par défaut, pas de nouvelle question | |
| Fourchette d'aides sans reste à charge précis | Pas de collecte de données sensibles | |

**Résultat :** aucune des 3 options proposées n'a été retenue telle quelle. L'utilisateur a proposé une alternative : un **second tunnel séparé** dédié au calcul des aides, plutôt que d'intégrer la question revenu dans le tunnel de travaux existant.

**Notes :** l'utilisateur a explicitement voulu protéger le tunnel principal du simulateur (Phase 5.6), optimisé pour la conversion ("max 5 clics"), de toute friction supplémentaire.

---

## Articulation entre le tunnel existant et le nouveau tunnel aides

| Option | Description | Selected |
|--------|-------------|----------|
| Suite optionnelle après le 1er tunnel | CTA post-lead dans simulateur.vue | |
| Tunnel indépendant, point d'entrée séparé | Route dédiée /calculateur-aides | |

**User's choice:** les deux, via un seul composant réutilisable avec plusieurs points d'entrée ("ponts") — pas de duplication de logique.

---

## Appel API Mes Aides Réno

| Option | Description | Selected |
|--------|-------------|----------|
| Proxy serveur Nitro | Nouvel endpoint /api/v1/aides-reno, appel côté serveur | ✓ |
| Appel direct depuis le navigateur | Fetch client-side comme computeEstimate() | |

**User's choice:** Proxy serveur Nitro (recommandé).

---

## Fallback si l'API Mes Aides Réno est indisponible

| Option | Description | Selected |
|--------|-------------|----------|
| Masquer le bloc aides, message discret | Estimation de base reste affichée, rien n'est bloqué | ✓ |
| Retry automatique puis message d'erreur | 1-2 tentatives silencieuses avant échec affiché | |

**User's choice:** Masquer le bloc aides, message discret (recommandé).

---

## Persistance du reste à charge

| Option | Description | Selected |
|--------|-------------|----------|
| Étendre calculator_data existant (JSONB) | Pas de migration, cohérent avec pattern Phase 5.6 | ✓ |
| Nouvelle colonne dédiée sur projects | Colonne typée numeric, requêtable en SQL | |

**User's choice:** Étendre calculator_data existant (recommandé).

---

## Claude's Discretion

- Nom exact de l'endpoint serveur et de la route standalone.
- Wording du CTA et du message de fallback.
- Format d'affichage du reste à charge.
- Mapping des paramètres du simulateur vers les paramètres de l'API Publicodes.

## Deferred Ideas

- Benchmark Arti-Box / sous-traitance B2B — noté "Phase 7+" par le client, hors scope.
- Arbitrage APIs IA (OCR, dictaphone, rendus 3D, agents vocaux) — tous rejetés/différés dans le brief.
