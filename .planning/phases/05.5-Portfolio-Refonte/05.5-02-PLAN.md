---
phase: 05.5-portfolio-refonte
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/api/presign-headers.test.ts
  - server/api/v1/pro/profile/logo-presign.post.ts
  - server/api/v1/pro/realisations/presign.post.ts
autonomous: true
requirements: [R2-FIX, BACKOFFICE]
must_haves:
  truths:
    - "Le presign du logo ne bake plus de Content-Length dans la signature AWS v4"
    - "Un endpoint de presign multi-fichiers existe pour les photos de réalisations"
    - "Un test bloque toute régression du bug SignatureDoesNotMatch"
  artifacts:
    - path: "server/api/v1/pro/profile/logo-presign.post.ts"
      provides: "Presign logo corrigé (sans headers dans aws.sign)"
    - path: "server/api/v1/pro/realisations/presign.post.ts"
      provides: "Presign R2 pour upload photos de chantiers"
    - path: "tests/api/presign-headers.test.ts"
      provides: "Garde anti-régression du bug Content-Length"
  key_links:
    - from: "logo-presign.post.ts"
      to: "aws.sign()"
      via: "signature PUT sans header Content-Length"
      pattern: "aws\\.sign"
---

<objective>
Corriger le bug de soumission du logo pro (Success Criterion 1) et fournir l'endpoint de presign R2 multi-fichiers requis par le formulaire d'ajout de réalisation.

Purpose: Root cause = `logo-presign.post.ts` passe `headers: { 'Content-Length': String(MAX_SIZE) }` à `aws.sign()`, ce qui fige un Content-Length de 5 Mo dans la signature v4. Le navigateur envoie le Content-Length réel du fichier → `SignatureDoesNotMatch`. Le sibling `documents/presign.post.ts` est déjà correct (sans headers).
Output: Fix du logo, nouvel endpoint réalisations, test de non-régression.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.5-Portfolio-Refonte/05.5-RESEARCH.md
@.planning/phases/05.5-Portfolio-Refonte/05.5-PATTERNS.md

<interfaces>
Référence correcte (server/api/v1/pro/documents/presign.post.ts, extrait) :
`const request = await aws.sign(url, { method: 'PUT', aws: { signQuery: true } })` — AUCUN objet `headers`.
Réponse standard : `{ status: 'SUCCESS', signedUrl: request.url, fileKey, publicUrl }`.
Helper R2 : `getAwsClient(config, env)`, `buildStorageKey({ shortId, slug, userId, type, filename })`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Wave 0 / RED): Test anti-régression Content-Length</name>
  <read_first>
    - tests/badges.test.ts (pattern vitest existant : describe/it/expect, imports relatifs)
    - vitest.config.ts (glob `tests/**/*.test.ts`, environment node par défaut, alias `~`)
    - server/api/v1/pro/profile/logo-presign.post.ts (état bugué courant)
    - server/api/v1/pro/documents/presign.post.ts (état correct de référence)
  </read_first>
  <behavior>
    - Test 1: le source de `logo-presign.post.ts` ne contient pas `Content-Length` à l'intérieur de l'appel `aws.sign` (assertion source : le fichier ne doit plus passer de header Content-Length)
    - Test 2: le source de `realisations/presign.post.ts` ne contient pas `Content-Length`
    - Test 3 (référence): `documents/presign.post.ts` sert de baseline correcte et ne contient pas non plus `Content-Length` passé à `aws.sign`
  </behavior>
  <files>tests/api/presign-headers.test.ts</files>
  <action>
    Créer un test vitest qui lit le contenu des fichiers source de presign avec `readFileSync` et affirme qu'aucun ne passe un header `Content-Length` à la signature. Motivation : tester la génération réelle de l'URL R2 nécessiterait de mocker aws4fetch + Supabase (coûteux, fragile) ; le bug est purement structurel (un header baké dans la signature), donc une assertion source est la garde la plus petite qui échoue exactement quand le bug réapparaît. Le test doit être RED tant que `logo-presign.post.ts` contient encore `Content-Length`, et GREEN après le fix de la Task 2. Fichier `realisations/presign.post.ts` créé en Task 3 — le test tolère son absence initiale via un guard `existsSync` OU la Task 3 crée le fichier avant que ce test soit vert (préférer : le test skip le check realisations si le fichier n'existe pas encore).
  </action>
  <verify>
    <automated>npx vitest run tests/api/presign-headers.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Le test échoue (RED) tant que `logo-presign.post.ts` contient `Content-Length` dans l'appel sign
    - Après fix (Task 2), `npx vitest run tests/api/presign-headers.test.ts` passe
    - Le test lit les sources via `readFileSync`, n'appelle pas le réseau, s'exécute en < 2s
  </acceptance_criteria>
  <done>Test de non-régression en place, RED contre l'état bugué actuel.</done>
</task>

<task type="auto">
  <name>Task 2 (GREEN): Corriger logo-presign.post.ts</name>
  <read_first>
    - server/api/v1/pro/profile/logo-presign.post.ts (lignes ~55-62 : l'appel `aws.sign` avec `headers: { 'Content-Type', 'Content-Length' }`)
    - server/api/v1/pro/documents/presign.post.ts (pattern cible sans headers)
  </read_first>
  <files>server/api/v1/pro/profile/logo-presign.post.ts</files>
  <action>
    Supprimer entièrement l'objet `headers` de l'appel `aws.sign(url, { method: 'PUT', aws: { signQuery: true } })`, exactement comme `documents/presign.post.ts`. Retirer aussi la constante `MAX_SIZE` si elle ne sert plus qu'à ce header (la limite 5 Mo reste appliquée côté client dans `espace/profil.vue`). Ne rien changer d'autre : garder la résolution défensive du `userId`, `buildStorageKey`, la construction de `publicUrl` et le catch qui force un statut 400.
  </action>
  <verify>
    <automated>npx vitest run tests/api/presign-headers.test.ts && npm run test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Content-Length" server/api/v1/pro/profile/logo-presign.post.ts` retourne 0
    - L'appel `aws.sign` du fichier est `aws.sign(url, { method: 'PUT', aws: { signQuery: true } })` (aucune clé `headers`)
    - `npx vitest run tests/api/presign-headers.test.ts` passe (GREEN)
    - La suite `npm run test` reste verte
  </acceptance_criteria>
  <done>Le presign logo ne fige plus de Content-Length ; le test passe.</done>
</task>

<task type="auto">
  <name>Task 3: Créer l'endpoint presign multi-fichiers réalisations</name>
  <read_first>
    - server/api/v1/pro/documents/presign.post.ts (analog exact : imports, auth `serverSupabaseUser`, getAwsClient, buildStorageKey, catch 400)
    - server/api/v1/pro/profile/logo-presign.post.ts (après fix Task 2 — même structure, type 'logo')
  </read_first>
  <files>server/api/v1/pro/realisations/presign.post.ts</files>
  <action>
    Créer l'endpoint de presign pour les photos de réalisations, calqué sur `documents/presign.post.ts`. Auth via `serverSupabaseUser(event)` → 401 si absent (résolution défensive du `userId` via `.id ?? .sub`). Valider le body avec Zod : `filename` (string) et `content_type` (string commençant par `image/`). Générer la clé via `buildStorageKey({ shortId, slug, userId, type: 'realisation', filename })` en récupérant `short_id`/`canonical_slug` du pro via service role. Signer avec `aws.sign(url, { method: 'PUT', aws: { signQuery: true } })` — SANS headers (ne pas reproduire le bug). Retourner `{ status: 'SUCCESS', signedUrl, fileKey, publicUrl }`. Catch qui `throw createError({ statusCode: 400, statusMessage: 'Storage Error', message })` (Nuxt masque les 500 en prod). Le client appelle cet endpoint une fois par fichier (fan-out géré côté RealisationForm, plan 05).
  </action>
  <verify>
    <automated>test -f server/api/v1/pro/realisations/presign.post.ts && grep -q "signQuery: true" server/api/v1/pro/realisations/presign.post.ts && ! grep -q "Content-Length" server/api/v1/pro/realisations/presign.post.ts && npx vue-tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Le fichier existe et exporte un `defineEventHandler`
    - `grep -c "Content-Length" server/api/v1/pro/realisations/presign.post.ts` retourne 0
    - L'appel `aws.sign` utilise `{ method: 'PUT', aws: { signQuery: true } }` sans headers
    - Auth 401 si `serverSupabaseUser` retourne null ; Zod valide `content_type` image/*
    - Réponse shape `{ status: 'SUCCESS', signedUrl, fileKey, publicUrl }`
    - `npx vitest run tests/api/presign-headers.test.ts` reste vert et couvre désormais ce fichier
  </acceptance_criteria>
  <done>Endpoint presign réalisations opérationnel, headerless, testé contre la régression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navigateur → Nitro presign | Pro authentifié demande une URL signée d'upload R2 |
| navigateur → R2 (PUT direct) | Upload direct du fichier vers Cloudflare via URL signée |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.5-03 | Spoofing | presign endpoints | mitigate | `serverSupabaseUser` requis (401 sinon) ; la clé R2 est scopée au short_id/slug du pro |
| T-05.5-04 | Tampering | content_type | mitigate | Zod exige `image/*` ; extension dérivée de filename par buildStorageKey |
| T-05.5-05 | DoS | taille fichier | accept | Limite 5 Mo appliquée côté client (espace/profil.vue) ; R2 zéro-egress, faible valeur cible |
</threat_model>

<verification>
Le bug Content-Length est éliminé des deux routes de presign et gardé par un test. L'endpoint réalisations réutilise le pattern correct de `documents/presign.post.ts`.
</verification>

<success_criteria>
ROADMAP Success Criterion 1 (échec soumission logo corrigé) satisfait ; presign multi-fichiers prêt pour le formulaire d'ajout (Criterion 3).
</success_criteria>

<output>
Create `.planning/phases/05.5-Portfolio-Refonte/05.5-02-SUMMARY.md` when done
</output>
