---
phase: 05.5-portfolio-refonte
plan: 05
type: execute
wave: 3
depends_on: ["05.5-02", "05.5-03"]
files_modified:
  - app/components/RealisationForm.vue
  - app/pages/espace/profil.vue
autonomous: false
requirements: [BACKOFFICE]
must_haves:
  truths:
    - "Un pro voit un bouton 'Ajouter une réalisation' dans /espace/profil"
    - "Le formulaire (modale) permet titre, description, ville et upload multi-photos"
    - "Les photos s'uploadent en parallèle vers R2, chacune avec sa progression"
    - "La soumission crée la réalisation et l'affiche dans la liste du pro"
  artifacts:
    - path: "app/components/RealisationForm.vue"
      provides: "Modale d'ajout de réalisation avec upload R2 multi-fichiers"
      min_lines: 60
    - path: "app/pages/espace/profil.vue"
      provides: "Bouton d'entrée + liste des réalisations existantes"
  key_links:
    - from: "RealisationForm.vue"
      to: "/api/v1/pro/realisations/presign + index.post"
      via: "presign par fichier puis create"
      pattern: "realisations/presign|realisations"
---

<objective>
Formulaire backoffice d'ajout de réalisation avec upload R2 multi-fichiers asynchrone (Success Criterion 3, côté UI).

Purpose: Permettre au pro de constituer sa galerie. Réutilise le mécanisme XHR-with-progress déjà présent pour le logo, en fan-out parallèle.
Output: Composant `RealisationForm.vue` (modale) + intégration dans `espace/profil.vue`.

Design: appliquer le contrat UI-SPEC 05.5. Skill obligatoire ui-ux-pro-max déjà synthétisé dans l'UI-SPEC — appliquer les classes existantes, pas de nouvelle exploration de style.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.5-Portfolio-Refonte/05.5-UI-SPEC.md
@.planning/phases/05.5-Portfolio-Refonte/05.5-PATTERNS.md
@.agent/skills/ui-ux-pro-max/SKILL.md

<interfaces>
CRITIQUE — librairie d'icônes : le paquet installé est `@lucide/vue` (PAS `lucide-vue-next` comme l'écrivent UI-SPEC/PATTERNS). Importer : `import { Upload, ImagePlus, X } from '@lucide/vue'`. Vérifié : ces exports existent.
Endpoints (plans 02/03) :
- POST `/api/v1/pro/realisations/presign` body `{ filename, content_type }` -> `{ status, signedUrl, fileKey, publicUrl }`
- POST `/api/v1/pro/realisations` body `{ title, description?, city?, image_urls: string[] }` -> `{ status, realisation }`
- GET `/api/v1/pro/realisations` -> `{ status, realisations }`
Helpers réutilisables dans espace/profil.vue : `putWithProgress` (XHR PUT + onprogress, ~lignes 100-114), `compressImage`, composant `UploadProgress.vue`. Classes CSS déjà définies dans tailwind.css : `.modal`, `.modal-overlay`, `.input`, `.bento-card`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Composant RealisationForm.vue (modale + upload parallèle)</name>
  <read_first>
    - app/pages/espace/profil.vue (lignes ~63-151 : handleLogoUpload, compressImage, putWithProgress ; lignes ~208-239 : markup carte upload logo)
    - app/components/UploadProgress.vue (barre de progression à réutiliser par fichier)
    - app/assets/css/tailwind.css (lignes ~300-336 : .modal, .modal-overlay, .input, .bento-card — aucune nouvelle CSS)
    - .planning/phases/05.5-Portfolio-Refonte/05.5-UI-SPEC.md (section 2 Backoffice + Copywriting Contract)
  </read_first>
  <files>app/components/RealisationForm.vue</files>
  <action>
    Créer la modale d'ajout. Structure : overlay `.modal-overlay flex items-center justify-center` + `.modal` (fermeture sur `@click.self` et bouton texte "Annuler"). Champs : `title` (input `.input`), `description` (textarea `.input`), `city` (input `.input`, préremplir depuis la prop `zone` du profil si fournie), sélecteur multi-fichiers `<input type="file" multiple accept="image/*">`. Icônes depuis `@lucide/vue` : `Upload`/`ImagePlus` (zone de dépôt), `X` (retirer un fichier avant envoi, couleur destructive `text-red-600`), taille fixe `w-5 h-5`.
    Upload : pour chaque fichier sélectionné, en PARALLÈLE (`Promise.all`), appeler `/api/v1/pro/realisations/presign` puis `putWithProgress` (helper réutilisé verbatim depuis espace/profil.vue) vers `signedUrl`, chaque fichier suivant sa propre ref de progression + vignette + un `UploadProgress`. NE PAS séquentialiser. Collecter les `publicUrl` retournés dans `image_urls`.
    Bouton "Publier la réalisation" : validation client — titre requis ET au moins une photo uploadée, sinon afficher "Merci de renseigner un titre et au moins une photo avant de publier." POST `/api/v1/pro/realisations` avec `{ title, description, city, image_urls }`. Échec d'upload d'une photo -> "L'envoi de la photo a échoué. Vérifiez votre connexion et réessayez." Émettre un événement `created` (avec la réalisation retournée) et fermer la modale au succès.
    Ponytail : réutiliser classes CSS et helpers existants, aucune dépendance ni composant UI supplémentaire.
  </action>
  <verify>
    <automated>test -f app/components/RealisationForm.vue && grep -q "@lucide/vue" app/components/RealisationForm.vue && grep -q "realisations/presign" app/components/RealisationForm.vue && grep -q "Promise.all" app/components/RealisationForm.vue && ! grep -q "lucide-vue-next" app/components/RealisationForm.vue && npm run test</automated>
  </verify>
  <acceptance_criteria>
    - Import icônes depuis `@lucide/vue` uniquement (`grep -c lucide-vue-next` = 0)
    - Upload multi-fichiers en parallèle via `Promise.all` (pas de boucle await séquentielle bloquante)
    - Validation client : titre + >=1 photo requis avant POST, message d'erreur exact du Copywriting Contract
    - Utilise les classes `.modal` / `.modal-overlay` / `.input` existantes, aucune nouvelle règle CSS ajoutée
    - Émet `created` avec la réalisation au succès
    - `npm run test` reste vert
  </acceptance_criteria>
  <done>La modale uploade les photos en parallèle vers R2 et crée la réalisation.</done>
</task>

<task type="auto">
  <name>Task 2: Intégration dans espace/profil.vue (bouton + liste)</name>
  <read_first>
    - app/pages/espace/profil.vue (fichier entier — état courant, bloc bio/catégories, bouton save ~lignes 324-328)
    - .planning/phases/05.5-Portfolio-Refonte/05.5-UI-SPEC.md (Copywriting : CTA "Ajouter une réalisation", empty state backoffice)
  </read_first>
  <files>app/pages/espace/profil.vue</files>
  <action>
    Ajouter un état `showRealisationModal` (ref false) et un tableau `realisations` chargé via `useFetch('/api/v1/pro/realisations')` (ou `$fetch` au mount). Insérer un bouton CTA "Ajouter une réalisation" (classes du bouton safety orange existant : `inline-flex items-center justify-center gap-2 h-11 px-6 bg-safety text-white text-sm font-semibold rounded-full hover:scale-105`) au-dessus/à côté du bloc bio, ouvrant `RealisationForm`. Afficher la liste des réalisations existantes (vignette première image + titre + ville) ; si vide, empty state "Aucune réalisation pour l'instant" + corps "Ajoutez vos chantiers terminés pour rassurer les futurs clients — photos avant/après, ville, description." À la réception de l'événement `created`, ajouter la réalisation en tête de liste et fermer la modale. Passer `zone` du profil au composant pour préremplir la ville.
    Ne PAS toucher au bloc d'upload logo existant (déjà corrigé plan 02).
  </action>
  <verify>
    <automated>grep -q "RealisationForm" app/pages/espace/profil.vue && grep -q "Ajouter une réalisation" app/pages/espace/profil.vue && grep -q "/api/v1/pro/realisations" app/pages/espace/profil.vue && npm run test</automated>
  </verify>
  <acceptance_criteria>
    - Bouton "Ajouter une réalisation" présent, ouvre la modale `RealisationForm`
    - Liste des réalisations affichée avec empty state exact du Copywriting Contract quand vide
    - `created` ajoute la nouvelle réalisation à la liste sans rechargement de page
    - Le bloc upload logo existant est inchangé
    - `npm run test` reste vert
  </acceptance_criteria>
  <done>Le pro accède au formulaire et voit ses réalisations dans /espace/profil.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Formulaire d'ajout de réalisation avec upload R2 multi-fichiers parallèle, intégré dans /espace/profil.</what-built>
  <how-to-verify>
    1. Lancer `npm run dev`, se connecter en pro sur http://localhost:3000/espace/profil
    2. Cliquer "Ajouter une réalisation" -> la modale s'ouvre
    3. Renseigner un titre, une ville, sélectionner 2-3 photos -> chaque photo montre sa propre barre de progression, en parallèle
    4. Cliquer "Publier la réalisation" -> la réalisation apparaît dans la liste, la modale se ferme
    5. Recharger la page -> la réalisation persiste (vérifie l'upload R2 réel + l'insert DB)
  </how-to-verify>
  <resume-signal>Tapez "approved" ou décrivez les problèmes rencontrés</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navigateur → R2 (PUT signé) | Upload direct des photos via URL signée obtenue du Nitro |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.5-12 | Tampering | fichiers uploadés | mitigate | `accept="image/*"` + presign exige content_type image/* (plan 02) ; limite taille client 5 Mo |
| T-05.5-13 | Elevation | création pour autrui | mitigate | POST realisations via serverSupabaseClient + RLS (plan 03), professional_id = user courant |
</threat_model>

<verification>
Upload parallèle fonctionnel, réalisation persistée en DB et R2, liste rafraîchie. Icônes depuis `@lucide/vue`. Checkpoint humain confirme le flux réel (R2 + persistance).
</verification>

<success_criteria>
ROADMAP Success Criterion 3 satisfait côté pro : formulaire d'ajout avec upload multiple opérationnel.
</success_criteria>

<output>
Create `.planning/phases/05.5-Portfolio-Refonte/05.5-05-SUMMARY.md` when done
</output>
