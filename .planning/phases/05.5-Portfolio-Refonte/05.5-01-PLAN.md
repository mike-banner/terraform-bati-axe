---
phase: 05.5-portfolio-refonte
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260717000000_phase055_portfolio.sql
autonomous: true
requirements: [DB-MODEL]
must_haves:
  truths:
    - "La table completed_projects existe avec toutes les colonnes du SPEC"
    - "La table likes existe avec une contrainte anti-doublon (project_id, ip_hash)"
    - "RLS: SELECT public sur completed_projects, écriture restreinte au propriétaire"
    - "RLS activé sur likes sans policy publique (writes via service_role uniquement)"
  artifacts:
    - path: "supabase/migrations/20260717000000_phase055_portfolio.sql"
      provides: "Schéma completed_projects + likes + policies RLS"
      contains: "CREATE TABLE"
  key_links:
    - from: "completed_projects.professional_id"
      to: "professionals.id"
      via: "FOREIGN KEY ON DELETE CASCADE"
      pattern: "REFERENCES public.professionals"
    - from: "likes.project_id"
      to: "completed_projects.id"
      via: "FOREIGN KEY ON DELETE CASCADE"
      pattern: "REFERENCES public.completed_projects"
---

<objective>
Créer le modèle de données du portfolio pro et de la preuve sociale : tables `completed_projects` (galerie de réalisations) et `likes` (compteur anti-spam), avec leurs policies RLS.

Purpose: Fondation DB pour tous les endpoints CRUD et UI des vagues suivantes (Success Criterion 2 du ROADMAP).
Output: Un fichier de migration SQL Supabase appliqué en local, deux tables opérationnelles.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.5-Portfolio-Refonte/05.5-RESEARCH.md
@.planning/phases/05.5-Portfolio-Refonte/05.5-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Écrire la migration completed_projects + likes + RLS</name>
  <read_first>
    - supabase/migrations/20260603000000_schema_initial.sql (pattern policies `select_public_professionals` + `manage_own_professional`, lignes 174-195)
    - supabase/migrations/20260609000000_phase45_schema.sql (pattern contrainte UNIQUE anti-doublon `unique_pro_lead_grant`, lignes 12-23)
    - supabase/migrations/20260609213210_phase5_messaging_schema.sql (pattern idempotent `DO $$ IF NOT EXISTS ... END $$` optionnel)
  </read_first>
  <files>supabase/migrations/20260717000000_phase055_portfolio.sql</files>
  <action>
    Créer le fichier de migration. Table `public.completed_projects` : `id UUID PRIMARY KEY DEFAULT gen_random_uuid()`, `professional_id UUID NOT NULL REFERENCES public.professionals(id) ON DELETE CASCADE`, `title TEXT NOT NULL`, `description TEXT`, `city TEXT`, `image_urls TEXT[] NOT NULL DEFAULT '{}'`, `is_showcased BOOLEAN NOT NULL DEFAULT false`, `created_at TIMESTAMPTZ NOT NULL DEFAULT clock_timestamp()`.
    Activer RLS puis créer deux policies : `select_public_completed_projects` FOR SELECT TO public USING (true) ; `manage_own_completed_projects` FOR ALL TO authenticated USING (auth.uid() = professional_id). Ne PAS créer de policy pour `is_showcased` : le toggle admin passe par service_role côté API (voir plan 04).
    Table `public.likes` : `id UUID PRIMARY KEY DEFAULT gen_random_uuid()`, `project_id UUID NOT NULL REFERENCES public.completed_projects(id) ON DELETE CASCADE`, `ip_hash TEXT NOT NULL`, `created_at TIMESTAMPTZ NOT NULL DEFAULT clock_timestamp()`, plus `CONSTRAINT unique_project_ip_like UNIQUE (project_id, ip_hash)`. Créer un index `idx_likes_project_id ON public.likes(project_id)`. Activer RLS sur `likes` SANS aucune policy (accès public/auth refusé par défaut, seuls les endpoints service_role écrivent/comptent).
    Ne jamais stocker d'IP brute : `ip_hash` reçoit un SHA-256 calculé côté endpoint (plan 04). C'est une colonne de texte opaque ici.
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260717000000_phase055_portfolio.sql && grep -q "CREATE TABLE IF NOT EXISTS public.completed_projects" supabase/migrations/20260717000000_phase055_portfolio.sql && grep -q "unique_project_ip_like" supabase/migrations/20260717000000_phase055_portfolio.sql</automated>
  </verify>
  <acceptance_criteria>
    - Le fichier contient `CREATE TABLE IF NOT EXISTS public.completed_projects` avec les 8 colonnes exactes du SPEC
    - Le fichier contient `CREATE TABLE IF NOT EXISTS public.likes` avec `CONSTRAINT unique_project_ip_like UNIQUE (project_id, ip_hash)`
    - `completed_projects.professional_id` référence `public.professionals(id) ON DELETE CASCADE`
    - `likes.project_id` référence `public.completed_projects(id) ON DELETE CASCADE`
    - Deux policies présentes sur completed_projects : `select_public_completed_projects` (SELECT public) et `manage_own_completed_projects` (ALL authenticated, `auth.uid() = professional_id`)
    - `ALTER TABLE public.likes ENABLE ROW LEVEL SECURITY` présent, aucune `CREATE POLICY` sur `likes`
  </acceptance_criteria>
  <done>Migration écrite, les deux tables et leurs contraintes/policies sont déclarées conformément au SPEC.</done>
</task>

<task type="auto">
  <name>Task 2: Appliquer et vérifier la migration en local</name>
  <read_first>
    - supabase/config.toml (ports Supabase local — DB généralement sur 54322)
    - .env (URL Supabase local 127.0.0.1:54321)
  </read_first>
  <files>supabase/migrations/20260717000000_phase055_portfolio.sql</files>
  <action>
    Appliquer la migration sur la stack Supabase locale (`supabase migration up` ; si la migration doit être rejouée proprement, `supabase db reset` recrée tout le schéma depuis les migrations). Vérifier ensuite que les deux tables et la contrainte unique existent réellement dans la base Postgres locale via une requête sur `information_schema` / `pg_constraint`. Ne pas toucher à la base de production.
  </action>
  <verify>
    <automated>psql "postgresql://postgres:postgres@127.0.0.1:54322/postgres" -tAc "select count(*) from information_schema.tables where table_schema='public' and table_name in ('completed_projects','likes')" | grep -q "^2$"</automated>
  </verify>
  <acceptance_criteria>
    - La requête sur `information_schema.tables` retourne exactement 2 pour (`completed_projects`,`likes`)
    - `psql ... -tAc "select conname from pg_constraint where conname='unique_project_ip_like'"` retourne `unique_project_ip_like`
    - `psql ... -tAc "select count(*) from pg_policies where tablename='completed_projects'"` retourne 2
  </acceptance_criteria>
  <done>Migration appliquée en local, les deux tables, la contrainte anti-doublon et les policies RLS sont vérifiées présentes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB RLS | Sépare lecture publique de la galerie et écriture réservée au pro propriétaire |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.5-01 | Elevation | completed_projects writes | mitigate | RLS `manage_own_completed_projects` (auth.uid() = professional_id) empêche un pro de modifier les réalisations d'un autre |
| T-05.5-02 | Information disclosure | likes.ip_hash | mitigate | Colonne stocke un SHA-256 opaque, jamais l'IP brute (calcul côté endpoint plan 04) |
</threat_model>

<verification>
Les deux tables existent en local avec RLS, FK CASCADE et contrainte anti-doublon. `select_public_completed_projects` autorise la lecture publique de la galerie.
</verification>

<success_criteria>
ROADMAP Success Criterion 2 satisfait pour la partie schéma : tables `completed_projects` et `likes` opérationnelles avec RLS.
</success_criteria>

<output>
Create `.planning/phases/05.5-Portfolio-Refonte/05.5-01-SUMMARY.md` when done
</output>
