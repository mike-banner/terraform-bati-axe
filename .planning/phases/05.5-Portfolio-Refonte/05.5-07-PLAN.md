---
phase: 05.5-portfolio-refonte
plan: 07
type: execute
wave: 4
depends_on: ["05.5-04", "05.5-06"]
files_modified:
  - app/pages/pro/[dept]/[slug].vue
  - tests/ui/profile-layout.test.ts
autonomous: false
requirements: [PROFILE-UI]
must_haves:
  truths:
    - "Le profil public s'affiche en pleine page immersive, sans navbar générique"
    - "Un bouton flottant permet de revenir à l'accueil"
    - "La galerie des réalisations du pro s'affiche avec compteurs de likes"
    - "Le layout est mobile-first sans scroll horizontal"
  artifacts:
    - path: "app/pages/pro/[dept]/[slug].vue"
      provides: "Profil public immersif + galerie réalisations"
    - path: "tests/ui/profile-layout.test.ts"
      provides: "Garde: layout:false présent, pas de header générique"
  key_links:
    - from: "pro/[dept]/[slug].vue"
      to: "RealisationCard"
      via: "galerie v-for sur realisations du profil"
      pattern: "RealisationCard"
---

<objective>
Refonte immersive pleine page du profil public avec galerie de réalisations (Success Criterion 4).

Purpose: Supprimer la navbar générique (layout: false), navigation minimale (bouton flottant), intégration Bento des données existantes + galerie likes. Approche mobile-first absolue.
Output: `pro/[dept]/[slug].vue` refondu + test de garde layout.

Design: appliquer strictement le contrat UI-SPEC 05.5 section 1 et le skill ui-ux-pro-max (Bento, floating navbar top-4 left-4, glass light-mode). La logique de fetch existante (useFetch + needsRedirect + skeleton) reste inchangée.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.5-Portfolio-Refonte/05.5-UI-SPEC.md
@.planning/phases/05.5-Portfolio-Refonte/05.5-PATTERNS.md
@.agent/skills/ui-ux-pro-max/SKILL.md
@.agent/skills/sketch-findings-bati-axe/SKILL.md

<interfaces>
CRITIQUE — icônes : `import { ArrowLeft } from '@lucide/vue'` (PAS lucide-vue-next).
Le profil est fourni par `/api/v1/pro/profile/[slug]` qui retourne désormais (plan 04) une clé `realisations: [{ id, title, city, image_urls, likes:[{count}] }]` pour ce pro.
Composant `RealisationCard` (plan 06) : prop `project`. Badges existants réutilisés tels quels : `BadgeEntrepriseVerifiee`, `BadgeDecennaleCertifiee`.
Classes `.bento-card` déjà utilisées dans le fichier (blocs documents ~lignes 299-332).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Layout immersif + bouton flottant retour</name>
  <read_first>
    - app/pages/pro/[dept]/[slug].vue (fichier entier : useFetch ~ligne 53, needsRedirect, skeleton ~lignes 49-125, branche "PUBLIC VERIFIED PROFILE" ~lignes 266-349)
    - app/components/IdentityBreadcrumbs.vue (tokens couleur du breadcrumb : `text-[#64748B]`, hover `text-[#F97316]`)
    - .planning/phases/05.5-Portfolio-Refonte/05.5-UI-SPEC.md (section 1 Immersive Public Profile)
  </read_first>
  <files>app/pages/pro/[dept]/[slug].vue</files>
  <action>
    Ajouter `definePageMeta({ layout: false })` en tête du `<script setup>` pour supprimer la navbar générique. Ajouter un bouton flottant retour accueil : `fixed top-4 left-4 z-50 w-11 h-11 rounded-full bg-white shadow-lg flex items-center justify-center hover:-translate-y-0.5 transition-transform`, icône `ArrowLeft` de `@lucide/vue` `w-6 h-6 text-slate-700`, `@click="navigateTo('/')"`, `aria-label="Retour à l'accueil"`. Sur desktop >=768px, un variant breadcrumb (réutilisant IdentityBreadcrumbs) est acceptable ; mobile utilise toujours le bouton flottant. NE PAS modifier la logique de fetch / needsRedirect / skeleton existante ni l'invariant `is_verified: false` (pas de 404 pour profil existant non vérifié).
  </action>
  <verify>
    <automated>grep -q "layout: false" app/pages/pro/[dept]/[slug].vue && grep -q "Retour à l'accueil" app/pages/pro/[dept]/[slug].vue && grep -q "@lucide/vue" app/pages/pro/[dept]/[slug].vue && npm run test</automated>
  </verify>
  <acceptance_criteria>
    - `definePageMeta({ layout: false })` présent -> aucun `<header>` de layout par défaut rendu
    - Bouton flottant `fixed top-4 left-4`, `aria-label="Retour à l'accueil"`, icône ArrowLeft de `@lucide/vue`
    - Logique fetch/skeleton/needsRedirect inchangée ; invariant profil non vérifié préservé
    - `npm run test` reste vert
  </acceptance_criteria>
  <done>Profil pleine page sans navbar, navigation retour flottante.</done>
</task>

<task type="auto">
  <name>Task 2: Galerie Réalisations + Bento + test de garde layout</name>
  <read_first>
    - app/pages/pro/[dept]/[slug].vue (après Task 1 ; blocs Bento documents à réutiliser comme base visuelle)
    - app/components/RealisationCard.vue (plan 06 — prop project)
    - .planning/phases/05.5-Portfolio-Refonte/05.5-UI-SPEC.md (section 1 : grille mobile-first, empty state profil)
    - tests/badges.test.ts (pattern test happy-dom pour le stub)
  </read_first>
  <files>app/pages/pro/[dept]/[slug].vue, tests/ui/profile-layout.test.ts</files>
  <action>
    Ajouter une section "Réalisations" après les blocs Bento existants : grille `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6` de `RealisationCard` (`v-for` sur `profile.realisations`), aucun scroll horizontal à 375/768/1024/1440. Si `realisations` est vide : empty state "Cette galerie prend forme" + corps "Ce professionnel n'a pas encore publié de réalisation. Revenez bientôt pour découvrir ses derniers chantiers." Structure de contenu globale : hero (logo, nom entreprise en Display, badges existants) -> Bento (bio, SIRET, catégories) -> galerie.
    Créer `tests/ui/profile-layout.test.ts` (Wave 0 requirement VALIDATION.md) : assertion source lisant `pro/[dept]/[slug].vue` via readFileSync, affirmant qu'il contient `layout: false` et référence `RealisationCard`. Ponytail : le rendu pleine page réel (useFetch + definePageMeta) n'est pas montable proprement en vitest ; l'assertion source garde exactement la régression visée (retour de la navbar / disparition de la galerie), le rendu visuel est vérifié au checkpoint humain.
  </action>
  <verify>
    <automated>npx vitest run tests/ui/profile-layout.test.ts && grep -q "RealisationCard" app/pages/pro/[dept]/[slug].vue && grep -q "Cette galerie prend forme" app/pages/pro/[dept]/[slug].vue</automated>
  </verify>
  <acceptance_criteria>
    - Galerie `RealisationCard` en grille mobile-first (`grid-cols-1` -> `md:grid-cols-2` -> `lg:grid-cols-3`)
    - Empty state profil exact du Copywriting Contract quand aucune réalisation
    - `tests/ui/profile-layout.test.ts` affirme la présence de `layout: false` et de `RealisationCard` (passe)
    - `npm run test` reste vert
  </acceptance_criteria>
  <done>Galerie des réalisations intégrée au profil immersif, garde de layout en place.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Profil public immersif pleine page (sans navbar), bouton flottant retour, galerie de réalisations avec likes, mobile-first.</what-built>
  <how-to-verify>
    1. `npm run dev`, ouvrir http://localhost:3000/pro/[dept]/[slug] d'un pro vérifié ayant des réalisations
    2. Vérifier l'ABSENCE de la navbar générique de l'application (pleine page immersive)
    3. Le bouton flottant en haut à gauche ramène à l'accueil
    4. La galerie affiche les réalisations avec compteurs de likes ; cliquer un cœur -> fill orange immédiat
    5. Réduire à 375px (mobile) : une seule colonne, aucun scroll horizontal ; tester 768 / 1024 / 1440
    6. Ouvrir un profil sans réalisation -> empty state "Cette galerie prend forme"
  </how-to-verify>
  <resume-signal>Tapez "approved" ou décrivez les problèmes rencontrés</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visiteur → profil public | Lecture publique (données déjà allowlistées côté endpoint plan 04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.5-16 | Information disclosure | données profil | mitigate | La galerie n'affiche que des champs déjà publics (title, city, photos) ; aucune donnée sensible pro/prospect exposée |
</threat_model>

<verification>
Layout immersif sans header, galerie mobile-first avec likes, empty state. Test source garde `layout: false` + `RealisationCard`. Checkpoint humain valide le rendu réel multi-breakpoints.
</verification>

<success_criteria>
ROADMAP Success Criterion 4 satisfait : profil public immersif mobile-first avec galerie et likes.
</success_criteria>

<output>
Create `.planning/phases/05.5-Portfolio-Refonte/05.5-07-SUMMARY.md` when done
</output>
