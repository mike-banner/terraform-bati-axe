---
phase: 05.5-portfolio-refonte
plan: 08
subsystem: ui
tags: [vue, nuxt, landing, ssr, vitest]

# Dependency graph
requires:
  - phase: 05.5-04
    provides: "API GET /api/v1/completed-projects (is_showcased=true, likes(count), limit 12)"
  - phase: 05.5-06
    provides: "Composant RealisationCard (prop project, like optimiste)"
provides:
  - "Section landing 'Chantiers Réalisés' : carousel CSS scroll-snap SSR des projets showcased avec likes"
affects: []

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Carousel CSS-only via scroll-snap-type/scroll-snap-align, aucune dépendance JS (Embla explicitement exclu)"
    - "Fallthrough attrs (class/style) sur composant racine RealisationCard pour composer la mise en page carousel sans dupliquer le markup carte"

key-files:
  created: []
  modified:
    - app/pages/index.vue

key-decisions:
  - "Section insérée entre 'Comment ça marche' et 'Preuve : avant/après' (ordre naturel preuve sociale avant preuve technique)."

requirements-completed: [LANDING-SOCIAL]

# Metrics
duration: 15min
completed: 2026-07-19
---

# Phase 05.5 Plan 08: Landing "Chantiers Réalisés" Summary

**Section landing "Chantiers Réalisés" en carousel CSS scroll-snap SSR (sans dépendance JS), affichant les projets `is_showcased` via `RealisationCard` avec likes optimistes ; section omise si aucun projet showcased.**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-07-19T03:35:00+02:00
- **Completed:** 2026-07-19T03:50:00+02:00
- **Tasks:** 2 (1 auto + 1 checkpoint humain)
- **Files modified:** 1

## Accomplishments
- Fetch SSR `useFetch('/api/v1/completed-projects')` en tête de `<script setup>`, dérivé en `computed` `showcasedProjects` — aucun `onMounted`/client-only, `<img>` réels présents dans le HTML serveur (SEO/CLS)
- Nouvelle `<section v-if="showcasedProjects.length">` "Chantiers Réalisés" entre "Comment ça marche" et "Preuve : avant/après", copy exact du contrat (`Découvrez les derniers travaux menés par nos artisans certifiés près de chez vous.`)
- Carousel CSS-only : conteneur `flex gap-6 overflow-x-auto` + `scroll-snap-type: x mandatory`, chaque `RealisationCard` en `shrink-0 w-[85%] sm:w-[45%] lg:w-[30%]` + `scroll-snap-align: start` — zéro dépendance carousel (`grep -i embla` = 0), fallthrough `class`/`style` sur le composant réutilisé sans dupliquer son markup
- Section entièrement omise (`v-if`) si aucun projet `is_showcased`, vérifié SSR (le `<h2>` n'apparaît que dans un commentaire de code source, pas dans le DOM rendu, quand la liste est vide)

## Task Commits

Each task was committed atomically:

1. **Task 1: Section Chantiers Réalisés (carousel CSS scroll-snap SSR)** - `f133c27` (feat)
2. **Task 2: Checkpoint humain (vérification visuelle/SSR)** - approuvé, pas de commit dédié

**Plan metadata:** (commit à suivre)

## Files Created/Modified
- `app/pages/index.vue` - fetch SSR `completed-projects`, nouvelle section carousel CSS scroll-snap réutilisant `RealisationCard`

## Decisions Made
- Section positionnée juste après "Comment ça marche" plutôt qu'en toute fin de page : la preuve sociale (réalisations réelles + likes) précède la preuve technique (avant/après) dans le narratif de conversion, sans changer le contrat d'interface du plan.

## Deviations from Plan

None - plan exécuté exactement tel qu'écrit.

## Verification automatisée effectuée (avant checkpoint)
- `grep` : présence "Chantiers Réalisés", `completed-projects`, `scroll-snap-type`, `RealisationCard` ; absence "embla" (insensible à la casse) — tous verts
- `npm run test` : 46/46 tests passent, 1 suite pré-existante (`tests/badges.test.ts`) échoue pour une raison sans rapport (documentée dans `deferred-items.md`, hors scope 05.5-02), non touchée par ce plan
- Validation SSR manuelle via `curl` sur le serveur dev réutilisé (`:3000`, pas de doublon lancé) :
  - Sans projet showcased : `<h2>Chantiers Réalisés</h2>` absent du HTML rendu (uniquement présent dans le commentaire source), section omise via `<!---->` — comportement conforme au contrat "pas d'empty state public"
  - Avec 2 projets showcased seedés temporairement (`completed_projects`, `is_showcased=true`) : la section apparaît en SSR avec `<img>` réels, `alt="{title} — {city}"` correct, `scroll-snap-type:x mandatory` et `scroll-snap-align:start` présents dans le HTML
  - Données de test intégralement supprimées après vérification (`delete from completed_projects where id in (...)`), `select count(*) where is_showcased=true` confirmé à 0 — aucun résidu

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Success Criterion 5 de la ROADMAP satisfait : carousel/grille SEO-friendly Nuxt exposant les chantiers sélectionnés avec likes, sans nouvelle dépendance
- Phase 05.5 (Portfolio, Refonte Profil & Social) complète après approbation du checkpoint humain restant (plan 08, dernier plan de la phase)

---
*Phase: 05.5-portfolio-refonte*
*Completed: 2026-07-19*

## Self-Check: PASSED

- FOUND: app/pages/index.vue
- FOUND: f133c27
