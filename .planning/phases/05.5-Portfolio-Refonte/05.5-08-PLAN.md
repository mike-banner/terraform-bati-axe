---
phase: 05.5-portfolio-refonte
plan: 08
type: execute
wave: 4
depends_on: ["05.5-04", "05.5-06"]
files_modified:
  - app/pages/index.vue
autonomous: false
requirements: [LANDING-SOCIAL]
must_haves:
  truths:
    - "La landing expose une section 'Chantiers Réalisés' avec les projets is_showcased"
    - "Le carousel est SSR-friendly (CSS scroll-snap, <img> réels rendus serveur)"
    - "Les compteurs de likes s'affichent, le bouton like est optimiste"
    - "La section est omise si aucun projet showcased"
  artifacts:
    - path: "app/pages/index.vue"
      provides: "Section landing Chantiers Réalisés (carousel CSS scroll-snap)"
  key_links:
    - from: "index.vue"
      to: "/api/v1/completed-projects"
      via: "useFetch SSR"
      pattern: "completed-projects"
    - from: "index.vue"
      to: "RealisationCard"
      via: "v-for showcasedProjects"
      pattern: "RealisationCard"
---

<objective>
Section "Chantiers Réalisés" sur la landing : preuve sociale SEO-friendly avec likes (Success Criterion 5).

Purpose: Carousel CSS scroll-snap (pas d'Astro, pas de nouvelle dépendance JS) affichant les réalisations `is_showcased`, rendu serveur pour le SEO/CLS.
Output: nouvelle section dans `app/pages/index.vue`.

Design: contrat UI-SPEC 05.5 section 4 + skill ui-ux-pro-max/sketch-findings (Bento, `.reveal` stagger, palette Industrial/Safety).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.5-Portfolio-Refonte/05.5-UI-SPEC.md
@.planning/phases/05.5-Portfolio-Refonte/05.5-PATTERNS.md
@.agent/skills/ui-ux-pro-max/SKILL.md
@.agent/skills/sketch-findings-bati-axe/SKILL.md

<interfaces>
Endpoint (plan 04) : GET `/api/v1/completed-projects` -> `{ status, projects: [{ id, title, city, image_urls, likes:[{count}], professionals:{company_name, canonical_slug} }] }` (is_showcased=true, limit 12).
Composant `RealisationCard` (plan 06) : prop `project`, gère lui-même le like optimiste + localStorage.
Rythme de section existant : `mx-auto w-full max-w-7xl px-6 py-[clamp(3.5rem,7vw,6rem)]`. Animation `.reveal` / `.reveal-item` déjà en place (sections "Comment ça marche" ~lignes 108-133).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Section Chantiers Réalisés (carousel CSS scroll-snap SSR)</name>
  <read_first>
    - app/pages/index.vue (sections existantes "Comment ça marche" ~lignes 108-133, BeforeAfterSlider ~135-151 ; pattern useFetch SSR)
    - app/components/RealisationCard.vue (plan 06)
    - .planning/phases/05.5-Portfolio-Refonte/05.5-UI-SPEC.md (section 4 : CSS scroll-snap, copy, SEO)
  </read_first>
  <files>app/pages/index.vue</files>
  <action>
    En tête du `<script setup>`, fetch SSR : `const { data: showcased } = await useFetch('/api/v1/completed-projects')` puis dériver `showcasedProjects = showcased.value?.projects ?? []` (garantit des `<img>` rendus serveur pour le SEO, pas de carousel client-only).
    Insérer une nouvelle `<section class="mx-auto w-full max-w-7xl px-6 py-[clamp(3.5rem,7vw,6rem)]">` entre deux sections existantes, avec `v-if="showcasedProjects.length"` (la section est OMISE entièrement si zéro projet showcased — pas d'empty state public). Titre `<h2>` "Chantiers Réalisés", sous-titre "Découvrez les derniers travaux menés par nos artisans certifiés près de chez vous."
    Carousel : conteneur `flex gap-6 overflow-x-auto pb-4` avec `style="scroll-snap-type: x mandatory;"`, chaque `RealisationCard` en `shrink-0 w-[85%] sm:w-[45%] lg:w-[30%]` + `style="scroll-snap-align: start;"`, classe `.reveal`/`.reveal-item` pour le stagger. Ponytail : CSS scroll-snap natif, AUCUNE dépendance (pas d'Embla), SSR-safe par construction. Les `<img>` portent un `alt` réel (géré par RealisationCard : "{title} — {city}").
  </action>
  <verify>
    <automated>grep -q "Chantiers Réalisés" app/pages/index.vue && grep -q "completed-projects" app/pages/index.vue && grep -q "scroll-snap-type" app/pages/index.vue && grep -q "RealisationCard" app/pages/index.vue && ! grep -qi "embla" app/pages/index.vue && npm run test</automated>
  </verify>
  <acceptance_criteria>
    - Fetch via `useFetch('/api/v1/completed-projects')` en SSR (pas de `onMounted`/client-only)
    - Section rendue uniquement si `showcasedProjects.length` > 0 (omise sinon, aucun empty state public)
    - Carousel = CSS `scroll-snap-type: x mandatory` + `scroll-snap-align: start`, aucune dépendance carousel (`grep -i embla` = 0)
    - Réutilise `RealisationCard` (pas de markup carte dupliqué)
    - Titre + sous-titre exacts du Copywriting Contract
    - `npm run test` reste vert
  </acceptance_criteria>
  <done>Section preuve sociale SSR fonctionnelle avec likes, sans nouvelle dépendance.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Section "Chantiers Réalisés" sur la landing : carousel CSS scroll-snap SSR des projets showcased avec likes optimistes.</what-built>
  <how-to-verify>
    1. En admin, mettre en avant (is_showcased) au moins 2 réalisations (via la console admin, plan 06)
    2. `npm run dev`, ouvrir http://localhost:3000/ -> la section "Chantiers Réalisés" apparaît
    3. Vérifier le SSR : `curl -s localhost:3000/ | grep "Chantiers Réalisés"` doit retourner la balise (rendu serveur, bon pour le SEO)
    4. Faire défiler horizontalement -> snap fluide sur chaque carte, aucun jank
    5. Cliquer un cœur -> fill orange immédiat + compteur +1 ; recharger -> le cœur reste grisé (localStorage)
    6. Retirer tous les is_showcased -> la section disparaît entièrement (pas d'empty state)
  </how-to-verify>
  <resume-signal>Tapez "approved" ou décrivez les problèmes rencontrés</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visiteur → landing | Lecture publique + like anonyme (endpoint sécurisé plan 04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.5-17 | Tampering | données affichées | mitigate | Seuls les champs publics allowlistés par l'endpoint (plan 04) sont rendus ; is_showcased contrôlé par admin |
| T-05.5-18 | DoS | like spam depuis landing | mitigate | Contrainte unique IP-hash (plan 04) + garde localStorage (RealisationCard, plan 06) |
</threat_model>

<verification>
Section SSR (img réels + alt), carousel CSS-only sans dépendance, likes optimistes, omission si zéro showcased. Checkpoint humain valide SSR (curl), scroll-snap et persistance du like.
</verification>

<success_criteria>
ROADMAP Success Criterion 5 satisfait : carousel/grille SEO-friendly Nuxt exposant les chantiers sélectionnés avec likes.
</success_criteria>

<output>
Create `.planning/phases/05.5-Portfolio-Refonte/05.5-08-SUMMARY.md` when done
</output>
