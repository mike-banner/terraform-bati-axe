---
phase: 05.5-portfolio-refonte
plan: 06
type: execute
wave: 3
depends_on: ["05.5-04"]
files_modified:
  - app/components/RealisationCard.vue
  - tests/ui/realisation-card.test.ts
  - app/pages/admin/index.vue
autonomous: true
requirements: [PROFILE-UI, LANDING-SOCIAL, BACKOFFICE]
must_haves:
  truths:
    - "Une carte de réalisation partagée affiche photo, titre, ville, compteur de likes"
    - "Le bouton like est optimiste (fill orange immédiat) avec garde localStorage"
    - "L'admin peut basculer 'Mettre en avant sur la page d'accueil' sur une réalisation"
  artifacts:
    - path: "app/components/RealisationCard.vue"
      provides: "Carte galerie réutilisée par profil public + landing"
      min_lines: 40
    - path: "tests/ui/realisation-card.test.ts"
      provides: "Test de rendu de la carte (alt, aria, compteur)"
    - path: "app/pages/admin/index.vue"
      provides: "Toggle showcase dans la console admin"
  key_links:
    - from: "RealisationCard.vue"
      to: "/api/v1/projects/[id]/like"
      via: "like optimiste + localStorage flag"
      pattern: "projects/.*like|localStorage"
    - from: "admin/index.vue"
      to: "/api/v1/admin/realisations-showcase"
      via: "toggleShowcase handler"
      pattern: "realisations-showcase"
---

<objective>
Composant carte de réalisation partagé (profil + landing) et intégration du toggle showcase dans la console admin (Success Criteria 3, 4, 5 — briques partagées).

Purpose: `RealisationCard.vue` est la dépendance visuelle commune du profil immersif (plan 07) et de la landing (plan 08) — construite une seule fois ici. Le toggle admin réutilise le pattern `approvePro`.
Output: composant carte + test de rendu + handler admin.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.5-Portfolio-Refonte/05.5-UI-SPEC.md
@.planning/phases/05.5-Portfolio-Refonte/05.5-PATTERNS.md
@.agent/skills/ui-ux-pro-max/SKILL.md
@.agent/skills/sketch-findings-bati-axe/SKILL.md

<interfaces>
CRITIQUE — icônes : importer depuis `@lucide/vue` (PAS `lucide-vue-next`). `import { Heart } from '@lucide/vue'`.
Endpoint like (plan 04) : POST `/api/v1/projects/{id}/like` -> `{ status, likes }`.
Endpoint showcase (plan 04) : POST `/api/v1/admin/realisations-showcase` body `{ project_id, is_showcased }` -> `{ status, is_showcased }`.
Forme d'un projet : `{ id, title, city, image_urls: string[], likes?: [{ count }] }`. Le compteur initial se lit `project.likes?.[0]?.count ?? 0`.
Classes CSS existantes : `.bento-card rounded-3xl border border-slate-200 bg-white shadow-sm`. Chip glass light-mode : `bg-white/90` (jamais `bg-white/10` — règle ui-ux-pro-max). Cœur actif : `fill-[#F97316] text-[#F97316]`.
Pattern admin (app/pages/admin/index.vue ~lignes 138-149 `approvePro`) : `actionLoading` ref, try/finally, `$fetch` POST puis refetch.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RealisationCard.vue + test de rendu (Wave 0 UI proxy)</name>
  <read_first>
    - app/pages/pro/[dept]/[slug].vue (lignes ~299-332 : classes exactes `.bento-card` des cartes documents)
    - app/pages/index.vue (lignes ~111-131 : cartes `.bento-card` "Comment ça marche")
    - tests/badges.test.ts (pattern test composant : `// @vitest-environment happy-dom`, `mount` de @vue/test-utils)
    - .planning/phases/05.5-Portfolio-Refonte/05.5-UI-SPEC.md (section 1 gallery card + section 4 like button)
  </read_first>
  <behavior>
    - Test 1: monté avec un projet `{ id, title:'Toiture', city:'Poissy', image_urls:['x.jpg'], likes:[{count:3}] }`, l'`<img>` a `alt="Toiture — Poissy"` et le compteur affiche `3`
    - Test 2: le bouton like porte `aria-label="Aimer cette réalisation"`
    - Test 3: la carte utilise la classe `bg-white/90` (chip) et jamais `bg-white/10`
  </behavior>
  <files>app/components/RealisationCard.vue, tests/ui/realisation-card.test.ts</files>
  <action>
    Créer le composant carte, prop `project`. Coquille `.bento-card rounded-3xl border border-slate-200 bg-white overflow-hidden shadow-sm`. Image `aspect-[4/3] object-cover loading="lazy"`, `alt` = `` `${project.title} — ${project.city}` ``. Bouton like superposé bas-droite : chip `bg-white/90 shadow-lg rounded-full`, hit area >= 44x44px (padding), icône `Heart` de `@lucide/vue` `w-5 h-5`, `aria-label="Aimer cette réalisation"`, compteur `tabular-nums` initialisé à `project.likes?.[0]?.count ?? 0`. Sous l'image : titre `text-lg font-semibold text-slate-900`, ville `text-sm text-slate-500`.
    Like optimiste : au clic, si déjà liké (state local ou `localStorage.getItem('liked_'+project.id)`) ne rien faire ; sinon incrémenter le compteur + remplir le cœur en orange immédiatement, poser `localStorage.setItem('liked_'+project.id,'1')`, puis `$fetch` POST `/api/v1/projects/{id}/like` en arrière-plan. En cas d'échec : rollback silencieux (décrémenter, vider le cœur, retirer le flag) — aucun toast (contrat copywriting).
    Écrire le test dans `tests/ui/realisation-card.test.ts` avec `// @vitest-environment happy-dom` (le glob vitest est `tests/**/*.test.ts`). Ponytail : ce test composant est le proxy automatisé du rendu galerie (le rendu pleine page du profil reste vérifié manuellement, cf plan 07).
  </action>
  <verify>
    <automated>npx vitest run tests/ui/realisation-card.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `import { Heart } from '@lucide/vue'` (grep lucide-vue-next = 0)
    - `<img>` a `alt` = `"{title} — {city}"`, bouton like `aria-label="Aimer cette réalisation"`
    - Chip like utilise `bg-white/90` (grep `bg-white/10` = 0)
    - Garde localStorage `liked_{id}` empêche le double-like côté client, rollback silencieux au fail
    - `npx vitest run tests/ui/realisation-card.test.ts` passe (3 assertions)
  </acceptance_criteria>
  <done>Carte partagée rendue et testée, like optimiste avec garde localStorage.</done>
</task>

<task type="auto">
  <name>Task 2: Toggle showcase dans la console admin</name>
  <read_first>
    - app/pages/admin/index.vue (fichier entier ; handler `approvePro` ~lignes 138-149, appels `/api/v1/admin/projects` ~ligne 106)
    - .planning/phases/05.5-Portfolio-Refonte/05.5-UI-SPEC.md (section 3 Admin Showcase Toggle + label copy)
  </read_first>
  <files>app/pages/admin/index.vue</files>
  <action>
    Charger les réalisations (via l'endpoint public `/api/v1/completed-projects` pour la liste showcased, ou une liste dédiée si présente ; à défaut, afficher les projets d'un pro sélectionné). Ajouter un handler `toggleShowcase(projectId, isShowcased)` calqué sur `approvePro` : `actionLoading.value = ${projectId}-showcase`, try `$fetch('/api/v1/admin/realisations-showcase', { method:'POST', body:{ project_id: projectId, is_showcased: isShowcased } })`, refetch la liste, catch -> `errorMessage`, finally reset `actionLoading`. UI : checkbox/toggle avec label exact "Mettre en avant sur la page d'accueil", état ON en orange safety (optimiste, cohérent avec la console 04.7). Icône éventuelle depuis `@lucide/vue`.
  </action>
  <verify>
    <automated>grep -q "realisations-showcase" app/pages/admin/index.vue && grep -q "Mettre en avant sur la page d'accueil" app/pages/admin/index.vue && ! grep -q "lucide-vue-next" app/pages/admin/index.vue && npm run test</automated>
  </verify>
  <acceptance_criteria>
    - Handler `toggleShowcase` appelle POST `/api/v1/admin/realisations-showcase` avec `{ project_id, is_showcased }`
    - Label exact "Mettre en avant sur la page d'accueil" présent
    - Pattern de loading/erreur identique à `approvePro` (actionLoading + try/finally)
    - Aucune icône `lucide-vue-next`
    - `npm run test` reste vert
  </acceptance_criteria>
  <done>L'admin bascule is_showcased depuis la console, avec retour visuel.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navigateur → Nitro like | Écriture publique déclenchée par la carte (endpoint sécurisé plan 04) |
| console admin → Nitro showcase | Action privilégiée (garde admin côté endpoint) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.5-14 | Spoofing/DoS | like client | mitigate | Garde localStorage grise le cœur après like ; l'anti-spam autoritaire est la contrainte unique DB (plan 04) — le client n'est qu'une commodité UX |
| T-05.5-15 | Elevation | toggle showcase | accept | La sécurité est côté endpoint (garde admin plan 04) ; l'UI n'est qu'un déclencheur, pas la frontière de confiance |
</threat_model>

<verification>
La carte partagée est testée (alt/aria/compteur), le like est optimiste avec rollback silencieux, le toggle admin persiste is_showcased. Icônes exclusivement `@lucide/vue`.
</verification>

<success_criteria>
Brique visuelle commune prête pour profil (plan 07) et landing (plan 08) ; toggle admin fonctionnel (Criterion 3).
</success_criteria>

<output>
Create `.planning/phases/05.5-Portfolio-Refonte/05.5-06-SUMMARY.md` when done
</output>
