---
phase: 05.5-portfolio-refonte
plan: 02
subsystem: api
tags: [r2, presign, aws4fetch, vitest, tdd, zod]

requires: []
provides:
  - "Presign logo R2 corrigé (aws.sign sans Content-Length figé)"
  - "Endpoint presign R2 multi-fichiers pour photos de réalisations"
  - "Garde vitest anti-régression Content-Length sur les endpoints presign"
affects: [05.5-05, 05.5-06]

tech-stack:
  added: []
  patterns:
    - "aws.sign(url, { method: 'PUT', aws: { signQuery: true } }) — jamais de headers dans la signature v4 pour un PUT presigné, le client envoie ses propres headers réels"

key-files:
  created:
    - tests/api/presign-headers.test.ts
    - server/api/v1/pro/realisations/presign.post.ts
  modified:
    - server/api/v1/pro/profile/logo-presign.post.ts

key-decisions:
  - "Test anti-régression par lecture de source (readFileSync) plutôt que mock réseau : le bug est structurel (header baké dans la signature), pas comportemental — l'assertion la plus petite qui échoue exactement quand le bug réapparaît"

patterns-established:
  - "Endpoint presign R2 : auth serverSupabaseUser (401), résolution défensive userId (.id ?? .sub ?? .user_metadata.sub), buildStorageKey via short_id/canonical_slug, aws.sign sans headers, catch → 400 (Nuxt masque les 500 en prod)"

requirements-completed: [R2-FIX, BACKOFFICE]

duration: 25min
completed: 2026-07-17
---

# Phase 05.5 Plan 02: Fix presign logo + endpoint réalisations Summary

**Suppression du Content-Length figé dans la signature aws4fetch du presign logo (bug SignatureDoesNotMatch), et nouvel endpoint presign R2 pour les photos de réalisations, tous deux gardés par un test anti-régression.**

## Performance

- **Duration:** 25 min
- **Started:** 2026-07-17T16:22:00Z
- **Completed:** 2026-07-17T16:48:01Z
- **Tasks:** 3
- **Files modified:** 3

## Accomplissements
- `logo-presign.post.ts` ne bake plus de Content-Length dans `aws.sign` — corrige le blocage de soumission du logo pro (ROADMAP Success Criterion 1)
- Nouvel endpoint `server/api/v1/pro/realisations/presign.post.ts`, calqué sur `documents/presign.post.ts`, headerless dès sa création
- `tests/api/presign-headers.test.ts` : garde source (readFileSync) qui échoue si un futur presign réintroduit un header `Content-Length`/`Content-Type` figé dans la signature

## Task Commits

Chaque tâche a été commitée atomiquement :

1. **Task 1 (RED) : Test anti-régression Content-Length** - `fb72513` (test)
2. **Task 2 (GREEN) : Corriger logo-presign.post.ts** - `8becd80` (fix)
3. **Task 3 : Créer l'endpoint presign réalisations** - `a4e4b38` (feat)

## Files Created/Modified
- `tests/api/presign-headers.test.ts` - Garde anti-régression source (RED avant fix, GREEN après)
- `server/api/v1/pro/profile/logo-presign.post.ts` - Retrait de `headers: { Content-Type, Content-Length }` de l'appel `aws.sign` ; `content_type` non utilisé retiré de la destructuration
- `server/api/v1/pro/realisations/presign.post.ts` (nouveau) - Presign R2 headerless pour photos de réalisations, type `'realisation'`, Zod `content_type` en `image/*`

## Decisions Made
- Test structurel (lecture de source) plutôt que test comportemental mockant aws4fetch + Supabase : coût/bénéfice — le bug est un header en trop, une assertion de contenu source le détecte à coup sûr sans fragilité de mock.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Variable `content_type` devenue inutilisée après retrait des headers**
- **Found during:** Task 2
- **Issue:** Après suppression du header `Content-Type`/`Content-Length` dans `aws.sign`, la destructuration `const { content_type, filename } = parsed.data` laissait `content_type` inutilisé (le champ reste validé par Zod mais n'était plus consommé).
- **Fix:** Destructuration réduite à `const { filename } = parsed.data` ; la validation Zod du `content_type` (format MIME accepté) reste intacte.
- **Files modified:** server/api/v1/pro/profile/logo-presign.post.ts
- **Verification:** `npx vitest run tests/api/presign-headers.test.ts` GREEN
- **Committed in:** 8becd80 (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (Rule 1)
**Impact on plan:** Correction mineure de cohérence, aucun changement de comportement fonctionnel. Aucun scope creep.

## Issues Encountered
- `npx vue-tsc --noEmit` échoue dans ce worktree car `.nuxt/` n'est pas généré (config volontaire, cf. commentaire `tsconfig.test.json` dans `vitest.config.ts` : évite de générer `.nuxt/` dans le worktree). Vérifié à la place via les grep du plan (`signQuery: true` présent, `Content-Length` absent) et la suite vitest, tous verts.
- `npm run test` révèle 2 échecs pré-existants dans `tests/badges.test.ts` (classes `bg-cyan-100/80` / `bg-green-100/80` obsolètes vs nouvelle charte `bg-[#F8FAFC]`). Hors scope (fichiers non listés dans `files_modified` du plan), non touchés, documentés dans `.planning/phases/05.5-Portfolio-Refonte/deferred-items.md`.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Le presign logo fonctionne sans Content-Length figé : le formulaire `espace/profil.vue` peut soumettre un logo sans `SignatureDoesNotMatch`.
- L'endpoint `realisations/presign.post.ts` est prêt à être consommé par `RealisationForm` (plan 05) pour l'upload multi-fichiers des photos de chantiers.

---
*Phase: 05.5-Portfolio-Refonte*
*Completed: 2026-07-17*
