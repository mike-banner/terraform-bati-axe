---
phase: 05.5-portfolio-refonte
plan: 03
type: execute
wave: 2
depends_on: ["05.5-01"]
files_modified:
  - server/api/v1/pro/realisations/index.get.ts
  - server/api/v1/pro/realisations/index.post.ts
  - server/api/v1/pro/realisations/[id].delete.ts
autonomous: true
requirements: [BACKOFFICE]
must_haves:
  truths:
    - "Un pro peut lister ses propres réalisations"
    - "Un pro peut créer une réalisation (titre + photos R2)"
    - "Un pro peut supprimer une de ses réalisations, jamais celle d'un autre"
  artifacts:
    - path: "server/api/v1/pro/realisations/index.get.ts"
      provides: "Liste des réalisations du pro authentifié"
    - path: "server/api/v1/pro/realisations/index.post.ts"
      provides: "Création d'une réalisation"
    - path: "server/api/v1/pro/realisations/[id].delete.ts"
      provides: "Suppression d'une réalisation possédée"
  key_links:
    - from: "realisations CRUD"
      to: "completed_projects (RLS)"
      via: "serverSupabaseClient (RLS auth.uid()=professional_id)"
      pattern: "serverSupabaseClient"
---

<objective>
Endpoints CRUD de l'espace pro pour gérer ses réalisations (Success Criterion 3, côté API).

Purpose: Alimenter le formulaire backoffice (plan 05). La sécurité repose sur la policy RLS `manage_own_completed_projects` — on utilise `serverSupabaseClient` (pas service role) pour que le RLS scope naturellement les écritures au propriétaire.
Output: Trois routes CRUD légères calquées sur `me.patch.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.5-Portfolio-Refonte/05.5-PATTERNS.md

<interfaces>
Table `completed_projects` (plan 01) : id, professional_id, title, description, city, image_urls TEXT[], is_showcased, created_at.
Auth pattern (server/api/v1/pro/profile/me.patch.ts) : `const supabase = await serverSupabaseClient(event) as any; const { data: { user } } = await supabase.auth.getUser(); if (!user) throw createError({ statusCode: 401 })`.
Réponse standard : `{ status: 'SUCCESS', ...data }`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Liste + création des réalisations</name>
  <read_first>
    - server/api/v1/pro/profile/me.get.ts (analog liste : auth + select scoping RLS)
    - server/api/v1/pro/profile/me.patch.ts (analog écriture : Zod .strict(), safeParse → 400, serverSupabaseClient)
  </read_first>
  <files>server/api/v1/pro/realisations/index.get.ts, server/api/v1/pro/realisations/index.post.ts</files>
  <action>
    `index.get.ts` : auth via `serverSupabaseClient` + `auth.getUser()` (401 sinon), puis `select('*').eq('professional_id', user.id).order('created_at', { ascending: false })` sur `completed_projects`. Retourner `{ status: 'SUCCESS', realisations: data ?? [] }`.
    `index.post.ts` : auth identique. Zod `.strict()` : `title` string min 1 requis, `description` string max 500 nullable optionnel, `city` string nullable optionnel, `image_urls` array de strings URL (min 1). safeParse échoué → 400 avec `error.message`. Insert dans `completed_projects` avec `professional_id: user.id` (le RLS `manage_own_completed_projects` garantit le scope). Retourner `{ status: 'SUCCESS', realisation: data }`. Erreur DB → 500 avec `error.message`.
    Ne PAS accepter `is_showcased` dans le body (réservé admin, plan 04).
  </action>
  <verify>
    <automated>test -f server/api/v1/pro/realisations/index.get.ts && test -f server/api/v1/pro/realisations/index.post.ts && grep -q "serverSupabaseClient" server/api/v1/pro/realisations/index.post.ts && ! grep -q "is_showcased" server/api/v1/pro/realisations/index.post.ts</automated>
  </verify>
  <acceptance_criteria>
    - `index.get.ts` filtre par `professional_id` = user authentifié, tri `created_at` desc, retourne `{ status: 'SUCCESS', realisations }`
    - `index.post.ts` valide via Zod `.strict()` : title requis, image_urls tableau non vide
    - Le body de create ne peut pas positionner `is_showcased` (`grep -c is_showcased` = 0 dans index.post.ts)
    - Les deux routes retournent 401 sans session
    - `npm run test` reste vert
  </acceptance_criteria>
  <done>Un pro peut lister et créer ses réalisations via l'API, scoping RLS garanti.</done>
</task>

<task type="auto">
  <name>Task 2: Suppression d'une réalisation</name>
  <read_first>
    - server/api/v1/pro/profile/me.patch.ts (pattern auth + écriture scopée RLS)
    - server/api/v1/pro/realisations/index.post.ts (créé Task 1 — même style)
  </read_first>
  <files>server/api/v1/pro/realisations/[id].delete.ts</files>
  <action>
    Route DELETE : auth via `serverSupabaseClient` + `auth.getUser()` (401 sinon). Récupérer `id` via `getRouterParam(event, 'id')`, valider comme UUID (Zod ou regex → 400 sinon). `delete().eq('id', id)` sur `completed_projects` — le RLS `manage_own_completed_projects` empêche de supprimer la réalisation d'un autre pro (aucune ligne affectée si non-propriétaire). Retourner `{ status: 'SUCCESS' }`. La suppression CASCADE des `likes` liés est assurée par la FK (plan 01), aucune action supplémentaire.
  </action>
  <verify>
    <automated>test -f "server/api/v1/pro/realisations/[id].delete.ts" && grep -q "serverSupabaseClient" "server/api/v1/pro/realisations/[id].delete.ts" && grep -q "getRouterParam" "server/api/v1/pro/realisations/[id].delete.ts"</automated>
  </verify>
  <acceptance_criteria>
    - La route lit `id` via `getRouterParam` et valide le format UUID (400 si invalide)
    - Utilise `serverSupabaseClient` (RLS) — pas de service role, pas de check ownership manuel
    - Retourne 401 sans session, `{ status: 'SUCCESS' }` sinon
    - `npm run test` reste vert
  </acceptance_criteria>
  <done>Un pro supprime ses propres réalisations ; celles des autres restent inaccessibles via RLS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navigateur → Nitro CRUD | Pro authentifié gère ses réalisations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.5-06 | Elevation | delete/update d'autrui | mitigate | `serverSupabaseClient` + RLS `manage_own_completed_projects` : impossible d'affecter les lignes d'un autre professional_id |
| T-05.5-07 | Tampering | is_showcased via create | mitigate | Champ absent du schéma Zod de create ; seul l'endpoint admin service_role peut le positionner (plan 04) |
</threat_model>

<verification>
Les trois routes s'appuient sur RLS pour le scoping propriétaire. `is_showcased` ne peut être positionné que par l'admin.
</verification>

<success_criteria>
API backoffice pro (liste/création/suppression) prête pour le formulaire d'ajout de réalisation.
</success_criteria>

<output>
Create `.planning/phases/05.5-Portfolio-Refonte/05.5-03-SUMMARY.md` when done
</output>
