---
phase: 05.5-portfolio-refonte
plan: 04
type: execute
wave: 2
depends_on: ["05.5-01"]
files_modified:
  - server/api/v1/completed-projects/index.get.ts
  - server/api/v1/pro/profile/[slug].get.ts
  - server/api/v1/projects/[id]/like.post.ts
  - server/api/v1/admin/realisations-showcase.post.ts
autonomous: true
requirements: [LANDING-SOCIAL, BACKOFFICE]
must_haves:
  truths:
    - "La landing peut lister les réalisations is_showcased avec leur compteur de likes"
    - "Le profil public expose les réalisations du pro avec compteurs de likes"
    - "Un visiteur peut liker une réalisation une seule fois par IP (anti-spam)"
    - "Un admin peut basculer is_showcased sur une réalisation"
  artifacts:
    - path: "server/api/v1/completed-projects/index.get.ts"
      provides: "Liste publique des réalisations showcased + likes(count)"
    - path: "server/api/v1/projects/[id]/like.post.ts"
      provides: "Endpoint like anti-spam (IP hash + contrainte unique)"
    - path: "server/api/v1/admin/realisations-showcase.post.ts"
      provides: "Toggle admin is_showcased + audit log"
  key_links:
    - from: "like.post.ts"
      to: "likes (unique constraint)"
      via: "insert service_role, tolère 23505"
      pattern: "23505"
    - from: "admin/realisations-showcase.post.ts"
      to: "app_metadata.role === 'admin'"
      via: "garde admin JWT claim"
      pattern: "app_metadata"
---

<objective>
Endpoints publics de lecture (galerie/likes) et d'écriture sociale (like anti-spam, toggle admin showcase) — Success Criteria 3 (admin) et 5 (landing/likes).

Purpose: Fournir aux UI des vagues 3-4 les données SSR (réalisations + compteurs de likes) et les actions (like optimiste, mise en avant admin).
Output: Deux endpoints de lecture (dont extension du profil public) + like + toggle admin.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.5-Portfolio-Refonte/05.5-PATTERNS.md

<interfaces>
Table `likes` (plan 01) : id, project_id, ip_hash (SHA-256 opaque), created_at, UNIQUE(project_id, ip_hash). RLS activé, aucune policy → écriture/lecture uniquement via service_role.
Admin garde (server/api/v1/admin/approve-pro.post.ts) : `const isAdmin = (user as any).app_metadata?.role === 'admin'` → 403 sinon. Audit : `supabase.from('audit_logs').insert({ actor_id, action, target_table, target_id, metadata })`.
Agrégat likes via relation Supabase : `.select('..., likes(count)')` retourne `likes: [{ count: n }]`. Le client lira `project.likes?.[0]?.count ?? 0`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Liste publique showcased + réalisations sur profil public</name>
  <read_first>
    - server/api/v1/pro/profile/[slug].get.ts (analog lecture service_role + allowlist de champs publics ; fichier à MODIFIER)
    - .planning/phases/05.5-Portfolio-Refonte/05.5-PATTERNS.md (section completed-projects/index.get.ts)
  </read_first>
  <files>server/api/v1/completed-projects/index.get.ts, server/api/v1/pro/profile/[slug].get.ts</files>
  <action>
    Créer `completed-projects/index.get.ts` : `serverSupabaseServiceRole`, `select('id, professional_id, title, description, city, image_urls, created_at, likes(count), professionals(company_name, canonical_slug)')`, `.eq('is_showcased', true)`, `.order('created_at', { ascending: false })`, `.limit(12)`. Erreur → 500. Retourner `{ status: 'SUCCESS', projects: data ?? [] }`.
    Modifier `pro/profile/[slug].get.ts` : ajouter, dans la réponse du profil vérifié, la liste des réalisations de CE pro (showcased ou non) via une requête `select('id, professional_id, title, description, city, image_urls, created_at, likes(count)').eq('professional_id', <id du pro>).order('created_at', { ascending: false })` en service role. Ajouter le tableau au payload existant sous la clé `realisations` sans casser les champs déjà retournés (garder le comportement `is_verified: false` pour profil non vérifié, et 404 uniquement si profil introuvable — invariant projet).
  </action>
  <verify>
    <automated>test -f server/api/v1/completed-projects/index.get.ts && grep -q "is_showcased" server/api/v1/completed-projects/index.get.ts && grep -q "likes(count)" server/api/v1/completed-projects/index.get.ts && grep -q "realisations" server/api/v1/pro/profile/[slug].get.ts</automated>
  </verify>
  <acceptance_criteria>
    - `completed-projects/index.get.ts` filtre `is_showcased = true`, limite 12, inclut `likes(count)` et le nom d'entreprise
    - `pro/profile/[slug].get.ts` retourne désormais une clé `realisations` (tableau, avec `likes(count)`) pour le pro concerné
    - L'invariant profil non vérifié (`is_verified: false`, pas de 404) reste intact
    - Les deux utilisent `serverSupabaseServiceRole`
    - `npm run test` reste vert
  </acceptance_criteria>
  <done>Landing et profil public disposent de leurs données SSR (réalisations + compteurs de likes).</done>
</task>

<task type="auto">
  <name>Task 2: Endpoint like anti-spam</name>
  <read_first>
    - server/api/v1/admin/verify.post.ts (pattern validate + insert + shape réponse)
    - .planning/phases/05.5-Portfolio-Refonte/05.5-PATTERNS.md (section like.post.ts : IP hash + tolérance 23505)
  </read_first>
  <files>server/api/v1/projects/[id]/like.post.ts</files>
  <action>
    Créer l'endpoint. Zod : `project_id` UUID (lu depuis le body OU `getRouterParam(event, 'id')` — choisir le param de route `id` pour coller au chemin, valider UUID → 400 sinon). Calculer `ip = getRequestIP(event, { xForwardedFor: true }) || 'unknown'`, puis `ipHash = createHash('sha256').update(\`${ip}:${projectId}\`).digest('hex')` (import `node:crypto`). Ne JAMAIS stocker l'IP brute. Insert `{ project_id, ip_hash }` via `serverSupabaseServiceRole`. Si `error.code === '23505'` (violation contrainte unique) → déjà liké, ce n'est pas une erreur, continuer. Autre erreur → 500. Puis compter : `.from('likes').select('*', { count: 'exact', head: true }).eq('project_id', projectId)`. Retourner `{ status: 'SUCCESS', likes: count ?? 0 }`. Pas de wrapper try/catch global (createError propagé nativement par Nitro).
  </action>
  <verify>
    <automated>test -f "server/api/v1/projects/[id]/like.post.ts" && grep -q "createHash" "server/api/v1/projects/[id]/like.post.ts" && grep -q "23505" "server/api/v1/projects/[id]/like.post.ts" && ! grep -q "getRequestIP(event)\`\|ip_raw\|remote_ip:" "server/api/v1/projects/[id]/like.post.ts"</automated>
  </verify>
  <acceptance_criteria>
    - Le hash IP est calculé avec `createHash('sha256')` sur `ip:project_id` ; aucune IP brute stockée en DB
    - Un doublon (code Postgres `23505`) est traité comme succès idempotent, pas comme erreur
    - La réponse renvoie le compteur de likes à jour (`{ status: 'SUCCESS', likes }`)
    - Body/param `project_id` validé UUID (400 sinon)
    - `npm run test` reste vert
  </acceptance_criteria>
  <done>Un visiteur ne peut liker qu'une fois par IP ; le compteur reste juste.</done>
</task>

<task type="auto">
  <name>Task 3: Toggle admin is_showcased</name>
  <read_first>
    - server/api/v1/admin/approve-pro.post.ts (analog exact : garde `app_metadata.role === 'admin'`, update service role, insert audit_logs)
  </read_first>
  <files>server/api/v1/admin/realisations-showcase.post.ts</files>
  <action>
    Créer l'endpoint. Auth `serverSupabaseUser(event)` → 401 sinon. Garde admin : `(user as any).app_metadata?.role === 'admin'` → 403 sinon (identique à approve-pro.post.ts ; ne PAS réimplémenter une garde ADMIN_EMAILS — les routes API du projet utilisent le claim JWT `role`). Zod : `project_id` UUID, `is_showcased` boolean. safeParse échoué → 400. Update `completed_projects` set `is_showcased` where `id = project_id` via `serverSupabaseServiceRole` (bypass RLS, c'est le seul chemin autorisé pour ce champ). Insert audit : `{ actor_id: user.id, action: 'showcase_toggled', target_table: 'completed_projects', target_id: project_id, metadata: { is_showcased } }`. Retourner `{ status: 'SUCCESS', is_showcased }`.
  </action>
  <verify>
    <automated>test -f server/api/v1/admin/realisations-showcase.post.ts && grep -q "app_metadata?.role === 'admin'" server/api/v1/admin/realisations-showcase.post.ts && grep -q "audit_logs" server/api/v1/admin/realisations-showcase.post.ts</automated>
  </verify>
  <acceptance_criteria>
    - Garde admin via `app_metadata?.role === 'admin'` → 403 si non-admin, 401 si non authentifié
    - Update `is_showcased` via `serverSupabaseServiceRole` uniquement
    - Une entrée `audit_logs` avec `action: 'showcase_toggled'` est insérée
    - Zod valide `project_id` UUID + `is_showcased` boolean
    - `npm run test` reste vert
  </acceptance_criteria>
  <done>L'admin peut mettre en avant/retirer une réalisation de la page d'accueil, tracé en audit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visiteur anonyme → Nitro like | Écriture publique non authentifiée (premier endpoint de ce type dans le code) |
| admin → Nitro showcase | Action privilégiée sur données d'un pro |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.5-08 | Spoofing/DoS | like.post.ts (public write) | mitigate | Contrainte UNIQUE(project_id, ip_hash) bloque le spam par IP ; flag localStorage côté client (plan 06) grise le cœur après like |
| T-05.5-09 | Information disclosure | IP du visiteur | mitigate | Seul un SHA-256 est stocké, jamais l'IP brute |
| T-05.5-10 | Elevation | showcase toggle | mitigate | Garde `app_metadata.role === 'admin'` (403) + service_role, tracé dans audit_logs |
| T-05.5-11 | Repudiation | action admin | mitigate | audit_logs enregistre actor_id + action + target |
</threat_model>

<verification>
Les données publiques exposent les compteurs de likes ; l'anti-spam repose sur la contrainte unique DB (mécanisme le plus petit correct, pas d'Edge Function ni CAPTCHA). Le toggle showcase est réservé admin et audité.
</verification>

<success_criteria>
ROADMAP Criteria 3 (toggle admin) et 5 (likes + liste showcased) satisfaits côté API.
</success_criteria>

<output>
Create `.planning/phases/05.5-Portfolio-Refonte/05.5-04-SUMMARY.md` when done
</output>
