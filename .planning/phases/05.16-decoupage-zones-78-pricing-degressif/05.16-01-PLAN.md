# Plan Phase 05.16-01: Découpage 78 & Pricing Dégressif MRR

## Objectives
1. Créer la migration BDD `zones` & `pro_zones` pour le département 78.
2. Définir le matching automatique `postal_code` -> `zone_id`.
3. Intégrer la grille tarifaire dégressive sans engagement mensuel + annuel économiqu.
4. Créer l'interface de sélection des zones dans le dashboard pro.

## Proposed Tasks
- Task 1 : Migration SQL `supabase/migrations/20260827000001_zones_78.sql`.
- Task 2 : API matching `server/utils/zoneMatcher.ts` & update `projects.post.ts`.
- Task 3 : Composant UI `ZoneSelector.vue` avec toggle mensuel/annuel et intégration Stripe Checkout.

## Verification Protocol
- `npm run test` pass 100%.
- Lead déposé à Poissy (`78300`) attribué à `78-stgermain`.
- Toggle mensuel/annuel fonctionnel. Facturation Stripe ajustée dynamiquement de 150€ à 350€.
