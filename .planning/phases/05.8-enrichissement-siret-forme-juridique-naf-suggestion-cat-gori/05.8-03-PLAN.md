---
phase: 05.8-enrichissement-siret-forme-juridique-naf-suggestion-cat-gori
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - server/api/v1/pro/documents/upload.post.ts
autonomous: true
requirements:
  - 05.8-R3
must_haves:
  truths:
    - "Un Kbis uploade par un pro dont siret_status vaut 'active' est insere avec status 'approved'"
    - "Un Kbis uploade par un pro dont siret_status n'est pas 'active' reste 'pending' (revue admin)"
  artifacts:
    - path: "server/api/v1/pro/documents/upload.post.ts"
      provides: "Auto-approbation conditionnelle du Kbis selon siret_status du pro"
  key_links:
    - from: "server/api/v1/pro/documents/upload.post.ts"
      to: "professionals.siret_status"
      via: "select siret_status avant decision de statut Kbis"
      pattern: "siret_status"
---

<objective>
Auto-approuver le document Kbis a l'upload lorsque l'existence/activite de l'entreprise est deja confirmee par l'API gouv au claim (professionals.siret_status === 'active'), sur le meme modele que l'auto-approbation existante de la decennale dans ce fichier. Evite une revue admin manuelle redondante quand l'Etat confirme deja l'entreprise.

Purpose: Reduire la friction de validation pour les pros dont le SIRET est actif, sans affaiblir le controle : un Kbis reste 'pending' des que le SIRET n'est pas confirme actif.
Output: upload.post.ts avec statut Kbis conditionnel a siret_status.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@server/api/v1/pro/documents/upload.post.ts

<interfaces>
Etat existant (server/api/v1/pro/documents/upload.post.ts) :
- Ligne ~34-35 : const isDecennale = document_type === 'decennale'; const status = isDecennale ? 'approved' : 'pending'.
- Insert dans verifications avec { pro_id: uid, document_type, file_key, status, ... }.
- Un client service role (serverSupabaseServiceRole) est deja instancie ligne ~31.
- professionals.siret_status est une colonne existante (valeurs 'active'|'closed'|'not_found'|'error'), peuplee au claim.
- Le bloc d'auto-approbation decennale (lignes ~51-69) met a jour professionals ; NE PAS le modifier.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auto-approuver le Kbis si siret_status === 'active'</name>
  <files>server/api/v1/pro/documents/upload.post.ts</files>
  <read_first>
    - server/api/v1/pro/documents/upload.post.ts
  </read_first>
  <action>
    Avant de calculer le statut d'insertion, lire siret_status du pro courant : supabase.from('professionals').select('siret_status').eq('id', uid).maybeSingle().
    Introduire une variable isKbisAutoApproved = document_type === 'kbis' && proRow?.siret_status === 'active'.
    Modifier la ligne de statut pour que le Kbis soit 'approved' quand isKbisAutoApproved est vrai : remplacer la logique actuelle (isDecennale ? 'approved' : 'pending') par un statut qui vaut 'approved' si isDecennale OU isKbisAutoApproved, sinon 'pending'. Garder isDecennale tel quel pour ne pas casser le bloc decennale existant.
    Ne PAS ajouter au Kbis les champs specifiques decennale (policy_number, expiry_date) : le bloc conditionnel `...(isDecennale ? {...} : {})` reste inchange. Pour le Kbis auto-approuve, poser reviewed_at: new Date().toISOString() dans l'insert (coherent avec un document deja statue), sans toucher aux labels ni a decennal_status.
    Ne rien changer au bloc d'auto-approbation decennale (mise a jour labels/decennal_status).
    Dans le retour JSON final, refleter l'auto-approbation : approved doit etre vrai aussi quand le Kbis est auto-approuve (ex : approved: isDecennale || isKbisAutoApproved).
  </action>
  <verify>
    <automated>cd /home/mike/projects/bati-axe && grep -q "siret_status" server/api/v1/pro/documents/upload.post.ts && grep -qi "kbis" server/api/v1/pro/documents/upload.post.ts && npx nuxi typecheck 2>&1 | grep -i "upload.post" && exit 1 || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "siret_status" server/api/v1/pro/documents/upload.post.ts
    - Une condition combine document_type === 'kbis' et siret_status === 'active' pour le statut 'approved'
    - Le bloc decennale (labels / decennal_status) est inchange
    - Un Kbis dont le pro n'est pas siret_status 'active' donne toujours status 'pending'
  </acceptance_criteria>
  <done>Le Kbis est insere 'approved' seulement si siret_status du pro vaut 'active', sinon 'pending' ; la logique decennale est intacte.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> upload.post.ts | pro uploade un document ; la decision d'auto-approbation ne doit dependre que d'un etat serveur, pas d'un input client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-058-06 | Elevation of Privilege | Kbis auto-approuve a tort | mitigate | La condition lit siret_status depuis professionals (etabli au claim via l'API gouv), jamais depuis le body client ; un SIRET non 'active' reste en revue manuelle |
</threat_model>

<verification>
- Un pro avec siret_status='active' qui uploade un Kbis : ligne verifications inseree avec status='approved'.
- Un pro avec siret_status distinct de 'active' : Kbis insere avec status='pending'.
- La decennale continue de s'auto-approuver comme avant (labels + decennal_status).
</verification>

<success_criteria>
- Auto-approbation Kbis conditionnee au seul etat serveur siret_status='active'.
- Aucune regression sur l'auto-approbation decennale.
</success_criteria>

<output>
Creer .planning/phases/05.8-enrichissement-siret-forme-juridique-naf-suggestion-cat-gori/05.8-03-SUMMARY.md quand termine.
</output>
