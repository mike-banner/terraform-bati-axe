---
phase: 05.8-enrichissement-siret-forme-juridique-naf-suggestion-cat-gori
plan: 02
type: execute
wave: 2
depends_on:
  - 05.8-01
files_modified:
  - server/api/v1/pro/claim.post.ts
  - server/api/v1/pro/siret-preview.post.ts
  - app/pages/pro/claim.vue
autonomous: true
requirements:
  - 05.8-R1
  - 05.8-R2
must_haves:
  truths:
    - "Au claim, siret_legal_form et siret_naf_code sont persistes sur la ligne professionals"
    - "Quand un SIRET valide est saisi, les categories BTP suggerees par le NAF sont pre-cochees dans le formulaire"
    - "Le pro peut decocher ou modifier librement les categories pre-cochees avant de valider"
  artifacts:
    - path: "server/api/v1/pro/siret-preview.post.ts"
      provides: "Endpoint POST retournant company_name + categories suggerees pour un SIRET"
      exports: ["default"]
    - path: "server/api/v1/pro/claim.post.ts"
      provides: "Upsert professionals incluant siret_legal_form et siret_naf_code"
    - path: "app/pages/pro/claim.vue"
      provides: "Pre-cochage UX des categories a partir du SIRET saisi"
  key_links:
    - from: "app/pages/pro/claim.vue"
      to: "/api/v1/pro/siret-preview"
      via: "appel $fetch sur SIRET valide (blur)"
      pattern: "siret-preview"
    - from: "server/api/v1/pro/claim.post.ts"
      to: "professionals.siret_naf_code"
      via: "upsert au claim"
      pattern: "siret_naf_code"
---

<objective>
Cabler l'enrichissement au moment du claim : (1) persister siret_legal_form + siret_naf_code dans l'upsert professionals ; (2) exposer un endpoint de previsualisation cote client qui, pour un SIRET, renvoie le nom d'entreprise et les categories BTP suggerees par le mapping NAF ; (3) pre-cocher ces categories dans le formulaire claim.vue tout en laissant le pro libre de les modifier.

Purpose: Livrer la valeur UX (pre-remplissage des categories) et la persistance des nouveaux champs, sans jamais forcer la categorisation cote serveur — le pro reste maitre de sa selection.
Output: claim.post.ts enrichi, endpoint siret-preview neuf, claim.vue avec pre-cochage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@server/api/v1/pro/claim.post.ts
@app/pages/pro/claim.vue
@server/utils/siretLookup.ts
@server/utils/nafCategoryMap.ts

<interfaces>
Fournis par le plan 05.8-01 (deja en place a l'execution de ce plan) :
- lookupSiret(siret): Promise<SiretLookupResult> avec champs legal_form?, naf_code? en plus de status/company_name/address/verified_at.
- suggestCategoriesFromNaf(naf?: string): string[] (server/utils/nafCategoryMap.ts) -> sous-ensemble de VALID_CATEGORIES.
- Colonnes DB siret_legal_form TEXT, siret_naf_code TEXT sur professionals.

Cote claim.vue (etat existant a reutiliser) :
- proForm.categories: string[] ; proForm.siret (chaine formatee avec espaces).
- toggleCategory(id) mute proForm.categories.
- RE_SIRET valide "123 456 789 00012" ou 14 chiffres ; le claim envoie proForm.siret.replace(/\s/g,'').
- proTouched.siret passe a true au blur du champ SIRET (etape 2).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Persister legal_form + naf_code au claim</name>
  <files>server/api/v1/pro/claim.post.ts</files>
  <read_first>
    - server/api/v1/pro/claim.post.ts
    - server/utils/siretLookup.ts
  </read_first>
  <action>
    Dans l'objet passe a supabase.from('professionals').upsert({...}) (autour de la ligne 198-219, la ou siret_status / siret_company_name / siret_address sont deja assignes), ajouter deux champs :
      siret_legal_form: siretLookup.legal_form ?? null,
      siret_naf_code: siretLookup.naf_code ?? null,
    Ne rien changer d'autre a la logique du handler (pas de nouvel appel lookup — siretLookup est deja calcule ligne ~121). La categorisation reste celle envoyee par le client (data.categories) : NE PAS ecraser ni forcer les categories cote serveur a partir du NAF.
  </action>
  <verify>
    <automated>cd /home/mike/projects/bati-axe && grep -q "siret_legal_form: siretLookup.legal_form" server/api/v1/pro/claim.post.ts && grep -q "siret_naf_code: siretLookup.naf_code" server/api/v1/pro/claim.post.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "siret_legal_form" server/api/v1/pro/claim.post.ts
    - grep -q "siret_naf_code" server/api/v1/pro/claim.post.ts
    - Aucune ligne ne remplace data.categories par une valeur derivee du NAF (la selection client reste autoritaire)
  </acceptance_criteria>
  <done>L'upsert professionals ecrit siret_legal_form et siret_naf_code ; les categories restent celles choisies par le pro.</done>
</task>

<task type="auto">
  <name>Task 2: Endpoint siret-preview (categories suggerees)</name>
  <files>server/api/v1/pro/siret-preview.post.ts</files>
  <read_first>
    - server/api/v1/pro/claim.post.ts
    - server/utils/siretLookup.ts
    - server/utils/nafCategoryMap.ts
  </read_first>
  <action>
    Creer server/api/v1/pro/siret-preview.post.ts (defineEventHandler). But : fournir au client, avant soumission du claim, les categories a pre-cocher. C'est une aide UX — pas une source d'autorite (le claim re-fait le lookup serveur et stocke les valeurs authoritatives).
    - Valider le body avec Zod : { siret: z.string().regex(/^\d{14}$/) }. En cas d'echec -> createError 400 (meme style que claim.post.ts).
    - Exiger un utilisateur authentifie via serverSupabaseUser (401 sinon), comme les autres endpoints /pro. Pas besoin de service role : aucun acces DB.
    - Appeler lookupSiret(siret). Si status 'not_found' ou 'error' -> renvoyer { status, suggested_categories: [] } (200, pas d'exception : le pro pourra toujours choisir manuellement).
    - Sinon renvoyer { status, company_name: result.company_name ?? null, suggested_categories: suggestCategoriesFromNaf(result.naf_code) }.
    Ne renvoyer AUCUNE donnee sensible au-dela du nom d'entreprise public et des categories. Pas d'adresse, pas de forme juridique dans la reponse preview (non necessaire cote UX).
  </action>
  <verify>
    <automated>cd /home/mike/projects/bati-axe && test -f server/api/v1/pro/siret-preview.post.ts && grep -q "suggestCategoriesFromNaf" server/api/v1/pro/siret-preview.post.ts && grep -q "serverSupabaseUser" server/api/v1/pro/siret-preview.post.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - server/api/v1/pro/siret-preview.post.ts existe
    - Le body est valide par un schema Zod avec regex 14 chiffres
    - grep -q "suggestCategoriesFromNaf" server/api/v1/pro/siret-preview.post.ts
    - La reponse n'expose ni adresse ni forme juridique (grep -vq "siret_address" et pas de legal_form dans le retour)
  </acceptance_criteria>
  <done>POST /api/v1/pro/siret-preview renvoie les categories suggerees pour un SIRET valide, [] pour un SIRET introuvable, 401 si non authentifie.</done>
</task>

<task type="auto">
  <name>Task 3: Pre-cochage des categories dans claim.vue</name>
  <files>app/pages/pro/claim.vue</files>
  <read_first>
    - app/pages/pro/claim.vue
  </read_first>
  <action>
    Dans le script setup de claim.vue :
    - Ajouter un ref categoriesTouched = ref(false), passe a true dans toggleCategory() (des que le pro coche/decoche manuellement). Objectif : ne jamais ecraser un choix manuel par une suggestion.
    - Ajouter une fonction async fetchSuggestedCategories() : si RE_SIRET.test(proForm.siret), appeler $fetch('/api/v1/pro/siret-preview', { method:'POST', body:{ siret: proForm.siret.replace(/\s/g,'') } }). Si la reponse renvoie suggested_categories non vide ET !categoriesTouched.value ET proForm.categories.length === 0, affecter proForm.categories = [...suggested_categories]. Envelopper dans try/catch silencieux (une erreur reseau ne doit jamais bloquer le formulaire).
    - Declencher fetchSuggestedCategories() au blur du champ SIRET (etape 2), en plus du proTouched.siret = true existant (input #pro-siret, @blur). Ne pas declencher a chaque frappe.
    Ne rien changer a la soumission (handleRegisterCompany) : proForm.categories part deja dans le body.
  </action>
  <verify>
    <automated>cd /home/mike/projects/bati-axe && grep -q "siret-preview" app/pages/pro/claim.vue && grep -q "categoriesTouched" app/pages/pro/claim.vue && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "siret-preview" app/pages/pro/claim.vue
    - grep -q "categoriesTouched" app/pages/pro/claim.vue
    - toggleCategory positionne categoriesTouched a true (le choix manuel a priorite)
    - Le pre-cochage ne s'applique que si les categories sont vides et non touchees (pas d'ecrasement)
  </acceptance_criteria>
  <done>Saisir un SIRET valide pre-coche les categories suggerees ; toute modification manuelle du pro est preservee et prime sur la suggestion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> /api/v1/pro/siret-preview | SIRET fourni par le client, non fiable |
| client -> /api/v1/pro/claim | categories + SIRET fournis par le client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-058-03 | Spoofing | siret-preview sans auth | mitigate | serverSupabaseUser exige un utilisateur authentifie (401 sinon) |
| T-058-04 | Tampering | client falsifie naf_code ou categories | mitigate | Le NAF suggere n'est jamais persiste depuis le client ; claim.post.ts re-fait lookupSiret cote serveur pour siret_naf_code, et data.categories reste borne par z.enum(VALID_CATEGORIES) |
| T-058-05 | Information Disclosure | preview fuit des donnees entreprise | accept | Ne renvoie que nom public + categories (deja publics via l'annuaire gouv), aucune donnee sensible |
</threat_model>

<verification>
- POST /api/v1/pro/siret-preview avec un SIRET valide renvoie suggested_categories (sous-ensemble de VALID_CATEGORIES).
- Un claim avec un SIRET actif ecrit siret_legal_form + siret_naf_code en DB.
- Dans claim.vue, saisir un SIRET batiment pre-coche les bonnes categories ; les decocher puis re-blur ne les re-coche pas.
</verification>

<success_criteria>
- Les deux nouveaux champs sont persistes au claim.
- Le pre-cochage fonctionne et reste entierement modifiable par le pro.
- Aucune categorisation forcee cote serveur.
</success_criteria>

<output>
Creer .planning/phases/05.8-enrichissement-siret-forme-juridique-naf-suggestion-cat-gori/05.8-02-SUMMARY.md quand termine.
</output>
