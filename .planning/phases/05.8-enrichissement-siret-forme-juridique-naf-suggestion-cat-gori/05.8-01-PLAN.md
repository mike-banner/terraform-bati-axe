---
phase: 05.8-enrichissement-siret-forme-juridique-naf-suggestion-cat-gori
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/utils/siretLookup.ts
  - server/utils/nafCategoryMap.ts
  - supabase/migrations/20260720020000_phase058_siret_enrichment.sql
autonomous: true
requirements:
  - 05.8-R1
  - 05.8-R2
must_haves:
  truths:
    - "lookupSiret() renvoie legal_form et naf_code parses depuis la reponse API existante"
    - "professionals a deux nouvelles colonnes nullable siret_legal_form et siret_naf_code"
    - "Un code NAF batiment connu resout vers une ou plusieurs categories BTP valides du projet"
  artifacts:
    - path: "server/utils/siretLookup.ts"
      provides: "Parsing enrichi legal_form + naf_code sur SiretLookupResult"
    - path: "server/utils/nafCategoryMap.ts"
      provides: "Mapping statique NAF -> categories BTP + fonction de resolution"
    - path: "supabase/migrations/20260720020000_phase058_siret_enrichment.sql"
      provides: "Colonnes siret_legal_form TEXT, siret_naf_code TEXT sur professionals"
  key_links:
    - from: "server/utils/nafCategoryMap.ts"
      to: "VALID_CATEGORIES (claim.post.ts)"
      via: "les valeurs cibles du mapping sont EXACTEMENT maconnerie/toiture/electricite/plomberie/peinture/isolation"
      pattern: "electricite|plomberie|toiture|maconnerie|peinture|isolation"
---

<objective>
Fondations de l'enrichissement SIRET : (1) enrichir le parsing de lookupSiret() pour extraire la forme juridique et le code NAF deja presents dans la reponse de l'API recherche-entreprises.api.gouv.fr ; (2) creer un mapping statique NAF -> categories BTP du projet ; (3) ajouter deux colonnes nullable sur professionals pour stocker ces valeurs au claim.

Purpose: Poser les briques reutilisees par le plan 02 (stockage au claim + pre-cochage UX). Aucun appel reseau nouveau n'est introduit ici — on ne fait qu'exploiter davantage de champs de la reponse deja recuperee.
Output: siretLookup.ts enrichi, nafCategoryMap.ts neuf, migration DB.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@server/utils/siretLookup.ts
@server/api/v1/pro/claim.post.ts
@supabase/migrations/20260624000000_phase5_siret_badges.sql
@supabase/migrations/20260719000000_phase57_input_limits.sql

<interfaces>
Reponse live confirmee de l'API (SIRET test) — champs au niveau results[0] :
- results[0].nature_juridique = "5599"  (code forme juridique, string)
- results[0].activite_principale = "49.10Z"  (code NAF, format avec point + lettre majuscule)
- Les deux peuvent etre absents/undefined selon l'entreprise -> traiter comme optionnels.

SiretLookupResult actuel (server/utils/siretLookup.ts) :
  status: 'active' | 'closed' | 'not_found' | 'error'
  company_name?: string
  address?: string
  verified_at: string

VALID_CATEGORIES (server/api/v1/pro/claim.post.ts, ligne 9) — cibles EXACTES du mapping :
  'maconnerie', 'toiture', 'electricite', 'plomberie', 'peinture', 'isolation'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enrichir le parsing de lookupSiret()</name>
  <files>server/utils/siretLookup.ts</files>
  <read_first>
    - server/utils/siretLookup.ts
  </read_first>
  <action>
    Etendre l'interface SiretLookupResult avec deux champs optionnels : legal_form?: string (forme juridique) et naf_code?: string (code NAF).
    Etendre le type inline de json.results[0] pour inclure nature_juridique?: string et activite_principale?: string (champs de premier niveau, confirmes presents dans la reponse live).
    Apres extraction de entry, lire entry.nature_juridique -> legal_form et entry.activite_principale -> naf_code. Normaliser naf_code en majuscules + trim (ex "49.10Z"). Ne PAS retirer le point ici — la normalisation pour le lookup se fera cote mapping (Task 2).
    Retourner legal_form et naf_code dans les branches active ET closed (donnees valides dans les deux cas). Les laisser absents dans not_found et error.
    Ne rien changer a l'URL du fetch, au timeout, ni a la logique de statut existante.
  </action>
  <verify>
    <automated>cd /home/mike/projects/bati-axe && npx nuxi typecheck 2>&1 | grep -i "siretLookup" && exit 1 || echo "OK siretLookup typecheck"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "legal_form" server/utils/siretLookup.ts
    - grep -q "naf_code" server/utils/siretLookup.ts
    - grep -q "nature_juridique" server/utils/siretLookup.ts
    - grep -q "activite_principale" server/utils/siretLookup.ts
    - URL fetch inchangee : grep -q "recherche-entreprises.api.gouv.fr/search" server/utils/siretLookup.ts
  </acceptance_criteria>
  <done>SiretLookupResult expose legal_form et naf_code, peuples depuis la reponse API pour les statuts active/closed, sans changement de l'appel reseau.</done>
</task>

<task type="auto">
  <name>Task 2: Mapping statique NAF -> categories BTP</name>
  <files>server/utils/nafCategoryMap.ts</files>
  <read_first>
    - server/api/v1/pro/claim.post.ts
  </read_first>
  <action>
    Creer server/utils/nafCategoryMap.ts exportant :
    1. Une constante NAF_TO_CATEGORIES: Record<string, string[]> mappant des codes NAF normalises (cle SANS point, majuscule — ex "4321A") vers un tableau de categories parmi EXACTEMENT : maconnerie, toiture, electricite, plomberie, peinture, isolation (memes valeurs que VALID_CATEGORIES dans claim.post.ts — ne pas inventer d'autres libelles).
    2. Une fonction suggestCategoriesFromNaf(naf?: string): string[] qui normalise l'entree (upper, retirer points et espaces) puis retourne NAF_TO_CATEGORIES[normalized] ?? []. Entree vide/undefined -> [].

    Codes de depart pre-remplis (NAF rev. 2, division 43 + 41.20). VERIFIER chaque code contre une source INSEE fiable (nomenclature NAF rev. 2) via context7 ou une recherche officielle AVANT de finaliser ; ne pas ajouter de code dont le libelle exact n'est pas confirme — dans le doute, l'omettre et poser un commentaire // ponytail: nommant l'incertitude plutot que d'inventer.
      - 4321A (installation electrique tous locaux) -> electricite
      - 4321B (installation electrique voie publique) -> electricite
      - 4322A (installation eau et gaz tous locaux) -> plomberie
      - 4322B (installation equipements thermiques / climatisation) -> plomberie
      - 4329A (travaux d'isolation) -> isolation
      - 4331Z (travaux de platrerie) -> isolation
      - 4333Z (revetement des sols et des murs) -> peinture
      - 4334Z (peinture et vitrerie) -> peinture
      - 4391A (travaux de charpente) -> toiture
      - 4391B (travaux de couverture par elements) -> toiture
      - 4399A (travaux d'etancheite) -> toiture
      - 4399C (maconnerie generale et gros oeuvre de batiment) -> maconnerie
      - 4120A (construction de maisons individuelles) -> maconnerie
      - 4120B (construction d'autres batiments) -> maconnerie

    Marquer d'un commentaire // ponytail: les deux rattachements de jugement (4331Z platrerie -> isolation/cloisons ; 4399A etancheite -> toiture) : ce sont des choix, pas des equivalences officielles.
    Ajouter en bas du fichier un self-check exportable minimal (petite fonction _selfCheck() avec assert Node, ou bloc de test) verifiant : suggestCategoriesFromNaf('49.10Z') renvoie [] et suggestCategoriesFromNaf('4321A') inclut 'electricite'.
  </action>
  <verify>
    <automated>cd /home/mike/projects/bati-axe && npx tsx -e "import {suggestCategoriesFromNaf} from './server/utils/nafCategoryMap.ts'; const a=suggestCategoriesFromNaf('4321A'); const b=suggestCategoriesFromNaf('49.10Z'); if(!a.includes('electricite')||b.length){throw new Error('FAIL '+JSON.stringify([a,b]))}; console.log('OK',a,b)"</automated>
  </verify>
  <acceptance_criteria>
    - server/utils/nafCategoryMap.ts existe
    - grep -q "suggestCategoriesFromNaf" server/utils/nafCategoryMap.ts
    - grep -q "NAF_TO_CATEGORIES" server/utils/nafCategoryMap.ts
    - Toutes les valeurs cibles sont dans l'ensemble VALID_CATEGORIES (aucun libelle hors maconnerie/toiture/electricite/plomberie/peinture/isolation)
    - La commande verify affiche OK
  </acceptance_criteria>
  <done>suggestCategoriesFromNaf resout les codes batiment connus vers des categories valides et renvoie [] pour un code inconnu ou vide.</done>
</task>

<task type="auto">
  <name>Task 3: Migration colonnes siret_legal_form + siret_naf_code</name>
  <files>supabase/migrations/20260720020000_phase058_siret_enrichment.sql</files>
  <read_first>
    - supabase/migrations/20260624000000_phase5_siret_badges.sql
    - supabase/migrations/20260719000000_phase57_input_limits.sql
  </read_first>
  <action>
    Creer la migration supabase/migrations/20260720020000_phase058_siret_enrichment.sql ajoutant deux colonnes nullable sur public.professionals :
      - siret_legal_form TEXT (forme juridique / nature_juridique brute de l'API — un code, ex "5599")
      - siret_naf_code TEXT (code NAF / activite_principale, ex "49.10Z")
    Utiliser ADD COLUMN IF NOT EXISTS (meme style que 20260624000000_phase5_siret_badges.sql).
    Ajouter une borne haute via CHECK NOT VALID puis VALIDATE (meme pattern que 20260719000000_phase57_input_limits.sql) : char_length(siret_legal_form) <= 50 et char_length(siret_naf_code) <= 20, chaque CHECK tolerant NULL (col IS NULL OR char_length(...) <= N).
    Commentaire d'en-tete court : colonnes peuplees au claim uniquement, pas de resynchronisation ulterieure (hors scope).
  </action>
  <verify>
    <automated>cd /home/mike/projects/bati-axe && test -f supabase/migrations/20260720020000_phase058_siret_enrichment.sql && grep -q "siret_legal_form" supabase/migrations/20260720020000_phase058_siret_enrichment.sql && grep -q "siret_naf_code" supabase/migrations/20260720020000_phase058_siret_enrichment.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Le fichier de migration existe avec le nom exact 20260720020000_phase058_siret_enrichment.sql
    - grep -q "ADD COLUMN IF NOT EXISTS siret_legal_form" (a la casse pres)
    - grep -q "ADD COLUMN IF NOT EXISTS siret_naf_code"
    - grep -q "VALIDATE CONSTRAINT" (bornes appliquees selon le pattern existant)
  </acceptance_criteria>
  <done>Deux colonnes nullable bornees existent sur professionals, migration appliquable sans scan bloquant.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API gouv -> serveur | Reponse externe non maitrisee ; champs peuvent etre absents ou de longueur imprevue |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-058-01 | Tampering | siret_legal_form / siret_naf_code stockes depuis reponse API | mitigate | Bornes CHECK <= 50 / <= 20 en DB + valeurs traitees comme optionnelles (nullable) cote parsing |
| T-058-02 | Denial of Service | Longueur de champ API imprevue | accept | Reponse gouv officielle, faible risque ; borne DB suffit comme dernier rempart |
</threat_model>

<verification>
- npx nuxi typecheck ne remonte aucune erreur sur siretLookup.ts ni nafCategoryMap.ts.
- La commande tsx du Task 2 affiche OK.
- La migration contient les deux colonnes et suit le pattern NOT VALID + VALIDATE.
</verification>

<success_criteria>
- lookupSiret() renvoie legal_form + naf_code pour un SIRET actif.
- suggestCategoriesFromNaf() mappe les codes batiment connus vers des categories VALID_CATEGORIES et renvoie [] sinon.
- professionals possede siret_legal_form et siret_naf_code (nullable, bornees).
</success_criteria>

<output>
Creer .planning/phases/05.8-enrichissement-siret-forme-juridique-naf-suggestion-cat-gori/05.8-01-SUMMARY.md quand termine.
</output>
