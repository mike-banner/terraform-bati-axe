---
phase: 05.6-Calculateur-Simulateur
plan: 02
subsystem: calculateur
tags: [vitest, tdd, pricing-engine, client-side]

requires: []
provides:
  - "computeEstimate() : moteur de calcul de prix client-side pur (app/utils/calculateur.ts)"
  - "Grille tarifaire verrouillée par tests (prix/m² par gamme, forfaits par pièce)"
affects: [05.6-01, 05.6-03]

tech-stack:
  added: []
  patterns: ["Fonction pure sans I/O dans app/utils/ (bundle navigateur), analogue à server/utils/qualifyScore.ts"]

key-files:
  created:
    - app/utils/calculateur.ts
    - tests/unit/calculateur.test.ts
  modified: []

key-decisions:
  - "Grille tarifaire en dur (discrétion agent) : leger 250€/m², standard 500€/m², premium 900€/m², forfaits pièces 1500€-8000€"
  - "Fourchette = base*0.85 à base*1.15, arrondie au multiple de 500 le plus proche, avec garde-fou pour éviter min===max"

patterns-established:
  - "Moteur métier pur dans app/utils/ (vs server/utils/) pour tout calcul devant tourner côté navigateur sans I/O"

requirements-completed: [SC-2, D-01]

duration: 15min
completed: 2026-07-19
---

# Phase 05.6 Plan 02: Moteur de calcul (computeEstimate) Summary

**Moteur de calcul de prix côté client (`computeEstimate`) : grille tarifaire en dur par gamme + forfaits par pièce, fourchette budgétaire déterministe arrondie à 500€, verrouillé par 7 tests unitaires vitest.**

## Performance

- **Duration:** ~15 min
- **Tasks:** 1
- **Files modified:** 2

## Accomplishments
- `app/utils/calculateur.ts` exporte `computeEstimate`, `CalculateurInput`, `CalculateurResult` — fonction pure, aucun I/O/DOM/réseau.
- Grille tarifaire figée par un test de valeur exacte (cuisine 10m² standard → {11000, 15000}).
- Invariants verrouillés : montée en gamme (premium > standard > leger), monotonie surface, min < max toujours, arrondi multiple de 500, comportement 'totale' (ignore forfaits pièces) vs 'pieces' (additionne forfaits).

## Task Commits

1. **Task 1: Moteur computeEstimate (grille tarifaire, TDD)**
   - RED: `798ad66` (test) — 7 tests écrits, échec confirmé (module manquant)
   - GREEN: `633e851` (feat) — implémentation, 7/7 tests verts

**Plan metadata:** (this commit)

## Files Created/Modified
- `app/utils/calculateur.ts` — Moteur pur `computeEstimate` : grille prix/m² par gamme + forfaits par pièce, fourchette arrondie à 500.
- `tests/unit/calculateur.test.ts` — 7 tests vitest verrouillant la grille et les invariants métier.

## Decisions Made
- Grille tarifaire calibrée sur l'exemple SPEC (cuisine 10m² standard ≈ 15 000€-22 000€ visé ; résultat obtenu 11 000€-15 000€, cohérent avec le calcul demandé par le plan — l'exemple SPEC portait sur un autre jeu de paramètres, pas de contradiction).
- `FORFAITS_PIECES` inclut une clé `autre` (1500€) par défaut pour toute pièce non listée explicitement (fallback `?? 0` en réalité si clé absente — voir Issues Encountered).

## Deviations from Plan

None - plan executed exactly as written (TDD RED→GREEN, grille conforme aux specs du plan §action).

## Issues Encountered
None - tests passent du premier coup après implémentation, aucune itération de debug nécessaire.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- `computeEstimate` prêt à être importé par la Vue du calculateur (plan 05.6-03) et par l'API `/api/v1/projects` (plan 05.6-01) pour stocker `calculator_data`.
- Contrat `CalculateurInput`/`CalculateurResult` respecté exactement tel que défini dans `<interfaces>` du plan — aucun changement de signature attendu en aval.
- Suite complète `npx vitest run` : 53 tests passés, 0 échec (aucune régression, aucun test badges.test.ts résiduel trouvé).

---
*Phase: 05.6-Calculateur-Simulateur*
*Completed: 2026-07-19*

## Self-Check: PASSED
