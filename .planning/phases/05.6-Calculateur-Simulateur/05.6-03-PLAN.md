---
phase: 05.6-Calculateur-Simulateur
plan: 03
type: execute
wave: 2
depends_on: ["05.6-01", "05.6-02"]
files_modified:
  - app/pages/simulateur.vue
autonomous: true
requirements: [SC-1, SC-2, SC-3, D-01, D-02]
must_haves:
  truths:
    - "L'utilisateur navigue par tuiles visuelles (Bento) sur 6 étapes : type de rénovation → pièces → surface → gamme → code postal → Lead Wall"
    - "L'étape 2 (pièces) est en sélection multiple ; les étapes type/gamme sont en sélection unique"
    - "L'estimation chiffrée n'est révélée qu'APRÈS soumission validée des coordonnées (Lead Wall, D-03/SC-3), jamais avant"
    - "La soumission POST /api/v1/projects envoie calculator_data (incl. estimate_min/max) et déclenche la révélation + CTA espace client"
  artifacts:
    - path: "app/pages/simulateur.vue"
      provides: "Calculateur tuilé 6 étapes + Lead Wall fluide + révélation estimation"
      min_lines: 300
  key_links:
    - from: "app/pages/simulateur.vue"
      to: "app/utils/calculateur.ts"
      via: "import computeEstimate"
      pattern: "computeEstimate"
    - from: "app/pages/simulateur.vue"
      to: "/api/v1/projects"
      via: "$fetch POST avec calculator_data"
      pattern: "calculator_data"
---

<objective>
Réécrire intégralement `app/pages/simulateur.vue` : remplacer le formulaire linéaire actuel par une navigation Bento tuilée en 6 étapes (SC-1), intégrer le moteur client `computeEstimate` (SC-2, D-01), et implémenter le Lead Wall en tuile fluide sans modal (D-02) qui ne révèle l'estimation qu'après capture validée des coordonnées (SC-3).

Purpose: Transformer le simulateur en Lead Magnet à forte conversion (mobile-first, max 5-6 clics).
Output: `app/pages/simulateur.vue` réécrit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.6-Calculateur-Simulateur/05.6-UI-SPEC.md
@.planning/phases/05.6-Calculateur-Simulateur/05.6-PATTERNS.md
@.agent/skills/sketch-findings-bati-axe/SKILL.md
@app/pages/simulateur.vue

<interfaces>
<!-- Contrats des plans amont — l'exécuteur s'y branche, pas d'exploration. -->

app/utils/calculateur.ts (plan 05.6-02) :
  CalculateurInput  = { renovation_type: string; pieces: string[]; surface_m2: number; gamme: 'leger'|'standard'|'premium' }
  CalculateurResult = { estimate_min: number; estimate_max: number }
  computeEstimate(input: CalculateurInput): CalculateurResult

POST /api/v1/projects (plan 05.6-01) attend un body :
  { calculator_data: { renovation_type, pieces, surface_m2, gamme, estimate_min, estimate_max },
    postal_code, customer_name, customer_email, customer_phone, cgu_accepted (true), sms_opt_in }
  => category / description / budget_range NE sont PLUS envoyés (dérivés server-side).
  Réponse succès : { status:'SUCCESS', projectId, zoneName, accessToken? }

Regex à réutiliser VERBATIM (app/pages/simulateur.vue:62-63) :
  RE_EMAIL    = /^[^\s@]+@[^\s@]+\.[^\s@]{2,}$/
  RE_PHONE_FR = /^(?:(?:\+|00)33[\s.-]?|0)[1-9](?:[\s.-]*\d{2}){4}$/

Tokens design (04.7) réutilisés : .bento-card, .reveal-item / reveal-up (app/assets/css/tailwind.css:317-352),
couleur active tuile = border-orange-500 bg-orange-50, accent CTA/progress = bg-safety (#F97316),
tuiles sélectionnables min-h-11 (44px). Zéro cyan/vert (invariant dur).
Icônes : lucide-vue-next (déjà installé) pour les pièces / types de travaux.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Script setup — état, données, moteur, validation, soumission</name>
  <files>app/pages/simulateur.vue</files>
  <read_first>
    - app/pages/simulateur.vue
    - .planning/phases/05.6-Calculateur-Simulateur/05.6-UI-SPEC.md
    - .planning/phases/05.6-Calculateur-Simulateur/05.6-PATTERNS.md
    - app/utils/calculateur.ts
  </read_first>
  <action>
    Réécrire le `<script setup lang="ts">`. Conserver `useHead` (titre/description existants).

    Données :
    - `renovationTypes`: [{ id:'totale', label:'Rénovation totale', desc:'…' }, { id:'pieces', label:'Pièce par pièce', desc:'…' }].
    - `piecesList`: cuisine, salle_de_bain, salon, chambre, exterieur, autre — chacun { id, label, icon } avec une icône lucide (ex. lucide-vue-next : `ChefHat`, `Bath`, `Sofa`, `BedDouble`, `Trees`, `LayoutGrid`).
    - `gammes`: [{ id:'leger', label:'Rafraîchissement' }, { id:'standard', label:'Standard' }, { id:'premium', label:'Premium' }] (ids alignés au type `gamme` du moteur).
    - `surfaceBrackets` optionnel OU input numérique/range natif (`<input type="range">`, rung 4 natif — pas de lib slider).

    État réactif `form` = { renovation_type:'', pieces:[] as string[], surface_m2:0, gamme:'', postal_code:'', customer_name:'', customer_email:'', customer_phone:'', cgu_accepted:false, sms_opt_in:false }. `touched` = { name, email, phone } comme aujourd'hui.

    `step` (1..6), `totalSteps = 6`. Réutiliser VERBATIM `RE_EMAIL`, `RE_PHONE_FR`, `normalizePhone`, `normalizePostalCode`, `progress`, `prevStep` de la version actuelle.

    `togglePiece(id)` : `const i = form.pieces.indexOf(id); i === -1 ? form.pieces.push(id) : form.pieces.splice(i, 1)` (multi-select — logique neuve, aucun analog).

    `isStepValid` (switch sur 6 étapes) :
    - 1: !!form.renovation_type
    - 2: si renovation_type==='totale' → true (pas de pièces requises) sinon form.pieces.length > 0
    - 3: form.surface_m2 > 0
    - 4: !!form.gamme
    - 5: form.postal_code === '78955' (zone pilote — même règle que l'existant ; garder la bannière d'éligibilité)
    - 6: form.customer_name.trim().length >= 2 && RE_EMAIL.test(email) && RE_PHONE_FR.test(phone) && form.cgu_accepted

    `estimate = computed(() => computeEstimate({ renovation_type: form.renovation_type, pieces: form.pieces, surface_m2: form.surface_m2, gamme: form.gamme as any }))` — calculé en continu mais JAMAIS affiché avant révélation (D-01/SC-3).

    `revealed = ref(false)`, `createdAccessToken = ref<string|null>(null)`.

    `handleSubmit` (adapté de l'actuel) : marquer touched, guard isStepValid, POST `/api/v1/projects` avec body { calculator_data: { renovation_type, pieces, surface_m2, gamme, estimate_min: estimate.value.estimate_min, estimate_max: estimate.value.estimate_max }, postal_code, customer_name, customer_email, customer_phone: phone.replace(/\s/g,''), cgu_accepted, sms_opt_in }. Sur `status==='SUCCESS'` : `revealed.value = true`, stocker accessToken. Conserver la gestion d'erreur `submitError` verbatim. NE PAS envoyer category/description/budget_range.

    Auto-advance sur sélection unique (type, gamme) comme l'actuel `selectCategory`→nextStep ; PAS d'auto-advance sur l'étape pièces multi-select (l'utilisateur clique "Suivant").
  </action>
  <verify>
    <automated>grep -q "computeEstimate" app/pages/simulateur.vue && grep -q "togglePiece" app/pages/simulateur.vue && grep -q "calculator_data" app/pages/simulateur.vue</automated>
  </verify>
  <done>Le script gère 6 étapes, la multi-sélection pièces, le calcul d'estimation via le moteur, et une soumission qui envoie calculator_data sans category/description/budget_range.</done>
  <acceptance_criteria>
    - `grep -q "totalSteps = 6\|totalSteps   = 6" app/pages/simulateur.vue`.
    - `grep -q "togglePiece" app/pages/simulateur.vue` et logique push/splice sur `form.pieces`.
    - `grep -q "computeEstimate" app/pages/simulateur.vue`.
    - Le body POST contient `calculator_data` et NE contient PAS `budget_range:` / `category:` (vérifiable par lecture du body $fetch).
    - `form.cgu_accepted` fait partie de la validation de l'étape 6.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Template — étapes 1 à 5 (tuiles Bento, transitions, éligibilité)</name>
  <files>app/pages/simulateur.vue</files>
  <read_first>
    - app/pages/simulateur.vue
    - .planning/phases/05.6-Calculateur-Simulateur/05.6-UI-SPEC.md
    - .planning/phases/05.6-Calculateur-Simulateur/05.6-PATTERNS.md
  </read_first>
  <action>
    Réécrire le `<template>` pour les étapes 1-5, en réutilisant l'enveloppe carte + barre de progression existantes (garder la structure header + progress bar `bg-safety`).

    Copywriting VERBATIM depuis UI-SPEC :
    - Étape 1 titre "Quel type de rénovation ?" — tuiles renovationTypes, sélection unique (radio-like), auto-advance.
    - Étape 2 titre "Quelles pièces sont concernées ?" — grille d'icônes lucide, MULTI-select via `togglePiece`, bouton "Suivant" explicite. (Masquer/sauter l'étape si renovation_type==='totale' → passer directement à l'étape 3.)
    - Étape 3 titre "Quelle surface au total ?" — input numérique ou `<input type="range">` natif, `form.surface_m2`.
    - Étape 4 titre "Quel niveau de prestation ?" — tuiles gammes, sélection unique, auto-advance.
    - Étape 5 titre "Où se situe le chantier ?" — input code postal (réutiliser `normalizePostalCode` + bannière d'éligibilité existante lignes 349-373 ; adapter la copie "Cette zone n'est pas encore couverte" / texte pilote 78955 d'UI-SPEC).

    État visuel des tuiles : `.bento-card`, actif = `border-orange-500 bg-orange-50`, inactif = `border-border hover:…`. Toutes les tuiles sélectionnables : `min-h-11`. Grille `grid-cols-1` mobile, `md:grid-cols-2`/`md:grid-cols-3` pour les icônes (pièces/types).

    Transitions : envelopper le contenu d'étape dans `<Transition>` réutilisant les keyframes `.reveal-item` / `reveal-up` déjà définies (`tailwind.css`). NE PAS inventer de nouvelle courbe d'easing. `prefers-reduced-motion` déjà géré globalement — ne rien ajouter.

    Zéro token cyan/vert. Accent orange réservé : CTA, tuile active, remplissage progress bar.
  </action>
  <verify>
    <automated>grep -q "Quelles pièces sont concernées" app/pages/simulateur.vue && grep -q "border-orange-500" app/pages/simulateur.vue && grep -q "Transition" app/pages/simulateur.vue</automated>
  </verify>
  <done>Les 5 premières étapes s'affichent en tuiles Bento avec la charte 04.7, multi-select fonctionnel à l'étape 2, saut de l'étape 2 si rénovation totale, bannière d'éligibilité code postal conservée.</done>
  <acceptance_criteria>
    - `grep -q "Quel type de rénovation" app/pages/simulateur.vue`.
    - `grep -q "Quel niveau de prestation" app/pages/simulateur.vue`.
    - `grep -q "border-orange-500" app/pages/simulateur.vue` (état actif tuile).
    - `grep -q "min-h-11" app/pages/simulateur.vue` (cible tactile 44px).
    - Aucune occurrence de classe cyan/emerald/green : `! grep -Eq "cyan|emerald|bg-green|text-green" app/pages/simulateur.vue`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Template — Lead Wall (étape 6) + révélation estimation + navigation</name>
  <files>app/pages/simulateur.vue</files>
  <read_first>
    - app/pages/simulateur.vue
    - .planning/phases/05.6-Calculateur-Simulateur/05.6-UI-SPEC.md
  </read_first>
  <action>
    Étape 6 — Lead Wall en tuile fluide (D-02, PAS de modal/overlay ; même enveloppe `.bento-card`, barre de progression pleine) :
    - Titre VERBATIM "Votre estimation est prête. À qui l'envoyons-nous ?", sous-texte UI-SPEC "Recevez votre fourchette de prix par email…".
    - Champs nom / email / téléphone réutilisant les inputs existants + messages d'erreur `contactErrors` (réutiliser le pattern actuel de l'étape 6 : bordure rouge + message).
    - Checkbox CGU obligatoire (réutiliser le bloc CGU + lien /legal/cgu de l'actuelle étape 7 — NE PAS supprimer, exigence légale) + checkbox sms_opt_in optionnelle.
    - CTA "Recevoir mon estimation gratuite" (UI-SPEC) déclenchant `handleSubmit`, avec spinner `isSubmitting`.
    - Bloc d'erreur `submitError` (réutiliser verbatim).

    Révélation (`v-if="revealed"`) — remplace l'ancien écran succès step 8 :
    - Afficher la fourchette `estimate.estimate_min` – `estimate.estimate_max` formatée en € (typo Display 40px mobile / clamp 56px desktop, chiffre en accent orange par UI-SPEC), titre rassurant.
    - CTA vers `/mon-projet/${createdAccessToken}` (réutiliser le bloc "Ouvrir l'Espace Client" de l'actuelle vue succès) + lien retour accueil.
    - L'estimation ne doit JAMAIS apparaître dans le template hors de ce bloc `v-if="revealed"` (SC-3).

    Navigation : bouton "Retour" (`prevStep`, désactivé étape 1) ; bouton "Suivant" pour les étapes à validation explicite (2, 3, 5) ; CTA submit à l'étape 6. Réutiliser la structure de barre de navigation existante, copie CTA "Suivant" (mid-flow) / "Recevoir mon estimation gratuite" (lead wall).
  </action>
  <verify>
    <automated>grep -q "Votre estimation est prête" app/pages/simulateur.vue && grep -q "revealed" app/pages/simulateur.vue && grep -q "cgu_accepted" app/pages/simulateur.vue && npm run build 2>&1 | tail -8</automated>
    <human-check>Lancer `npm run dev`, ouvrir /simulateur : parcourir les 6 étapes sur mobile (DevTools responsive), vérifier que (1) la multi-sélection pièces fonctionne, (2) l'estimation n'apparaît qu'après soumission des coordonnées, (3) l'accent orange est le seul accent, zéro cyan/vert, (4) les tuiles ont l'élévation .bento-card au survol.</human-check>
  </verify>
  <done>Le Lead Wall capture nom/email/téléphone + CGU en tuile fluide, la soumission révèle la fourchette chiffrée + CTA espace client, l'estimation reste masquée avant capture. Le build passe.</done>
  <acceptance_criteria>
    - `grep -q "Votre estimation est prête" app/pages/simulateur.vue`.
    - `grep -q "Recevoir mon estimation gratuite" app/pages/simulateur.vue`.
    - Le bloc affichant `estimate_min`/`estimate_max` est à l'intérieur d'un `v-if="revealed"` (vérifiable par lecture — pas d'affichage de l'estimation ailleurs).
    - `grep -q "cgu_accepted" app/pages/simulateur.vue` et lien `/legal/cgu` présent.
    - `grep -q "mon-projet/" app/pages/simulateur.vue` (CTA espace client).
    - `npm run build` réussit (exit 0).
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `npm run build` réussit.
- Parcours manuel /simulateur : 6 étapes, multi-select pièces, estimation révélée uniquement post-soumission, charte 04.7 (orange seul accent, zéro cyan/vert), mobile-first.
- Un lead soumis crée une ligne projects avec calculator_data (chaîne end-to-end avec plans 01/02).
</verification>

<success_criteria>
- Navigation tuilée Bento 6 étapes (SC-1).
- Estimation chiffrée calculée client-side (SC-2, D-01).
- Estimation dévoilée uniquement après capture validée des coordonnées (SC-3, D-02, tuile fluide sans modal).
</success_criteria>

<output>
Créer `.planning/phases/05.6-Calculateur-Simulateur/05.6-03-SUMMARY.md` à la fin.
</output>
