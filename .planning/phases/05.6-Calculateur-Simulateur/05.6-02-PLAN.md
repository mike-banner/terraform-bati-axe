---
phase: 05.6-Calculateur-Simulateur
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/utils/calculateur.ts
  - tests/unit/calculateur.test.ts
autonomous: true
requirements: [SC-2, D-01]
must_haves:
  truths:
    - "Un ensemble de choix (type de rénovation, pièces, surface, gamme) produit une fourchette budgétaire chiffrée déterministe"
    - "Le moteur tourne côté navigateur (importable depuis app/), sans I/O ni appel réseau"
    - "estimate_min < estimate_max, et l'estimation croît avec la surface et la montée en gamme"
  artifacts:
    - path: "app/utils/calculateur.ts"
      provides: "Moteur de calcul client-side computeEstimate (D-01)"
      exports: ["computeEstimate", "CalculateurInput", "CalculateurResult"]
    - path: "tests/unit/calculateur.test.ts"
      provides: "Verrouille la grille tarifaire et les invariants de la fourchette"
  key_links:
    - from: "tests/unit/calculateur.test.ts"
      to: "app/utils/calculateur.ts"
      via: "import computeEstimate"
      pattern: "computeEstimate"
---

<objective>
Créer le moteur de calcul de prix côté client (D-01) : une fonction pure `computeEstimate` transformant les choix du calculateur en une fourchette budgétaire chiffrée (SC-2). Grille tarifaire en dur (discrétion agent), verrouillée par tests.

Purpose: Fournir une estimation "instantanée" au particulier sans aller-retour serveur, exposable sans risque (l'artisan reste seul responsable du devis final).
Output: `app/utils/calculateur.ts` + tests unitaires.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.6-Calculateur-Simulateur/05.6-SPEC.md
@.planning/phases/05.6-Calculateur-Simulateur/05.6-PATTERNS.md

<interfaces>
<!-- Analog pur : server/utils/qualifyScore.ts (interfaces typées + fonction pure, aucun I/O). -->
<!-- Déviation structurelle unique : le fichier vit dans app/utils/ (bundle navigateur, D-01), pas server/utils/. -->

Contrat cible (à respecter pour que le plan 03 et l'API du plan 01 s'y branchent) :
  CalculateurInput  = { renovation_type: string; pieces: string[]; surface_m2: number; gamme: 'leger' | 'standard' | 'premium' }
  CalculateurResult = { estimate_min: number; estimate_max: number }
  computeEstimate(input: CalculateurInput): CalculateurResult
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Moteur computeEstimate (grille tarifaire, TDD)</name>
  <files>app/utils/calculateur.ts, tests/unit/calculateur.test.ts</files>
  <read_first>
    - server/utils/qualifyScore.ts
    - tests/unit/qualify-score.test.ts
    - .planning/phases/05.6-Calculateur-Simulateur/05.6-SPEC.md
    - .planning/phases/05.6-Calculateur-Simulateur/05.6-PATTERNS.md
  </read_first>
  <behavior>
    - Même surface, gammes différentes : premium > standard > leger (montée en gamme = estimation plus élevée).
    - Surface plus grande, mêmes autres choix : estimation plus élevée (croissance monotone en surface).
    - Toujours estimate_min < estimate_max (fourchette non dégénérée).
    - min et max arrondis au multiple de 500 le plus proche.
    - Input verrouillé → output verrouillé : un cas connu (ex. { renovation_type:'pieces', pieces:['cuisine'], surface_m2:10, gamme:'standard' }) produit une paire {estimate_min, estimate_max} exacte (fige la grille contre les régressions).
    - renovation_type 'totale' : estimation basée surface × prix/m² de la gamme, sans forfaits pièces.
    - renovation_type 'pieces' : estimation = surface × prix/m² gamme + somme des forfaits par pièce sélectionnée.
  </behavior>
  <action>
    Écrire d'abord `tests/unit/calculateur.test.ts` (vitest, style `qualify-score.test.ts`) couvrant les cas <behavior> ci-dessus, puis implémenter `app/utils/calculateur.ts` jusqu'au vert.

    Implémentation (grille en dur — discrétion agent, calibrée sur l'exemple SPEC `15 000 € - 22 000 €`) :
    - Interfaces `CalculateurInput` / `CalculateurResult` exactement comme le contrat <interfaces>.
    - Prix/m² par gamme : leger 250, standard 500, premium 900.
    - Forfaits par pièce (appliqués uniquement si renovation_type === 'pieces') : cuisine 8000, salle_de_bain 6000, salon 2500, chambre 2000, exterieur 5000, autre 1500.
    - `base = surface_m2 * prixM2[gamme] + (renovation_type === 'pieces' ? somme forfaits pièces : 0)`.
    - Fourchette : `estimate_min = round500(base * 0.85)`, `estimate_max = round500(base * 1.15)`, avec `round500(n) = Math.round(n / 500) * 500`. Garantir un écart strictement positif (si min === max après arrondi, forcer max = min + 500).
    - Fonction pure, aucun accès DOM/réseau/fs. Marquer la grille d'un commentaire `// ponytail: grille en dur, ajuster selon retours métier`.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/calculateur.test.ts</automated>
  </verify>
  <done>`computeEstimate` exporté depuis `app/utils/calculateur.ts`, pur, avec grille en dur. Tests verts couvrant monotonie gamme, monotonie surface, min<max, arrondi 500, et un cas de valeur exacte figé.</done>
  <acceptance_criteria>
    - `grep -q "export function computeEstimate" app/utils/calculateur.ts`.
    - `grep -q "export interface CalculateurInput" app/utils/calculateur.ts` et `CalculateurResult`.
    - Aucune import de `#supabase`, `fetch`, `fs`, ni référence DOM dans `app/utils/calculateur.ts`.
    - `npx vitest run tests/unit/calculateur.test.ts` : 0 failed, au moins un test asserte une paire {estimate_min, estimate_max} exacte.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `npx vitest run tests/unit/calculateur.test.ts` passe.
- Le fichier est importable depuis un composant `app/` (chemin `~/utils/calculateur` ou auto-import Nuxt).
</verification>

<success_criteria>
- Moteur de calcul client-side livré et testé (SC-2, D-01).
- Fourchette budgétaire déterministe, invariants (min<max, monotonie) verrouillés.
</success_criteria>

<output>
Créer `.planning/phases/05.6-Calculateur-Simulateur/05.6-02-SUMMARY.md` à la fin.
</output>
