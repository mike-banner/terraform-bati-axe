---
phase: 05.6-Calculateur-Simulateur
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260717000000_phase56_calculator_data.sql
  - server/utils/calculatorMapping.ts
  - tests/unit/calculator-mapping.test.ts
  - server/api/v1/projects.post.ts
autonomous: false
requirements: [SC-4, D-03]
must_haves:
  truths:
    - "Un lead soumis via le calculateur crée une ligne projects avec calculator_data (choix granulaires) persistée"
    - "La ligne projects reçoit une category dérivée pour rester visible dans le marché dynamique des pros (invariant Phase 4.6)"
    - "Le pro voit une description lisible et une fourchette budgétaire estimée par la plateforme sur la fiche chantier"
  artifacts:
    - path: "supabase/migrations/20260717000000_phase56_calculator_data.sql"
      provides: "Colonne JSONB calculator_data sur projects (D-03)"
      contains: "ADD COLUMN IF NOT EXISTS calculator_data"
    - path: "server/utils/calculatorMapping.ts"
      provides: "Dérivation trades[] + category primaire depuis les choix calculateur"
      exports: ["deriveTrades", "derivePrimaryCategory"]
    - path: "server/api/v1/projects.post.ts"
      provides: "Persistance calculator_data + category dérivée + description/budget composés"
      contains: "calculator_data"
  key_links:
    - from: "server/api/v1/projects.post.ts"
      to: "server/utils/calculatorMapping.ts"
      via: "import deriveTrades/derivePrimaryCategory"
      pattern: "calculatorMapping"
    - from: "server/api/v1/projects.post.ts"
      to: "projects.calculator_data"
      via: "insert payload"
      pattern: "calculator_data:"
---

<objective>
Étendre la couche de persistance pour que la soumission du calculateur crée un lead `projects` ultra-qualifié : colonne JSONB `calculator_data` (D-03), dérivation server-side de la `category` pour préserver le matching du marché dynamique (invariant Phase 4.6), et composition d'une `description` lisible + d'une `budget_range` (fourchette estimée par la plateforme, SC-4).

Purpose: Le calculateur B2C (Flux 1) alimente le marché des pros. Sans `category`, un lead calculateur n'apparaîtrait sur aucune fiche pro et casserait le modèle de revenus (Abonnement Exclusif).
Output: 1 migration additive, 1 util de mapping testé, l'API `/api/v1/projects` mise à jour.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.6-Calculateur-Simulateur/05.6-SPEC.md
@.planning/phases/05.6-Calculateur-Simulateur/05.6-CONTEXT.md
@.planning/phases/05.6-Calculateur-Simulateur/05.6-PATTERNS.md

<interfaces>
<!-- Contrat existant de l'API — l'exécuteur étend, ne réinvente pas. -->

server/api/v1/projects.post.ts (schéma Zod actuel, lignes 9-20) exige aujourd'hui :
  category (string min 1), description (string min 20), budget_range (string min 1),
  postal_code (/^\d{5}$/), customer_name, customer_email, customer_phone (regex FR),
  cgu_accepted (literal true), sms_opt_in (bool default false), timeline_range (enum optionnel)

L'insert (lignes 104-125) écrit ces champs + qualify_* (calculés server-side via computeQualifyScore).
Le matching pro (lignes 137-141) fait : .from('professionals').contains('categories', [data.category]).
=> data.category DOIT rester un id de métier existant (maconnerie, toiture, electricite, plomberie, peinture, isolation).

Régime d'erreurs : createError({ statusCode, statusMessage, data }), re-throw si error.statusCode existe.
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Décision — dérivation category pour le matching marché dynamique</name>
  <action>Présenter à l'utilisateur la question de dérivation de `category` et les 3 options ci-dessous. Attendre son choix (option-a par défaut) et la validation/amendement de la table de mapping pièce→métiers de la Task 2 avant de lancer l'implémentation. Aucune écriture de code dans cette task : c'est une porte de décision métier qui conditionne la logique server-side de la Task 3.</action>
  <decision>Comment un lead calculateur (qui ne choisit plus de métier, mais un type de rénovation + des pièces) obtient-il une `category` pour rester visible dans le marché dynamique des pros (Phase 4.6, `professionals.categories` ⊇ `projects.category`) ?</decision>
  <context>Le nouveau flux SPEC (6 étapes) remplace l'étape "type de travaux" (métier) par "type de rénovation" (Totale / Pièce par pièce) + "pièces concernées". L'API et le marché exigent une `category` = un id de métier unique. Sans décision, les leads calculateur n'apparaîtraient sur aucune fiche pro (criterion 4 cassé). Aucune de ces options n'ajoute d'étape UI (contrainte max 5-6 étapes respectée) : la dérivation est server-side.</context>
  <options>
    <option id="option-a">
      <name>Dérivation statique pièce/type → métier primaire (RECOMMANDÉ)</name>
      <pros>Zéro étape UI ajoutée. Le lead surface au moins auprès des pros du métier dominant maintenant. `calculator_data.trades[]` conserve tous les métiers impliqués pour un matching multi-métier ultérieur (forward-compatible).</pros>
      <cons>Un pro d'un métier secondaire ne voit pas le lead tant que le matching multi-catégorie n'existe pas (repoussé). Le mapping pièce→métiers est une heuristique métier à valider.</cons>
    </option>
    <option id="option-b">
      <name>category = 'renovation' (nouvelle valeur générique)</name>
      <pros>Simple, un seul chemin.</pros>
      <cons>Aucun pro existant n'a 'renovation' dans ses `categories` → lead invisible tant que les pros ne s'inscrivent pas à cette catégorie. Change l'inscription pro (hors scope).</cons>
    </option>
    <option id="option-c">
      <name>Réintroduire une mini-étape "métier principal" dans le flux</name>
      <pros>Category explicite, choisie par l'utilisateur.</pros>
      <cons>Contredit le SPEC (6 étapes fixes) et l'objectif "max 5 clics". Friction de conversion sur un Lead Magnet.</cons>
    </option>
  </options>
  <recommended>option-a</recommended>
  <read_first>
    - .planning/phases/05.6-Calculateur-Simulateur/05.6-SPEC.md
    - server/api/v1/projects.post.ts
    - CLAUDE.md (invariant "Marché dynamique" Phase 4.6)
  </read_first>
  <acceptance_criteria>
    - L'utilisateur a sélectionné une option (a / b / c) ou fourni son propre mapping.
    - La table de mapping pièce→métiers de la Task 2 est validée ou amendée par l'utilisateur avant implémentation.
  </acceptance_criteria>
  <resume-signal>Répondre : option-a (défaut), option-b, option-c, ou fournir un mapping pièce→métiers explicite.</resume-signal>
  <verify><human-check>L'utilisateur a répondu par une option (a/b/c) ou un mapping explicite ; le choix est consigné dans le SUMMARY.</human-check></verify>
  <done>Option de dérivation category retenue et table de mapping pièce→métiers validée ou amendée.</done>
</task>

<task type="auto">
  <name>Task 2: Migration calculator_data + util de mapping métiers (testé)</name>
  <files>supabase/migrations/20260717000000_phase56_calculator_data.sql, server/utils/calculatorMapping.ts, tests/unit/calculator-mapping.test.ts</files>
  <read_first>
    - supabase/migrations/20260624000000_phase5_siret_badges.sql
    - server/utils/qualifyScore.ts
    - tests/unit/qualify-score.test.ts
    - .planning/phases/05.6-Calculateur-Simulateur/05.6-PATTERNS.md
  </read_first>
  <action>
    Créer la migration additive `supabase/migrations/20260717000000_phase56_calculator_data.sql` suivant le pattern verbatim de `20260624000000_phase5_siret_badges.sql` : commentaire français d'une ligne expliquant le "pourquoi" (stockage structuré des choix du calculateur, Lead Magnet B2C, D-03), puis `ALTER TABLE public.projects ADD COLUMN IF NOT EXISTS calculator_data JSONB;`. Aucune contrainte destructive, pas d'ENUM.

    Créer `server/utils/calculatorMapping.ts` (fonction pure, même forme que `qualifyScore.ts` : interfaces typées, aucun I/O). Exporter :
    - `deriveTrades(renovation_type: string, pieces: string[]): string[]` — retourne l'union dédupliquée des métiers impliqués via la table statique ci-dessous. Pour `renovation_type === 'totale'`, retourner les 6 métiers.
    - `derivePrimaryCategory(renovation_type: string, pieces: string[]): string` — retourne le métier dominant (le plus fréquent parmi les pièces sélectionnées), départage par l'ordre de priorité fixe `['maconnerie','plomberie','electricite','peinture','isolation','toiture']` ; pour `'totale'` retourner `'maconnerie'`. Ne jamais retourner de valeur hors des 6 ids métiers existants (maconnerie, toiture, electricite, plomberie, peinture, isolation).

    Table de mapping pièce → métiers (défaut option-a, à appliquer sauf amendement Task 1) :
    - cuisine → plomberie, electricite, peinture
    - salle_de_bain → plomberie, electricite, peinture, isolation
    - salon → peinture, electricite, isolation
    - chambre → peinture, electricite, isolation
    - exterieur → maconnerie, toiture, isolation
    - autre → peinture

    Créer `tests/unit/calculator-mapping.test.ts` (vitest, même style que `qualify-score.test.ts`) : asserts sur (1) `deriveTrades('totale', [])` = les 6 métiers, (2) `deriveTrades('pieces', ['cuisine'])` = ['plomberie','electricite','peinture'] (union dédupliquée), (3) `derivePrimaryCategory` retourne toujours l'un des 6 ids, (4) tiebreak déterministe pour un cas à égalité (ex. ['cuisine','salon'] → un id précis attendu selon l'ordre de priorité).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/calculator-mapping.test.ts</automated>
  </verify>
  <done>La migration existe avec `ADD COLUMN IF NOT EXISTS calculator_data JSONB`. `calculatorMapping.ts` exporte `deriveTrades` + `derivePrimaryCategory`. Le test passe et couvre l'union, le cas 'totale', et un tiebreak.</done>
  <acceptance_criteria>
    - `grep -q "ADD COLUMN IF NOT EXISTS calculator_data JSONB" supabase/migrations/20260717000000_phase56_calculator_data.sql`
    - `grep -q "export function deriveTrades" server/utils/calculatorMapping.ts` et `grep -q "export function derivePrimaryCategory" server/utils/calculatorMapping.ts`
    - `derivePrimaryCategory` ne peut retourner que l'un de : maconnerie, toiture, electricite, plomberie, peinture, isolation (asserté par test).
    - `npx vitest run tests/unit/calculator-mapping.test.ts` sort en succès (0 failed).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Étendre l'API /api/v1/projects pour persister calculator_data</name>
  <files>server/api/v1/projects.post.ts</files>
  <read_first>
    - server/api/v1/projects.post.ts
    - server/utils/calculatorMapping.ts
    - server/utils/qualifyScore.ts
    - .planning/phases/05.6-Calculateur-Simulateur/05.6-PATTERNS.md
  </read_first>
  <action>
    Étendre le schéma Zod `createProjectSchema` (garder la convention inline, pas de fichier séparé) :
    - Ajouter `calculator_data: z.object({ renovation_type: z.string(), pieces: z.array(z.string()), surface_m2: z.number().positive(), gamme: z.string(), estimate_min: z.number().nonnegative(), estimate_max: z.number().nonnegative() })` — REQUIS (le calculateur est désormais l'unique producteur de leads).
    - Rendre `category` optionnel (`.optional()`) : il sera dérivé server-side depuis `calculator_data`, plus envoyé par le client.
    - Rendre `description` et `budget_range` optionnels : ils seront composés server-side si absents.
    - Conserver `postal_code`, `customer_*`, `cgu_accepted` (literal true — NE PAS supprimer, exigence légale), `sms_opt_in`, `timeline_range` inchangés.

    Après validation, avant l'insert :
    - Importer `deriveTrades`, `derivePrimaryCategory` depuis `../../utils/calculatorMapping`.
    - `const category = data.category ?? derivePrimaryCategory(data.calculator_data.renovation_type, data.calculator_data.pieces)`.
    - `const trades = deriveTrades(data.calculator_data.renovation_type, data.calculator_data.pieces)`.
    - Composer `description` si absente : `Rénovation ${renovation_type === 'totale' ? 'totale' : 'pièce par pièce'} — ${pieces.join(', ')} — ${surface_m2} m² — gamme ${gamme}.`
    - Composer `budget_range` si absent : fourchette formatée FR via un helper local `formatEuro(n)` insérant une espace fine insécable tous les 3 chiffres, résultat `${formatEuro(estimate_min)} € – ${formatEuro(estimate_max)} €`.
    - Passer ces valeurs (`category`, `description`, `budget_range`) dans `computeQualifyScore` et dans l'insert exactement comme aujourd'hui.
    - Ajouter au payload d'insert : `calculator_data: { ...data.calculator_data, trades }`. Le reste de l'insert (qualify_*, zone_id, status, matching pros ligne 137, consents, audit_logs) reste inchangé — réutiliser verbatim.

    Le matching pro (ligne 137 `.contains('categories', [data.category])`) doit utiliser la nouvelle `category` dérivée, pas `data.category`.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/calculator-mapping.test.ts && npx nuxi typecheck 2>&1 | tail -5</automated>
  </verify>
  <done>L'API accepte un body avec `calculator_data` (sans `category`/`description`/`budget_range`), dérive `category` via le util, compose description + budget_range, et insère `calculator_data` (avec `trades`) dans projects. Le matching pro utilise la category dérivée.</done>
  <acceptance_criteria>
    - `grep -q "calculator_data" server/api/v1/projects.post.ts` (schéma + insert).
    - `grep -q "derivePrimaryCategory\|deriveTrades" server/api/v1/projects.post.ts`.
    - Le schéma Zod rend `category`, `description`, `budget_range` optionnels et `calculator_data` requis (vérifiable à la lecture).
    - `cgu_accepted: z.literal(true` toujours présent (consentement conservé).
    - Le typecheck ne remonte aucune nouvelle erreur sur `projects.post.ts`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (simulateur) → POST /api/v1/projects | `calculator_data` (dont estimate_min/max) est fourni par le navigateur, non fiable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-056-01 | Tampering | body `calculator_data` (forme) | mitigate | Validation Zod stricte (types, `surface_m2` positif, estimates non négatifs) à l'entrée du handler avant tout usage. |
| T-056-02 | Tampering | `estimate_min/max`, `budget_range` affichés aux pros | accept | Estimation purement informative (D-01) ; l'artisan reste seul responsable du devis final. Valeur d'attaque nulle, pas de PII exposée par la falsification. |
| T-056-03 | Tampering | `category` de matching | mitigate | `category` dérivée server-side via `derivePrimaryCategory` (non lue du client par défaut), bornée aux 6 ids métiers valides — un client ne peut pas cibler un métier arbitraire. |
| T-056-04 | Information Disclosure | fiche lead côté pro | accept | Masquage serveur ADR-004 inchangé ; ce plan n'expose aucune coordonnée avant déblocage. |
</threat_model>

<verification>
- `npx vitest run tests/unit/calculator-mapping.test.ts` passe.
- La migration s'applique proprement en local (`supabase migration up` ou reset local) sans erreur.
- Un POST de test avec un body `calculator_data` (sans category) crée une ligne projects dont `calculator_data` est non nul et `category` ∈ 6 métiers.
</verification>

<success_criteria>
- Colonne `calculator_data JSONB` présente sur `projects` (D-03).
- L'API persiste les choix granulaires du calculateur + `trades[]` (SC-4).
- Chaque lead calculateur reçoit une `category` valide → reste visible dans le marché dynamique (invariant Phase 4.6 préservé).
- Le pro dispose d'une description lisible et d'une fourchette budgétaire estimée par la plateforme.
</success_criteria>

<output>
Créer `.planning/phases/05.6-Calculateur-Simulateur/05.6-01-SUMMARY.md` à la fin.
</output>
