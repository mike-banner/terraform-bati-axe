# Plan FIX Phase 05.14-01: Multi-Buckets R2 & Terraform Prod

## Objectives
1. Fix de la lecture des bindings `NUXT_R2_*` dans les 7 endpoints API server (`server/api/v1/`).
2. Routage vers les 3 buckets isolés (`Public`, `Vault`, `B2B`).
3. Propagation Terraform & GitHub Actions pour le déploiement Cloudflare prod client.

## Proposed Tasks
- Task 1 : Modifier `nuxt.config.ts` et `.env.example`.
- Task 2 : Mettre à jour les 7 endpoints API `presign` et `view`.
- Task 3 : Mettre à jour `terraform/modules/platform` et `terraform/environments/prod`.
- Task 4 : Alignement des workflows `.github/workflows/terraform-dev.yml` et `terraform-prod.yml`.

## Verification Protocol
- `npm run test` 100% OK.
- Presigned URLs pointent sur les vrais buckets R2 (plus de domaine `mock`).
