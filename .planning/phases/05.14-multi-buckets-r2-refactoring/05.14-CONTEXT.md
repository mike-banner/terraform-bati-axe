# FIX Phase 05.14 Context: Multi-Buckets Cloudflare R2 & Terraform Prod

## Core Scope (Phase 05.14 Exclusif)

### 1. Storage R2 & Bindings `NUXT_R2_*`
- Correction de la lecture des variables `NUXT_R2_*` sur Cloudflare Pages (fin du fallback `mock.r2.cloudflarestorage.com`).
- Séparation en 3 buckets :
  - `batiaxe-public-[env]` (`NUXT_R2_BUCKET_PUBLIC`) : Logos & Portfolio
  - `batiaxe-vault-[env]` (`NUXT_R2_BUCKET_VAULT`) : KBIS, Décennales, CNI (`kbis/`, `decennales/`, `cni/`)
  - `batiaxe-b2b-[env]` (`NUXT_R2_BUCKET_B2B`) : Fichiers CCTP & Devis B2B

### 2. Infrastructure Terraform & CI/CD
- Propagation des 3 variables dans `terraform/modules/platform` et `terraform/environments/prod`.
- Alignement de `.github/workflows/terraform-prod.yml` pour le compte Cloudflare client (`bati-axe.fr`).
