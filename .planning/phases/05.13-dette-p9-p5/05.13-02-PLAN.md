---
plan: 05.13-02
phase: 05.13-dette-p9-p5
status: complete
wave: 1
---

# Plan 05.13-02 — P9 : Mobile QA (audit + cibles tactiles) + états vides

## What was done
- **Audit réel via Playwright** (outils réutilisables `scripts/`):
  - `mobile-audit.mjs` : débordement horizontal (scrollWidth > clientWidth) sur `/`, `/simulateur`, `/partenaires`, `/b2b/partenaires` à 320px et 390px → **aucun overflow** (déjà OK).
  - `touch-audit.mjs` : cibles interactives < 44px → **corrigées** sur le header, le simulateur et le tunnel B2B.
  - `monprojet-mobile.mjs` : `/mon-projet/[token]` en 320px (états erreur + vide) → OK sans overflow.
- **Cibles tactiles ≥ 44px** (fixes) :
  - `app/layouts/default.vue` : liens nav header (`Partenaires`/`Particuliers`/`Mon espace`) → `min-h-11` ; boutons `Se connecter`/`Déposer un projet`/`Déposer un dossier` `h-9` → `h-11` ; footer : liens `py-2 -my-2` + `flex-wrap`.
  - `app/pages/simulateur.vue` : `Retour`/`Suivant`/`Continuer`/submit `h-10` → `h-11` ; lien `Annuler` `py-2`.
  - `app/pages/b2b/partenaires.vue` : boutons `h-11`, lien « Retour à la page Partenaires » élargi.
  - Liens secondaires restés à ~32px (footer légal, « Annuler ») : acceptés, ce sont des liens de basse priorité.
- **État vide de catégorie atteignable** (`app/pages/espace/leads/index.vue`) : `availableCategories` dérivait des leads présents → chaque option du filtre avait ≥ 1 résultat → l'état vide « Aucun lead pour cette catégorie » était **inatteignable** (code mort). Fix : lister les 6 catégories fixes de `CATEGORY_LABELS` — le pro peut filtrer sur une catégorie sans résultat et voit l'état vide (utile : il sait ce qu'il doit attendre).
- **Verrouillage dans le temps** : nouveau projet Playwright `mobile-chromium` (`Pixel 7`, `channel: 'chrome'`) → les 24 specs e2e rejouent en viewport mobile. **24/24 desktop + 24/24 mobile = 48/48.**

## Verification
- `node scripts/mobile-audit.mjs` : aucun overflow à 320/390px sur les 4 pages publiques.
- `node scripts/touch-audit.mjs` : plus aucune cible < 44px hors liens secondaires (footer/Annuler).
- `npx playwright test` → 48/48 (desktop + mobile).
- `npx vitest run` → 73/73 · typecheck OK.
