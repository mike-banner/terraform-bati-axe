---
plan: 05.13-01
phase: 05.13-dette-p9-p5
status: complete
wave: 1
---

# Plan 05.13-01 — Dette technique : suite e2e câblée, test badge réparé

## What was done
- **`playwright.config.ts`** — le bundle Chromium de Playwright ne se lançait pas dans le sandbox (dette connue « passe Playwright jamais câblée »). Fix : `channel: 'chrome'` sur le projet `chromium` (utilise le Chrome système installé). La suite e2e tourne désormais : **24 specs → 24 pass** (avant : 0, jamais lancée).
- **`tests/e2e/simulateur.spec.ts`** — réécrit pour le flux actuel 6 étapes : type de rénovation (auto-advance) → surface (« Suivant ») → gamme (auto-advance) → localisation (« Continuer ») → fork aides (« Non, voir mon estimation ») → coordonnées + CGU → submit. Ajouts : test parcours « Pièce par pièce » (étape 2 pièces, bouton désactivé sans sélection) et test auto-advance des étapes 1/4. Le POST `/api/v1/projects` est mocké pour le test de soumission.
- **`tests/e2e/leads.spec.ts`** — l'UI affiche maintenant « X opportunités » (et plus « X leads ») : assertions mises à jour. L'état vide de catégorie devient testable (voir 05.13-02).
- **`app/components/BadgeEntrepriseVerifiee.vue`** — `bg-green-100` → `bg-[#F8FAFC]` (palette Sketch 001, cohérent avec `BadgeDecennaleCertifiee`). Répare `tests/badges.test.ts` (5/5) sans bidouiller le test.
- **Branche `test-fix`** supprimée (3 commits de docs obsolètes, superseded par le resync 2026-08-23).

## Verification
- `npx vitest run` → 64/64 avant ajout des tests P5 (73/73 après, cf. 05.13-03).
- `npx playwright test` → 24/24 (desktop), puis 48/48 avec le projet mobile (cf. 05.13-02).
- `npx nuxi typecheck` → OK.
