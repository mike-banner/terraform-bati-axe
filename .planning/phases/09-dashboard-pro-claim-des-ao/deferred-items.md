# Deferred Items — Phase 09

- **`npm run build` cassé indépendamment du plan** (Plan 03) : `TypeError: trustedFunctions.difference is not a function` dès `nuxt build`, reproduit avec ou sans les fichiers de ce plan (testé en déplaçant temporairement `server/api/v1/tenders/`). Cause probable : `Set.prototype.difference` (ES2024) requiert Node >= 22, environnement local sur Node 20.20.0. Hors périmètre de ce plan — à traiter dans une passe dédiée à l'environnement de build.
- **Email partenaire au claim non vérifié en conditions réelles** (Plan 04, vérification checkpoint du 2026-09-07) : le claim déclenche bien l'appel `sendEmail`/`notifyB2bPros`, mais en local `EMAIL_LIVE=true` pointe vers le vrai Resend avec le domaine `bati-axe.com` non vérifié → échec 403 (`The bati-axe.com domain is not verified`). Décision utilisateur : ne pas changer la config locale maintenant. **À revérifier en staging/prod une fois le domaine Resend vérifié** (DNS DKIM/SPF, cf. blocage déjà noté dans STATE.md pour la Phase 06.3) — le rendu exact de l'email (sujet/statut identifiables, TEND-16) n'a pas pu être inspecté visuellement pendant ce test.
