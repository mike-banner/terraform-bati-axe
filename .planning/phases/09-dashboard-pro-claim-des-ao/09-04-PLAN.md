---
phase: 09-dashboard-pro-claim-des-ao
plan: 04
type: execute
wave: 3
depends_on: ["09-02", "09-03"]
files_modified:
  - app/pages/espace/leads/index.vue
  - app/components/espace/TenderCard.vue
  - app/components/espace/TenderClaimModal.vue
  - app/components/espace/TenderReportModal.vue
autonomous: false
requirements: [TEND-07, TEND-08, TEND-09, TEND-13, TEND-15]

must_haves:
  truths:
    - "L'artisan voit deux onglets dans /espace/leads : « Chantiers particuliers » et « Appels d'offres »"
    - "Une phrase explique que l'abonnement zone couvre désormais les deux flux"
    - "Les cartes AO portent un badge « AO Partenaire » qui les distingue des cartes leads"
    - "Cliquer « Je suis intéressé » ouvre une modale rappelant l'exclusivité avant de confirmer"
    - "Après confirmation, les coordonnées du partenaire s'affichent sur la carte"
    - "Un lien « Signaler cet appel d'offres » ouvre une modale de signalement"
  artifacts:
    - path: "app/components/espace/TenderCard.vue"
      provides: "Carte AO masquée/révélée, badge AO Partenaire"
      min_lines: 80
    - path: "app/components/espace/TenderClaimModal.vue"
      provides: "Confirmation de claim avec avertissement d'exclusivité"
      min_lines: 40
    - path: "app/components/espace/TenderReportModal.vue"
      provides: "Formulaire de signalement (raison + détails)"
      min_lines: 40
    - path: "app/pages/espace/leads/index.vue"
      provides: "Système d'onglets + phrase explicative + fetch /api/v1/tenders"
      contains: "Appels d'offres"
  key_links:
    - from: "app/pages/espace/leads/index.vue"
      to: "/api/v1/tenders"
      via: "useAsyncData + requestFetch"
      pattern: "/api/v1/tenders"
    - from: "app/components/espace/TenderClaimModal.vue"
      to: "/api/v1/tenders/{id}/claim"
      via: "$fetch POST à la confirmation"
      pattern: "tenders/.*claim"
    - from: "app/components/espace/TenderReportModal.vue"
      to: "/api/v1/tenders/{id}/report"
      via: "$fetch POST"
      pattern: "tenders/.*report"
---

<objective>
Exposer les appels d'offres partenaires dans l'espace artisan : onglets dans la page
`/espace/leads` existante (D-01), cartes AO au même style visuel que les cartes leads mais
badgées « AO Partenaire » (D-02/D-03), modale de confirmation de claim rappelant l'exclusivité
(D-04), révélation des coordonnées après claim (D-05), et modale de signalement (D-09).

Purpose: Dernière brique de la phase — l'artisan peut enfin consulter, réclamer et signaler
les AO sans confusion sur ce que couvre son abonnement (TEND-15).
Output: 3 composants + la page leads mise à jour.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-dashboard-pro-claim-des-ao/09-CONTEXT.md
@.planning/phases/09-dashboard-pro-claim-des-ao/09-02-SUMMARY.md
@.planning/phases/09-dashboard-pro-claim-des-ao/09-03-SUMMARY.md
@CLAUDE.md

<interfaces>
API livrée par 09-02 :
```
GET /api/v1/tenders
→ { tenders: TenderView[], zones: { id, name }[], canClaim: boolean }

// TenderView non claim (maskTender(lot, false)) :
{ lot_id, request_ref, category, lot_status, zone_name, budget_range,
  decision_status /* 'confirme' | 'en_attente' */, created_at, project_location,
  status: 'locked', description /* tronquée 200 car. */,
  contact_name: '*** *** ***', contact_company: '*** *** ***',
  contact_phone: '*** *** ***', contact_email: 'contact@***.fr' }
// PAS de postal_code

// TenderView claim (maskTender(lot, true)) :
{ ...mêmes champs communs, status: 'claimed', claimed_at, description /* complète */,
  postal_code, contact_name, contact_company, contact_phone, contact_email }

POST /api/v1/tenders/{lot_id}/claim
→ 200 { status: 'SUCCESS' | 'ALREADY_CLAIMED', lotClosed: boolean, tender: TenderView }
→ 403 'Votre abonnement ne couvre pas la zone de cet appel d'offres.'
→ 403 profil non vérifié / décennale manquante
→ 409 'Cet appel d'offres est clos.'
```

API livrée par 09-03 :
```
POST /api/v1/tenders/{lot_id}/report
body: { reason: 'coordonnees_suspectes'|'hors_perimetre'|'doublon'|'contenu_abusif'|'autre', details?: string (max 500) }
→ 200 { status: 'SUCCESS' }
```

Conventions existantes de `app/pages/espace/leads/index.vue` (à réutiliser, ne pas réinventer) :
- `useRequireAuth()` en tête de `<script setup>` (JAMAIS `watchEffect` sur `useSupabaseUser()`)
- `const requestFetch = useRequestFetch()` puis `useAsyncData('clé', () => requestFetch(...))`
- `CATEGORY_LABELS` (6 métiers) déjà défini dans la page
- Style de carte : `class="bento-card bg-white rounded-sm border border-slate-200 shadow-sm overflow-hidden"`
- Badge « Flouté » existant : `inline-flex items-center gap-1.5 text-xs font-semibold px-3 py-1.5 border rounded-full border-amber-300 text-amber-700 bg-amber-50`
- Composant `<LeadAge :created-at="..." />` pour l'ancienneté
- Cibles tactiles >= 44px sur mobile (`h-11` / `min-h-11`) — acquis de la QA mobile 05.13-02
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TenderCard.vue — carte AO masquée/révélée</name>
  <files>app/components/espace/TenderCard.vue</files>
  <read_first>
    - app/pages/espace/leads/index.vue (variantes de carte « Locked » et « Unlocked » : classes exactes, structure, badges, boutons de copie)
    - .planning/phases/09-dashboard-pro-claim-des-ao/09-02-SUMMARY.md (forme exacte de TenderView)
  </read_first>
  <action>
Créer `app/components/espace/TenderCard.vue`.

Props : `tender: any` (TenderView), `canClaim: boolean`, `claiming: boolean`.
Emits : `claim` (lot_id), `report` (lot_id).

Structure — reprendre à l'identique la coque de carte des leads
(`bento-card bg-white rounded-sm border border-slate-200 shadow-sm overflow-hidden`) et
ses deux variantes, avec ces spécificités AO (D-02/D-03) :

En-tête (toujours visible) :
- Ligne 1 : libellé de catégorie via un mapping `CATEGORY_LABELS` local identique à celui de la page leads
  + badge de distinction **AO Partenaire** :
  `<span class="inline-flex items-center gap-1 text-[11px] font-bold px-2.5 py-1 rounded-full border border-copper/40 bg-copper/10 text-copper uppercase tracking-wide">AO Partenaire</span>`
  (si l'univers `copper` n'est pas disponible en classe Tailwind, utiliser
  `border-orange-300 bg-orange-50 text-orange-700` — même intention chromatique, distincte de l'ambre « Flouté »).
- Ligne 2 : `<LeadAge :created-at="tender.created_at" />`, `zone_name`, `budget_range`, `réf. {{ tender.request_ref }}`.
- Badge de décision (D-04, information décisive pour l'artisan) :
  - `decision_status === 'confirme'` → `<span class="... border-emerald-300 bg-emerald-50 text-emerald-700">Chantier confirmé · exclusif</span>`
  - sinon → `<span class="... border-slate-300 bg-slate-50 text-slate-600">En attente de décision · jusqu'à 3 artisans</span>`

Variante A — `tender.status === 'locked'` :
- Description tronquée affichée en `text-sm text-slate-600`.
- Bloc coordonnées floutées : afficher les 4 valeurs masquées telles que reçues du serveur
  (`*** *** ***`, `contact@***.fr`) avec `class="blur-[3px] select-none"` — ne JAMAIS reconstruire
  ni deviner ces valeurs côté client (ADR-004).
- Bouton principal `h-11` pleine largeur mobile :
  `Je suis intéressé` → `emit('claim', tender.lot_id)`.
  `:disabled="!canClaim || claiming || tender.lot_status !== 'open'"`.
  Si `!canClaim`, afficher sous le bouton : « Profil vérifié + attestation décennale valide requis pour vous positionner. »
  Si `tender.lot_status !== 'open'`, afficher « Cet appel d'offres est clos. » et masquer le bouton.

Variante B — `tender.status === 'claimed'` :
- Badge `✓ Vous êtes positionné` (vert), `claimed_at` formaté en français.
- Description complète + `postal_code`.
- Les 4 coordonnées en clair, chacune avec un bouton de copie réutilisant le même pattern
  `navigator.clipboard.writeText` que la page leads, `tel:` sur le téléphone, `mailto:` sur l'email.

Pied de carte (les deux variantes) : lien discret
`<button class="text-xs text-muted-foreground underline underline-offset-2 hover:text-destructive" @click="$emit('report', tender.lot_id)">Signaler cet appel d'offres</button>`.

Aucune logique d'appel réseau dans ce composant : il émet, la page orchestre.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "AO Partenaire" app/components/espace/TenderCard.vue` réussit
    - `grep -q "blur-\[3px\]" app/components/espace/TenderCard.vue` réussit
    - `grep -q "Je suis intéressé" app/components/espace/TenderCard.vue` réussit
    - `grep -q "Signaler cet appel d'offres" app/components/espace/TenderCard.vue` réussit
    - `grep -q "exclusif" app/components/espace/TenderCard.vue` réussit (badge decision_status)
    - `grep "\\$fetch\|useFetch" app/components/espace/TenderCard.vue` ne retourne RIEN (composant présentationnel)
    - `grep -q "h-11" app/components/espace/TenderCard.vue` réussit (cible tactile mobile)
    - `npm run build` réussit
  </acceptance_criteria>
  <done>La carte AO affiche le badge « AO Partenaire », le badge de décision, les coordonnées floutées avant claim et en clair après, plus le lien de signalement.</done>
</task>

<task type="auto">
  <name>Task 2: Modales de claim et de signalement</name>
  <files>app/components/espace/TenderClaimModal.vue, app/components/espace/TenderReportModal.vue</files>
  <read_first>
    - app/components/espace/TenderCard.vue (créé en Task 1 — cohérence de style)
    - app/pages/espace/leads/index.vue (palette et classes de boutons utilisées dans l'espace pro)
    - CLAUDE.md § « Tout nouvel input » (maxlength HTML miroir de la contrainte Zod serveur)
  </read_first>
  <action>
**TenderClaimModal.vue** (D-04) — props : `tender: any`, `submitting: boolean` ; emits : `confirm`, `cancel`.
Overlay `fixed inset-0 z-50 bg-black/50 flex items-center justify-center p-4` + panneau
`bg-white rounded-lg max-w-md w-full p-6 space-y-4`, fermeture sur Échap et sur clic overlay.
Contenu :
- Titre : « Vous positionner sur cet appel d'offres ? »
- Récap : catégorie, `zone_name`, `réf. {{ tender.request_ref }}`.
- Encart d'avertissement conditionnel, texte **exact** :
  - si `tender.decision_status === 'confirme'` (bloc `border border-amber-300 bg-amber-50 p-3 rounded-sm`) :
    « Ce chantier est **confirmé** : en vous positionnant, vous fermez cet appel d'offres pour tous les autres artisans. Vous vous engagez à recontacter le partenaire sous 48h. »
  - sinon : « Ce chantier est **en attente de décision** : jusqu'à 3 artisans peuvent se positionner. Le partenaire reçoit vos coordonnées immédiatement. »
- Rappel : « Vos coordonnées professionnelles seront transmises au partenaire, et les siennes vous seront révélées. » (RGPD/ADR-007 — information préalable au traitement).
- Note : « Ce positionnement ne peut pas être annulé depuis votre espace. » (D-06 assumé explicitement).
- Boutons `h-11` : « Annuler » (secondaire) → `emit('cancel')` ; « Confirmer mon intérêt » (primaire,
  `:disabled="submitting"`, libellé « Envoi… » pendant) → `emit('confirm')`.

**TenderReportModal.vue** (D-09) — props : `tender: any`, `submitting: boolean` ; emits : `submit` (payload `{ reason, details }`), `cancel`.
Même coque d'overlay. Contenu :
- Titre : « Signaler cet appel d'offres »
- `<select v-model="reason">` avec les 5 valeurs **exactes** de l'enum serveur :
  `coordonnees_suspectes` → « Coordonnées ou identité suspectes »
  `hors_perimetre` → « Hors de mon périmètre / de ma zone »
  `doublon` → « Doublon d'un autre appel d'offres »
  `contenu_abusif` → « Contenu abusif ou trompeur »
  `autre` → « Autre »
- `<textarea v-model="details" maxlength="500" rows="4" placeholder="Précisez (facultatif)">` +
  compteur `{{ details.length }}/500` (miroir du `.max(500)` Zod, cf. CLAUDE.md).
- Mention : « Votre signalement est transmis à l'équipe BÂTI-AXE. L'appel d'offres reste visible tant qu'aucune décision n'est prise. » (D-10).
- Boutons `h-11` : « Annuler » → `emit('cancel')` ; « Envoyer le signalement » → `emit('submit', { reason, details })`,
  `:disabled="!reason || submitting"`.

Aucun appel réseau dans les modales : elles émettent, la page orchestre.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "confirmé" app/components/espace/TenderClaimModal.vue` et `grep -q "fermez cet appel d'offres pour tous les autres artisans" app/components/espace/TenderClaimModal.vue` réussissent
    - `grep -q "ne peut pas être annulé" app/components/espace/TenderClaimModal.vue` réussit (D-06)
    - `grep -c "coordonnees_suspectes\|hors_perimetre\|doublon\|contenu_abusif\|autre" app/components/espace/TenderReportModal.vue` >= 5
    - `grep -q 'maxlength="500"' app/components/espace/TenderReportModal.vue` réussit
    - `grep -q "reste visible" app/components/espace/TenderReportModal.vue` réussit (D-10)
    - `grep "\\$fetch\|useFetch" app/components/espace/*Modal.vue` ne retourne RIEN
    - `npm run build` réussit
  </acceptance_criteria>
  <done>Les deux modales existent, portent les textes d'avertissement exigés par D-04/D-06/D-10, et n'effectuent aucun appel réseau.</done>
</task>

<task type="auto">
  <name>Task 3: Onglets « Chantiers particuliers » / « Appels d'offres » dans /espace/leads</name>
  <files>app/pages/espace/leads/index.vue</files>
  <read_first>
    - app/pages/espace/leads/index.vue (fichier complet, 550 lignes — structure du script setup et du template avant modification)
    - app/components/espace/TenderCard.vue, app/components/espace/TenderClaimModal.vue, app/components/espace/TenderReportModal.vue (créés aux tasks 1 et 2)
  </read_first>
  <action>
Modifier `app/pages/espace/leads/index.vue`. **Ne pas réécrire la page** : ajouts ciblés.

Script setup :
1. `const activeTab = ref<'leads' | 'tenders'>(route.query.tab === 'ao' ? 'tenders' : 'leads')`.
   Si `route.query.src === 'email'`, forcer `'tenders'` (les emails de diffusion Phase 8 pointent
   sur `/espace/leads?src=email`).
2. Chargement des AO, même pattern que les leads (cookie SSR obligatoire) :
   ```ts
   const { data: tendersData, pending: tendersPending, error: tendersError, refresh: refreshTenders } =
     await useAsyncData('pro-tenders', () =>
       requestFetch<{ tenders: any[]; zones: any[]; canClaim: boolean }>('/api/v1/tenders'))
   const tenders = computed(() => tendersData.value?.tenders || [])
   const canClaimTenders = computed(() => tendersData.value?.canClaim ?? false)
   ```
3. État des modales : `claimTarget = ref<any|null>(null)`, `reportTarget = ref<any|null>(null)`,
   `claimSubmitting = ref(false)`, `reportSubmitting = ref(false)`, `tenderMessage = ref<string|null>(null)`.
4. Handlers :
   ```ts
   async function confirmClaim() {
     if (!claimTarget.value) return
     claimSubmitting.value = true
     try {
       await $fetch(`/api/v1/tenders/${claimTarget.value.lot_id}/claim`, { method: 'POST' })
       claimTarget.value = null
       await refreshTenders()
       tenderMessage.value = 'Vous êtes positionné. Les coordonnées du partenaire sont maintenant visibles.'
     } catch (err: any) {
       tenderMessage.value = err?.data?.statusMessage || "Impossible de vous positionner sur cet appel d'offres."
     } finally { claimSubmitting.value = false }
   }
   async function submitReport(payload: { reason: string; details: string }) { /* même forme, POST .../report, message 'Signalement transmis à l\'équipe BÂTI-AXE.' */ }
   ```

Template :
5. Barre d'onglets insérée **au-dessus** de tous les bandeaux existants (blocker documents, paywall, etc.)
   et juste sous le header de page — deux boutons `h-11 px-4 text-sm font-semibold rounded-full`,
   actif = `bg-foreground text-background`, inactif = `border border-border text-muted-foreground` :
   « Chantiers particuliers » (compteur `{{ leads.length }}`) et « Appels d'offres » (compteur `{{ tenders.length }}`).
6. **TEND-15** — phrase explicative affichée sous les onglets, quel que soit l'onglet actif,
   texte exact :
   « Votre abonnement de zone couvre désormais deux flux : les chantiers déposés par des particuliers et les appels d'offres de nos partenaires professionnels (syndics, architectes, bureaux d'études). »
   dans un encart `p-3 border border-border rounded-sm bg-muted/40 text-xs text-muted-foreground`.
7. Envelopper **tout le contenu existant** de la page (bandeaux, filtres, grille de leads, pagination)
   dans `<template v-if="activeTab === 'leads'">` — sans en modifier le contenu.
8. Nouveau bloc `<template v-else>` pour les AO :
   - squelette `v-if="tendersPending"` (3 cartes `animate-pulse`, même classe que les leads)
   - erreur `v-else-if="tendersError"` avec bouton « Réessayer » → `refreshTenders()`
   - état vide `v-else-if="!tenders.length"` : « Aucun appel d'offres partenaire pour l'instant. Les AO de vos corps de métier, dans vos zones abonnées, apparaîtront ici. »
   - sinon grille `grid grid-cols-1 md:grid-cols-2 xl:grid-cols-3 gap-4` de
     `<TenderCard v-for="t in tenders" :key="t.lot_id" :tender="t" :can-claim="canClaimTenders" :claiming="claimSubmitting" @claim="claimTarget = tenders.find(x => x.lot_id === $event)" @report="reportTarget = tenders.find(x => x.lot_id === $event)" />`
   - bandeau `v-if="tenderMessage"` (dismissible) au-dessus de la grille.
9. Modales en fin de template :
   `<TenderClaimModal v-if="claimTarget" :tender="claimTarget" :submitting="claimSubmitting" @confirm="confirmClaim" @cancel="claimTarget = null" />`
   `<TenderReportModal v-if="reportTarget" :tender="reportTarget" :submitting="reportSubmitting" @submit="submitReport" @cancel="reportTarget = null" />`

Ne pas toucher à `useRequireAuth()`, ni au bandeau bloquant décennale, ni à la logique de pagination des leads.
  </action>
  <verify>
    <automated>npm run build && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "Appels d'offres" app/pages/espace/leads/index.vue` réussit
    - `grep -q "couvre désormais deux flux" app/pages/espace/leads/index.vue` réussit (TEND-15)
    - `grep -q "/api/v1/tenders" app/pages/espace/leads/index.vue` réussit
    - `grep -q "TenderCard" app/pages/espace/leads/index.vue` et `grep -q "TenderClaimModal" app/pages/espace/leads/index.vue` réussissent
    - `grep -q "useRequireAuth()" app/pages/espace/leads/index.vue` réussit encore (non régressé)
    - `grep "watchEffect" app/pages/espace/leads/index.vue` ne retourne RIEN (anti-pattern d'hydratation interdit)
    - `npm run build` réussit et `npx vitest run` reste vert
  </acceptance_criteria>
  <done>La page expose deux onglets fonctionnels, la phrase explicative, la grille d'AO et les deux modales câblées sur les endpoints.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Vérification visuelle et fonctionnelle du flux AO</name>
  <what-built>
    Onglet « Appels d'offres » dans `/espace/leads` avec cartes badgées « AO Partenaire »,
    coordonnées floutées avant claim, modale de confirmation d'exclusivité, révélation des
    coordonnées après claim, email au partenaire, modale de signalement, badge « ⚠ Signalé »
    côté admin.
  </what-built>
  <how-to-verify>
1. `npm run dev` puis se connecter avec un compte pro vérifié, décennale valide, abonné sur une zone
   qui porte au moins un lot AO ouvert (créer au besoin une demande B2B syndic via `/b2b/partenaires`
   puis la qualifier + diffuser depuis `/admin` onglet « Dossiers B2B »).
2. Ouvrir `http://localhost:3000/espace/leads` :
   - les deux onglets sont présents et la phrase « votre abonnement de zone couvre désormais deux flux » est visible ;
   - l'onglet « Appels d'offres » liste le lot, badge « AO Partenaire » visible, coordonnées floutées.
3. Cliquer « Je suis intéressé » : la modale rappelle bien l'exclusivité si l'AO est `confirme`.
   Confirmer → les coordonnées (nom, entreprise, téléphone, email) s'affichent en clair.
4. Vérifier la console serveur : un email « Un artisan est intéressé… » est logué (`EMAIL_LIVE` non défini en dev).
5. Vérifier en base que le lot `confirme` est passé `status = 'closed'`, `closed_reason = 'cap'`.
6. Sur un second AO, cliquer « Signaler cet appel d'offres », choisir une raison, envoyer.
   Aller dans `/admin` → onglet « Dossiers B2B » : le dossier porte le badge « ⚠ Signalé » et,
   une fois déplié, la raison — et il reste diffusé (aucun changement de statut).
7. Vérifier en 390px de large (DevTools) : pas de débordement horizontal, boutons >= 44px.
  </how-to-verify>
  <action>Pause pour vérification humaine : dérouler les 7 étapes de `<how-to-verify>` ci-dessus et rapporter le résultat. Aucune modification de code par l'exécutant tant que le retour n'est pas donné.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>L'utilisateur a répondu « approuvé », ou les écarts constatés sont consignés pour correction.</done>
  <resume-signal>Répondre « approuvé » ou décrire les écarts constatés</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| serveur → navigateur artisan | Le payload rendu ne doit contenir aucune coordonnée avant claim (le flou CSS ne protège rien à lui seul) |
| navigateur → API claim/report | Toute garde côté UI est cosmétique ; la décision fait autorité côté serveur |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-15 | Information Disclosure | `TenderCard.vue` | mitigate | Le composant affiche uniquement les valeurs reçues (`*** *** ***` / `contact@***.fr`) ; `blur-[3px]` est décoratif, la vraie protection est `maskTender()` côté serveur (ADR-004) |
| T-09-16 | Elevation of Privilege | bouton « Je suis intéressé » désactivé | mitigate | `canClaim` n'est qu'un confort UI — `claim.post.ts` refait les 3 contrôles (vérifié, décennale, zone) côté serveur (leçon 05.15) |
| T-09-17 | Tampering | double-clic sur « Confirmer » | mitigate | `claimSubmitting` désactive le bouton ; côté serveur la contrainte `UNIQUE (lot_id, pro_id)` rend l'opération idempotente |
| T-09-18 | Information Disclosure | onglet AO d'un pro sans zone active | mitigate | `GET /api/v1/tenders` retourne une liste vide si aucune `pro_zones` active — le filtrage n'est jamais fait côté client |
</threat_model>

<verification>
- `npm run build` vert
- `npx vitest run` vert (aucune régression des 24+ specs existantes)
- `npx playwright test` : la suite existante reste verte (la page `/espace/leads` est modifiée, les specs mobiles/desktop la traversent)
- Checkpoint humain (Task 4) approuvé
</verification>

<success_criteria>
- Onglet « Appels d'offres » présent dans `/espace/leads`, distinct via le badge « AO Partenaire » (TEND-07)
- Phrase expliquant les deux flux couverts par l'abonnement zone, visible sur la page (TEND-15)
- Modale de confirmation avant claim, avec avertissement d'exclusivité si `decision_status === 'confirme'` (D-04)
- Coordonnées du partenaire révélées sur la carte après claim (TEND-09)
- Signalement possible depuis la carte, visible côté admin (TEND-13)
- Aucune régression sur l'onglet « Chantiers particuliers »
</success_criteria>

<output>
After completion, create `.planning/phases/09-dashboard-pro-claim-des-ao/09-04-SUMMARY.md`
</output>
