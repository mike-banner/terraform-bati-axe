---
phase: 09-dashboard-pro-claim-des-ao
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260907000000_phase9_tender_claims.sql
  - app/types/database.types.ts
  - server/utils/maskTender.ts
  - tests/unit/mask-tender.test.ts
autonomous: true
requirements: [TEND-03, TEND-09, TEND-12, TEND-13]

must_haves:
  truths:
    - "La table b2b_tender_claims existe en base distante et trace (lot_id, pro_id, claimed_at)"
    - "Un lot clos porte la raison de sa clôture (cap atteint / expiration)"
    - "Un AO signalé porte un compteur et les raisons du signalement sur b2b_requests"
    - "Une fonction pure masque les coordonnées du partenaire tant que l'artisan n'a pas claim"
  artifacts:
    - path: "supabase/migrations/20260907000000_phase9_tender_claims.sql"
      provides: "Schéma claims + colonnes clôture + colonnes signalement"
      contains: "CREATE TABLE IF NOT EXISTS b2b_tender_claims"
    - path: "server/utils/maskTender.ts"
      provides: "Masquage/révélation des coordonnées partenaire"
      exports: ["maskTender"]
    - path: "tests/unit/mask-tender.test.ts"
      provides: "Tests du masquage"
      min_lines: 30
    - path: "app/types/database.types.ts"
      provides: "Types Supabase régénérés incluant b2b_tender_claims"
      contains: "b2b_tender_claims"
  key_links:
    - from: "supabase/migrations/20260907000000_phase9_tender_claims.sql"
      to: "base distante xpwoczcbyamnjknloxgz"
      via: "npx supabase db push --yes"
      pattern: "b2b_tender_claims"
---

<objective>
Poser la fondation base de données + masquage de la Phase 9 : table `b2b_tender_claims`
(miroir de `leads`), colonnes de clôture sur `b2b_tender_lots`, colonnes de signalement
sur `b2b_requests`, et la fonction pure `maskTender()` (miroir de `maskLead()`).

Purpose: Tous les autres plans de la phase (API claim, cron de clôture, signalement, UI)
lisent ce schéma. Rien ne peut être exécuté avant que la migration soit poussée en distant.
Output: 1 migration appliquée, types régénérés, 1 util pur testé.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-dashboard-pro-claim-des-ao/09-CONTEXT.md
@CLAUDE.md

<interfaces>
Schéma existant à respecter (supabase/migrations/20260904000000_phase7_tender_lots.sql) :

```sql
CREATE TABLE b2b_tender_lots (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    request_id UUID NOT NULL REFERENCES b2b_requests(id) ON DELETE CASCADE,
    category TEXT NOT NULL,              -- maconnerie|toiture|electricite|plomberie|peinture|isolation
    zone_id UUID REFERENCES zones(id),
    status TEXT NOT NULL DEFAULT 'open', -- CHECK IN ('open','claimed','closed')
    created_at TIMESTAMPTZ DEFAULT clock_timestamp(),
    UNIQUE (request_id, category)
);
```

`b2b_requests` porte déjà : `decision_status TEXT CHECK IN ('confirme','en_attente')`,
`project_postal_code`, `contact_name`, `contact_company`, `contact_phone`, `contact_email`,
`project_location`, `budget_range`, `description`.

Signature de référence à répliquer (server/utils/maskLead.ts) :
```ts
export function maskLead(lead: any, isPremium: boolean, now: Date, isFreeGranted?: boolean): Record<string, any>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration schéma claims + clôture + signalement</name>
  <files>supabase/migrations/20260907000000_phase9_tender_claims.sql</files>
  <read_first>
    - supabase/migrations/20260905000000_phase8_b2b_tender_notifications.sql (pattern exact à répliquer : table de junction + index + RLS service_role)
    - supabase/migrations/20260904000000_phase7_tender_lots.sql (schéma b2b_tender_lots à étendre)
  </read_first>
  <action>
Créer `supabase/migrations/20260907000000_phase9_tender_claims.sql` avec exactement ce contenu
(commentaires en français, en-tête calqué sur la migration Phase 8) :

```sql
-- =====================================================
-- Phase 9 — Claim des AO par l'artisan
-- TEND-03 (cap exclusif), TEND-09 (révélation), TEND-12 (clôture auto), TEND-13 (signalement)
-- =====================================================

-- D-05 / TEND-09 : une ligne n'existe qu'au claim (ADR-004, miroir de `leads`).
-- Pas de colonne de statut : le retrait de claim est hors périmètre (D-06).
CREATE TABLE IF NOT EXISTS b2b_tender_claims (
    id         UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    lot_id     UUID NOT NULL REFERENCES b2b_tender_lots(id) ON DELETE CASCADE,
    pro_id     UUID NOT NULL REFERENCES professionals(id) ON DELETE CASCADE,
    claimed_at TIMESTAMPTZ NOT NULL DEFAULT clock_timestamp(),
    UNIQUE (lot_id, pro_id)
);

CREATE INDEX IF NOT EXISTS idx_b2b_tender_claims_lot ON b2b_tender_claims (lot_id);
CREATE INDEX IF NOT EXISTS idx_b2b_tender_claims_pro ON b2b_tender_claims (pro_id);

ALTER TABLE b2b_tender_claims ENABLE ROW LEVEL SECURITY;

DROP POLICY IF EXISTS "admin_all_b2b_tender_claims" ON b2b_tender_claims;
CREATE POLICY "admin_all_b2b_tender_claims" ON b2b_tender_claims
    FOR ALL
    USING (auth.jwt()->>'role' = 'admin');

-- TEND-12 / D-07 : traçabilité de la clôture automatique (cap atteint ou 14 jours).
ALTER TABLE b2b_tender_lots ADD COLUMN IF NOT EXISTS closed_at TIMESTAMPTZ;
ALTER TABLE b2b_tender_lots ADD COLUMN IF NOT EXISTS closed_reason TEXT;

ALTER TABLE b2b_tender_lots DROP CONSTRAINT IF EXISTS b2b_tender_lots_closed_reason_check;
ALTER TABLE b2b_tender_lots
    ADD CONSTRAINT b2b_tender_lots_closed_reason_check
    CHECK (closed_reason IS NULL OR closed_reason IN ('cap', 'expired', 'manual'));

-- TEND-13 / D-09 / D-10 : signalement purement informatif porté par le dossier
-- lui-même (pas de table dédiée, pas d'écran de modération — le badge se lit
-- directement dans AdminB2bTab.vue). Aucun masquage/blocage automatique.
ALTER TABLE b2b_requests ADD COLUMN IF NOT EXISTS reported_count INTEGER NOT NULL DEFAULT 0;
ALTER TABLE b2b_requests ADD COLUMN IF NOT EXISTS reported_at TIMESTAMPTZ;
ALTER TABLE b2b_requests ADD COLUMN IF NOT EXISTS report_reasons TEXT[] NOT NULL DEFAULT '{}';
```

Ne pas rééditer une migration déjà appliquée. Si le nom `20260907000000_phase9_tender_claims.sql`
existe déjà, incrémenter le suffixe (`20260907000001_...`).
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260907000000_phase9_tender_claims.sql && grep -q "b2b_tender_claims" supabase/migrations/20260907000000_phase9_tender_claims.sql</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "CREATE TABLE IF NOT EXISTS b2b_tender_claims" supabase/migrations/20260907000000_phase9_tender_claims.sql` retourne 1
    - `grep -q "UNIQUE (lot_id, pro_id)"` sur le fichier réussit
    - `grep -q "ENABLE ROW LEVEL SECURITY"` sur le fichier réussit
    - `grep -q "closed_reason IN ('cap', 'expired', 'manual')"` sur le fichier réussit
    - `grep -q "reported_count INTEGER NOT NULL DEFAULT 0"` sur le fichier réussit
    - Le fichier ne contient AUCUNE colonne `status` sur `b2b_tender_claims` (D-06)
  </acceptance_criteria>
  <done>La migration existe, contient les 3 blocs (claims, clôture, signalement), avec RLS service_role uniquement.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Push migration distante + régénération des types</name>
  <files>app/types/database.types.ts</files>
  <read_first>
    - supabase/migrations/20260907000000_phase9_tender_claims.sql (fichier créé en Task 1)
    - .planning/STATE.md § « Known Patterns » (procédure migration + types)
  </read_first>
  <action>
Appliquer la migration sur la base distante puis régénérer le fichier de types committé.
Deux commandes, dans cet ordre exact :

```bash
npx supabase db push --yes
npx supabase gen types typescript --project-id xpwoczcbyamnjknloxgz --schema public > app/types/database.types.ts
```

Le projet est déjà lié (`xpwoczcbyamnjknloxgz`, session CLI authentifiée — cf. STATE.md).
Si `db push` échoue sur une migration antérieure déjà appliquée, ne PAS rééditer les
migrations existantes : reporter l'erreur exacte et s'arrêter.
Si `gen types` produit un fichier vide ou une erreur, ne pas committer le fichier tronqué —
restaurer via `git checkout app/types/database.types.ts` et reporter.
  </action>
  <verify>
    <automated>grep -q "b2b_tender_claims" app/types/database.types.ts && grep -q "reported_count" app/types/database.types.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx supabase db push --yes` sort en code 0
    - `grep -c "b2b_tender_claims" app/types/database.types.ts` >= 1
    - `grep -q "closed_reason" app/types/database.types.ts` réussit
    - `grep -q "report_reasons" app/types/database.types.ts` réussit
    - `wc -l app/types/database.types.ts` > 100 (fichier non tronqué)
  </acceptance_criteria>
  <done>La table `b2b_tender_claims` et les nouvelles colonnes existent en distant et apparaissent dans `app/types/database.types.ts`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: maskTender() — masquage des coordonnées partenaire</name>
  <files>server/utils/maskTender.ts, tests/unit/mask-tender.test.ts</files>
  <read_first>
    - server/utils/maskLead.ts (implémentation de référence, forme du retour à répliquer)
    - tests/unit/leads-masking.test.ts (style de test à répliquer)
    - docs/decisions/ADR-004-BLUR-LOCK-MECHANISM.md (le masquage reste serveur, jamais client)
  </read_first>
  <behavior>
    - `maskTender(lot, false)` retourne `status: 'locked'` et AUCUN des champs `contact_name`, `contact_phone`, `contact_email`, `contact_company` en clair — ils valent respectivement `'*** *** ***'`, `'*** *** ***'`, `'contact@***.fr'`, `'*** *** ***'`
    - `maskTender(lot, false)` expose bien `lot_id`, `category`, `zone_name`, `budget_range`, `decision_status`, `created_at`, `project_location` (ville/zone, pas d'adresse) et une `description` tronquée à 200 caractères suffixée par `'…'`
    - `maskTender(lot, true)` retourne `status: 'claimed'` et les 4 coordonnées en clair + `description` complète + `claimed_at`
    - `maskTender` ne retourne jamais `postal_code` quand `claimed=false` (le code postal est géolocalisant, ADR-004)
    - Une description de moins de 200 caractères n'est pas suffixée par `'…'`
  </behavior>
  <action>
Créer `server/utils/maskTender.ts` — fonction pure, aucune dépendance Supabase/Nitro
(elle doit être importable sous vitest, cf. la note de `server/utils/b2bTender.ts`) :

```ts
export interface TenderLotRow {
  id: string
  category: string
  status: string
  created_at: string | null
  zone_name?: string | null
  claimed_at?: string | null
  b2b_requests?: {
    id: string
    description?: string | null
    project_location?: string | null
    project_postal_code?: string | null
    budget_range?: string | null
    decision_status?: string | null
    contact_name?: string | null
    contact_company?: string | null
    contact_phone?: string | null
    contact_email?: string | null
  } | null
}

/**
 * TEND-09 / D-05 — miroir de maskLead() pour les AO partenaires.
 * Tant que l'artisan n'a pas claim le lot, aucune coordonnée ni donnée
 * géolocalisante ne sort du serveur (ADR-004).
 */
export function maskTender(lot: TenderLotRow, claimed: boolean): Record<string, any>
```

Implémentation :
- `const req = lot.b2b_requests || {}`
- Champs communs aux deux branches : `lot_id: lot.id`, `request_ref: (req.id || '').slice(0, 8).toUpperCase()`,
  `category: lot.category`, `lot_status: lot.status`, `zone_name: lot.zone_name ?? null`,
  `budget_range: req.budget_range ?? null`, `decision_status: req.decision_status ?? 'en_attente'`,
  `created_at: lot.created_at`, `project_location: req.project_location ?? null`
- Branche `claimed === false` : ajouter `status: 'locked'`,
  `description: truncate(req.description, 200)`,
  `contact_name: '*** *** ***'`, `contact_company: '*** *** ***'`,
  `contact_phone: '*** *** ***'`, `contact_email: 'contact@***.fr'`.
  Ne PAS inclure de clé `postal_code`.
- Branche `claimed === true` : ajouter `status: 'claimed'`, `claimed_at: lot.claimed_at ?? null`,
  `description: req.description ?? null`, `postal_code: req.project_postal_code ?? null`,
  `contact_name`, `contact_company`, `contact_phone`, `contact_email` en clair (valeurs de `req`, `?? null`).
- Helper local `truncate(s, n)` : retourne `null` si vide, la chaîne telle quelle si `s.length <= n`,
  sinon `s.slice(0, n) + '…'`.

Créer `tests/unit/mask-tender.test.ts` couvrant les 5 comportements ci-dessus avec `describe`/`it`/`expect` (vitest).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/mask-tender.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/unit/mask-tender.test.ts` passe, au moins 5 assertions/tests
    - `grep -q "export function maskTender" server/utils/maskTender.ts` réussit
    - `grep -q "import .*#supabase" server/utils/maskTender.ts` NE retourne RIEN (fonction pure)
    - `grep -q "contact@\*\*\*\.fr" server/utils/maskTender.ts` réussit
    - Un test vérifie explicitement `expect(masked.postal_code).toBeUndefined()` sur la branche non claim
  </acceptance_criteria>
  <done>`maskTender()` existe, est pure, et ses 5 comportements sont couverts par des tests verts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| artisan authentifié → API AO | Un pro peut demander n'importe quel `lot_id` : les coordonnées partenaire ne doivent sortir que sur claim prouvé côté serveur |
| client → base Supabase (RLS) | `b2b_tender_claims` ne doit être lisible/écrivable que par le service role |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Information Disclosure | `maskTender()` | mitigate | Masquage serveur exclusif (ADR-004) : la branche non-claim n'émet aucune coordonnée ni `postal_code`, vérifié par test unitaire |
| T-09-02 | Elevation of Privilege | `b2b_tender_claims` RLS | mitigate | `ENABLE ROW LEVEL SECURITY` + unique policy `auth.jwt()->>'role' = 'admin'` — aucun accès anon/authenticated, tout passe par le service role |
| T-09-03 | Tampering | migration distante | accept | Push manuel via CLI authentifiée, périmètre additif uniquement (`ADD COLUMN IF NOT EXISTS`), aucune donnée existante réécrite |
</threat_model>

<verification>
- `npx supabase db push --yes` appliqué sans erreur
- `npm run test` (vitest run) : suite verte, y compris `tests/unit/mask-tender.test.ts`
- `git diff --stat app/types/database.types.ts` montre l'ajout de `b2b_tender_claims`
</verification>

<success_criteria>
- Table `b2b_tender_claims` présente en base distante avec contrainte `UNIQUE (lot_id, pro_id)` et RLS activée
- `b2b_tender_lots.closed_at` / `closed_reason` et `b2b_requests.reported_count` / `reported_at` / `report_reasons` existent en distant
- `app/types/database.types.ts` régénéré et committé
- `server/utils/maskTender.ts` pur, testé, ne fuit aucune coordonnée avant claim
</success_criteria>

<output>
After completion, create `.planning/phases/09-dashboard-pro-claim-des-ao/09-01-SUMMARY.md`
</output>
