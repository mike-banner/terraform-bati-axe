---
phase: 04-le-verrou-stripe-billing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260606000000_phase4_schema_gaps.sql
  - supabase/migrations/20260606000001_phase4_unlock_cron.sql
  - nuxt.config.ts
  - .env.example
autonomous: true
requirements: [LCK-01, LCK-02, LCK-03]

must_haves:
  truths:
    - "Migration file 20260606000000 contains ALTER TYPE lead_status ADD VALUE 'claimed' and ALTER TABLE projects ADD COLUMN timeline_range"
    - "Migration file 20260606000001 contains pg_cron extension, index on leads, and cron.schedule('auto-unlock-leads-72h') job"
    - "nuxt.config.ts has runtimeConfig block with stripeSecretKey, stripePriceId, stripeWebhookSecret server-side and siteUrl public"
    - ".env.example documents STRIPE_SECRET_KEY, STRIPE_PRICE_ID, STRIPE_WEBHOOK_SECRET, NUXT_PUBLIC_SITE_URL"
  artifacts:
    - path: "supabase/migrations/20260606000000_phase4_schema_gaps.sql"
      provides: "ENUM + column migrations"
      contains: "ALTER TYPE lead_status ADD VALUE"
    - path: "supabase/migrations/20260606000001_phase4_unlock_cron.sql"
      provides: "pg_cron job for 72h auto-unlock"
      contains: "cron.schedule"
    - path: "nuxt.config.ts"
      provides: "runtimeConfig for Stripe secrets"
      contains: "stripeSecretKey"
    - path: ".env.example"
      provides: "Required env var documentation"
      contains: "STRIPE_SECRET_KEY"
  key_links:
    - from: "nuxt.config.ts runtimeConfig.stripeSecretKey"
      to: "server/api/v1/stripe/checkout.post.ts"
      via: "useRuntimeConfig(event).stripeSecretKey"
      pattern: "stripeSecretKey"
    - from: "supabase/migrations/20260606000000"
      to: "leads table"
      via: "ALTER TYPE lead_status ADD VALUE 'claimed'"
      pattern: "claimed"
---

<objective>
Write all migration files and configuration needed by Wave 1 API code.

Purpose: Schema gaps (missing 'claimed' enum value, missing timeline_range column) and pg_cron job must exist in files before supabase db push runs (Plan 02). The nuxt.config.ts runtimeConfig block must exist before any Stripe endpoint can access secrets.

Output:
- 2 migration SQL files in supabase/migrations/
- nuxt.config.ts with runtimeConfig block added
- .env.example with 4 new Stripe/URL variables
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-le-verrou-stripe-billing/04-CONTEXT.md
@.planning/phases/04-le-verrou-stripe-billing/04-RESEARCH.md
@.planning/phases/04-le-verrou-stripe-billing/04-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write schema gap migrations</name>
  <files>
    supabase/migrations/20260606000000_phase4_schema_gaps.sql,
    supabase/migrations/20260606000001_phase4_unlock_cron.sql
  </files>

  <read_first>
    - supabase/migrations/20260603000000_schema_initial.sql — current schema: leads table, lead_status ENUM ('new','contacted','won','lost'), projects table (no timeline_range column). Understand existing column names before ALTERing.
    - .planning/phases/04-le-verrou-stripe-billing/04-RESEARCH.md §Gap 1 and §Gap 2 — exact SQL to apply
    - .planning/phases/04-le-verrou-stripe-billing/04-PATTERNS.md §supabase/migrations section — migration file conventions (header comment format, ordering)
    - .planning/phases/04-le-verrou-stripe-billing/04-CONTEXT.md §D-07 and §D-08 — confirms 72h delay and 'claimed' status semantics
  </read_first>

  <action>
  Create 20260606000000_phase4_schema_gaps.sql with:
  1. Header comment: `-- Supabase Migration: Phase 4 — Schema Gaps` with date 2026-06-06
  2. `ALTER TYPE lead_status ADD VALUE IF NOT EXISTS 'claimed';` — adds the claimed value to the existing lead_status ENUM (D-08/D-10 require this)
  3. `ALTER TABLE projects ADD COLUMN IF NOT EXISTS timeline_range TEXT;` — adds the delay field that D-05/D-17 require for BASIC lead display (values: '1_semaine', '1_mois', '3_mois', '6_mois', 'flexible')

  Create 20260606000001_phase4_unlock_cron.sql with:
  1. Header comment: `-- Supabase Migration: Phase 4 — Unlock Cron (pg_cron)` with date 2026-06-06
  2. `CREATE EXTENSION IF NOT EXISTS pg_cron;` — enables the extension (safe on Supabase cloud; may fail locally per RESEARCH.md Pitfall 4 — not a blocker)
  3. `CREATE INDEX IF NOT EXISTS idx_leads_unlock_check ON leads (unlocked_at, created_at, status) WHERE unlocked_at IS NULL;` — partial index optimizing the hourly cron query
  4. `SELECT cron.schedule('auto-unlock-leads-72h', '0 * * * *', $$ UPDATE leads SET unlocked_at = created_at + INTERVAL '72 hours' WHERE unlocked_at IS NULL AND status != 'claimed' AND created_at + INTERVAL '72 hours' <= NOW() $$);` — D-08: runs hourly, skips claimed leads per D-08 rule
  </action>

  <verify>
    <automated>grep -l "ADD VALUE IF NOT EXISTS 'claimed'" supabase/migrations/20260606000000_phase4_schema_gaps.sql && grep -l "timeline_range" supabase/migrations/20260606000000_phase4_schema_gaps.sql && grep -l "cron.schedule" supabase/migrations/20260606000001_phase4_unlock_cron.sql && grep -l "idx_leads_unlock_check" supabase/migrations/20260606000001_phase4_unlock_cron.sql</automated>
  </verify>

  <done>
  - 20260606000000_phase4_schema_gaps.sql exists with ALTER TYPE and ALTER TABLE statements
  - 20260606000001_phase4_unlock_cron.sql exists with pg_cron extension, partial index, and cron.schedule() call
  - Both files have header comments matching migration file convention
  </done>
</task>

<task type="auto">
  <name>Task 2: Add runtimeConfig to nuxt.config.ts and document env vars</name>
  <files>
    nuxt.config.ts,
    .env.example
  </files>

  <read_first>
    - nuxt.config.ts — current state (53 lines, no runtimeConfig block). Existing blocks: compatibilityDate, devtools, css, modules, vite, nitro, supabase, shadcn, future. Insert runtimeConfig between shadcn and future blocks.
    - .planning/phases/04-le-verrou-stripe-billing/04-PATTERNS.md §nuxt.config.ts — exact runtimeConfig block to add
    - .planning/phases/04-le-verrou-stripe-billing/04-RESEARCH.md §Variables d'environnement — confirms 4 required vars and their names
    - .planning/phases/04-le-verrou-stripe-billing/04-RESEARCH.md §Anti-Patterns — STRIPE_SECRET_KEY must NEVER appear in runtimeConfig.public
  </read_first>

  <action>
  Edit nuxt.config.ts: insert the following runtimeConfig block between the `shadcn:` block and the `future:` block:

  ```
  runtimeConfig: {
    stripeSecretKey: process.env.STRIPE_SECRET_KEY,
    stripePriceId: process.env.STRIPE_PRICE_ID,
    stripeWebhookSecret: process.env.STRIPE_WEBHOOK_SECRET,
    public: {
      siteUrl: process.env.NUXT_PUBLIC_SITE_URL || 'http://localhost:3000',
    }
  },
  ```

  The three Stripe keys are server-only (under runtimeConfig root, not runtimeConfig.public). Only siteUrl goes in public because checkout success_url/cancel_url are used client-side to build the premium page link.

  Append to .env.example the following block (after existing variables, with a blank line separator and section comment):

  ```
  # Phase 4 — Stripe Billing
  STRIPE_SECRET_KEY=sk_test_...
  STRIPE_PRICE_ID=price_...
  STRIPE_WEBHOOK_SECRET=whsec_...
  NUXT_PUBLIC_SITE_URL=https://bati-axe.com
  ```
  </action>

  <verify>
    <automated>grep -c "stripeSecretKey" nuxt.config.ts && grep -c "stripeWebhookSecret" nuxt.config.ts && grep -v "public" nuxt.config.ts | grep -c "stripeSecretKey" && grep -c "STRIPE_SECRET_KEY" .env.example && grep -c "STRIPE_WEBHOOK_SECRET" .env.example</automated>
  </verify>

  <done>
  - nuxt.config.ts contains stripeSecretKey, stripePriceId, stripeWebhookSecret at root runtimeConfig level (server-side only)
  - nuxt.config.ts contains runtimeConfig.public.siteUrl
  - .env.example contains all 4 new variables with placeholder values
  - `npm run typecheck` passes (nuxt config is valid TypeScript)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env vars → runtimeConfig | STRIPE_SECRET_KEY must stay server-side only, never in runtimeConfig.public |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | nuxt.config.ts runtimeConfig | mitigate | stripeSecretKey/stripePriceId/stripeWebhookSecret placed under runtimeConfig root (server-only), NOT under runtimeConfig.public |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | stripe package already installed and verified [OK] in RESEARCH.md Package Legitimacy Audit |
</threat_model>

<verification>
After both tasks complete:
- `grep "stripeSecretKey" nuxt.config.ts` — must return a line NOT containing "public:"
- `ls supabase/migrations/20260606*` — must list both migration files
- `grep "cron.schedule" supabase/migrations/20260606000001_phase4_unlock_cron.sql` — must return the schedule call
- `npm run typecheck` — must exit 0
</verification>

<success_criteria>
- Both migration files written with correct SQL matching RESEARCH.md patterns
- nuxt.config.ts has runtimeConfig block with 3 server-side Stripe keys and 1 public siteUrl
- .env.example documents all 4 new variables
- TypeScript compilation passes
</success_criteria>

<output>
Create `.planning/phases/04-le-verrou-stripe-billing/04-01-SUMMARY.md` when done
</output>
