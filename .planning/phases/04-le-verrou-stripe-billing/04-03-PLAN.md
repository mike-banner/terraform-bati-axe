---
phase: 04-le-verrou-stripe-billing
plan: 03
type: execute
wave: 2
depends_on: ["04-02"]
files_modified:
  - server/utils/maskLead.ts
  - server/api/v1/admin/qualify.post.ts
  - server/api/v1/leads/index.get.ts
  - server/api/v1/leads/[id].get.ts
autonomous: true
requirements: [LCK-01]

must_haves:
  truths:
    - "POST /api/v1/admin/qualify with a valid project_id qualifies the project and creates leads for all verified pros of the same category"
    - "GET /api/v1/leads returns masked data for BASIC pro with locked leads (customer_name: '*** *** ***')"
    - "GET /api/v1/leads returns full data for Premium pro (subscription_status: 'active')"
    - "GET /api/v1/leads/[id] returns 404 if the lead does not belong to the requesting pro"
    - "maskLead unit tests turn GREEN after this plan"
  artifacts:
    - path: "server/utils/maskLead.ts"
      provides: "Pure masking function, testable without DB"
      exports: ["maskLead"]
    - path: "server/api/v1/admin/qualify.post.ts"
      provides: "Admin qualify endpoint"
      exports: ["default (defineEventHandler)"]
    - path: "server/api/v1/leads/index.get.ts"
      provides: "Pro leads list with server-side masking"
      exports: ["default (defineEventHandler)"]
    - path: "server/api/v1/leads/[id].get.ts"
      provides: "Pro lead detail with server-side masking"
      exports: ["default (defineEventHandler)"]
  key_links:
    - from: "server/api/v1/leads/index.get.ts"
      to: "server/utils/maskLead.ts"
      via: "import { maskLead } from '~/server/utils/maskLead'"
      pattern: "maskLead"
    - from: "server/api/v1/leads/[id].get.ts"
      to: "server/utils/maskLead.ts"
      via: "import { maskLead } from '~/server/utils/maskLead'"
      pattern: "maskLead"
    - from: "server/api/v1/admin/qualify.post.ts"
      to: "leads table"
      via: "supabase.from('leads').upsert(..., { onConflict: 'project_id,pro_id' })"
      pattern: "onConflict"
---

<objective>
Implement the core Le Verrou backend: masking helper, admin qualify endpoint, and the two leads read endpoints (list + detail).

Purpose: These four files are the heart of LCK-01. The maskLead helper extracts the masking logic into a pure function that is independently testable (turning the RED stubs from Plan 02 GREEN). The qualify endpoint creates leads when the admin qualifies a project. The leads endpoints apply server-side masking per D-05 through D-10.

Output:
- server/utils/maskLead.ts — pure function, no DB dependency
- server/api/v1/admin/qualify.post.ts — admin-only POST
- server/api/v1/leads/index.get.ts — pro-facing GET (list)
- server/api/v1/leads/[id].get.ts — pro-facing GET (detail)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-le-verrou-stripe-billing/04-CONTEXT.md
@.planning/phases/04-le-verrou-stripe-billing/04-RESEARCH.md
@.planning/phases/04-le-verrou-stripe-billing/04-PATTERNS.md
@server/api/v1/admin/approve-pro.post.ts
@server/api/v1/admin/queue.get.ts

<interfaces>
<!-- Extracted from existing analogs -->

From server/api/v1/admin/approve-pro.post.ts:
```typescript
// Admin auth pattern (lines 9-14)
const user = await serverSupabaseUser(event)
if (!user) throw createError({ statusCode: 401, statusMessage: 'Non autorisé.' })
const isAdmin = (user as any).app_metadata?.role === 'admin'
if (!isAdmin) throw createError({ statusCode: 403, statusMessage: 'Accès réservé aux administrateurs.' })

// Zod schema pattern (lines 4-7)
const schema = z.object({ pro_id: z.string().uuid(), approved: z.boolean() })
const body = await readBody(event)
const parsed = schema.safeParse(body)
if (!parsed.success) throw createError({ statusCode: 400, statusMessage: 'Données invalides.' })

// Service role pattern (line 21)
const supabase = await serverSupabaseServiceRole(event) as any
```

From server/api/v1/admin/queue.get.ts:
```typescript
// Pro auth (no admin check) + service role pattern (lines 3-10)
const user = await serverSupabaseUser(event)
if (!user) throw createError({ statusCode: 401, statusMessage: 'Non autorisé.' })
const supabase = await serverSupabaseServiceRole(event) as any
```

Schema facts (from PATTERNS.md §Schema Facts):
- leads has UNIQUE constraint on (project_id, pro_id) — UPSERT safe with onConflict
- professionals has subscription_status subscription_status DEFAULT 'none'
- leads has unlocked_at TIMESTAMPTZ
- leads has status lead_status DEFAULT 'new' — now includes 'claimed'
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create maskLead pure helper</name>
  <files>server/utils/maskLead.ts</files>

  <read_first>
    - tests/unit/leads-masking.test.ts — the RED tests written in Plan 02. The maskLead function signature must match exactly what the tests import and call.
    - .planning/phases/04-le-verrou-stripe-billing/04-RESEARCH.md Pattern 1 — full masking logic with isPremium/unlocked/claimed branching
    - .planning/phases/04-le-verrou-stripe-billing/04-CONTEXT.md §D-05 through D-10 — masking rules
  </read_first>

  <behavior>
    - maskLead(lead, isPremium=true, now) → status:'unlocked' with real customer_name, customer_phone, customer_email, postal_code, description
    - maskLead(lead, isPremium=false, now) where unlocked_at IS NULL → status:'locked', customer_name:'*** *** ***', customer_phone:'*** *** ***', customer_email:'contact@***.fr', postal_code:'***', no description field
    - maskLead(lead, isPremium=false, now) where unlocked_at <= now → status:'unlocked' with real data
    - maskLead(lead with status='claimed', isPremium=false, now) → status:'claimed' with only {id, status, created_at, category, budget_range} — no customer fields
  </behavior>

  <action>
  Create server/utils/maskLead.ts exporting a single pure function:
  `export function maskLead(lead: any, isPremium: boolean, now: Date): Record<string, any>`

  Implement the four branches exactly as described in RESEARCH.md Pattern 1 (D-05/D-06/D-07/D-08/D-09/D-10):
  1. isClaimed = lead.status === 'claimed'. If isClaimed AND NOT isPremium → return minimal object: {id, status:'claimed', created_at, category: lead.projects.category, budget_range: lead.projects.budget_range}. No customer data, no description (D-10).
  2. isUnlocked = isPremium OR (lead.unlocked_at !== null AND new Date(lead.unlocked_at) <= now). If NOT isUnlocked → return masked object: {id, status:'locked', unlocked_at: lead.unlocked_at, created_at, category, budget_range, timeline_range: lead.projects.timeline_range, customer_name:'*** *** ***', customer_phone:'*** *** ***', customer_email:'contact@***.fr', postal_code:'***'}. D-05: commune (postal_code) is masked deliberately.
  3. Unlocked (Premium or 72h elapsed) → return full object: {id, status:'unlocked', unlocked_at, created_at, category, budget_range, timeline_range, description: lead.projects.description, customer_name: lead.projects.customer_name, customer_phone: lead.projects.customer_phone, customer_email: lead.projects.customer_email, postal_code: lead.projects.postal_code}.

  No imports needed — pure TypeScript. No database calls. No side effects.

  After creating maskLead.ts, run `npm run test -- --reporter=verbose` to verify the RED tests from Plan 02 turn GREEN.
  </action>

  <verify>
    <automated>npm run test -- tests/unit/leads-masking.test.ts 2>&1 | grep -E "PASS|✓|pass" | head -5</automated>
  </verify>

  <done>
  - server/utils/maskLead.ts exports maskLead function
  - All 4 tests in tests/unit/leads-masking.test.ts pass GREEN
  - Function is pure (no imports from #supabase/server or any DB client)
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement qualify.post.ts and leads API endpoints</name>
  <files>
    server/api/v1/admin/qualify.post.ts,
    server/api/v1/leads/index.get.ts,
    server/api/v1/leads/[id].get.ts
  </files>

  <read_first>
    - server/api/v1/admin/approve-pro.post.ts — admin auth + Zod pattern to replicate exactly
    - server/api/v1/admin/queue.get.ts — service role + multi-fetch pattern
    - .planning/phases/04-le-verrou-stripe-billing/04-RESEARCH.md Pattern 5 — qualify full logic
    - .planning/phases/04-le-verrou-stripe-billing/04-RESEARCH.md Pattern 1 — leads index full logic
    - .planning/phases/04-le-verrou-stripe-billing/04-PATTERNS.md §qualify.post.ts and §leads/index.get.ts — adaptation notes
    - .planning/phases/04-le-verrou-stripe-billing/04-CONTEXT.md §D-01 D-03 D-04 — qualify matching rules
  </read_first>

  <action>
  Create server/api/v1/admin/qualify.post.ts:
  - Imports: `import { z } from 'zod'` and `import { serverSupabaseUser, serverSupabaseServiceRole } from '#supabase/server'`
  - Auth: `serverSupabaseUser` check → 401; admin check via `app_metadata?.role === 'admin'` → 403
  - Schema: `z.object({ project_id: z.string().uuid() })`, use `schema.safeParse(await readBody(event))` → 400 on failure
  - Service role client
  - Step 1: UPDATE projects SET status='qualified' WHERE id=project_id, SELECT category back with `.select('category').single()`
  - Step 2: SELECT professionals WHERE category=project.category AND is_verified=true — only `id` field needed (D-01: category matching only, D-02: single category)
  - Step 3: If pros exist, UPSERT leads with `{ project_id, pro_id: p.id, status: 'new', unlocked_at: null }` array, onConflict: 'project_id,pro_id' (idempotent — per RESEARCH.md anti-patterns)
  - Return: `{ qualified: true, leads_created: pros?.length ?? 0 }`

  Create server/api/v1/leads/index.get.ts:
  - Import maskLead from '~/server/utils/maskLead'
  - Auth: `serverSupabaseUser` → 401 (NO admin check — this is pro-facing)
  - Service role client (ADR-004: must bypass RLS to read full data for server-side masking)
  - Fetch pro profile: `.from('professionals').select('id, subscription_status').eq('id', user.id).single()` → 404 if not found
  - `const isPremium = pro.subscription_status === 'active'`
  - Fetch leads with Supabase join: `.from('leads').select('id, status, unlocked_at, created_at, projects(id, category, budget_range, timeline_range, description, customer_name, customer_email, customer_phone, postal_code)').eq('pro_id', user.id).order('created_at', { ascending: false })`
  - 500 on DB error
  - `const now = new Date()`
  - Map: `(leads || []).map((lead: any) => maskLead(lead, isPremium, now))`
  - Return: `{ leads: masked }`

  Create server/api/v1/leads/[id].get.ts:
  - Import maskLead from '~/server/utils/maskLead'
  - Auth: `serverSupabaseUser` → 401
  - `const id = getRouterParam(event, 'id')` → 400 if missing
  - Service role client
  - Fetch pro profile (same as index.get.ts) → 404 if not found
  - `const isPremium = pro.subscription_status === 'active'`
  - Fetch single lead: `.from('leads').select('id, status, unlocked_at, created_at, projects(id, category, budget_range, timeline_range, description, customer_name, customer_email, customer_phone, postal_code)').eq('id', id).eq('pro_id', user.id).single()` — ownership check via `.eq('pro_id', user.id)` prevents pro A seeing pro B's lead
  - 404 if error or no data
  - Apply: `const masked = maskLead(lead, isPremium, new Date())`
  - Return: `{ lead: masked }`

  SECURITY: All three files — `serverSupabaseUser(event)` check is the FIRST line in defineEventHandler. Never log lead objects (only log lead.id if needed).
  </action>

  <verify>
    <automated>npm run typecheck 2>&1 | grep -E "error TS" | head -10</automated>
  </verify>

  <done>
  - qualify.post.ts: admin auth → 403, Zod validation → 400, UPSERT leads with onConflict, returns leads_created count
  - leads/index.get.ts: pro auth → 401, service role, maskLead applied to each lead, returns { leads: [] }
  - leads/[id].get.ts: ownership check (.eq('pro_id', user.id)), 404 if not found, maskLead applied, returns { lead: {} }
  - `npm run typecheck` exits 0 (no TypeScript errors)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/v1/leads | Unauthenticated request must never receive any lead data |
| client → /api/v1/admin/qualify | Non-admin must never be able to trigger lead creation |
| API response → browser | No raw customer_name/phone/email must ever appear for non-Premium BASIC with locked lead |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Elevation of Privilege | GET /api/v1/leads | mitigate | serverSupabaseUser FIRST line → 401; service role only used after auth confirmed |
| T-04-05 | Elevation of Privilege | GET /api/v1/admin/qualify | mitigate | admin check app_metadata.role === 'admin' → 403 after 401 check |
| T-04-06 | Information Disclosure | maskLead output | mitigate | All three return branches verified by unit tests: locked returns '*** *** ***' strings, no real data |
| T-04-07 | Elevation of Privilege | GET /api/v1/leads/[id] | mitigate | .eq('pro_id', user.id) ownership check prevents cross-pro data access |
| T-04-08 | Tampering | qualify.post.ts leads upsert | mitigate | UPSERT with onConflict:'project_id,pro_id' — idempotent, no duplicate leads created |
| T-04-SC | Tampering | npm installs | accept | No new npm packages in this plan |
</threat_model>

<verification>
After both tasks complete:
- `npm run test -- tests/unit/leads-masking.test.ts` → all 4 tests GREEN
- `npm run typecheck` → exit 0
- `grep "serverSupabaseUser" server/api/v1/leads/index.get.ts | head -1` → must be first statement in defineEventHandler
- `grep "pro_id.*user.id" server/api/v1/leads/[id].get.ts` → ownership check present
- `grep "onConflict.*project_id" server/api/v1/admin/qualify.post.ts` → UPSERT idempotence guard present
</verification>

<success_criteria>
- maskLead tests pass GREEN
- qualify endpoint creates leads for verified pros of matching category (idempotent)
- leads/index.get returns masked data for BASIC and full data for Premium
- leads/[id].get enforces ownership (pro can only see their own leads)
- TypeScript compiles clean
</success_criteria>

<output>
Create `.planning/phases/04-le-verrou-stripe-billing/04-03-SUMMARY.md` when done
</output>
