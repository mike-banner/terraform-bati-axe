---
phase: 04-le-verrou-stripe-billing
plan: 02
type: execute
wave: 1
depends_on: ["04-01"]
files_modified:
  - vitest.config.ts
  - tests/unit/leads-masking.test.ts
  - tests/unit/stripe-webhook.test.ts
autonomous: false
requirements: [LCK-01, LCK-02, LCK-03]

user_setup:
  - service: supabase
    why: "Schema push requires Supabase project access"
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard → Account → Access Tokens"
    dashboard_config:
      - task: "Ensure pg_cron extension is enabled"
        location: "Supabase Dashboard → Database → Extensions → pg_cron (enable if not already)"

must_haves:
  truths:
    - "supabase db push completes without error (migrations applied)"
    - "lead_status ENUM contains 'claimed' value in production schema"
    - "projects table has timeline_range column in production schema"
    - "vitest.config.ts exists and npm run test command is configured"
    - "tests/unit/leads-masking.test.ts contains failing stubs for LCK-01 masking logic"
    - "tests/unit/stripe-webhook.test.ts contains failing stubs for LCK-02 webhook logic"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Test framework configuration"
      contains: "defineConfig"
    - path: "tests/unit/leads-masking.test.ts"
      provides: "RED tests for masking logic (will pass after Plan 03)"
      contains: "maskLead"
    - path: "tests/unit/stripe-webhook.test.ts"
      provides: "RED tests for webhook signature (will pass after Plan 05)"
      contains: "constructEventAsync"
  key_links:
    - from: "tests/unit/leads-masking.test.ts"
      to: "server/utils/maskLead.ts"
      via: "import { maskLead } from '../utils/maskLead'"
      pattern: "maskLead"
---

<objective>
[BLOCKING] Push schema migrations to Supabase, install vitest, and write RED test stubs for the masking and webhook logic.

Purpose: The [BLOCKING] schema push must complete before any API code can run against the new columns and enum value. Test stubs create the Nyquist coverage contract — they fail RED now and turn GREEN when Plans 03 and 05 implement the logic.

Output:
- Supabase schema updated in production (leads.status has 'claimed', projects has timeline_range, pg_cron job registered)
- vitest.config.ts configured
- 2 test stub files with failing assertions covering LCK-01 and LCK-02

Note: This plan has a [BLOCKING] schema push task and a human-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-le-verrou-stripe-billing/04-CONTEXT.md
@.planning/phases/04-le-verrou-stripe-billing/04-RESEARCH.md
@.planning/phases/04-le-verrou-stripe-billing/04-VALIDATION.md
@.planning/phases/04-le-verrou-stripe-billing/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install vitest and write test configuration</name>
  <files>
    vitest.config.ts,
    package.json
  </files>

  <read_first>
    - package.json — current devDependencies (no vitest present). Check existing test/scripts section to add npm run test command.
    - .planning/phases/04-le-verrou-stripe-billing/04-VALIDATION.md — test framework spec: vitest, npm run test command, ~30s runtime
  </read_first>

  <action>
  Run: `npm install -D vitest`

  Create vitest.config.ts at project root:
  ```
  import { defineConfig } from 'vitest/config'

  export default defineConfig({
    test: {
      globals: true,
      environment: 'node',
      include: ['tests/**/*.test.ts'],
      reporters: ['verbose'],
    },
  })
  ```

  Add to package.json scripts section: `"test": "vitest run"` (run once, not watch mode — per Nyquist rule no watch flags).

  Do NOT add `--watch` flag anywhere. Do NOT add `@vitest/ui` unless already present.
  </action>

  <verify>
    <automated>node -e "require('./node_modules/vitest/package.json')" && grep -c '"test"' package.json && grep -c "vitest run" package.json</automated>
  </verify>

  <done>
  - vitest installed in node_modules
  - vitest.config.ts exists at project root with globals:true, environment:node, include pattern
  - package.json has "test": "vitest run" in scripts
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Push schema to Supabase</name>

  <what-built>
  Migration files 20260606000000_phase4_schema_gaps.sql and 20260606000001_phase4_unlock_cron.sql were written in Plan 01. This task applies them to the live Supabase project.

  Run: `supabase db push`

  If this fails with `ERROR: schema "cron" does not exist` on the second migration — this is expected in local-only environments where pg_cron is not enabled. The first migration (schema gaps) must succeed. For the cron migration failure, check Supabase Dashboard → Database → Extensions and enable pg_cron, then re-run.

  If SUPABASE_ACCESS_TOKEN is not set, export it first: `export SUPABASE_ACCESS_TOKEN=your_token`
  </what-built>

  <how-to-verify>
  1. Run `supabase db push` — confirm it exits 0 (or exits with only pg_cron warning, not schema gap error)
  2. In Supabase Dashboard → Table Editor → leads → check that 'claimed' appears in the status column enum options
  3. In Supabase Dashboard → Table Editor → projects → check that timeline_range column exists
  4. In Supabase Dashboard → Database → Cron Jobs — check that 'auto-unlock-leads-72h' job is listed (if pg_cron succeeded)
  </how-to-verify>

  <resume-signal>Type "schema-pushed" once supabase db push completes and the leads.status enum contains 'claimed', or describe the error.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Write RED test stubs for masking and webhook logic</name>
  <files>
    tests/unit/leads-masking.test.ts,
    tests/unit/stripe-webhook.test.ts
  </files>

  <read_first>
    - .planning/phases/04-le-verrou-stripe-billing/04-RESEARCH.md Pattern 1 — maskLead logic: isPremium, unlocked_at conditions, isClaimed branching. The helper will live at server/utils/maskLead.ts (created in Plan 03).
    - .planning/phases/04-le-verrou-stripe-billing/04-CONTEXT.md §D-05 through D-10 — the exact masking rules these tests must verify
    - .planning/phases/04-le-verrou-stripe-billing/04-VALIDATION.md §Phase Requirements → Test Map — LCK-01 behaviors to cover
  </read_first>

  <action>
  Create tests/unit/leads-masking.test.ts. This file imports from `../../server/utils/maskLead` (does not exist yet — tests will fail RED until Plan 03 creates the helper).

  The test file must cover exactly these 4 behaviors (from D-05/D-06/D-07/D-10):
  - BASIC + unlocked_at IS NULL + not claimed → returns status:'locked', customer_name:'*** *** ***', customer_phone:'*** *** ***', customer_email:'contact@***.fr', postal_code:'***'
  - BASIC + unlocked_at in the past (72h elapsed) + not claimed → returns status:'unlocked' with real customer data
  - Premium (isPremium=true) + any unlocked_at → returns status:'unlocked' with real customer data
  - claimed + not premium → returns status:'claimed' with only id, category, budget_range (no customer fields)

  Test input shape: `{ id, status, unlocked_at, created_at, projects: { category, budget_range, timeline_range, description, customer_name, customer_email, customer_phone, postal_code } }`.
  Test helper signature to test: `maskLead(lead: any, isPremium: boolean, now: Date): MaskedLead`

  Create tests/unit/stripe-webhook.test.ts. This file verifies the webhook handler behavior using a mock Stripe event. Since the actual Stripe SDK and constructEventAsync are hard to unit-test without a real signature, write stubs that will verify the UPDATE logic by mocking the supabase client. These can be integration-style unit tests using vitest's vi.mock.

  Cover: checkout.session.completed event → subscription_status set to 'active' and stripe_customer_id set. customer.subscription.deleted event → subscription_status set to 'canceled'. invoice.payment_failed event → subscription_status set to 'unpaid'.

  Both files should run `vitest run` as failing tests now (RED). They will turn GREEN when Plans 03 and 05 implement the logic.
  </action>

  <verify>
    <automated>npm run test 2>&1 | grep -E "FAIL|Cannot find module" | head -5</automated>
  </verify>

  <done>
  - tests/unit/leads-masking.test.ts exists with 4 describe/it blocks covering D-05/D-06/D-07/D-10 masking behaviors
  - tests/unit/stripe-webhook.test.ts exists with 3 describe/it blocks covering D-14 webhook event handling
  - `npm run test` runs and reports failures (RED — expected: modules don't exist yet)
  - No watch-mode flags anywhere in test scripts
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration SQL → Supabase DB | Schema changes are irreversible (ENUM ADD VALUE cannot be rolled back) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Tampering | supabase db push | mitigate | Migrations use IF NOT EXISTS / IF NOT EXISTS guards — idempotent on retry |
| T-04-03 | Information Disclosure | test files | accept | Test files mock real data — no real customer_phone/email in test fixtures; use fabricated strings like '06 00 00 00 00' |
| T-04-SC | Tampering | npm install vitest | accept | vitest is a well-known package (vitejs org, 100M+ downloads) — no legitimacy concern |
</threat_model>

<verification>
After all tasks complete:
- `supabase db push` has completed without schema gap errors
- `npm run test` runs vitest and reports test failures (modules not found) — not a config failure
- `grep "maskLead" tests/unit/leads-masking.test.ts` returns import line
- `grep "constructEventAsync\|subscription_status" tests/unit/stripe-webhook.test.ts` returns matches
</verification>

<success_criteria>
- Schema pushed: leads.status enum includes 'claimed', projects has timeline_range column
- vitest configured and running (even with failing tests)
- RED test stubs exist for LCK-01 (masking) and LCK-02 (webhook)
</success_criteria>

<output>
Create `.planning/phases/04-le-verrou-stripe-billing/04-02-SUMMARY.md` when done
</output>
