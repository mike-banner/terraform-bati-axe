---
phase: 04-le-verrou-stripe-billing
plan: 06
type: execute
wave: 4
depends_on: ["04-03", "04-04"]
files_modified:
  - app/pages/espace/leads/index.vue
  - app/pages/espace/leads/[id].vue
  - app/components/LeadCountdown.vue
autonomous: true
requirements: [LCK-01, LCK-02]

must_haves:
  truths:
    - "Pro can visit /espace/leads and see their leads as cards (grid sm:grid-cols-2 lg:grid-cols-3)"
    - "Locked lead card shows masked fields with text-muted-foreground font-mono (no blur CSS), amber countdown, and 'Passer Premium' CTA"
    - "Unlocked lead card uses border-foreground/30 badge and text-foreground for contact data, and 'Voir le contact' CTA"
    - "Claimed lead card uses border-border text-muted-foreground badge, masked fields, and no CTA"
    - "BASIC pro with ≥1 locked lead sees the amber banner at top of page"
    - "Unauthenticated user is redirected to /pro/claim"
    - "3 skeleton cards shown during useAsyncData loading"
    - "?upgrade=success banner appears and auto-dismisses after 6s"
    - "Lead detail page /espace/leads/[id] shows project info + conditional contact card"
  artifacts:
    - path: "app/pages/espace/leads/index.vue"
      provides: "Lead dashboard with 3 card variants"
      contains: "divide-border"
    - path: "app/pages/espace/leads/[id].vue"
      provides: "Lead detail with conditional contact card"
      contains: "border-foreground/30"
    - path: "app/components/LeadCountdown.vue"
      provides: "Reusable countdown component"
      contains: "stroke-linecap"
  key_links:
    - from: "app/pages/espace/leads/index.vue"
      to: "server/api/v1/leads/index.get.ts"
      via: "$fetch('/api/v1/leads')"
      pattern: "api/v1/leads"
    - from: "app/pages/espace/leads/[id].vue"
      to: "server/api/v1/leads/[id].get.ts"
      via: "$fetch(`/api/v1/leads/${route.params.id}`)"
      pattern: "api/v1/leads"
---

<objective>
Build the leads dashboard (/espace/leads) and lead detail (/espace/leads/[id]) with all three card variants and the shared LeadCountdown component.

Purpose: This is the primary user-facing surface for LCK-01. Pros see their leads with the teasing blur effect on locked cards, emerald highlighting on unlocked cards, and a clear "Déjà attribué" state for claimed cards. The data arrives pre-masked from the API — the UI never unmasks data, only renders what the server sends.

Output:
- app/pages/espace/leads/index.vue
- app/pages/espace/leads/[id].vue
- app/components/LeadCountdown.vue
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-le-verrou-stripe-billing/04-CONTEXT.md
@.planning/phases/04-le-verrou-stripe-billing/04-RESEARCH.md
@.planning/phases/04-le-verrou-stripe-billing/04-UI-SPEC.md
@.planning/phases/04-le-verrou-stripe-billing/04-PATTERNS.md
@app/pages/app/dashboard.vue

<interfaces>
<!-- API response shapes from Plans 03/04 -->

GET /api/v1/leads → { leads: MaskedLead[] }

MaskedLead shapes:
```typescript
// Locked (BASIC, <72h, not claimed)
{ id, status: 'locked', unlocked_at: string | null, created_at, category, budget_range, timeline_range,
  customer_name: '*** *** ***', customer_phone: '*** *** ***', customer_email: 'contact@***.fr', postal_code: '***' }

// Unlocked (Premium or BASIC + 72h elapsed)
{ id, status: 'unlocked', unlocked_at, created_at, category, budget_range, timeline_range, description,
  customer_name, customer_phone, customer_email, postal_code }

// Claimed (Premium took it, BASIC sees this)
{ id, status: 'claimed', created_at, category, budget_range }
```

GET /api/v1/leads/[id] → { lead: MaskedLead }
(same shapes as above, single object)

<!-- Established Vue patterns from dashboard.vue -->
Auth guard: watchEffect(() => { if (user.value === null) navigateTo('/pro/claim') })
useAsyncData key convention: 'pro-leads', 'lead-detail-${id}'
Layout: max-w-5xl mx-auto px-4 sm:px-6 (upgraded from dashboard's max-w-2xl per UI-SPEC container spec)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build LeadCountdown component and leads index page</name>
  <files>
    app/components/LeadCountdown.vue,
    app/pages/espace/leads/index.vue
  </files>

  <read_first>
    - .planning/phases/04-le-verrou-stripe-billing/04-UI-SPEC.md §Page 1 and §Tokens & Classes Partagés — full card layout, 3 variants, banner, skeleton, badge tokens, countdown component spec, blur CSS rules
    - app/pages/app/dashboard.vue — auth guard (lines 7-10), useAsyncData pattern (lines 13-21), template structure (lines 81-179)
    - .planning/phases/04-le-verrou-stripe-billing/04-PATTERNS.md §leads/index.vue — adaptation notes including $fetch usage (not direct Supabase client)
    - .planning/phases/04-le-verrou-stripe-billing/04-CONTEXT.md §D-16 D-17 D-18 — cards layout, CTA rules per status
  </read_first>

  <action>
  Create app/components/LeadCountdown.vue:
  Props: `{ unlockedAt: string }` (the unlocked_at ISO string from the lead)
  Computed: calculate remaining time as `Math.max(0, new Date(unlockedAt).getTime() - Date.now())`; derive hours = Math.floor(ms / 3600000); minutes = Math.floor((ms % 3600000) / 60000)
  Interval: use `onMounted` + `setInterval` with 60000ms (60s) to refresh countdown — not aggressive polling
  Use `onUnmounted` to clear the interval
  Template (SVG inline horloge from UI-SPEC §Icônes — NO lucide import):
  ```html
  <span class="flex items-center gap-1 text-amber-700 text-xs font-semibold">
    <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" d="M12 6v6h4.5m4.5 0a9 9 0 11-18 0 9 9 0 0118 0z"/>
    </svg>
    Disponible dans {{ hours }}h {{ minutes }}min
  </span>
  ```

  Create app/pages/espace/leads/index.vue:

  Script setup:
  - Imports: useSupabaseUser, watchEffect, navigateTo, useAsyncData, useRoute, ref, computed, onMounted
  - Import LeadCountdown from '@/components/LeadCountdown.vue'
  - NO lucide imports. NO icon library. All icons are SVG inline in the template.
  - useHead({ title: 'Mes leads — BÂTI-AXE' })
  - Auth guard: `watchEffect(() => { if (user.value === null) navigateTo('/pro/claim') })`
  - useAsyncData: key 'pro-leads', fetches `$fetch('/api/v1/leads')`, returns `data?.leads || []`
  - Computed `hasLockedLeads`: `leads.value?.some(l => l.status === 'locked') ?? false`
  - route to detect `?upgrade=success`: `const showSuccessBanner = ref(useRoute().query.upgrade === 'success')`; set up `onMounted` to auto-dismiss after 6000ms: `if (showSuccessBanner.value) setTimeout(() => { showSuccessBanner.value = false }, 6000)`
  - CATEGORY_LABELS map: same as admin/index.vue

  Template structure (per UI-SPEC §Page 1 — monochrome tokens only):
  - Outer: `<div class="max-w-2xl mx-auto px-6 py-16">`
  - Page header: `<h1 class="text-3xl font-black tracking-tight text-foreground">Mes leads</h1>` + `<p class="text-sm text-muted-foreground mt-1">Leads qualifiés pour votre métier</p>`
  - Success banner (v-if="showSuccessBanner"): `<div class="flex items-center gap-3 p-4 border border-foreground/30 bg-background rounded-lg mb-8">` — svg coche (stroke-width="2.5") + "Bienvenue dans BÂTI-AXE Premium ! Vos leads sont maintenant débloqués."
  - Premium banner (v-if="hasLockedLeads"): exact pattern from UI-SPEC §Banner Premium — `border border-amber-300 bg-amber-50 rounded-lg` with SVG étoile + text + NuxtLink "Voir l'offre" underlined
  - Skeleton loading (v-if="pending"): `<div v-for="i in 3" :key="i" class="border border-border rounded-lg h-[160px] animate-pulse bg-muted" />` inside grid `grid grid-cols-1 sm:grid-cols-2 gap-4`
  - Empty state (v-else-if="!leads?.length"): SVG inline bâtiment/building (HeroIcons outline) + `<p class="text-muted-foreground text-sm">Aucun lead pour l'instant</p>`
  - Lead grid (v-else): `<div class="grid grid-cols-1 sm:grid-cols-2 gap-4">`

  Card structure (per UI-SPEC §Card Lead — same as dashboard.vue checklist pattern):
  ALL cards: `<div class="border border-border rounded-lg divide-y divide-border">`

  Card rendering — use v-for on leads, with v-if/v-else-if/v-else on lead.status:

  Variant A (status === 'locked'):
  - Header div `flex items-center justify-between px-5 py-4`: category label + badge:
    `<span class="inline-flex items-center gap-1.5 text-xs font-semibold px-3 py-1.5 border rounded-full border-amber-300 text-amber-700 bg-amber-50">` + SVG verrou fermé + "Flouté"
  - Body div `px-5 py-4 space-y-2`: budget_range, timeline_range in `text-sm text-muted-foreground`
  - Masked fields (NO blur CSS — server already sent '*** *** ***'): `<span class="text-muted-foreground font-mono select-none">*** *** ***</span>`
  - LeadCountdown: `<LeadCountdown :unlocked-at="lead.unlocked_at" v-if="lead.unlocked_at" />`
  - Footer div `px-5 py-4`: CTA `<NuxtLink to="/espace/premium" class="bg-foreground text-background text-sm font-semibold py-2 px-4 rounded-md w-full text-center block">` + SVG étoile inline + "Passer Premium"

  Variant B (status === 'unlocked'):
  - Header: badge `<span class="inline-flex items-center gap-1.5 text-xs font-semibold px-3 py-1.5 border rounded-full border-foreground/30 text-foreground">` + SVG verrou ouvert + "Débloqué"
  - Body: budget_range, timeline_range, customer_name, postal_code, customer_phone all in `text-sm text-foreground`
  - Footer: CTA `<NuxtLink :to="\`/espace/leads/${lead.id}\`" class="border border-border text-foreground hover:bg-muted text-sm font-semibold py-2 px-4 rounded-md w-full text-center block">→ Voir le contact</NuxtLink>`

  Variant C (status === 'claimed'):
  - Header: badge `<span class="inline-flex items-center gap-1.5 text-xs font-semibold px-3 py-1.5 border rounded-full border-border text-muted-foreground">Déjà attribué</span>`
  - Body: category, budget visible in `text-sm text-muted-foreground`; masked fields as `<span class="text-muted-foreground font-mono select-none">*** *** ***</span>`
  - Footer: `<p class="text-xs text-muted-foreground text-center py-2">Ce lead a déjà été attribué</p>`

  IMPORTANT (UI-SPEC anti-patterns): NO blur CSS on any element. NO lucide or any icon library import. NO emerald/sky/slate color classes. All icons are SVG inline.
  </action>

  <verify>
    <automated>npm run typecheck 2>&1 | grep -E "error TS" | head -10</automated>
  </verify>

  <done>
  - LeadCountdown.vue: SVG horloge inline, 60s interval, hours/minutes computed from unlocked_at, NO lucide import
  - leads/index.vue: auth guard (watchEffect), useAsyncData with $fetch, 3 card variants using divide-border pattern, skeleton loading, empty state, Premium banner (v-if hasLockedLeads), success banner (?upgrade=success, 6s auto-dismiss)
  - NO blur CSS anywhere — server sends '*** *** ***' strings, render as text-muted-foreground font-mono select-none
  - NO lucide imports in any file
  - NO emerald/sky/slate color classes anywhere
  - TypeScript compiles clean
  </done>
</task>

<task type="auto">
  <name>Task 2: Build lead detail page /espace/leads/[id]</name>
  <files>app/pages/espace/leads/[id].vue</files>

  <read_first>
    - .planning/phases/04-le-verrou-stripe-billing/04-UI-SPEC.md §Page 2 — full detail layout: back link, status badge, project info card (always visible), contact card (2 variants: unlocked with call/email CTAs, locked with amber bg + countdown + Premium CTA)
    - app/pages/app/dashboard.vue — auth guard, useAsyncData, NuxtLink patterns
    - .planning/phases/04-le-verrou-stripe-billing/04-PATTERNS.md §leads/[id].vue — route param usage, useAsyncData key
    - .planning/phases/04-le-verrou-stripe-billing/04-CONTEXT.md §D-19 — detail shows all project info + coordonnées if débloqué
  </read_first>

  <action>
  Create app/pages/espace/leads/[id].vue:

  Script setup:
  - useRoute() to get route.params.id
  - Auth guard: `watchEffect(() => { if (user.value === null) navigateTo('/pro/claim') })`
  - useAsyncData key: `\`lead-detail-${route.params.id}\``; fetch `$fetch(\`/api/v1/leads/${route.params.id}\`)`; return `data?.lead`
  - NO lucide imports. All icons are SVG inline in the template.
  - Import LeadCountdown from '@/components/LeadCountdown.vue'
  - useHead({ title: computed(() => `Lead ${lead.value?.category ?? ''} — BÂTI-AXE`) })

  Template structure (per UI-SPEC §Page 2 — monochrome tokens only):
  - Outer: `<div class="max-w-2xl mx-auto px-6 py-16">`
  - Back link: `<NuxtLink to="/espace/leads" class="flex items-center gap-1 text-sm text-muted-foreground mb-6 hover:text-foreground">← Retour à mes leads</NuxtLink>`
  - Loading skeleton (v-if="pending"): `<div class="h-8 bg-muted rounded animate-pulse mb-4 w-64" />`
  - Error (v-else-if="error"): `border border-destructive/30 text-destructive text-sm p-4 rounded-lg`, error message
  - Content (v-else-if="lead"): status badge (use same badge tokens as index page) + category h1 `text-3xl font-black tracking-tight text-foreground` + received date `text-xs text-muted-foreground`
  - Two-column grid on md+: `<div class="grid md:grid-cols-2 gap-6 mt-6">`

  Left card — "Infos projet" (always visible): `<div class="border border-border rounded-lg divide-y divide-border">`
  - Header `px-5 py-4`: `<p class="text-xs font-medium text-muted-foreground tracking-widest uppercase">Infos projet</p>`
  - Body `px-5 py-4 space-y-2`: dl pairs — Catégorie (CATEGORY_LABELS), Budget (lead.budget_range), Délai (lead.timeline_range) — all `text-sm text-muted-foreground`
  - Description row: if status === 'unlocked' → `text-sm text-foreground`; else `<span class="text-muted-foreground font-mono select-none">*** *** ***</span>`

  Right card — "Contact prospect" (conditional on status):
  - If status === 'unlocked': `<div class="border border-foreground/30 rounded-lg divide-y divide-border">`
    - Header `px-5 py-4 flex items-center gap-2`: SVG coche + `<p class="text-sm font-semibold text-foreground">Coordonnées disponibles</p>`
    - Body `px-5 py-4 space-y-3`: customer_name `font-medium text-foreground`; customer_phone as `<a :href="\`tel:${lead.customer_phone}\`" class="text-foreground font-medium hover:underline flex items-center gap-1.5">` + SVG téléphone; customer_email similarly + SVG email
    - Footer `px-5 py-4 flex gap-2`: `<a :href="\`tel:${lead.customer_phone}\`" class="flex-1 bg-foreground text-background text-sm font-semibold py-2 px-3 rounded-md text-center">` SVG téléphone "Appeler" and `<a :href="\`mailto:${lead.customer_email}\`" class="flex-1 border border-border text-foreground text-sm font-semibold py-2 px-3 rounded-md text-center">` SVG email "Email"
  - If status === 'locked': `<div class="border border-amber-300 bg-amber-50 rounded-lg divide-y divide-amber-200">`
    - Header `px-5 py-4 flex items-center gap-2`: SVG verrou fermé `text-amber-700` + `<p class="text-sm font-semibold text-amber-700">Coordonnées non disponibles</p>`
    - Body `px-5 py-4 space-y-2`: masked fields `<span class="text-muted-foreground font-mono select-none">*** *** ***</span>`; LeadCountdown :unlocked-at="lead.unlocked_at"
    - Footer `px-5 py-4`: CTA `<NuxtLink to="/espace/premium" class="bg-foreground text-background text-sm font-semibold py-2 px-4 rounded-md w-full text-center block">` SVG étoile "Passer Premium pour voir maintenant"
  - If status === 'claimed': `<div class="border border-border rounded-lg p-5">`
    - `<p class="text-sm text-muted-foreground text-center">Ce lead a déjà été attribué</p>`
  </action>

  <verify>
    <automated>npm run typecheck 2>&1 | grep -E "error TS" | head -10</automated>
  </verify>

  <done>
  - leads/[id].vue: auth guard, useAsyncData with dynamic key, back link to /espace/leads
  - Project info card always visible (category, budget, timeline, description)
  - Contact card: unlocked shows real data with tel:/mailto: links and call/email buttons; locked shows blur + countdown + Premium CTA; claimed shows attribution message
  - LeadCountdown reused from Plan 1 of this wave
  - aria-hidden on masked spans
  - TypeScript clean
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API response → Vue template | Data arrives pre-masked; Vue must not perform any further unmasking logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-17 | Information Disclosure | leads/index.vue template | mitigate | UI renders '*** *** ***' strings from server — no conditional JavaScript that reveals underlying data. blur-[2px] is decorative only (ADR-004) |
| T-04-18 | Information Disclosure | leads/[id].vue template | mitigate | Same — all masking is done server-side. Vue only checks lead.status to decide which card variant to render |
| T-04-19 | Elevation of Privilege | /espace/leads navigation | mitigate | watchEffect auth guard redirects to /pro/claim if user.value === null |
| T-04-SC | Tampering | npm installs | accept | No new npm packages in this plan |
</threat_model>

<verification>
After both tasks complete:
- `grep "lucide" app/pages/espace/leads/index.vue app/pages/espace/leads/[id].vue app/components/LeadCountdown.vue` → must return nothing (no lucide imports)
- `grep -E "emerald|sky-[0-9]|slate-[0-9]" app/pages/espace/leads/index.vue app/pages/espace/leads/[id].vue` → must return nothing
- `grep "divide-border" app/pages/espace/leads/index.vue` → card structure present
- `grep "\$fetch.*api/v1/leads" app/pages/espace/leads/index.vue` → API endpoint wired correctly
- `grep "watchEffect" app/pages/espace/leads/index.vue app/pages/espace/leads/[id].vue` → auth guard in both files
- `grep "upgrade=success" app/pages/espace/leads/index.vue` → success banner condition present
- `npm run typecheck` → exit 0
</verification>

<success_criteria>
- /espace/leads renders 3 card variants correctly based on lead.status
- Masked fields have blur-[2px] CSS (decorative) AND aria-hidden (accessibility)
- LeadCountdown component shows hours/minutes remaining until unlock
- Premium banner shown when BASIC pro has locked leads
- ?upgrade=success banner shown and auto-dismissed after 6s
- /espace/leads/[id] shows conditional contact card based on status
- TypeScript clean
</success_criteria>

<output>
Create `.planning/phases/04-le-verrou-stripe-billing/04-06-SUMMARY.md` when done
</output>
