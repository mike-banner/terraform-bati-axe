---
phase: 04-le-verrou-stripe-billing
plan: 06
type: execute
wave: 4
depends_on: ["04-03", "04-04"]
files_modified:
  - app/pages/espace/leads/index.vue
  - app/pages/espace/leads/[id].vue
  - app/components/LeadCountdown.vue
autonomous: true
requirements: [LCK-01, LCK-02]

must_haves:
  truths:
    - "Pro can visit /espace/leads and see their leads as cards (grid sm:grid-cols-2 lg:grid-cols-3)"
    - "Locked lead card shows masked fields with blur-[2px] CSS, amber countdown, and 'Passer Premium' CTA"
    - "Unlocked lead card shows emerald-50 background with real data and 'Voir le contact' CTA"
    - "Claimed lead card shows slate-100 background with masked data and no CTA"
    - "BASIC pro with ≥1 locked lead sees the amber banner at top of page"
    - "Unauthenticated user is redirected to /pro/claim"
    - "3 skeleton cards shown during useAsyncData loading"
    - "?upgrade=success banner appears and auto-dismisses after 6s"
    - "Lead detail page /espace/leads/[id] shows project info + conditional contact card"
  artifacts:
    - path: "app/pages/espace/leads/index.vue"
      provides: "Lead dashboard with 3 card variants"
      contains: "blur-[2px]"
    - path: "app/pages/espace/leads/[id].vue"
      provides: "Lead detail with conditional contact card"
      contains: "emerald-50"
    - path: "app/components/LeadCountdown.vue"
      provides: "Reusable countdown component"
      contains: "ClockIcon"
  key_links:
    - from: "app/pages/espace/leads/index.vue"
      to: "server/api/v1/leads/index.get.ts"
      via: "$fetch('/api/v1/leads')"
      pattern: "api/v1/leads"
    - from: "app/pages/espace/leads/[id].vue"
      to: "server/api/v1/leads/[id].get.ts"
      via: "$fetch(`/api/v1/leads/${route.params.id}`)"
      pattern: "api/v1/leads"
---

<objective>
Build the leads dashboard (/espace/leads) and lead detail (/espace/leads/[id]) with all three card variants and the shared LeadCountdown component.

Purpose: This is the primary user-facing surface for LCK-01. Pros see their leads with the teasing blur effect on locked cards, emerald highlighting on unlocked cards, and a clear "Déjà attribué" state for claimed cards. The data arrives pre-masked from the API — the UI never unmasks data, only renders what the server sends.

Output:
- app/pages/espace/leads/index.vue
- app/pages/espace/leads/[id].vue
- app/components/LeadCountdown.vue
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-le-verrou-stripe-billing/04-CONTEXT.md
@.planning/phases/04-le-verrou-stripe-billing/04-RESEARCH.md
@.planning/phases/04-le-verrou-stripe-billing/04-UI-SPEC.md
@.planning/phases/04-le-verrou-stripe-billing/04-PATTERNS.md
@app/pages/app/dashboard.vue

<interfaces>
<!-- API response shapes from Plans 03/04 -->

GET /api/v1/leads → { leads: MaskedLead[] }

MaskedLead shapes:
```typescript
// Locked (BASIC, <72h, not claimed)
{ id, status: 'locked', unlocked_at: string | null, created_at, category, budget_range, timeline_range,
  customer_name: '*** *** ***', customer_phone: '*** *** ***', customer_email: 'contact@***.fr', postal_code: '***' }

// Unlocked (Premium or BASIC + 72h elapsed)
{ id, status: 'unlocked', unlocked_at, created_at, category, budget_range, timeline_range, description,
  customer_name, customer_phone, customer_email, postal_code }

// Claimed (Premium took it, BASIC sees this)
{ id, status: 'claimed', created_at, category, budget_range }
```

GET /api/v1/leads/[id] → { lead: MaskedLead }
(same shapes as above, single object)

<!-- Established Vue patterns from dashboard.vue -->
Auth guard: watchEffect(() => { if (user.value === null) navigateTo('/pro/claim') })
useAsyncData key convention: 'pro-leads', 'lead-detail-${id}'
Layout: max-w-5xl mx-auto px-4 sm:px-6 (upgraded from dashboard's max-w-2xl per UI-SPEC container spec)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build LeadCountdown component and leads index page</name>
  <files>
    app/components/LeadCountdown.vue,
    app/pages/espace/leads/index.vue
  </files>

  <read_first>
    - .planning/phases/04-le-verrou-stripe-billing/04-UI-SPEC.md §Page 1 and §Tokens & Classes Partagés — full card layout, 3 variants, banner, skeleton, badge tokens, countdown component spec, blur CSS rules
    - app/pages/app/dashboard.vue — auth guard (lines 7-10), useAsyncData pattern (lines 13-21), template structure (lines 81-179)
    - .planning/phases/04-le-verrou-stripe-billing/04-PATTERNS.md §leads/index.vue — adaptation notes including $fetch usage (not direct Supabase client)
    - .planning/phases/04-le-verrou-stripe-billing/04-CONTEXT.md §D-16 D-17 D-18 — cards layout, CTA rules per status
  </read_first>

  <action>
  Create app/components/LeadCountdown.vue:
  Props: `{ unlockedAt: string }` (the unlocked_at ISO string from the lead)
  Computed: calculate remaining time as `Math.max(0, new Date(unlockedAt).getTime() - Date.now())`; derive hours = Math.floor(ms / 3600000); minutes = Math.floor((ms % 3600000) / 60000)
  Interval: use `onMounted` + `setInterval` with 60000ms (60s) to refresh countdown — not aggressive polling
  Use `onUnmounted` to clear the interval
  Template: `<span class="flex items-center gap-1 text-amber-600 text-xs font-semibold"><ClockIcon class="w-3 h-3" />Disponible dans {{ hours }}h {{ minutes }}min</span>`
  Import ClockIcon from 'lucide-vue-next'

  Create app/pages/espace/leads/index.vue:

  Script setup:
  - Imports: useSupabaseUser, useSupabaseClient (for auth only, not for data), watchEffect, navigateTo, useAsyncData, useRoute, ref, computed
  - Import lucide icons: LockKeyholeIcon, UnlockIcon, StarIcon, BuildingIcon from 'lucide-vue-next'
  - Import Badge from '@/components/ui/badge' (shadcn)
  - Import LeadCountdown from '@/components/LeadCountdown.vue'
  - useHead({ title: 'Mes leads — BÂTI-AXE' })
  - Auth guard: `watchEffect(() => { if (user.value === null) navigateTo('/pro/claim') })`
  - useAsyncData: key 'pro-leads', fetches `$fetch('/api/v1/leads')`, returns `data?.leads || []`
  - Computed `hasLockedLeads`: `leads.value?.some(l => l.status === 'locked') ?? false`
  - Computed `isBasic`: check from leads data — if any lead exists and pro has locked leads, implies BASIC. Actually: detect from API — the presence of locked leads means BASIC. (The API itself applies masking based on subscription_status server-side)
  - route to detect `?upgrade=success`: `const showSuccessBanner = ref(useRoute().query.upgrade === 'success')`; set up `onMounted` to auto-dismiss after 6000ms: `if (showSuccessBanner.value) setTimeout(() => { showSuccessBanner.value = false }, 6000)`
  - CATEGORY_LABELS map: same as admin/index.vue

  Template structure (per UI-SPEC §Page 1):
  - Outer: `<div class="min-h-screen bg-slate-50"><div class="max-w-5xl mx-auto px-4 sm:px-6 py-10">`
  - Success banner (v-if="showSuccessBanner"): bg-emerald-50 border border-emerald-200 p-4 rounded-xl, CheckCircle2Icon, "Bienvenue dans BÂTI-AXE Premium ! Vos leads sont maintenant débloqués."
  - Premium banner (v-if="hasLockedLeads"): bg-amber-50 border border-amber-200 rounded-xl p-4, StarIcon, text + NuxtLink to /espace/premium
  - Page header: h1 "Mes leads" text-2xl font-bold text-slate-900 + Badge with count
  - Skeleton loading (v-if="pending"): `<div v-for="i in 3" :key="i" class="bg-slate-100 rounded-xl h-[180px] animate-pulse" />` inside grid
  - Empty state (v-else-if="!leads?.length"): BuildingIcon w-12 h-12 + "Aucun lead pour l'instant"
  - Lead grid (v-else): `<div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">`

  Card rendering — use v-for on leads, with v-if/v-else-if/v-else on lead.status:

  Variant A (status === 'locked'): bg-white border border-slate-200 rounded-xl p-5 shadow-sm
  - Badge: bg-slate-100 text-slate-500 + LockKeyholeIcon "Flouté"
  - Category: CATEGORY_LABELS[lead.category]
  - Budget: lead.budget_range
  - Timeline: lead.timeline_range
  - Masked fields: `<span class="text-slate-300 font-mono blur-[2px] select-none pointer-events-none aria-hidden">*** *** ***</span>` for postal_code, customer_name, customer_phone — aria-hidden="true" on each
  - LeadCountdown component: `<LeadCountdown :unlocked-at="lead.unlocked_at" />` (only if unlocked_at is not null)
  - CTA: `<NuxtLink to="/espace/premium" class="bg-amber-500 hover:bg-amber-600 text-white text-sm font-semibold py-2 px-4 rounded-lg w-full text-center block transition-colors duration-150 focus-visible:ring-2 focus-visible:ring-amber-500 focus-visible:ring-offset-2"><StarIcon class="w-4 h-4 inline mr-1" />Passer Premium</NuxtLink>`

  Variant B (status === 'unlocked'): bg-emerald-50 border border-emerald-200 rounded-xl p-5
  - Badge: bg-emerald-100 text-emerald-700 + UnlockIcon "Débloqué"
  - Category, budget, timeline visible
  - postal_code, customer_name in text-slate-900, customer_phone in text-sky-700
  - CTA: `<NuxtLink :to="\`/espace/leads/${lead.id}\`" class="border border-sky-600 text-sky-700 hover:bg-sky-50 text-sm font-semibold py-2 px-4 rounded-lg w-full text-center block transition-colors duration-150">→ Voir le contact</NuxtLink>`

  Variant C (status === 'claimed'): bg-slate-100 border border-slate-200 rounded-xl p-5 opacity-75
  - Badge: bg-slate-200 text-slate-500 "Déjà attribué"
  - Category, budget visible in text-slate-400
  - Masked fields (same blur pattern as Variant A) for consistency
  - No CTA: `<p class="text-slate-400 text-xs text-center py-2">Ce lead a déjà été attribué</p>`

  IMPORTANT from UI-SPEC anti-patterns: no blur on entire card (only on masked text fields), no emoji icons (Lucide only), no table layout.
  </action>

  <verify>
    <automated>npm run typecheck 2>&1 | grep -E "error TS" | head -10</automated>
  </verify>

  <done>
  - LeadCountdown.vue: ClockIcon, 60s interval, hours/minutes computed from unlocked_at
  - leads/index.vue: auth guard (watchEffect), useAsyncData with $fetch, 3 card variants, skeleton loading, empty state, Premium banner (v-if hasLockedLeads), success banner (?upgrade=success, 6s auto-dismiss)
  - blur-[2px] applied only to masked text spans, not to cards
  - aria-hidden="true" on all masked spans
  - All masked spans have select-none pointer-events-none
  - TypeScript compiles clean
  </done>
</task>

<task type="auto">
  <name>Task 2: Build lead detail page /espace/leads/[id]</name>
  <files>app/pages/espace/leads/[id].vue</files>

  <read_first>
    - .planning/phases/04-le-verrou-stripe-billing/04-UI-SPEC.md §Page 2 — full detail layout: back link, status badge, project info card (always visible), contact card (2 variants: unlocked with call/email CTAs, locked with amber bg + countdown + Premium CTA)
    - app/pages/app/dashboard.vue — auth guard, useAsyncData, NuxtLink patterns
    - .planning/phases/04-le-verrou-stripe-billing/04-PATTERNS.md §leads/[id].vue — route param usage, useAsyncData key
    - .planning/phases/04-le-verrou-stripe-billing/04-CONTEXT.md §D-19 — detail shows all project info + coordonnées if débloqué
  </read_first>

  <action>
  Create app/pages/espace/leads/[id].vue:

  Script setup:
  - useRoute() to get route.params.id
  - Auth guard: `watchEffect(() => { if (user.value === null) navigateTo('/pro/claim') })`
  - useAsyncData key: `\`lead-detail-${route.params.id}\``; fetch `$fetch(\`/api/v1/leads/${route.params.id}\`)`; return `data?.lead`
  - Import: LockKeyholeIcon, UnlockIcon, PhoneIcon, MailIcon, ClockIcon, CheckCircle2Icon from 'lucide-vue-next'
  - Import LeadCountdown from '@/components/LeadCountdown.vue'
  - useHead({ title: computed(() => `Lead ${lead.value?.category ?? ''} — BÂTI-AXE`) })

  Template structure (per UI-SPEC §Page 2):
  - Outer: `<div class="max-w-5xl mx-auto px-4 sm:px-6 py-10">`
  - Back link: `<NuxtLink to="/espace/leads" class="flex items-center gap-1 text-sm text-sky-700 mb-6 hover:text-sky-800">← Retour à mes leads</NuxtLink>`
  - Loading skeleton (v-if="pending"): `<div class="h-8 bg-slate-100 rounded animate-pulse mb-4 w-64" />`
  - Error (v-else-if="error"): bg-red-50 border border-red-200, error message
  - Content (v-else-if="lead"): status badge + category title + received date (formatted fr-FR)
  - Two-column grid on md+: `<div class="grid md:grid-cols-2 gap-6 mt-6">`

  Left card — "Infos projet" (always visible): bg-white border border-slate-200 rounded-xl p-5
  - dl with dt/dd pairs: Catégorie (CATEGORY_LABELS), Budget (lead.budget_range), Délai (lead.timeline_range)
  - Description: if status === 'unlocked' → lead.description; else `<span class="text-slate-300 blur-[2px] select-none" aria-hidden="true">*** *** ***</span>`

  Right card — "Contact prospect" (conditional on status):
  - If status === 'unlocked': bg-emerald-50 border border-emerald-200 rounded-xl p-5
    - CheckCircle2Icon + "Coordonnées disponibles" text-emerald-700
    - customer_name (text-slate-900 font-medium)
    - customer_phone as `<a :href="\`tel:${lead.customer_phone}\`" class="text-sky-700 font-medium hover:underline">` with PhoneIcon
    - customer_email as `<a :href="\`mailto:${lead.customer_email}\`" class="text-sky-700 font-medium hover:underline">` with MailIcon
    - Two buttons: `<a :href="\`tel:${lead.customer_phone}\`" class="...outline sm button...">` PhoneIcon "Appeler" and `<a :href="\`mailto:${lead.customer_email}\`" class="...">` MailIcon "Envoyer un email"
  - If status === 'locked': bg-amber-50 border border-amber-200 rounded-xl p-5
    - LockKeyholeIcon + "Coordonnées non disponibles" text-amber-700
    - Masked fields (blur-[2px] select-none aria-hidden)
    - LeadCountdown :unlocked-at="lead.unlocked_at"
    - CTA: `<NuxtLink to="/espace/premium" class="bg-amber-500 hover:bg-amber-600 text-white ...">★ Passer Premium pour voir maintenant</NuxtLink>`
  - If status === 'claimed': bg-slate-100 border border-slate-200 rounded-xl p-5
    - "Ce lead a déjà été attribué" text-slate-500
  </action>

  <verify>
    <automated>npm run typecheck 2>&1 | grep -E "error TS" | head -10</automated>
  </verify>

  <done>
  - leads/[id].vue: auth guard, useAsyncData with dynamic key, back link to /espace/leads
  - Project info card always visible (category, budget, timeline, description)
  - Contact card: unlocked shows real data with tel:/mailto: links and call/email buttons; locked shows blur + countdown + Premium CTA; claimed shows attribution message
  - LeadCountdown reused from Plan 1 of this wave
  - aria-hidden on masked spans
  - TypeScript clean
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API response → Vue template | Data arrives pre-masked; Vue must not perform any further unmasking logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-17 | Information Disclosure | leads/index.vue template | mitigate | UI renders '*** *** ***' strings from server — no conditional JavaScript that reveals underlying data. blur-[2px] is decorative only (ADR-004) |
| T-04-18 | Information Disclosure | leads/[id].vue template | mitigate | Same — all masking is done server-side. Vue only checks lead.status to decide which card variant to render |
| T-04-19 | Elevation of Privilege | /espace/leads navigation | mitigate | watchEffect auth guard redirects to /pro/claim if user.value === null |
| T-04-SC | Tampering | npm installs | accept | No new npm packages in this plan |
</threat_model>

<verification>
After both tasks complete:
- `grep "blur-\[2px\]" app/pages/espace/leads/index.vue` → present on masked text spans, not on card divs
- `grep "aria-hidden" app/pages/espace/leads/index.vue` → present on each masked span
- `grep "\$fetch.*api/v1/leads" app/pages/espace/leads/index.vue` → API endpoint wired correctly
- `grep "watchEffect" app/pages/espace/leads/index.vue app/pages/espace/leads/[id].vue` → auth guard in both files
- `grep "upgrade=success" app/pages/espace/leads/index.vue` → success banner condition present
- `npm run typecheck` → exit 0
</verification>

<success_criteria>
- /espace/leads renders 3 card variants correctly based on lead.status
- Masked fields have blur-[2px] CSS (decorative) AND aria-hidden (accessibility)
- LeadCountdown component shows hours/minutes remaining until unlock
- Premium banner shown when BASIC pro has locked leads
- ?upgrade=success banner shown and auto-dismissed after 6s
- /espace/leads/[id] shows conditional contact card based on status
- TypeScript clean
</success_criteria>

<output>
Create `.planning/phases/04-le-verrou-stripe-billing/04-06-SUMMARY.md` when done
</output>
