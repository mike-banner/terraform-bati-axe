---
phase: 04-le-verrou-stripe-billing
plan: 07
type: execute
wave: 4
depends_on: ["04-05"]
files_modified:
  - app/pages/espace/premium.vue
  - app/pages/admin/index.vue
  - server/api/v1/admin/projects.get.ts
autonomous: false
requirements: [LCK-01, LCK-02]

must_haves:
  truths:
    - "Pro can visit /espace/premium and see Hero → comparison table → price card → FAQ"
    - "Price card is bg-amber-50 border-2 border-amber-400 with amber-500 CTA button"
    - "'Démarrer Premium' button calls POST /api/v1/stripe/checkout and redirects to Stripe Checkout URL"
    - "If pro is already Premium, the page shows their active subscription status"
    - "Admin panel has a 'Qualifier' button on each project card"
    - "Qualifier button calls POST /api/v1/admin/qualify and shows leads_created count"
    - "Qualifier button is disabled if project is already qualified"
  artifacts:
    - path: "app/pages/espace/premium.vue"
      provides: "Premium subscription page with Stripe Checkout CTA"
      contains: "bg-amber-50"
    - path: "app/pages/admin/index.vue"
      provides: "Admin panel with Qualify button added to project section"
      contains: "qualify"
  key_links:
    - from: "app/pages/espace/premium.vue"
      to: "server/api/v1/stripe/checkout.post.ts"
      via: "$fetch('/api/v1/stripe/checkout', { method: 'POST' })"
      pattern: "stripe/checkout"
    - from: "app/pages/admin/index.vue"
      to: "server/api/v1/admin/qualify.post.ts"
      via: "$fetch('/api/v1/admin/qualify', { method: 'POST', body: { project_id } })"
      pattern: "qualify"
---

<objective>
Build the Premium subscription page and add the Qualify button to the admin panel.

Purpose: The Premium page is the monetization entry point (LCK-02 front-end) — it presents the value proposition and initiates the Stripe Checkout flow. The Qualify button in the admin panel (LCK-01 front-end) is how leads are created — admins click it after reviewing a project and it triggers the qualify endpoint to generate leads for all matching pros.

Output:
- app/pages/espace/premium.vue — full Premium sales page
- app/pages/admin/index.vue — Qualify button added to projects section (MODIFY existing file)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-le-verrou-stripe-billing/04-CONTEXT.md
@.planning/phases/04-le-verrou-stripe-billing/04-RESEARCH.md
@.planning/phases/04-le-verrou-stripe-billing/04-UI-SPEC.md
@.planning/phases/04-le-verrou-stripe-billing/04-PATTERNS.md
@app/pages/app/dashboard.vue
@app/pages/admin/index.vue

<interfaces>
<!-- From admin/index.vue (existing file — to be modified) -->
Key structures:
- Professional interface at line 29 — does NOT yet have `projects` array or qualify-related fields
- professionals ref contains the list of professionals
- The admin panel currently only shows PRO cards (professionals list), NOT projects
- The qualify button needs to appear on the PROJECTS section, which does not currently exist in the admin panel
- The admin panel must be extended: add a new "Projets" tab or section that fetches projects from a new API endpoint (or directly from Supabase client with service role context)

IMPORTANT: The admin panel currently does NOT have a projects tab. The qualify button must be added to a new "Projets en attente" section. The simplest approach: add a third tab "Projets" that fetches projects where status='pending' or status='qualified', and shows a "Qualifier" button for pending projects.

Alternatively: use the existing admin/queue.get.ts model and add a new endpoint server/api/v1/admin/projects.get.ts — however this would require an additional file. Since the task is specifically "add Qualifier button to admin panel", the approach is:
1. Add a new ref `projects` and a `fetchProjects()` method that calls $fetch('/api/v1/admin/projects')
2. Add a "Projets" tab to the header tab row
3. Add a projects list section showing project cards with "Qualifier" button

However: creating server/api/v1/admin/projects.get.ts is OUT OF SCOPE for Plan 07 (would increase context cost beyond budget). Instead, use direct Supabase client in the Vue component for the admin projects fetch — admin panel already uses direct Supabase client for other views. The RLS policy for projects is service_role only, but the admin user's session has direct service role access in this app pattern (see queue.get.ts — actually the admin fetches via $fetch, not direct Supabase).

REVISED APPROACH: Create server/api/v1/admin/projects.get.ts inline as part of this plan's context, but document it in files_modified. Wait — this would exceed the 2-3 task budget with reasonable context use.

FINAL APPROACH (minimal scope): Add Qualify button to the existing admin panel's "Tous les pros" list. For each VERIFIED pro, show a "Qualifier un projet" UI — but this is wrong (qualify is per PROJECT not per pro).

CORRECT FINAL APPROACH: Add a new "Projets" tab to the admin panel. The tab fetches `/api/v1/admin/projects` (create this lightweight GET endpoint in this plan). Show projects list with status and Qualify button. This is 2 tasks: (1) projects.get.ts + premium.vue, (2) admin index.vue modification + checkpoint.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build /espace/premium page and admin projects endpoint</name>
  <files>
    app/pages/espace/premium.vue,
    server/api/v1/admin/projects.get.ts
  </files>

  <read_first>
    - .planning/phases/04-le-verrou-stripe-billing/04-UI-SPEC.md §Page 3 — full Premium page layout: Hero, stat blocks, comparison table, price card (bg-amber-50 border-2 border-amber-400), FAQ Accordion
    - .planning/phases/04-le-verrou-stripe-billing/04-PATTERNS.md §premium.vue — auth guard, pro profile fetch, startCheckout handler
    - app/pages/app/dashboard.vue — auth guard + useAsyncData pattern
    - server/api/v1/admin/queue.get.ts — admin auth + service role pattern (replicate for projects.get.ts)
    - .planning/phases/04-le-verrou-stripe-billing/04-CONTEXT.md §D-12 D-13 — 39€/mois placeholder via env var, page at /espace/premium
  </read_first>

  <action>
  Create app/pages/espace/premium.vue:

  Script setup:
  - Auth guard: `watchEffect(() => { if (user.value === null) navigateTo('/pro/claim') })`
  - useAsyncData key 'pro-premium': fetch pro profile from Supabase client directly: `.from('professionals').select('id, subscription_status').eq('id', user.value?.id).maybeSingle()`
  - `const isCheckingOut = ref(false)`
  - startCheckout handler:
    ```
    const startCheckout = async () => {
      isCheckingOut.value = true
      try {
        const data = await $fetch('/api/v1/stripe/checkout', { method: 'POST' })
        if (data?.url) window.location.href = data.url
      } catch (err: any) {
        // show error toast or inline error
      } finally {
        isCheckingOut.value = false
      }
    }
    ```
  - NO lucide imports. All icons are SVG inline (see UI-SPEC v2 §Icônes — verrou fermé, étoile, coche).
  - useHead({ title: 'Passer Premium — BÂTI-AXE' })

  Template (per UI-SPEC §Page 3 layout):
  Outer: `<div class="min-h-screen bg-slate-50"><div class="max-w-3xl mx-auto px-4 sm:px-6 py-12">`

  If pro.subscription_status === 'active': show "Votre abonnement Premium est actif" banner (emerald-50) with CheckCircle2Icon, no CTA to checkout.

  HERO section:
  - SVG étoile inline (from UI-SPEC §Icônes) `class="w-8 h-8 text-amber-500 mx-auto mb-4"`
  - h1: "Débloquez tous vos leads instantanément" text-3xl font-bold text-slate-900 text-center
  - Subtitle: "Ne perdez plus de chantiers à cause du délai 72h." text-lg text-slate-500 mt-2 text-center

  Stat blocks (3 columns): grid grid-cols-3 gap-4 mt-8
  - "+200 pros" | "Accès immédiat" | "39€/mois"

  COMPARISON TABLE (mt-12):
  - `<table class="w-full border border-slate-200 rounded-xl overflow-hidden text-sm">`
  - Header: "" | "BASIC" | "PREMIUM ★"
  - Rows: "Voir les leads" ✓/✓, "Coordonnées immédiates" ✗/✓, "Accès après 72h" ✓/✓, "Priorité contacts" ✗/✓
  - Use SVG coche inline `stroke="currentColor" class="w-3.5 h-3.5 text-foreground"` for ✓ and SVG verrou fermé `class="w-3.5 h-3.5 text-muted-foreground"` for ✗

  PRICE CARD (mt-12): `<div class="bg-amber-50 border-2 border-amber-400 rounded-2xl p-8 max-w-md mx-auto">`
  - Badge "★ PREMIUM" bg-amber-400 text-white text-xs px-3 py-1 rounded-full inline-block mb-4
  - "39 €" text-4xl font-bold text-slate-900 + " / mois" text-lg text-slate-500
  - "Sans engagement · Annulable à tout moment" text-sm text-slate-500 mt-1
  - Feature list (3 items with SVG coche inline `class="w-3.5 h-3.5 text-foreground"`):
    - "Accès immédiat à toutes les coordonnées"
    - "Leads illimités dans votre catégorie"
    - "Avant les pros BASIC (exclusivité 72h)"
  - CTA button: `<button @click="startCheckout" :disabled="isCheckingOut" :aria-busy="isCheckingOut" class="bg-amber-500 hover:bg-amber-600 text-white font-semibold py-3 px-6 rounded-xl w-full text-base transition-colors duration-150 mt-6 focus-visible:ring-2 focus-visible:ring-amber-500 focus-visible:ring-offset-2 disabled:opacity-60">`
      - When isCheckingOut: `<svg class="w-4 h-4 animate-spin inline mr-2" fill="none" viewBox="0 0 24 24"><circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"/><path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4z"/></svg>Chargement...`
      - Else: "Démarrer Premium — 39€/mois"
  - Footer: `<p class="text-xs text-muted-foreground mt-3 text-center flex items-center justify-center gap-1">` + SVG verrou fermé `class="w-3.5 h-3.5"` + "Paiement sécurisé par Stripe"

  FAQ (mt-12): Use `<details>/<summary>` HTML native accordion (no shadcn Accordion dependency needed):
  - Q: "Puis-je annuler à tout moment ?" → A: "Oui, sans frais et immédiatement depuis votre espace."
  - Q: "Que se passe-t-il si j'annule ?" → A: "Votre accès Premium reste actif jusqu'à la fin de la période en cours."
  - Q: "Le paiement est-il sécurisé ?" → A: "Oui, les paiements sont traités par Stripe, certifié PCI DSS."

  Create server/api/v1/admin/projects.get.ts (lightweight — needed to populate the admin Projets tab):
  - Exact same admin auth pattern as queue.get.ts: serverSupabaseUser → 401, admin check → 403, serverSupabaseServiceRole
  - Fetch: `.from('projects').select('id, category, status, description, budget_range, timeline_range, created_at').order('created_at', { ascending: false })`
  - Return: `{ projects }`
  </action>

  <verify>
    <automated>npm run typecheck 2>&1 | grep -E "error TS" | head -10</automated>
  </verify>

  <done>
  - premium.vue: auth guard, Pro subscription status shown if already active, Hero → comparison → price card (amber-50 border-amber-400) → FAQ
  - Price card CTA calls /api/v1/stripe/checkout, disables button during loading (aria-busy, Loader2Icon)
  - admin/projects.get.ts: admin-only, returns projects list ordered by created_at desc
  - TypeScript clean
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Projets tab with Qualify button to admin panel</name>
  <files>app/pages/admin/index.vue</files>

  <read_first>
    - app/pages/admin/index.vue — FULL FILE. Understand the existing tab structure (activeTab ref: 'pending' | 'all'), the fetchQueue() pattern, the $fetch calls, the Professional interface and template structure. The Qualify button must be added to a NEW "Projets" tab — not to the existing pros tabs.
    - .planning/phases/04-le-verrou-stripe-billing/04-CONTEXT.md §D-03 — qualify flow: project → status 'qualified' + leads created
    - server/api/v1/admin/qualify.post.ts — the endpoint called: POST { project_id: string }, returns { qualified: boolean, leads_created: number }
  </read_first>

  <action>
  Modify app/pages/admin/index.vue to add a "Projets" tab section. This is an ADDITIVE modification — do not remove or rewrite existing pro management code.

  Changes to `<script setup>`:
  1. Extend activeTab type: `ref<'pending' | 'all' | 'projects'>('pending')`
  2. Add new interface above Professional interface:
     ```typescript
     interface Project {
       id: string
       category: string | null
       status: string
       description: string | null
       budget_range: string | null
       timeline_range: string | null
       created_at: string
     }
     ```
  3. Add state: `const projects = ref<Project[]>([])` and `const qualifyLoading = ref<string | null>(null)` and `const qualifyResult = ref<Record<string, number>>({})` (to show leads_created per project)
  4. Add fetchProjects function:
     ```typescript
     const fetchProjects = async () => {
       isLoading.value = true
       errorMessage.value = null
       try {
         const data = await $fetch('/api/v1/admin/projects')
         projects.value = (data as any)?.projects || []
       } catch (err: any) {
         errorMessage.value = err.data?.statusMessage || err.message
       } finally {
         isLoading.value = false
       }
     }
     ```
  5. Update onMounted: `if (isAdmin.value) { fetchQueue(); fetchProjects() }`
  6. Add qualifyProject function:
     ```typescript
     const qualifyProject = async (projectId: string) => {
       qualifyLoading.value = projectId
       errorMessage.value = null
       try {
         const data = await $fetch('/api/v1/admin/qualify', { method: 'POST', body: { project_id: projectId } })
         qualifyResult.value[projectId] = (data as any)?.leads_created ?? 0
         await fetchProjects()
       } catch (err: any) {
         errorMessage.value = err.data?.statusMessage || err.message
       } finally {
         qualifyLoading.value = null
       }
     }
     ```

  Changes to `<template>`:
  1. Add third tab button in the header tab row (after "Tous les pros" button):
     ```html
     <button
       @click="activeTab = 'projects'; categoryFilter = ''"
       class="h-9 px-4 text-sm font-medium rounded-md border transition-colors"
       :class="activeTab === 'projects' ? 'bg-foreground text-background border-foreground' : 'border-border text-muted-foreground hover:text-foreground hover:bg-muted'"
     >
       Projets
     </button>
     ```

  2. Add projects list section inside `<div v-else class="space-y-6">` (after the existing Pro cards section), wrapped in `<template v-if="activeTab === 'projects'">`:
     - Empty state if no projects
     - Project cards: `<div v-for="project in projects" :key="project.id" class="border border-border rounded-lg overflow-hidden">`
       - Header: category badge (CATEGORY_LABELS), status badge (pending=amber, qualified=emerald), created_at date
       - Description and budget_range
       - Qualify button row:
         ```html
         <div class="flex items-center justify-between px-5 py-3 bg-muted/30 border-t border-border">
           <span v-if="qualifyResult[project.id] !== undefined" class="text-xs text-emerald-700 font-medium">
             ✓ {{ qualifyResult[project.id] }} lead(s) créé(s)
           </span>
           <span v-else class="text-xs text-muted-foreground">
             {{ project.status === 'qualified' ? 'Déjà qualifié' : 'Non qualifié' }}
           </span>
           <button
             @click="qualifyProject(project.id)"
             :disabled="project.status === 'qualified' || qualifyLoading === project.id"
             class="h-8 px-3 bg-sky-600 text-white text-xs font-semibold rounded-md hover:bg-sky-700 transition-colors disabled:opacity-40 disabled:cursor-not-allowed flex items-center gap-1.5"
           >
             <svg v-if="qualifyLoading === project.id" class="w-3 h-3 animate-spin" fill="none" viewBox="0 0 24 24">...</svg>
             Qualifier
           </button>
         </div>
         ```

  3. Wrap the EXISTING pros list in `<template v-if="activeTab !== 'projects'">` so it hides when Projets tab is active.
  </action>

  <verify>
    <automated>npm run typecheck 2>&1 | grep -E "error TS" | head -10</automated>
  </verify>

  <done>
  - admin/index.vue has 3 tabs: "En attente", "Tous les pros", "Projets"
  - Projets tab fetches from /api/v1/admin/projects
  - Each project shows category, status, description, budget_range
  - "Qualifier" button calls /api/v1/admin/qualify with project_id
  - Button disabled if project.status === 'qualified'
  - After qualify: shows "N lead(s) créé(s)" confirmation
  - Existing pro management tabs unaffected
  - TypeScript clean
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Premium page and Qualify flow end-to-end</name>

  <what-built>
  - /espace/premium page with Hero, comparison table, price card (amber-50), and Stripe Checkout CTA
  - Admin panel "Projets" tab with Qualify button per project

  Before this checkpoint: ensure you have a Stripe test mode price set up (Dashboard → Products → create a 39€/mois product → copy the price ID to STRIPE_PRICE_ID in .env). Also ensure STRIPE_SECRET_KEY is set to your sk_test_... key.
  </what-built>

  <how-to-verify>
  1. Start the dev server: `npm run dev`
  2. Visit http://localhost:3000/espace/premium — confirm the page loads with Hero, stat blocks, comparison table, amber price card, and Démarrer Premium CTA button
  3. If subscription_status is not 'active': click "Démarrer Premium" — confirm it redirects to Stripe Checkout (test mode). Complete with Stripe test card 4242 4242 4242 4242. Confirm redirect to /espace/leads?upgrade=success. Confirm success banner appears and disappears after ~6 seconds.
  4. Visit http://localhost:3000/admin — go to "Projets" tab. Confirm projects list appears. Click "Qualifier" on a pending project. Confirm "N lead(s) créé(s)" confirmation appears. Confirm button becomes disabled.
  5. Visit http://localhost:3000/espace/leads as a verified pro — confirm lead cards appear (may need to refresh after qualifying a project in step 4).
  </how-to-verify>

  <resume-signal>Type "verified" if the Premium page renders correctly and the Qualify button creates leads, or describe any issues found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → POST /api/v1/stripe/checkout | User-authenticated — must not expose STRIPE_SECRET_KEY to client |
| admin → POST /api/v1/admin/qualify | Admin-only — Projets tab must only be accessible to admins |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-20 | Elevation of Privilege | admin/index.vue Projets tab | mitigate | Tab is inside `v-else` of `v-if="!isAdmin"` block — only rendered for isAdmin === true users |
| T-04-21 | Information Disclosure | premium.vue startCheckout | mitigate | STRIPE_SECRET_KEY accessed server-side only via runtimeConfig; client only receives { url } redirect string |
| T-04-22 | Tampering | Qualify button | mitigate | project_id sent as body param; server-side qualify.post.ts validates UUID with Zod and enforces admin check |
| T-04-23 | Tampering | premium.vue double-click | mitigate | isCheckingOut ref disables button during $fetch in-flight — prevents duplicate Checkout Sessions |
| T-04-SC | Tampering | npm installs | accept | No new npm packages in this plan |
</threat_model>

<verification>
After all tasks complete:
- `grep "lucide" app/pages/espace/premium.vue` → must return nothing (no lucide imports)
- `grep "bg-amber-50.*border-amber-400\|border-amber-400.*bg-amber-50\|border-2 border-amber-400" app/pages/espace/premium.vue` → price card styling present
- `grep "stripe/checkout" app/pages/espace/premium.vue` → checkout endpoint wired
- `grep "qualify" app/pages/admin/index.vue` → qualify function and button present
- `grep "projects" app/pages/admin/index.vue | grep "tab\|activeTab"` → Projets tab present
- `grep "leads_created\|qualifyResult" app/pages/admin/index.vue` → result display present
- `npm run typecheck` → exit 0
- Human checkpoint: Premium page renders, Qualify creates leads
</verification>

<success_criteria>
- /espace/premium renders amber price card with Stripe CTA (39€/mois, D-12/D-13)
- Stripe Checkout initiated from CTA, success redirect to /espace/leads?upgrade=success
- Admin "Projets" tab shows projects with Qualify button (D-03)
- Qualify button creates leads and shows confirmation count
- TypeScript clean
</success_criteria>

<output>
Create `.planning/phases/04-le-verrou-stripe-billing/04-07-SUMMARY.md` when done
</output>
