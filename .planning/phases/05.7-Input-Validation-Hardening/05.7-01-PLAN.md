---
phase: 05.7-Input-Validation-Hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/api/v1/pro/claim.post.ts
  - server/api/v1/projects.post.ts
  - server/api/v1/messages/index.post.ts
  - server/api/v1/pro/realisations/index.post.ts
  - supabase/migrations/20260719000000_phase57_input_limits.sql
autonomous: false
requirements: []
must_haves:
  truths:
    - "Tous les schémas Zod ont une borne haute stricte (.max) pour les strings"
    - "La base de données rejette les strings anormalement longs via des contraintes CHECK"
  artifacts:
    - path: "supabase/migrations/20260719000000_phase57_input_limits.sql"
      provides: "CHECK constraints pour full_name, company_name, email, policy_number, etc."

---

<objective>
Durcir la validation backend en ajoutant des limites hautes `.max()` sur tous les schémas Zod identifiés dans l'audit, et créer une migration Supabase appliquant des `CHECK` constraints sur les tables correspondantes pour prévenir toute injection ou payload disproportionné.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/05.7-Input-Validation-Hardening/05.7-CONTEXT.md
@.planning/phases/05.7-Input-Validation-Hardening/05.7-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sécurisation Zod API</name>
  <files>
    - server/api/v1/pro/claim.post.ts
    - server/api/v1/projects.post.ts
    - server/api/v1/messages/index.post.ts
    - server/api/v1/pro/realisations/index.post.ts
  </files>
  <read_first>
    - .planning/phases/05.7-Input-Validation-Hardening/05.7-CONTEXT.md
    - .planning/phases/05.7-Input-Validation-Hardening/05.7-AUDIT.md
  </read_first>
  <action>
    Ajouter `.max(100)` pour full_name/company_name/title/city, `.max(254)` pour email, `.max(500)` pour bio/description courte, `.max(1000)` pour messages longs/description détaillée dans tous les schémas Zod pertinents listés dans l'audit.
    Ajouter `.regex(RE_PHONE_FR)` (ou validation format spécifique) pour phone et decennale si manquant.
  </action>
  <acceptance_criteria>
    - grep -q "max(100)" server/api/v1/pro/claim.post.ts
    - grep -q "max(254)" server/api/v1/projects.post.ts
  </acceptance_criteria>
</task>

<task type="auto" gate="blocking">
  <name>Task 2: Hardening DB Supabase (Schema Push)</name>
  <files>
    - supabase/migrations/20260719000000_phase57_input_limits.sql
  </files>
  <read_first>
    - .planning/phases/05.7-Input-Validation-Hardening/05.7-CONTEXT.md
  </read_first>
  <action>
    Créer une nouvelle migration qui ajoute des `ALTER TABLE ... ADD CONSTRAINT ... CHECK (char_length(col) <= X)` pour les tables `professionals`, `projects`, `messages`, `realisations` afin d'appliquer les mêmes limites (100, 254, 500, 1000) au niveau SQL.
  </action>
  <acceptance_criteria>
    - supabase/migrations/20260719000000_phase57_input_limits.sql exists
    - grep -q "CHECK" supabase/migrations/20260719000000_phase57_input_limits.sql
  </acceptance_criteria>
</task>

<task type="manual">
  <name>Task 3: [BLOCKING] Appliquer la Migration Locale</name>
  <action>
    Exécuter la commande `supabase db push` ou `supabase migration up` pour valider et appliquer la nouvelle migration sur la DB locale, vérifiant que la contrainte passe sur les données existantes.
  </action>
  <acceptance_criteria>
    - La migration est appliquée avec succès sans erreur de données existantes hors-limite.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Les tests unitaires API existants passent toujours.
- Tenter d'insérer un email de >254 caractères via l'API retourne une erreur 400 Zod.
</verification>
