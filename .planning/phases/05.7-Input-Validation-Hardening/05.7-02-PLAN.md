---
phase: 05.7-Input-Validation-Hardening
plan: 02
type: execute
wave: 2
depends_on: ["05.7-01"]
files_modified:
  - app/pages/simulateur.vue
  - app/pages/pro/claim.vue
  - app/pages/espace/profil.vue
  - app/components/RealisationForm.vue
  - app/pages/espace/leads/[id].vue
  - app/pages/espace/dashboard.vue
autonomous: true
requirements: []
must_haves:
  truths:
    - "Tous les 29 champs listés dans l'audit possèdent un attribut maxlength strict et aligné sur l'API"
    - "Les champs frontend utilisent des placeholders explicites de formats (ex: 06 12 34 56 78)"
    - "Seuls les champs longs (bio, description) affichent un compteur de caractères visuel"
---

<objective>
Répercuter les limites strictes sur le frontend via les attributs natifs HTML `maxlength` et améliorer l'UX avec des placeholders explicites (numéros FR, décennales) et des compteurs ciblés pour les textes longs, en traitant les 29 inputs de l'audit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/05.7-Input-Validation-Hardening/05.7-CONTEXT.md
@.planning/phases/05.7-Input-Validation-Hardening/05.7-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Maxlength et Placeholders UX (29 inputs)</name>
  <files>
    - app/pages/simulateur.vue
    - app/pages/pro/claim.vue
    - app/pages/espace/dashboard.vue
    - app/pages/espace/profil.vue
  </files>
  <read_first>
    - .planning/phases/05.7-Input-Validation-Hardening/05.7-CONTEXT.md
    - .planning/phases/05.7-Input-Validation-Hardening/05.7-AUDIT.md
  </read_first>
  <action>
    Ajouter `maxlength="100"` aux champs name/company, `maxlength="254"` aux emails, et les `maxlength` correspondants pour le reste.
    Ajouter ou modifier les `placeholder` pour afficher le format visuel (ex: `placeholder="06 12 34 56 78"` pour les téléphones, masque décennale).
    Ne PAS ajouter de compteurs visuels sur ces champs courts (le blocage silencieux `maxlength` suffit).
  </action>
  <acceptance_criteria>
    - grep -q "maxlength=\"100\"" app/pages/simulateur.vue
    - grep -q "maxlength=\"254\"" app/pages/pro/claim.vue
    - grep -q "06 " app/pages/simulateur.vue
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Compteurs de caractères pour textes longs</name>
  <files>
    - app/pages/espace/profil.vue
    - app/components/RealisationForm.vue
    - app/pages/espace/leads/[id].vue
  </files>
  <read_first>
    - .planning/phases/05.7-Input-Validation-Hardening/05.7-CONTEXT.md
  </read_first>
  <action>
    Ajouter des indicateurs visuels du nombre de caractères restants (ex: `{{ bio.length }} / 500`) sous les `<textarea>` de `bio`, `description`, et `chatInput`. 
    Appliquer `maxlength="500"` ou `1000` natif sur ces mêmes `<textarea>`.
  </action>
  <acceptance_criteria>
    - grep -q "maxlength=\"500\"" app/pages/espace/profil.vue
    - grep -q "maxlength=\"1000\"" app/components/RealisationForm.vue
    - grep -Eq "{{.*length.*\/.*(500|1000)}}" app/pages/espace/profil.vue
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Nettoyage front avant envoi (Trim / Sanitize)</name>
  <files>
    - app/pages/simulateur.vue
    - app/pages/pro/claim.vue
  </files>
  <read_first>
    - .planning/phases/05.7-Input-Validation-Hardening/05.7-CONTEXT.md
  </read_first>
  <action>
    Dans les fonctions de soumission (`handleSubmit`, `onSubmit`), ajouter un `.trim()` automatique sur les payloads textuels (noms, emails) et nettoyer les espaces sur les numéros de téléphone (`.replace(/\s/g, '')`) avant envoi via `$fetch`.
  </action>
  <acceptance_criteria>
    - grep -q ".trim()" app/pages/pro/claim.vue
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Les champs courts n'ont pas de compteur et bloquent nativement à la limite (100 ou 254).
- Les textareas longs affichent bien "XX / 500".
- Les numéros de téléphone sont formatés visuellement via le placeholder.
</verification>
