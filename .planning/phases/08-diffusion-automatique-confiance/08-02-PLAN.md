---
phase: 08-diffusion-automatique-confiance
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - server/utils/b2bDiffusion.ts
  - server/api/v1/admin/b2b-requests/[id]/diffuse.post.ts
  - nuxt.config.ts
  - .env.example
  - tests/unit/b2b-diffuse.test.ts
autonomous: true
requirements: [TEND-04, TEND-06, TEND-10, TEND-11]
user_setup: []

must_haves:
  truths:
    - "Un appel à l'endpoint sans session admin est rejeté (401/403), quel que soit l'état du bouton côté client"
    - "Un dossier sans decision_status ou sans project_postal_code valide ne peut pas être diffusé (422 avec message explicite)"
    - "Un partenaire dépassant le plafond d'AO actifs reçoit un 409 avec un message clair, aucune diffusion partielle"
    - "Le zone_id de chaque lot est résolu au clic via matchZone(project_postal_code)"
    - "Un lot dont le code postal n'est couvert par aucune zone est ignoré sans faire échouer la diffusion des autres lots"
    - "La diffusion est tracée dans audit_logs avec l'action b2b_tender_diffused"
  artifacts:
    - path: "server/utils/b2bDiffusion.ts"
      provides: "Gardes pures : statuts actifs, gating diffusion, plafond AO/partenaire"
      exports: ["ACTIVE_TENDER_STATUSES", "assertDiffusable", "assertTenderQuota"]
    - path: "server/api/v1/admin/b2b-requests/[id]/diffuse.post.ts"
      provides: "Endpoint admin « Diffuser » — résolution zone, matching, notification, audit"
      min_lines: 80
    - path: "tests/unit/b2b-diffuse.test.ts"
      provides: "Couverture TEND-10 (gating) et TEND-11 (plafonds)"
  key_links:
    - from: "server/api/v1/admin/b2b-requests/[id]/diffuse.post.ts"
      to: "server/utils/notifyB2bPros.ts"
      via: "notifyMatchedB2bPros par lot"
      pattern: "notifyMatchedB2bPros\\("
    - from: "server/api/v1/admin/b2b-requests/[id]/diffuse.post.ts"
      to: "server/utils/zoneMatcher.ts"
      via: "matchZone(project_postal_code)"
      pattern: "matchZone\\("
    - from: "server/api/v1/admin/b2b-requests/[id]/diffuse.post.ts"
      to: "audit_logs"
      via: "insert action b2b_tender_diffused"
      pattern: "b2b_tender_diffused"
---

<objective>
Créer l'endpoint admin « Diffuser » : il vérifie les droits, les prérequis du
dossier, le plafond d'AO actifs par partenaire, résout la zone de chaque lot
ouvert au moment du clic, puis délègue la notification à
`notifyMatchedB2bPros()` (plan 01) et trace l'action en audit log.

Purpose : TEND-10 (déclenchement à la qualification DirCo, pas à l'intake public),
TEND-11 (rate-limit anti-spam), plus le câblage TEND-04/TEND-06.
Output : 1 utilitaire de gardes testé, 1 endpoint Nitro, 2 variables d'env
documentées, 1 fichier de tests vert.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-diffusion-automatique-confiance/08-CONTEXT.md
@.planning/phases/08-diffusion-automatique-confiance/08-RESEARCH.md
@.planning/phases/08-diffusion-automatique-confiance/08-01-SUMMARY.md
@CLAUDE.md
</context>

<interfaces>
<!-- Contrats disponibles à ce stade — pas d'exploration nécessaire. -->

Produit par le plan 08-01 (`server/utils/notifyB2bPros.ts`) :
```typescript
export async function notifyMatchedB2bPros(
  supabase: any,
  request: { id: string; project_location?: string | null; project_postal_code?: string | null; budget_range?: string | null },
  lot: { id: string; category: string; zone_id: string },
  opts: { siteUrl: string; maxPerDay: number }
): Promise<{ sent: number; skipped: number; failed: number }>
```

Existant (`server/utils/zoneMatcher.ts`) :
```typescript
export async function matchZone(postalCode: string): Promise<{ id: string; name: string } | null>
```

Schéma réel de `b2b_requests` utilisé ici :
- `status` TEXT CHECK IN ('nouveau','en_cours','rappele','qualifie','converti','perdu')  ← **pas de valeur `clos`**
- `decision_status` TEXT NOT NULL DEFAULT 'en_attente' CHECK IN ('confirme','en_attente')
- `project_postal_code` TEXT CHECK (NULL OR `^\d{5}$`)
- `contact_email` TEXT (identifiant de fait du partenaire — aucune table partenaires n'existe)
- `recommended_pros` UUID[] (conservée, **non alimentée** par ce flux — D-05)

Schéma `b2b_tender_lots` : `id`, `request_id`, `category`, `zone_id` (nullable),
`status` CHECK IN ('open','claimed','closed').
</interfaces>

<notes>
## Résolution de l'écart D-09 « clos » (DÉCISION DE PLAN, à ne pas re-trancher)

CONTEXT.md D-09 dit compter les AO actifs via
`b2b_requests.status NOT IN ('converti', 'perdu', 'clos')`.
**La valeur `clos` n'existe pas** dans la contrainte CHECK réelle de
`b2b_requests.status` (`nouveau|en_cours|rappele|qualifie|converti|perdu`,
migrations 20260822000002/003). La confusion vient de `b2b_tender_lots.status`
qui, lui, a bien `open|claimed|closed` — deux colonnes, deux tables.

**Décision retenue : option (a) de RESEARCH.md — `clos` est omis.**
Aucun mécanisme de la Phase 8 ne fait passer un `b2b_requests.status` à `clos`,
et la clôture automatique (TEND-12) est en Phase 9. Aucune migration ne sera
ajoutée pour étendre le CHECK.

Implémentation : liste **positive** plutôt que `NOT IN`, pour que toute future
valeur de statut soit un choix explicite et non une inclusion accidentelle :
`ACTIVE_TENDER_STATUSES = ['nouveau', 'en_cours', 'rappele', 'qualifie']`.

## Autres décisions de plan

- **Emplacement du check D-09** (Claude's Discretion) : au clic « Diffuser »,
  côté serveur, via un `createError` 409 — pas à la transition de
  `decision_status`. Motif : un seul point d'application, cohérent avec
  `assertSubscriptionModifiable` (`server/utils/zoneMatcher.ts`) déjà utilisé
  dans le repo, et pas besoin d'enrichir le payload de listing admin.
- **Identifiant du partenaire** : `b2b_requests.contact_email` (aucune table
  partenaires n'existe en base). Documenté comme tel dans le code.
- **`recommended_pros`** : ce flux ne l'écrit ni ne le lit (D-05, dépréciation
  douce). L'endpoint `restitution.post.ts` reste inchangé.
</notes>

<tasks>

<task type="auto">
  <name>Task 1: Variables d'env des plafonds (runtimeConfig + .env.example)</name>
  <files>nuxt.config.ts, .env.example</files>
  <read_first>
    - nuxt.config.ts (bloc `runtimeConfig`, lignes ~69-100 — voir le pattern `cronSecret` / `adminEmail`)
    - .env.example (section 1, variables applicatives)
  </read_first>
  <action>
D-09/D-10/D-11 : deux seuils configurables par variable d'env, sans table de
config ni UI admin.

1. Dans `nuxt.config.ts`, à l'intérieur de `runtimeConfig` (au même niveau que
   `cronSecret` et `adminEmail`, **avant** la clé `public:`), ajouter :
```typescript
    // Phase 8 (D-09/D-10) — garde-fous anti-spam de la diffusion AO partenaires.
    // Seuils en env vars (pas de table de config : aucun besoin de réglage à chaud au pilote).
    b2bMaxActiveTendersPerPartner: Number(process.env.NUXT_B2B_MAX_ACTIVE_TENDERS_PER_PARTNER || process.env.B2B_MAX_ACTIVE_TENDERS_PER_PARTNER || 3),
    b2bMaxNotificationsPerArtisanPerDay: Number(process.env.NUXT_B2B_MAX_NOTIFICATIONS_PER_ARTISAN_PER_DAY || process.env.B2B_MAX_NOTIFICATIONS_PER_ARTISAN_PER_DAY || 5),
```

2. Dans `.env.example`, à la fin de la section des variables applicatives (juste
   après le bloc `# Analytics Umami`, avant le séparateur
   `# 2. SECRETS GITHUB ACTIONS`), ajouter :
```
# Phase 8 — Garde-fous anti-spam diffusion AO partenaires
# Nombre max d'AO actifs simultanés par partenaire (identifié par contact_email).
# Au-delà : la diffusion est refusée (409) avec un message au DirCo.
B2B_MAX_ACTIVE_TENDERS_PER_PARTNER=3
# Nombre max de notifications AO reçues par artisan et par jour, tous partenaires
# confondus. Au-delà : l'artisan est exclu silencieusement de la diffusion en cours.
B2B_MAX_NOTIFICATIONS_PER_ARTISAN_PER_DAY=5
```
  </action>
  <acceptance_criteria>
    - `grep -q "b2bMaxActiveTendersPerPartner" nuxt.config.ts` → succès
    - `grep -q "b2bMaxNotificationsPerArtisanPerDay" nuxt.config.ts` → succès
    - `grep -q "^B2B_MAX_ACTIVE_TENDERS_PER_PARTNER=3$" .env.example` → succès
    - `grep -q "^B2B_MAX_NOTIFICATIONS_PER_ARTISAN_PER_DAY=5$" .env.example` → succès
    - Les deux clés sont bien dans `runtimeConfig` et NON dans `runtimeConfig.public` (secrets serveur) : `grep -n "b2bMax" nuxt.config.ts` retourne des lignes antérieures à celle de `public:`
  </acceptance_criteria>
  <verify>
    <automated>grep -q "b2bMaxActiveTendersPerPartner" nuxt.config.ts && grep -q "^B2B_MAX_ACTIVE_TENDERS_PER_PARTNER=3$" .env.example && (npx nuxi typecheck 2>/dev/null || npm run build --silent >/dev/null) && echo OK</automated>
  </verify>
  <done>Les deux seuils sont lisibles via `useRuntimeConfig(event)` côté serveur et documentés dans `.env.example`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gardes pures b2bDiffusion (gating TEND-10 + plafond TEND-11/D-09)</name>
  <files>server/utils/b2bDiffusion.ts, tests/unit/b2b-diffuse.test.ts</files>
  <read_first>
    - server/utils/zoneMatcher.ts (pattern `assertSubscriptionModifiable` : createError 409 avec message utilisateur français)
    - tests/unit/b2b-tender.test.ts (style des tests du repo)
    - .planning/phases/08-diffusion-automatique-confiance/08-CONTEXT.md (D-02, D-09)
  </read_first>
  <behavior>
    Tests à écrire AVANT l'implémentation, dans `tests/unit/b2b-diffuse.test.ts` :

    describe('ACTIVE_TENDER_STATUSES') — résolution de l'écart D-09
    - Test 1 : `ACTIVE_TENDER_STATUSES` vaut exactement `['nouveau','en_cours','rappele','qualifie']`.
    - Test 2 : `ACTIVE_TENDER_STATUSES` ne contient ni `'clos'`, ni `'converti'`, ni `'perdu'`.

    describe('assertDiffusable') — TEND-10 / D-02
    - Test 3 : `{ decision_status: 'confirme', project_postal_code: '78000' }` → ne lève rien.
    - Test 4 : `{ decision_status: null, project_postal_code: '78000' }` → lève, `statusCode` 422.
    - Test 5 : `{ decision_status: 'confirme', project_postal_code: null }` → lève, `statusCode` 422.
    - Test 6 : `{ decision_status: 'confirme', project_postal_code: '780' }` → lève 422 (regex 5 chiffres).

    describe('assertTenderQuota') — TEND-11 / D-09
    - Test 7 : `assertTenderQuota(3, 3)` → ne lève rien (au plafond, pas au-delà).
    - Test 8 : `assertTenderQuota(4, 3)` → lève, `statusCode` 409, `statusMessage` contient `3`.
    - Test 9 : `assertTenderQuota(0, 3)` → ne lève rien.
  </behavior>
  <action>
Créer `server/utils/b2bDiffusion.ts` — fonctions pures, aucun accès Supabase
(c'est ce qui les rend testables en Vitest node, comme `server/utils/b2bTender.ts`).

```typescript
/**
 * Phase 8 — Gardes de la diffusion automatique des AO partenaires.
 *
 * Écart D-09 résolu ici : CONTEXT.md évoque un statut 'clos' pour b2b_requests,
 * qui n'existe PAS dans la contrainte CHECK réelle
 * (nouveau|en_cours|rappele|qualifie|converti|perdu). Confusion avec
 * b2b_tender_lots.status (open|claimed|closed). Aucun mécanisme de la Phase 8 ne
 * pose 'clos' sur b2b_requests, et la clôture auto est en Phase 9 (TEND-12) :
 * on liste donc positivement les statuts actifs plutôt qu'un NOT IN.
 */
export const ACTIVE_TENDER_STATUSES = ['nouveau', 'en_cours', 'rappele', 'qualifie'] as const

/**
 * D-02 / TEND-10 : un AO n'est diffusable que si le DirCo a renseigné le statut
 * de décision ET un code postal exploitable par matchZone().
 */
export function assertDiffusable(req: { decision_status?: string | null; project_postal_code?: string | null }) {
  if (!req.decision_status) {
    throw createError({
      statusCode: 422,
      statusMessage: 'Renseignez le statut de décision du dossier avant de diffuser aux artisans.',
    })
  }
  if (!req.project_postal_code || !/^\d{5}$/.test(req.project_postal_code)) {
    throw createError({
      statusCode: 422,
      statusMessage: 'Renseignez un code postal de chantier valide (5 chiffres) avant de diffuser aux artisans.',
    })
  }
}

/**
 * D-09 / TEND-11 : plafond d'AO actifs simultanés par partenaire.
 * `activeCount` inclut le dossier en cours de diffusion.
 */
export function assertTenderQuota(activeCount: number, max: number) {
  if (activeCount > max) {
    throw createError({
      statusCode: 409,
      statusMessage: `Ce partenaire a déjà ${activeCount} appels d'offres actifs (plafond : ${max}). Clôturez-en un (statut « converti » ou « perdu ») avant de diffuser celui-ci.`,
    })
  }
}
```

`createError` est un auto-import Nitro/h3 disponible dans `server/utils/` (même
usage que `assertSubscriptionModifiable` dans `zoneMatcher.ts`). Dans les tests,
importer `createError` depuis `h3` si l'auto-import n'est pas résolu en contexte
Vitest — vérifier comment les tests existants gèrent ce cas et, si nécessaire,
ajouter en tête du fichier de test :
`import { createError } from 'h3'; (globalThis as any).createError = createError`.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/b2b-diffuse.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/unit/b2b-diffuse.test.ts` : 9 tests passent, 0 échec
    - `grep -q "export const ACTIVE_TENDER_STATUSES" server/utils/b2bDiffusion.ts` → succès
    - `grep -c "'clos'" server/utils/b2bDiffusion.ts` retourne 0 dans le code exécutable (la mention n'apparaît que dans le commentaire d'explication)
    - `grep -q "statusCode: 409" server/utils/b2bDiffusion.ts` → succès
    - `grep -q "statusCode: 422" server/utils/b2bDiffusion.ts` → succès
  </acceptance_criteria>
  <done>Les trois exports existent, les 9 tests décrits passent, et l'écart `clos` est documenté en commentaire dans le fichier source.</done>
</task>

<task type="auto">
  <name>Task 3: Endpoint admin diffuse.post.ts</name>
  <files>server/api/v1/admin/b2b-requests/[id]/diffuse.post.ts</files>
  <read_first>
    - server/api/v1/admin/b2b-requests/[id]/restitution.post.ts (squelette exact à cloner : auth admin, chargement dossier, action, audit_logs, réponse structurée)
    - server/utils/b2bDiffusion.ts (gardes créées en Task 2)
    - server/utils/notifyB2bPros.ts (produit par le plan 08-01)
    - server/utils/zoneMatcher.ts (matchZone)
  </read_first>
  <action>
Créer `server/api/v1/admin/b2b-requests/[id]/diffuse.post.ts` en clonant la
structure de `restitution.post.ts`, avec cette séquence exacte :

1. **Auth (V2/V4, mitigation T-08-02)** — jamais de confiance dans l'état du
   bouton client :
```typescript
const user = await serverSupabaseUser(event)
if (!user) throw createError({ statusCode: 401, statusMessage: 'Non autorisé.' })
const isAdmin = (user as any).app_metadata?.role === 'admin'
if (!isAdmin) throw createError({ statusCode: 403, statusMessage: 'Accès réservé aux administrateurs.' })
const id = getRouterParam(event, 'id')
if (!id) throw createError({ statusCode: 400, statusMessage: 'id requis.' })
```
2. `const supabase = await serverSupabaseServiceRole(event) as any`
3. Charger le dossier : `.from('b2b_requests').select('*').eq('id', id).single()` →
   404 `'Dossier introuvable.'` si absent.
4. `assertDiffusable(req)` (import depuis `../../../../../utils/b2bDiffusion` —
   aligner la profondeur relative sur celle utilisée par `restitution.post.ts`
   pour `utils/email`).
5. **Plafond D-09** — compter les AO actifs du partenaire (identifié par
   `contact_email`, aucune table partenaires n'existe) :
```typescript
const config = useRuntimeConfig(event)
const maxActive = Number(config.b2bMaxActiveTendersPerPartner ?? 3)
const { count: activeCount } = await supabase
  .from('b2b_requests')
  .select('id', { count: 'exact', head: true })
  .eq('contact_email', req.contact_email)
  .in('status', ACTIVE_TENDER_STATUSES)
assertTenderQuota(activeCount ?? 0, maxActive)
```
6. Charger les lots ouverts (D-03 : tous les lots `open` en une fois) :
```typescript
const { data: lots } = await supabase
  .from('b2b_tender_lots').select('id, category, zone_id, status')
  .eq('request_id', id).eq('status', 'open')
if (!lots || lots.length === 0) {
  throw createError({ statusCode: 422, statusMessage: 'Aucun lot ouvert à diffuser sur ce dossier.' })
}
```
7. **Résolution tardive de la zone (D-06)** — au clic, pas à la qualification.
   `matchZone` est appelé une seule fois (tous les lots partagent le code postal
   du dossier) ; si elle retourne `null`, aucun lot n'est diffusable :
```typescript
const zone = await matchZone(req.project_postal_code)
if (!zone) {
  throw createError({
    statusCode: 422,
    statusMessage: `Le code postal ${req.project_postal_code} n'est couvert par aucune zone active — diffusion impossible.`,
  })
}
```
   Puis, pour chaque lot dont `zone_id !== zone.id`, persister la zone résolue :
   `await supabase.from('b2b_tender_lots').update({ zone_id: zone.id }).eq('id', lot.id)`
   et mettre à jour l'objet lot en mémoire.
8. **Diffusion, non-bloquante par lot** (un lot en échec ne casse pas les autres) :
```typescript
const maxPerDay = Number(config.b2bMaxNotificationsPerArtisanPerDay ?? 5)
const siteUrl = (config.public?.siteUrl as string) || 'https://bati-axe.pages.dev'
let sent = 0, skipped = 0, failed = 0, lotsDiffused = 0
for (const lot of lots) {
  try {
    const r = await notifyMatchedB2bPros(supabase, req, { ...lot, zone_id: zone.id }, { siteUrl, maxPerDay })
    sent += r.sent; skipped += r.skipped; failed += r.failed; lotsDiffused++
  } catch (err) {
    console.error(`[P8] Échec diffusion lot ${lot.id}:`, err)
    failed++
  }
}
```
9. **Audit log** (pattern `b2b_restitution_sent`), non bloquant :
```typescript
try {
  await supabase.from('audit_logs').insert({
    actor_id: (user as any).id,
    action: 'b2b_tender_diffused',
    target_table: 'b2b_requests',
    target_id: id,
    metadata: { zone_id: zone.id, lots_diffused: lotsDiffused, notifications_sent: sent, notifications_skipped: skipped },
  })
} catch { /* non-blocking */ }
```
10. **Réponse — compteurs uniquement** (mitigation T-08-03, information
    disclosure : jamais la liste nominative des pros contactés) :
```typescript
return {
  status: 'SUCCESS',
  zone: zone.name,
  lots_diffused: lotsDiffused,
  notifications_sent: sent,
  notifications_skipped: skipped,
  notifications_failed: failed,
}
```

**Ne pas** écrire dans `recommended_pros` (D-05). **Ne pas** modifier
`restitution.post.ts`. **Ne pas** changer `b2b_requests.status`.
  </action>
  <verify>
    <automated>npm run build --silent && npx vitest run tests/unit/b2b-diffuse.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Le fichier `server/api/v1/admin/b2b-requests/[id]/diffuse.post.ts` existe
    - `grep -q "app_metadata?.role === 'admin'" server/api/v1/admin/b2b-requests/\[id\]/diffuse.post.ts` → succès
    - `grep -q "assertDiffusable" …/diffuse.post.ts` et `grep -q "assertTenderQuota" …/diffuse.post.ts` → succès
    - `grep -q "matchZone(" …/diffuse.post.ts` → succès
    - `grep -q "notifyMatchedB2bPros(" …/diffuse.post.ts` → succès
    - `grep -q "b2b_tender_diffused" …/diffuse.post.ts` → succès
    - `grep -c "recommended_pros" …/diffuse.post.ts` retourne 0
    - La réponse ne contient aucune clé listant des pros : `grep -c "pro_ids\|pros:" …/diffuse.post.ts` retourne 0
    - `npm run build` réussit
    - `npm test` : suite complète verte
  </acceptance_criteria>
  <done>L'endpoint existe, est protégé par le check admin serveur, applique les deux gardes, résout la zone au clic, délègue la notification par lot, trace en audit log et ne renvoie que des compteurs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client admin → `POST /api/v1/admin/b2b-requests/{id}/diffuse` | Entrée non fiable : `id` de route + session |
| endpoint → service_role Postgres | L'endpoint contourne le RLS pour lire `b2b_requests`, `b2b_tender_lots`, `pro_zones` |
| endpoint → Resend (déclenchement d'envois de masse) | Un appel déclenche N emails vers des tiers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02 | Elevation of Privilege | `diffuse.post.ts` | mitigate | `serverSupabaseUser` + `app_metadata?.role === 'admin'` vérifiés côté serveur (Task 3, étape 1) — l'état désactivé du bouton client n'est jamais une garantie |
| T-08-01 | Denial of Service (spam artisan / rejeu du clic) | `diffuse.post.ts` | mitigate | `assertTenderQuota` 409 (Task 2, tests 7-9) + plafond quotidien par artisan passé à `notifyMatchedB2bPros` + idempotence `UNIQUE(pro_id, lot_id, channel)` du plan 01 |
| T-08-07 | Tampering / Input Validation (V5) | Paramètre `id` de route | mitigate | 400 si `id` absent ; `.eq('id', id).single()` → 404 si introuvable ; aucun `id` non résolu n'atteint d'écriture. `project_postal_code` re-validé par regex `^\d{5}$` dans `assertDiffusable` en plus du CHECK SQL |
| T-08-03 | Information Disclosure | Réponse de l'endpoint | mitigate | Réponse limitée à des compteurs (`lots_diffused`, `notifications_sent/skipped/failed`) + nom de zone ; aucune liste nominative de pros (Task 3, étape 10) |
| T-08-08 | Repudiation | Action DirCo « Diffuser » | mitigate | `audit_logs` action `b2b_tender_diffused` avec `actor_id` et métriques (Task 3, étape 9) |
| T-08-09 | Denial of Service (rejeu massif par un admin compromis) | `diffuse.post.ts` | accept | Surface réservée aux comptes admin internes (population connue, très restreinte) ; l'idempotence limite l'impact d'un rejeu à zéro email supplémentaire. Pas de throttling par IP au pilote |
</threat_model>

<verification>
1. `npx vitest run tests/unit/b2b-diffuse.test.ts` → 9 tests verts
2. `npm test` → suite complète verte
3. `npm run build` → build Nuxt réussi
4. `grep -rn "b2b_tender_diffused" server/` → présent uniquement dans `diffuse.post.ts`
5. `git diff --name-only` ne contient ni `server/api/v1/admin/b2b-requests/[id]/restitution.post.ts` ni `server/utils/notifyProLead.ts`
</verification>

<success_criteria>
- L'endpoint refuse 401/403 sans session admin, 422 sans `decision_status`/code postal valide, 409 au-delà du plafond d'AO actifs.
- `zone_id` des lots ouverts est persisté au clic via `matchZone`.
- Un code postal hors zone couverte produit un 422 explicite, pas un 500.
- L'action est tracée en `audit_logs` sous `b2b_tender_diffused`.
- Les deux seuils sont configurables par env vars documentées dans `.env.example`.
- L'écart D-09 (`clos`) est résolu et documenté en commentaire dans `server/utils/b2bDiffusion.ts`.
</success_criteria>

<output>
Créer `.planning/phases/08-diffusion-automatique-confiance/08-02-SUMMARY.md`,
en y consignant explicitement la résolution de l'écart D-09 (`clos` omis, liste
positive `ACTIVE_TENDER_STATUSES`) pour que la décision remonte dans STATE.md.
</output>
