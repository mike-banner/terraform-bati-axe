---
phase: 08-diffusion-automatique-confiance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260905000000_phase8_b2b_tender_notifications.sql
  - app/types/database.types.ts
  - server/utils/notifyB2bPros.ts
  - tests/unit/notify-b2b-pros.test.ts
autonomous: false
requirements: [TEND-04, TEND-06, TEND-14]
user_setup: []

must_haves:
  truths:
    - "La table b2b_tender_notifications existe en base locale ET remote avec UNIQUE(pro_id, lot_id, channel)"
    - "La colonne professionals.b2b_alerts_email existe avec DEFAULT true NOT NULL"
    - "Un artisan sans ligne pro_zones active sur la zone du lot n'est jamais retenu comme cible"
    - "Un artisan déjà notifié sur un lot n'est pas re-notifié si la diffusion est rejouée"
    - "Un artisan ayant atteint son plafond quotidien de notifications B2B est exclu silencieusement"
    - "L'email généré contient le badge « AO qualifié BÂTI-AXE »"
  artifacts:
    - path: "supabase/migrations/20260905000000_phase8_b2b_tender_notifications.sql"
      provides: "Table d'idempotence + colonne opt-in B2B + RLS service_role"
      contains: "CREATE TABLE IF NOT EXISTS b2b_tender_notifications"
    - path: "server/utils/notifyB2bPros.ts"
      provides: "Matching zone×catégorie, sélection des cibles, rendu email, envoi non-bloquant"
      exports: ["selectB2bTargets", "renderTenderEmail", "notifyMatchedB2bPros"]
      min_lines: 120
    - path: "tests/unit/notify-b2b-pros.test.ts"
      provides: "Couverture TEND-04 / TEND-06 / TEND-14"
      min_lines: 60
  key_links:
    - from: "server/utils/notifyB2bPros.ts"
      to: "b2b_tender_notifications"
      via: "SELECT des déjà-notifiés + INSERT après succès d'envoi"
      pattern: "from\\('b2b_tender_notifications'\\)"
    - from: "server/utils/notifyB2bPros.ts"
      to: "server/utils/emailLayout.ts"
      via: "renderEmail()"
      pattern: "renderEmail\\("
    - from: "server/utils/notifyB2bPros.ts"
      to: "pro_zones"
      via: "filtre zone active D-07"
      pattern: "from\\('pro_zones'\\)"
---

<objective>
Poser les fondations données + moteur de notification B2B de la Phase 8 :
la table d'idempotence `b2b_tender_notifications`, l'opt-in
`professionals.b2b_alerts_email`, et la fonction `notifyMatchedB2bPros()` qui
matche les artisans (zone active × catégorie × vérifié × opt-in), respecte le
plafond quotidien par artisan et envoie l'email portant le badge
« AO qualifié BÂTI-AXE ».

Purpose : TEND-04 (matching), TEND-06 (notification email), TEND-14 (signal de
confiance). Sans ce plan, l'endpoint « Diffuser » (plan 02) n'a rien à appeler.
Output : 1 migration appliquée (locale + remote), 1 utilitaire serveur testé,
1 fichier de tests Vitest vert.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-diffusion-automatique-confiance/08-CONTEXT.md
@.planning/phases/08-diffusion-automatique-confiance/08-RESEARCH.md
@CLAUDE.md
</context>

<interfaces>
<!-- Contrats extraits du code existant. L'exécutant n'a pas à explorer le repo. -->

`server/utils/emailLayout.ts` :
```typescript
export interface EmailCta { label: string; href: string }
export interface EmailLayoutOptions {
  title: string; preheader?: string; intro?: string
  bodyHtml?: string; cta?: EmailCta; footerNote?: string
}
export function renderEmail(opts: EmailLayoutOptions): string
```

`server/utils/email.ts` :
```typescript
export async function sendEmail(options: {
  to: string; subject: string; html: string
  sender?: EmailSender   // 'contact' | 'no-reply' | 'notifications'
  replyTo?: string
}): Promise<{ success: boolean; ... }>
```

Schéma `b2b_tender_lots` (migration 20260904000000) :
```sql
id UUID PK, request_id UUID FK b2b_requests, category TEXT
  CHECK IN ('maconnerie','toiture','electricite','plomberie','peinture','isolation'),
zone_id UUID REFERENCES zones(id) NULL, status TEXT DEFAULT 'open'
  CHECK IN ('open','claimed','closed'), created_at TIMESTAMPTZ
```

Schéma `pro_zones` (migration 20260828000002) : colonnes `pro_id`, `zone_id`, `status`
(valeur active = `'active'`).

Schéma `lead_notifications` (référence miroir, migration 20260823000002) :
```sql
id UUID PK DEFAULT gen_random_uuid(),
pro_id UUID NOT NULL REFERENCES professionals(id) ON DELETE CASCADE,
project_id UUID NOT NULL REFERENCES projects(id) ON DELETE CASCADE,
channel TEXT NOT NULL DEFAULT 'email',
sent_at TIMESTAMPTZ NOT NULL DEFAULT clock_timestamp(),
UNIQUE (pro_id, project_id, channel)
```
</interfaces>

<notes>
## Décisions de plan (à ne pas re-trancher pendant l'exécution)

1. **Pas d'embedding PostgREST filtré** (`professionals!inner(pro_zones)`) pour le
   filtre zone active. RESEARCH.md A2 le signale sans précédent dans ce repo, et
   STATE.md documente un embedding cassé sur `auth.users`. → **Deux requêtes
   séquentielles** : `pro_zones` d'abord (récupère les `pro_id` actifs sur la
   zone), puis `professionals ... .in('id', proIds)`.
2. **`notifyMatchedPros` (`server/utils/notifyProLead.ts`) ne doit PAS être modifié.**
   C'est le flux particuliers (P4), ses filtres divergent (pas de `pro_zones`).
   Nouveau fichier séparé.
3. **Emplacement des tests** : `tests/unit/notify-b2b-pros.test.ts` (convention
   réelle du repo : `tests/unit/*.test.ts` kebab-case, ex. `tests/unit/b2b-tender.test.ts`),
   et non `tests/notifyMatchedB2bPros.test.ts` comme écrit dans 08-VALIDATION.md.
   Mettre à jour `08-VALIDATION.md` avec le chemin réel dans le SUMMARY.
</notes>

<tasks>

<task type="auto">
  <name>Task 1: Migration b2b_tender_notifications + opt-in b2b_alerts_email</name>
  <files>supabase/migrations/20260905000000_phase8_b2b_tender_notifications.sql</files>
  <read_first>
    - supabase/migrations/20260823000002_p4_lead_notifications.sql (schéma miroir à répliquer)
    - supabase/migrations/20260904000000_phase7_tender_lots.sql (FK cible b2b_tender_lots + pattern RLS admin)
  </read_first>
  <action>
Créer le fichier `supabase/migrations/20260905000000_phase8_b2b_tender_notifications.sql`
avec exactement ce contenu (D-04, D-08) :

```sql
-- =====================================================
-- Phase 8 — Diffusion automatique des AO partenaires
-- D-08 (opt-in B2B), D-04 (idempotence par pro/lot)
-- =====================================================

-- D-08 : opt-in email B2B, distinct de lead_alerts_email (l'artisan peut couper
-- les AO partenaires sans couper les leads particuliers). Défaut true, aligné
-- sur lead_alerts_email (migration 20260823000002).
ALTER TABLE professionals
    ADD COLUMN IF NOT EXISTS b2b_alerts_email BOOLEAN NOT NULL DEFAULT true;

-- D-04 : idempotence par (pro, lot, canal) — miroir de lead_notifications.
-- Un second clic « Diffuser » ne renvoie rien aux couples déjà tracés.
CREATE TABLE IF NOT EXISTS b2b_tender_notifications (
    id      UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    pro_id  UUID NOT NULL REFERENCES professionals(id) ON DELETE CASCADE,
    lot_id  UUID NOT NULL REFERENCES b2b_tender_lots(id) ON DELETE CASCADE,
    channel TEXT NOT NULL DEFAULT 'email',
    sent_at TIMESTAMPTZ NOT NULL DEFAULT clock_timestamp(),
    UNIQUE (pro_id, lot_id, channel)
);

-- Exclusion des déjà-notifiés sur un lot
CREATE INDEX IF NOT EXISTS idx_b2b_tender_notifications_lot
    ON b2b_tender_notifications (lot_id);

-- D-10 : comptage des notifications du jour par artisan
CREATE INDEX IF NOT EXISTS idx_b2b_tender_notifications_pro_sent
    ON b2b_tender_notifications (pro_id, sent_at);

-- RLS : aucun accès anonyme/authentifié, tout passe par le service role
-- (même politique que b2b_tender_lots, migration 20260904000000).
ALTER TABLE b2b_tender_notifications ENABLE ROW LEVEL SECURITY;

DROP POLICY IF EXISTS "admin_all_b2b_tender_notifications" ON b2b_tender_notifications;
CREATE POLICY "admin_all_b2b_tender_notifications" ON b2b_tender_notifications
    FOR ALL
    USING (auth.jwt()->>'role' = 'admin');
```

Ne PAS toucher à `recommended_pros` (D-05 : dépréciation douce, aucune migration
destructive cette phase).
  </action>
  <acceptance_criteria>
    - `grep -c "CREATE TABLE IF NOT EXISTS b2b_tender_notifications" supabase/migrations/20260905000000_phase8_b2b_tender_notifications.sql` retourne 1
    - `grep -q "UNIQUE (pro_id, lot_id, channel)"` sur ce fichier → succès
    - `grep -q "b2b_alerts_email BOOLEAN NOT NULL DEFAULT true"` → succès
    - `grep -q "ENABLE ROW LEVEL SECURITY"` → succès
    - `grep -q "idx_b2b_tender_notifications_pro_sent"` → succès
    - `grep -c "DROP TABLE\|DROP COLUMN" ` sur ce fichier retourne 0
  </acceptance_criteria>
  <verify>
    <automated>grep -q "UNIQUE (pro_id, lot_id, channel)" supabase/migrations/20260905000000_phase8_b2b_tender_notifications.sql && grep -q "b2b_alerts_email BOOLEAN NOT NULL DEFAULT true" supabase/migrations/20260905000000_phase8_b2b_tender_notifications.sql && echo OK</automated>
  </verify>
  <done>Le fichier de migration existe, contient la table, la colonne, les 2 index et la policy RLS, sans aucune instruction destructive.</done>
</task>

<task type="auto">
  <name>Task 2a: [BLOCKING] Appliquer la migration en local</name>
  <files>(aucun fichier créé — application de la migration existante en base locale)</files>
  <read_first>
    - supabase/migrations/20260905000000_phase8_b2b_tender_notifications.sql (le fichier à appliquer)
    - .planning/phases/07-formulaire-ao-mod-le-multi-lots/07-03-SUMMARY.md (dérive schema_migrations locale documentée en Phase 7 — lire AVANT de lancer quoi que ce soit)
    - .planning/STATE.md (§ « Nouvelle migration » et « Migration + types »)
  </read_first>
  <action>
**Tâche BLOQUANTE** : sans elle, `npm run build` et les types passent au vert alors
que la base n'a pas la table → faux positif de vérification. Ne jamais la sauter.
Ne PAS toucher au projet remote dans cette tâche — c'est l'objet de Task 2b
(checkpoint humain séparé).

La Phase 7 a documenté une dérive de `supabase_migrations.schema_migrations` en
local qui fait échouer `supabase migration up --local`. Appliquer uniquement en
local (port 54322) :

```bash
npx supabase migration up --local
```
Si la commande échoue à cause de la dérive `schema_migrations`, appliquer le
fichier directement (le SQL est idempotent, `IF NOT EXISTS` partout) :
```bash
psql "postgresql://postgres:postgres@127.0.0.1:54322/postgres" \
  -f supabase/migrations/20260905000000_phase8_b2b_tender_notifications.sql
```

Vérification obligatoire en local après application :
```bash
psql "postgresql://postgres:postgres@127.0.0.1:54322/postgres" -c "\d b2b_tender_notifications"
psql "postgresql://postgres:postgres@127.0.0.1:54322/postgres" -c "SELECT column_name, column_default FROM information_schema.columns WHERE table_name='professionals' AND column_name='b2b_alerts_email';"
```
  </action>
  <acceptance_criteria>
    - `psql "postgresql://postgres:postgres@127.0.0.1:54322/postgres" -tAc "SELECT to_regclass('public.b2b_tender_notifications')"` retourne `b2b_tender_notifications` (pas vide)
    - `psql "postgresql://postgres:postgres@127.0.0.1:54322/postgres" -tAc "SELECT column_default FROM information_schema.columns WHERE table_name='professionals' AND column_name='b2b_alerts_email'"` retourne `true`
  </acceptance_criteria>
  <verify>
    <automated>psql "postgresql://postgres:postgres@127.0.0.1:54322/postgres" -tAc "SELECT to_regclass('public.b2b_tender_notifications')" | grep -q b2b_tender_notifications && echo OK</automated>
  </verify>
  <done>La table et la colonne existent en base locale (port 54322). Le projet remote n'est pas touché par cette tâche.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2b: [BLOCKING] Confirmation avant push du schéma sur le projet Supabase distant</name>
  <files>app/types/database.types.ts</files>
  <what-built>
    La migration `20260905000000_phase8_b2b_tender_notifications.sql` (table
    d'idempotence `b2b_tender_notifications` + colonne `professionals.b2b_alerts_email`)
    est appliquée en local (Task 2a) et validée par les tests. Il reste à la
    pousser sur le projet Supabase distant lié (`xpwoczcbyamnjknloxgz`) — une base
    partagée/prod — et à régénérer les types committés depuis ce projet distant.
  </what-built>
  <action>
    Attendre le signal explicite de l'utilisateur avant toute commande touchant
    le projet distant. Ne rien pousser tant que le « GO » n'est pas donné.
    Une fois le signal reçu, exécuter dans cet ordre :

    1. **Base remote** (projet lié `xpwoczcbyamnjknloxgz`) :
       ```bash
       npx supabase db push --yes
       ```
       Si la commande demande une confirmation interactive impossible à
       supprimer, ou si le lien au projet remote est cassé : **s'arrêter et le
       signaler à l'utilisateur**, ne jamais contourner en écrivant du SQL
       ad-hoc directement sur la prod.

    2. **Régénérer les types committés** :
       ```bash
       npx supabase gen types typescript --project-id xpwoczcbyamnjknloxgz --schema public > app/types/database.types.ts
       ```

    3. Vérifier :
       ```bash
       grep -q "b2b_tender_notifications" app/types/database.types.ts
       grep -q "b2b_alerts_email" app/types/database.types.ts
       ```
  </action>
  <how-to-verify>
    1. Confirmer que la migration locale (Task 2a) est bien appliquée et les
       tests unitaires verts avant de donner le GO.
    2. Donner explicitement le feu vert pour pousser `20260905000000_phase8_b2b_tender_notifications.sql`
       sur le projet Supabase distant `xpwoczcbyamnjknloxgz` (base réelle/prod-liée).
    3. Une fois le push effectué, vérifier que `app/types/database.types.ts`
       contient bien `b2b_tender_notifications` et `b2b_alerts_email`.
  </how-to-verify>
  <resume-signal>Taper « GO » pour autoriser le push distant, ou décrire pourquoi il faut s'abstenir (ex. fenêtre de gel, autre migration en cours).</resume-signal>
  <acceptance_criteria>
    - `grep -q "b2b_tender_notifications" app/types/database.types.ts` → succès
    - `grep -q "b2b_alerts_email" app/types/database.types.ts` → succès
  </acceptance_criteria>
  <verify>
    <automated>grep -q "b2b_tender_notifications" app/types/database.types.ts && grep -q "b2b_alerts_email" app/types/database.types.ts && echo OK</automated>
  </verify>
  <done>L'utilisateur a donné le GO explicite, le schéma est poussé sur le projet distant, et `app/types/database.types.ts` régénéré contient `b2b_tender_notifications` et `b2b_alerts_email`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: notifyMatchedB2bPros — matching, idempotence, plafond/jour, email badgé</name>
  <files>server/utils/notifyB2bPros.ts, tests/unit/notify-b2b-pros.test.ts</files>
  <read_first>
    - server/utils/notifyProLead.ts (template exact à cloner : structure, non-blocage, trace après succès)
    - server/utils/emailLayout.ts (signature renderEmail)
    - server/utils/email.ts (signature sendEmail + senders disponibles)
    - tests/unit/b2b-tender.test.ts (style des tests unitaires du repo)
    - .planning/phases/08-diffusion-automatique-confiance/08-CONTEXT.md (D-07, D-08, D-10, D-12, D-13, D-14)
  </read_first>
  <behavior>
    Tests à écrire AVANT l'implémentation, dans `tests/unit/notify-b2b-pros.test.ts` :

    describe('selectB2bTargets') — TEND-04 / TEND-06 / TEND-11(D-10)
    - Test 1 : pros = [A, B], alreadyNotifiedProIds = ['A'] → retourne [B] (idempotence).
    - Test 2 : pros = [A], dailyCounts = { A: 5 }, maxPerDay = 5 → retourne [] (plafond atteint, exclusion silencieuse).
    - Test 3 : pros = [A], dailyCounts = { A: 4 }, maxPerDay = 5 → retourne [A] (sous le plafond).
    - Test 4 : pros = [A], dailyCounts = {} (aucune notif aujourd'hui) → retourne [A].
    - Test 5 : pros = [], … → retourne [] sans lever d'exception.

    describe('renderTenderEmail') — TEND-14
    - Test 6 : le HTML retourné contient la chaîne exacte `AO qualifié BÂTI-AXE`.
    - Test 7 : le HTML contient le libellé français de la catégorie (`renderTenderEmail({ lotCategory: 'toiture', ... }).html` contient `Charpente & Toiture`).
    - Test 8 : le HTML NE contient PAS `contact_email` ni `contact_phone` du partenaire (les coordonnées sont Phase 9) — assert sur des valeurs passées volontairement dans l'objet request de test.
    - Test 9 : `subject` contient `BÂTI-AXE` et le libellé de catégorie.
  </behavior>
  <action>
Créer `server/utils/notifyB2bPros.ts`. **Ne pas modifier `server/utils/notifyProLead.ts`**
(flux particuliers P4, filtres divergents — anti-pattern explicite de RESEARCH.md).

Contenu attendu, trois exports :

**(a) `selectB2bTargets` — fonction pure (testable sans Supabase)**
```typescript
export interface B2bTargetPro {
  id: string
  email: string
  company_name?: string | null
  full_name?: string | null
}

export function selectB2bTargets(opts: {
  pros: B2bTargetPro[]
  alreadyNotifiedProIds: string[]
  dailyCounts: Record<string, number>   // pro_id -> nb de notifs B2B envoyées aujourd'hui
  maxPerDay: number
}): B2bTargetPro[]
```
Implémentation : `const notified = new Set(opts.alreadyNotifiedProIds)` puis
`opts.pros.filter(p => !notified.has(p.id) && (opts.dailyCounts[p.id] ?? 0) < opts.maxPerDay)`.
D-10 : exclusion **silencieuse**, aucun throw, aucun log bloquant.

**(b) `renderTenderEmail` — fonction pure (testable, pas d'import Nuxt)**
```typescript
export function renderTenderEmail(opts: {
  lotCategory: string
  lotId: string
  requestRef: string          // ex. id.slice(0,8).toUpperCase()
  projectLocation?: string | null
  postalCode?: string | null
  budgetRange?: string | null
  siteUrl: string
}): { subject: string; html: string }
```
Réutiliser la constante de libellés métiers (copier depuis `notifyProLead.ts`) :
```typescript
const CATEGORY_LABELS: Record<string, string> = {
  maconnerie:  'Maçonnerie & Gros Œuvre',
  toiture:     'Charpente & Toiture',
  electricite: 'Électricité',
  plomberie:   'Plomberie & Chauffage',
  peinture:    'Peinture & Finitions',
  isolation:   'Isolation & Cloisons',
}
```
Le corps est construit avec `renderEmail()` (`server/utils/emailLayout.ts`).
`bodyHtml` DOIT commencer par le badge TEND-14 / D-13 / D-14, chaîne exacte
« AO qualifié BÂTI-AXE » :
```html
<p style="margin:0 0 16px;"><span style="display:inline-block;background:#ECFDF5;color:#047857;border:1px solid #A7F3D0;border-radius:999px;padding:6px 12px;font-size:12px;font-weight:700;">✓ AO qualifié BÂTI-AXE</span></p>
```
suivi d'un tableau récapitulatif (même style inline que `notifyProLead.ts`) avec
les lignes : `Corps de métier` (libellé), `Localisation` (projectLocation ou
postalCode ou `—`), `Budget` (budgetRange ou `—`), `Référence` (requestRef).
`footerNote` : `"Cet appel d'offres a été qualifié manuellement par l'équipe BÂTI-AXE avant diffusion. Vous pouvez désactiver les alertes appels d'offres partenaires depuis votre espace."`
`cta` : `{ label: 'Voir l\'appel d\'offres', href: `${siteUrl}/espace/leads?src=email` }`
(le dashboard AO dédié est Phase 9 — on pointe l'espace existant).
`subject` : `` `Nouvel appel d'offres : ${label} — BÂTI-AXE` ``.
**Aucune coordonnée du partenaire** (nom, email, téléphone) dans l'email : la
révélation des coordonnées est Phase 9.

**(c) `notifyMatchedB2bPros` — orchestrateur (Supabase, non-bloquant)**
```typescript
export async function notifyMatchedB2bPros(
  supabase: any,
  request: { id: string; project_location?: string | null; project_postal_code?: string | null; budget_range?: string | null },
  lot: { id: string; category: string; zone_id: string },
  opts: { siteUrl: string; maxPerDay: number }
): Promise<{ sent: number; skipped: number; failed: number }>
```
Séquence exacte :
1. Récupérer les pros actifs sur la zone du lot (D-07), **deux requêtes, pas
   d'embedding `!inner`** :
   ```typescript
   const { data: zoneRows } = await supabase
     .from('pro_zones').select('pro_id')
     .eq('zone_id', lot.zone_id).eq('status', 'active')
   const zoneProIds = (zoneRows ?? []).map((z: any) => z.pro_id)
   if (zoneProIds.length === 0) return { sent: 0, skipped: 0, failed: 0 }

   const { data: pros, error } = await supabase
     .from('professionals')
     .select('id, email, company_name, full_name')
     .in('id', zoneProIds)
     .contains('categories', [lot.category])
     .eq('is_verified', true)
     .eq('b2b_alerts_email', true)
   ```
2. Déjà notifiés sur CE lot (D-04) :
   ```typescript
   const { data: already } = await supabase
     .from('b2b_tender_notifications').select('pro_id').eq('lot_id', lot.id)
   ```
3. Compteur du jour par artisan (D-10, discrétion CONTEXT : COUNT sur la table,
   pas de table de compteurs) :
   ```typescript
   const startOfDay = new Date(); startOfDay.setHours(0, 0, 0, 0)
   const { data: todayRows } = await supabase
     .from('b2b_tender_notifications').select('pro_id')
     .in('pro_id', (pros ?? []).map((p: any) => p.id))
     .gte('sent_at', startOfDay.toISOString())
   const dailyCounts: Record<string, number> = {}
   for (const r of todayRows ?? []) dailyCounts[r.pro_id] = (dailyCounts[r.pro_id] ?? 0) + 1
   ```
4. `const targets = selectB2bTargets({ pros: pros ?? [], alreadyNotifiedProIds: (already ?? []).map(a => a.pro_id), dailyCounts, maxPerDay: opts.maxPerDay })`
5. Boucle séquentielle **non-bloquante** (clone exacte du pattern
   `notifyProLead.ts` lignes 74-91) : `try { sendEmail({ to: pro.email, sender: 'notifications', subject, html }) ; if (res.success) insert dans b2b_tender_notifications { pro_id, lot_id, channel: 'email' } } catch { console.error(...); continue }`.
   Aucun `throw` ne doit jamais sortir de la boucle.
6. L'ensemble de la fonction est enveloppé dans un `try/catch` global qui logge
   et retourne les compteurs (filet de sécurité, comme `notifyMatchedPros`).

Le retour ne contient QUE des compteurs — jamais la liste nominative des pros
(mitigation T-08-03, information disclosure).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/notify-b2b-pros.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/unit/notify-b2b-pros.test.ts` : 9 tests passent, 0 échec
    - `grep -q "AO qualifié BÂTI-AXE" server/utils/notifyB2bPros.ts` → succès
    - `grep -q "export function selectB2bTargets" server/utils/notifyB2bPros.ts` → succès
    - `grep -q "export function renderTenderEmail" server/utils/notifyB2bPros.ts` → succès
    - `grep -q "export async function notifyMatchedB2bPros" server/utils/notifyB2bPros.ts` → succès
    - `grep -q "b2b_alerts_email" server/utils/notifyB2bPros.ts` → succès
    - `grep -q "from('pro_zones')" server/utils/notifyB2bPros.ts` → succès
    - `grep -c "!inner" server/utils/notifyB2bPros.ts` retourne 0
    - `git diff --name-only` ne contient PAS `server/utils/notifyProLead.ts`
    - `npm test` : suite complète verte
  </acceptance_criteria>
  <done>`selectB2bTargets`, `renderTenderEmail` et `notifyMatchedB2bPros` existent et sont exportés ; les 9 tests décrits dans `<behavior>` passent ; `notifyProLead.ts` est intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service_role → Postgres | `notifyMatchedB2bPros` contourne le RLS ; toute erreur de filtre expose des pros hors périmètre |
| Nitro → Resend (email sortant) | Contenu de l'email envoyé à des tiers (artisans) — fuite de données partenaires possible |
| client authentifié → `b2b_tender_notifications` | Nouvelle table : sans RLS elle serait lisible par tout utilisateur authentifié |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Denial of Service (spam artisan) | `notifyMatchedB2bPros` | mitigate | Contrainte `UNIQUE(pro_id, lot_id, channel)` (Task 1) + `selectB2bTargets` exclut les déjà-notifiés et les artisans au plafond quotidien (Task 3, tests 1-3) |
| T-08-03 | Information Disclosure | Email artisan | mitigate | `renderTenderEmail` n'injecte aucune coordonnée partenaire (nom/email/téléphone) — assert explicite au test 8 ; seuls catégorie, localisation, budget, référence |
| T-08-04 | Information Disclosure | Table `b2b_tender_notifications` | mitigate | `ENABLE ROW LEVEL SECURITY` + policy `admin_all_b2b_tender_notifications` (`auth.jwt()->>'role' = 'admin'`), identique à `b2b_tender_lots` (Task 1) |
| T-08-05 | Tampering | Opt-in `b2b_alerts_email` | accept | Colonne modifiable uniquement via endpoints existants soumis au RLS `professionals` ; aucun endpoint d'écriture ajouté cette phase |
| T-08-06 | Repudiation | Envois email | mitigate | Trace `b2b_tender_notifications.sent_at` insérée après chaque succès d'envoi (Task 3, étape 5) |
</threat_model>

<verification>
1. `npx vitest run tests/unit/notify-b2b-pros.test.ts` → vert
2. `npm test` → suite complète verte
3. `psql "postgresql://postgres:postgres@127.0.0.1:54322/postgres" -tAc "SELECT to_regclass('public.b2b_tender_notifications')"` → non vide
4. `git diff --stat` ne montre aucune modification de `server/utils/notifyProLead.ts`
</verification>

<success_criteria>
- La table `b2b_tender_notifications` et la colonne `professionals.b2b_alerts_email` existent en base locale ET remote.
- `app/types/database.types.ts` régénéré et committé.
- `server/utils/notifyB2bPros.ts` exporte les 3 fonctions ; le matching filtre bien zone active + catégorie + `is_verified` + `b2b_alerts_email`.
- Le HTML de l'email contient « AO qualifié BÂTI-AXE » et aucune coordonnée partenaire.
- 9 tests unitaires verts, suite complète verte.
</success_criteria>

<output>
Créer `.planning/phases/08-diffusion-automatique-confiance/08-01-SUMMARY.md`.
Y consigner : chemin réel des tests (`tests/unit/notify-b2b-pros.test.ts` vs celui
annoncé dans 08-VALIDATION.md), et le résultat de l'application locale
(`migration up --local` ou repli `psql`).
</output>
