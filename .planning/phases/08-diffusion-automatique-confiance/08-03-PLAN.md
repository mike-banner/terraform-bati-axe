---
phase: 08-diffusion-automatique-confiance
plan: 03
type: execute
wave: 3
depends_on: ["08-01", "08-02"]
files_modified:
  - app/components/admin/AdminB2bTab.vue
autonomous: false
requirements: [TEND-10, TEND-14]
user_setup: []

must_haves:
  truths:
    - "Le DirCo voit un bouton « Diffuser aux artisans » dans le panneau de qualification d'un dossier"
    - "Le bouton est désactivé tant que decision_status et un code postal 5 chiffres ne sont pas renseignés"
    - "Un clic affiche le nombre de lots diffusés et de notifications envoyées"
    - "Une erreur serveur (409 plafond, 422 prérequis) s'affiche en clair au DirCo, pas en échec silencieux"
    - "Le picker manuel recommended_pros reste présent pour la restitution mais est signalé comme non utilisé par la diffusion"
  artifacts:
    - path: "app/components/admin/AdminB2bTab.vue"
      provides: "Bouton Diffuser + état + restitution du résultat"
      contains: "Diffuser aux artisans"
  key_links:
    - from: "app/components/admin/AdminB2bTab.vue"
      to: "/api/v1/admin/b2b-requests/{id}/diffuse"
      via: "$fetch POST dans diffuse(r)"
      pattern: "/diffuse"
---

<objective>
Ajouter le déclencheur DirCo : un bouton « Diffuser aux artisans » par dossier
dans `AdminB2bTab.vue`, gaté sur les prérequis D-02, qui appelle l'endpoint du
plan 02 et restitue le résultat (ou l'erreur) au DirCo.

Purpose : TEND-10 — le broadcast se déclenche à la qualification DirCo, pas à
l'intake public. C'est la dernière brique qui rend la phase utilisable de bout
en bout.
Output : composant admin modifié + vérification humaine du parcours complet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-diffusion-automatique-confiance/08-CONTEXT.md
@.planning/phases/08-diffusion-automatique-confiance/08-01-SUMMARY.md
@.planning/phases/08-diffusion-automatique-confiance/08-02-SUMMARY.md
@CLAUDE.md
</context>

<interfaces>
<!-- Contrat de l'endpoint créé au plan 08-02 -->
```
POST /api/v1/admin/b2b-requests/{id}/diffuse
→ 200 { status: 'SUCCESS', zone: string, lots_diffused: number,
         notifications_sent: number, notifications_skipped: number,
         notifications_failed: number }
→ 401 / 403  (pas admin)
→ 404 'Dossier introuvable.'
→ 409 'Ce partenaire a déjà N appels d'offres actifs (plafond : M)…'
→ 422 'Renseignez le statut de décision…' | 'Renseignez un code postal…'
     | 'Aucun lot ouvert à diffuser sur ce dossier.'
     | 'Le code postal XXXXX n'est couvert par aucune zone active…'
```

<!-- État existant du composant (app/components/admin/AdminB2bTab.vue) -->
```typescript
const requests = ref<B2bRequest[]>([])
const errorMessage = ref<string | null>(null)
const restitutionId = ref<string | null>(null)
function ensureDraft(id: string): B2bDraft   // { …, decision_status, project_postal_code, recommended }
async function fetchRequests()
async function sendRestitution(r: B2bRequest)  // pattern $fetch + try/catch/finally à cloner
```
Le bloc « Restitution au donneur d'ordres » est autour des lignes 519-533 du
template ; le picker `recommended_pros` autour des lignes 490-517.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Bouton « Diffuser aux artisans » dans AdminB2bTab.vue</name>
  <files>app/components/admin/AdminB2bTab.vue</files>
  <read_first>
    - app/components/admin/AdminB2bTab.vue (état actuel complet — script setup lignes 90-250, template lignes 460-540)
    - .planning/phases/08-diffusion-automatique-confiance/08-02-SUMMARY.md (contrat réel de l'endpoint tel qu'implémenté)
    - .planning/phases/08-diffusion-automatique-confiance/08-CONTEXT.md (D-01, D-02, D-03, D-05)
  </read_first>
  <action>
D-01 : **un seul** bouton par AO (pas un par lot). D-03 : le clic traite tous les
lots `open` du dossier. D-05 : le picker `recommended_pros` n'est PAS supprimé
(il sert encore à la restitution au donneur d'ordres) — on ajoute seulement une
mention indiquant qu'il n'alimente pas la diffusion.

**Dans `<script setup>`**, à côté de `const restitutionId = ref<string | null>(null)` :
```typescript
// Phase 8 (D-01) — diffusion automatique aux artisans matchés zone × catégorie.
const diffusingId = ref<string | null>(null)
const diffuseResult = reactive<Record<string, string>>({})

function canDiffuse(r: B2bRequest): boolean {
  const d = ensureDraft(r.id)
  return Boolean(d.decision_status) && /^\d{5}$/.test(d.project_postal_code || '')
}

async function diffuse(r: B2bRequest) {
  diffusingId.value = r.id
  errorMessage.value = null
  delete diffuseResult[r.id]
  try {
    const res = await $fetch<{
      status: string; zone: string; lots_diffused: number
      notifications_sent: number; notifications_skipped: number; notifications_failed: number
    }>(`/api/v1/admin/b2b-requests/${r.id}/diffuse`, { method: 'POST' })
    diffuseResult[r.id] = `${res.lots_diffused} lot(s) diffusé(s) sur ${res.zone} — ${res.notifications_sent} artisan(s) notifié(s)`
      + (res.notifications_skipped ? `, ${res.notifications_skipped} ignoré(s) (plafond quotidien ou déjà notifiés)` : '')
      + (res.notifications_failed ? `, ${res.notifications_failed} échec(s) d'envoi` : '')
    await fetchRequests()
  } catch (err: any) {
    errorMessage.value = err.data?.statusMessage || err.message || 'Erreur de diffusion aux artisans.'
  } finally {
    diffusingId.value = null
  }
}
```
Attention : `fetchRequests()` réinitialise `draft` — appeler `delete diffuseResult[r.id]`
AVANT l'appel, et assigner `diffuseResult[r.id]` APRÈS `fetchRequests()` si
nécessaire pour que le message survive au rechargement (l'ordre ci-dessus place
l'assignation avant `fetchRequests`, ce qui est correct : `diffuseResult` n'est
pas touché par `fetchRequests`).

**Dans le template**, insérer un nouveau bloc **juste avant** le commentaire
`<!-- Restitution au donneur d'ordres -->` (autour de la ligne 519) :
```html
            <!-- Phase 8 (D-01) — Diffusion automatique aux artisans matchés -->
            <div class="mt-4 border-t border-border pt-4">
              <div class="flex items-center justify-between gap-3 flex-wrap">
                <p class="text-[11px] text-muted-foreground max-w-md">
                  Diffuse tous les lots ouverts de ce dossier aux artisans vérifiés
                  ayant une zone active correspondante et la catégorie du lot.
                  Nécessite le statut de décision et le code postal du chantier.
                </p>
                <button
                  @click="diffuse(r)"
                  :disabled="diffusingId === r.id || !canDiffuse(r)"
                  class="inline-flex items-center h-9 px-4 text-sm font-medium rounded-sm bg-safety text-white hover:bg-safety/90 transition-colors disabled:opacity-40"
                >
                  <svg v-if="diffusingId === r.id" class="w-4 h-4 animate-spin mr-2" fill="none" viewBox="0 0 24 24"><circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"/><path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4z"/></svg>
                  Diffuser aux artisans
                </button>
              </div>
              <p v-if="!canDiffuse(r)" class="mt-1.5 text-[10px] text-muted-foreground">
                Renseignez le statut de décision et un code postal à 5 chiffres, puis enregistrez, pour activer la diffusion.
              </p>
              <p v-if="diffuseResult[r.id]" class="mt-1.5 text-[11px] text-emerald-500">
                {{ diffuseResult[r.id] }}
              </p>
            </div>
```

**Mention D-05** : dans le label du picker (ligne ~493), remplacer
`Sous-traitants recommandés ({{ ensureDraft(r.id).recommended.length }}/3)` par
`Sous-traitants recommandés ({{ ensureDraft(r.id).recommended.length }}/3) — restitution au donneur d'ordres uniquement`.

Ne modifier ni `sendRestitution`, ni `saveChanges`, ni `togglePro`.
  </action>
  <acceptance_criteria>
    - `grep -q "Diffuser aux artisans" app/components/admin/AdminB2bTab.vue` → succès
    - `grep -q "/diffuse" app/components/admin/AdminB2bTab.vue` → succès
    - `grep -q "const diffusingId" app/components/admin/AdminB2bTab.vue` → succès
    - `grep -q "function canDiffuse" app/components/admin/AdminB2bTab.vue` → succès
    - `grep -q "restitution au donneur d'ordres uniquement" app/components/admin/AdminB2bTab.vue` → succès
    - `grep -c "sendRestitution" app/components/admin/AdminB2bTab.vue` reste à 2 (déclaration + appel), fonction non modifiée
    - `npm run build` réussit
    - `npm test` : suite complète verte
  </acceptance_criteria>
  <verify>
    <automated>grep -q "Diffuser aux artisans" app/components/admin/AdminB2bTab.vue && npm run build --silent && npm test</automated>
  </verify>
  <done>Le bouton existe, est gaté par `canDiffuse`, appelle l'endpoint, affiche le résultat ou l'erreur, et le build + la suite de tests passent.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Vérification humaine du parcours de diffusion complet</name>
  <files>(aucun — vérification manuelle, aucune modification de fichier)</files>
  <action>Faire dérouler à l'utilisateur les 8 étapes de vérification décrites dans `<how-to-verify>` ci-dessous, puis attendre son signal de reprise avant de clôturer la phase. Ne rien modifier tant que le retour n'est pas donné ; si un écart est signalé, le corriger dans le fichier concerné avant de relancer la vérification.</action>
  <what-built>
    Chaîne complète Phase 8 : table `b2b_tender_notifications` + colonne
    `b2b_alerts_email` (plan 01), moteur `notifyMatchedB2bPros` avec badge
    « AO qualifié BÂTI-AXE » (plan 01), endpoint admin `diffuse.post.ts` avec
    gardes 422/409 (plan 02), bouton « Diffuser aux artisans » (plan 03).
  </what-built>
  <how-to-verify>
    Prérequis : un seul serveur dev sur :3000 (`pkill -f "nuxt dev"` puis `npm run dev`),
    Supabase local sur 127.0.0.1:54321, session admin (`admin@batiaxe.com`).
    Les emails partent en `[MOCK EMAIL]` dans la console dev sauf si `EMAIL_LIVE=true`.

    1. Ouvrir http://localhost:3000/admin, onglet « Dossiers B2B ».
    2. Prendre un dossier syndic ayant au moins un lot (Phase 7). Vider le champ
       code postal → **le bouton « Diffuser aux artisans » doit être grisé** et la
       phrase d'aide affichée.
    3. Renseigner le statut de décision + un code postal des Yvelines couvert par
       une zone active (ex. `78000`), enregistrer → **le bouton devient actif**.
    4. Cliquer « Diffuser aux artisans ». Attendu : message vert du type
       « N lot(s) diffusé(s) sur <zone> — M artisan(s) notifié(s) ».
    5. Dans la console `npm run dev`, vérifier qu'un `[MOCK EMAIL]` par artisan
       matché est loggé, et que le corps HTML contient bien
       **« AO qualifié BÂTI-AXE »** et **aucune coordonnée du partenaire**
       (ni nom de contact, ni email, ni téléphone).
    6. Recliquer « Diffuser aux artisans » sur le même dossier sans rien changer.
       Attendu : **0 artisan notifié** (idempotence D-04), aucun nouvel email en
       console.
    7. Vérifier en base :
       `psql "postgresql://postgres:postgres@127.0.0.1:54322/postgres" -c "SELECT pro_id, lot_id, channel, sent_at FROM b2b_tender_notifications ORDER BY sent_at DESC LIMIT 10;"`
       et `… -c "SELECT action, metadata FROM audit_logs WHERE action='b2b_tender_diffused' ORDER BY created_at DESC LIMIT 3;"`
    8. Saisir un code postal hors zone (ex. `13001`), enregistrer, cliquer.
       Attendu : message d'erreur rouge « … n'est couvert par aucune zone active »,
       pas de page blanche ni de 500.
  </how-to-verify>
  <resume-signal>Taper « approuvé » ou décrire les écarts constatés (étape n° + comportement observé).</resume-signal>
  <verify>
    <automated>psql "postgresql://postgres:postgres@127.0.0.1:54322/postgres" -tAc "SELECT count(*) > 0 FROM b2b_tender_notifications" | grep -q t && psql "postgresql://postgres:postgres@127.0.0.1:54322/postgres" -tAc "SELECT count(*) > 0 FROM audit_logs WHERE action='b2b_tender_diffused'" | grep -q t && echo OK</automated>
  </verify>
  <done>L'utilisateur a répondu « approuvé » sur les 8 étapes ; `b2b_tender_notifications` contient au moins une ligne et `audit_logs` au moins une entrée `b2b_tender_diffused`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navigateur admin → API Nitro | L'état désactivé du bouton est purement cosmétique ; la sécurité réelle est côté serveur |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02 | Elevation of Privilege | Bouton « Diffuser » | mitigate | Le gating client `canDiffuse` est un confort UX ; l'autorisation et les prérequis sont revérifiés par `diffuse.post.ts` (`app_metadata.role === 'admin'`, `assertDiffusable`, `assertTenderQuota`) — vérifié à l'étape 8 du checkpoint |
| T-08-03 | Information Disclosure | Affichage du résultat de diffusion | mitigate | Le composant n'affiche que les compteurs renvoyés par l'API (`lots_diffused`, `notifications_sent/skipped/failed`, nom de zone) — aucune liste nominative d'artisans contactés |
| T-08-10 | Denial of Service (double-clic) | Bouton « Diffuser » | mitigate | `:disabled="diffusingId === r.id"` empêche le double envoi côté UI ; l'idempotence `UNIQUE(pro_id, lot_id, channel)` garantit qu'un rejeu serveur n'envoie rien de plus |
</threat_model>

<verification>
1. `npm run build` → réussi
2. `npm test` → suite complète verte
3. Checkpoint humain approuvé (8 étapes)
4. `psql … -c "SELECT count(*) FROM b2b_tender_notifications"` > 0 après l'étape 4
</verification>

<success_criteria>
- Le DirCo peut diffuser un AO en un clic depuis l'admin, avec un gating visible sur les prérequis.
- Une seconde diffusion ne renvoie aucun email (idempotence vérifiée en conditions réelles).
- Les erreurs serveur (plafond, code postal hors zone) sont visibles en clair dans l'UI.
- Le badge « AO qualifié BÂTI-AXE » est constaté dans l'email généré.
</success_criteria>

<output>
Créer `.planning/phases/08-diffusion-automatique-confiance/08-03-SUMMARY.md` avec
le résultat des 8 étapes du checkpoint et tout écart constaté.
</output>
