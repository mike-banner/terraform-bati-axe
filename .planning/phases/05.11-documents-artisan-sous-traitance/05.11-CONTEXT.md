# Phase 05.11 — Coffre-Fort Juridique & Capacité Sous-traitance (documents_artisan) — CONTEXT

## Source
- Spec client : `.planning/clients/20260818-BENCHMARK_ARTIBOX_APIS_INTELLIGENTES-SPEC_CLIENT.md` — §2.2 (briques), §2.4 (directive immédiate), §6.7 (devoir de vigilance), §9.2 (plan d'action).

## Pourquoi « immédiate »
Le client (email 17/08 12:12) demande l'intégration **immédiate** du schéma `documents_artisan` + colonnes sous-traitance, pour **pré-qualifier administrativement la base d'artisans AVANT la bascule B2B Majors** (Phase 7). Ne pas attendre.

## Point de design à trancher (planification)
- La table `verifications` existe déjà (Kbis/décennale, Phase 3). `documents_artisan` **l'étend** (URSSAF + statuts de validation API + activités souscrites) ou **la remplace**.
- **Reco** : étendre `verifications` si possible ; sinon nouvelle table `documents_artisan` + migration des données existantes.

## Décisions par défaut
- V1 = **statuts manuels + dates d'expiration** (pattern décennale Phase 5) ; API Infolégale/Paperless = **V2**.
- **Décennale (V2)** : pas d'annuaire public universel des assureurs → API de conformité BTP spécialisées **Once / Provigis / BatiDocs** (OCR attestation + vérif date + codes NAF/APE cochés). L'API activité/SIRET est déjà **✅ faite** (`siretLookup.ts`).
- Suspension auto à expiration + re-contrôle 6 mois (devoir de vigilance) = même mécanisme (check à la lecture ou cron `pg_cron` existant Phase 4).

## Schéma (draft — à figer dans 05.11-01)
Table **`documents_artisan`** :
- `id` uuid PK
- `professional_id` uuid FK → `professionals`
- `doc_type` enum (`kbis` | `urssaf` | `decennale`)
- `file_key` text (R2)
- `status` enum (`pending` | `valid` | `expired` | `suspended`)
- `expires_at` timestamptz
- `activities_subscribed` text[] (activités souscrites décennale)
- `validated_by_api` bool default false
- `last_reviewed_at` timestamptz (devoir de vigilance 6 mois)
- `created_at`, `updated_at`

Colonnes **`professionals`** :
- `is_available_subcontracting` bool default false
- `workforce_size` int nullable

## Plans (4)
1. **05.11-01** — Migration `documents_artisan` + colonnes sous-traitance + types + RLS.
2. **05.11-02** — Switch « Alerte Capacité » + effectif dans `/espace/dashboard` + endpoint PATCH.
3. **05.11-03** — Suspension auto à expiration + re-contrôle 6 mois (devoir de vigilance).
4. **05.11-04** — Vue admin documents (statuts, expirations) — fusionnée Phase 06.1.

## Dépendances
- **Phase 5** (badges SIRET/décennale + pattern upload R2).
- **Phase 06.1** (Console Admin — revue documents).
- **pg_cron** (Phase 4) pour le check périodique d'expiration.

## Lien avec la Phase 05.10 (Espace Partenaires)
Le workflow DirCo de la Phase 05.10 sélectionne les 2-3 sous-traitants validés à présenter au donneur d'ordres **à partir de `documents_artisan` valides** → 05.11 est un pré-requis métier de la sélection, pas de la landing.
