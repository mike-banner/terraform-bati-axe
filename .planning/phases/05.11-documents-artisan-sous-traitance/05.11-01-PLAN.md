---
phase: 05.11-documents-artisan-sous-traitance
plan: 01
type: execute
wave: 1
files_modified:
  - supabase/migrations/20260823000000_documents_artisan.sql
autonomous: true
requirements: [B2B-SC-01]

must_haves:
  truths:
    - "Table documents_artisan créée (doc_type kbis/urssaf/decennale, status pending/valid/expired/suspended, expires_at, activities_subscribed, validated_by_api, last_reviewed_at)"
    - "Colonnes professionals.is_available_subcontracting (bool) + workforce_size (int)"
    - "RLS : pro propriétaire + admin tout"
    - "Index sur professional_id, status, expires_at"
---

# Plan 05.11-01 — Schéma documents_artisan + capacité sous-traitance

## Objective
Poser le schéma du coffre-fort juridique (KBIS/URSSAF/Décennale centralisés) + la capacité de sous-traitance exposée sur `professionals`.

## Deliverables
- Migration `20260823000000_documents_artisan.sql` : enums `artisan_doc_type` / `artisan_doc_status`, table `documents_artisan`, colonnes `professionals` (`is_available_subcontracting`, `workforce_size` avec CHECK 1-999), RLS, index.

## Notes
- V1 = statuts manuels + dates d'expiration (pattern décennale Phase 5). API de conformité = V2.
- `verifications` (Phase 3, flux claim) reste inchangé ; `documents_artisan` est le coffre-fort étendu (URSSAF + activités + re-contrôle).

## Verification
- [x] Migration appliquée (db push)
- [x] Table + enums + colonnes présents, RLS actif
