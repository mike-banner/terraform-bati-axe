---
phase: 05.11-documents-artisan-sous-traitance
plan: 03
type: execute
wave: 2
depends_on: ["05.11-01"]
files_modified:
  - supabase/migrations/20260823000001_documents_artisan_cron.sql
autonomous: true
requirements: [B2B-SC-03, B2B-SC-04]

must_haves:
  truths:
    - "Cron pg_cron : documents valides → expired quand expires_at dépassée"
    - "Si un document requis expire → is_available_subcontracting forcé à false (fonction sync)"
    - "Devoir de vigilance 6 mois : last_reviewed_at null ou > 6 mois → flag needs_review (calculé)"
---

# Plan 05.11-03 — Suspension auto à expiration + devoir de vigilance 6 mois

## Objective
Automatiser la suspension à l'expiration des documents légaux et tracer le re-contrôle 6 mois (obligation donneur d'ordres).

## Deliverables
- Migration `20260823000001_documents_artisan_cron.sql` :
  - fonction `expire_artisan_documents()` : `valid` → `expired` si `expires_at < NOW()`
  - fonction `sync_professional_subcontracting()` : `is_available_subcontracting = false` si un doc kbis/urssaf/decennale est `expired`/`suspended`
  - cron horaire exécutant les deux (pattern pg_cron Phase 4)
- Flag `needs_review` (devoir de vigilance) : calculé à la lecture (admin) — `last_reviewed_at` null ou > 6 mois. Pas de colonne.

## Verification
- [x] Cron créé (SELECT cron.job) + fonctions présentes
- [x] Expiration d'un doc → statut expired + capacité coupée
