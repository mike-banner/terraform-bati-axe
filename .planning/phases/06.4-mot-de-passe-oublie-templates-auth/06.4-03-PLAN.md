---
phase: 06.4-mot-de-passe-oublie-templates-auth
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - supabase/templates/README.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [AUTH-TPL-02, AUTH-PWD-01, AUTH-PWD-02, AUTH-TPL-01, AUTH-PWD-03]
user_setup:
  - service: supabase-prod
    why: "Les templates de config.toml ne s'appliquent qu'à l'instance locale — aucune commande CLI ne les synchronise vers un projet hébergé"
    dashboard_config:
      - task: "Coller le HTML de supabase/templates/recovery.html dans le template Reset Password + sujet « Réinitialisez votre mot de passe BÂTI-AXE »"
        location: "Dashboard Supabase (projet xpwoczcbyamnjknloxgz) → Authentication → Email Templates"
      - task: "Coller invite.html, confirmation.html, email_change.html dans les templates correspondants avec leurs sujets"
        location: "Dashboard Supabase → Authentication → Email Templates"
      - task: "Passer la longueur minimale de mot de passe à 8"
        location: "Dashboard Supabase → Authentication → Policies (ou Providers → Email)"
      - task: "Ajouter https://dev.bati-axe.fr/pro/reinitialiser-mot-de-passe (et le domaine prod) aux Redirect URLs"
        location: "Dashboard Supabase → Authentication → URL Configuration"

must_haves:
  truths:
    - "Le flux complet fonctionne bout en bout sur l'instance locale : demande, e-mail brandé reçu, nouveau mot de passe accepté, reconnexion réussie"
    - "Un lien déjà utilisé affiche le message d'expiration et ne permet aucune saisie"
    - "La configuration prod à poser manuellement est écrite et localisable, pas dans la tête de quelqu'un"
    - "Les exigences AUTH-* de cette phase existent dans REQUIREMENTS.md"
  artifacts:
    - path: "supabase/templates/README.md"
      provides: "Checklist de report manuel vers le Dashboard Supabase prod"
      contains: "Authentication → Email Templates"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Exigences AUTH-PWD-01..03 et AUTH-TPL-01..02"
      contains: "AUTH-PWD-01"
  key_links:
    - from: "supabase/templates/README.md"
      to: "supabase/config.toml"
      via: "avertissement local-only"
      pattern: "local"
---

<objective>
Fermer la phase : vérifier le flux réel de bout en bout sur l'instance locale (le seul
endroit où il est observable), et transformer la partie non automatisable — la
configuration prod — en checklist écrite plutôt qu'en dette invisible.

Purpose: après les plans 01 et 02, `supabase start` montrera un e-mail parfait dans
Inbucket alors que la production continuera d'envoyer les templates Supabase par défaut,
en anglais, et d'accepter des mots de passe de 6 caractères. Ce plan empêche cette
illusion de « phase terminée ».
Output: 1 README de report prod, les exigences AUTH-* formalisées, 1 vérification humaine.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.4-mot-de-passe-oublie-templates-auth/06.4-RESEARCH.md
@.planning/phases/06.4-mot-de-passe-oublie-templates-auth/06.4-PKCE-FINDINGS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: README de report prod + formalisation des exigences AUTH-*</name>
  <files>supabase/templates/README.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - supabase/config.toml (blocs `[auth.email.template.*]`, `minimum_password_length`, `additional_redirect_urls` posés par le plan 01)
    - .planning/REQUIREMENTS.md (format exact des entrées : `- [ ] **CODE-NN**: description`, et sections existantes)
    - .planning/phases/06.4-mot-de-passe-oublie-templates-auth/06.4-RESEARCH.md (Pitfall 4 : aucune commande CLI ne pousse ces templates en prod)
  </read_first>
  <action>
**A. Créer `supabase/templates/README.md`** (français), contenant :

- Un avertissement en tête : « Les `content_path` déclarés dans `supabase/config.toml`
  ne s'appliquent **qu'à l'instance locale** (`supabase start`). Aucune commande de la CLI
  Supabase ne synchronise les templates Auth vers un projet hébergé. Sans le report manuel
  ci-dessous, la production envoie toujours les templates Supabase par défaut, en anglais. »
- Une checklist à cocher, projet `xpwoczcbyamnjknloxgz` :
  - [ ] `Authentication → Email Templates → Reset Password` : coller le contenu de
        `recovery.html`, sujet `Réinitialisez votre mot de passe BÂTI-AXE`
  - [ ] `Authentication → Email Templates → Invite user` : `invite.html`, sujet
        `Vous êtes invité·e sur BÂTI-AXE`
  - [ ] `Authentication → Email Templates → Confirm signup` : `confirmation.html`, sujet
        `Confirmez votre adresse e-mail BÂTI-AXE`
  - [ ] `Authentication → Email Templates → Change Email Address` : `email_change.html`,
        sujet `Confirmez votre nouvelle adresse e-mail BÂTI-AXE`
  - [ ] `Authentication → Policies` (ou `Providers → Email`) : longueur minimale de mot de
        passe = **8** (miroir de `minimum_password_length = 8` dans `config.toml`)
  - [ ] `Authentication → URL Configuration → Redirect URLs` : ajouter
        `https://dev.bati-axe.fr/pro/reinitialiser-mot-de-passe` et l'équivalent sur le
        domaine de production. Sans cette entrée, GoTrue rejette silencieusement le
        `redirectTo` et renvoie l'utilisateur sur `site_url`.
- Une section « Après modification d'un template » : rappeler de reporter le même
  changement des deux côtés (fichier local **et** Dashboard), sinon les deux divergent.

**B. Ajouter dans `.planning/REQUIREMENTS.md`** une section `### Authentification & E-mails Auth`
(placée dans « v1 Requirements », après la dernière section existante), au format déjà
utilisé par le fichier :

```
### Authentification & E-mails Auth
- [ ] **AUTH-PWD-01**: Un pro peut demander un lien de réinitialisation depuis /pro/claim ; le message affiché ne révèle jamais si l'adresse correspond à un compte existant
- [ ] **AUTH-PWD-02**: Une page /pro/reinitialiser-mot-de-passe permet de définir un nouveau mot de passe via la session de récupération, gère le lien expiré et ferme la session après le changement
- [ ] **AUTH-PWD-03**: La longueur minimale de mot de passe (8 caractères) est appliquée par le serveur Auth, pas seulement par l'UI
- [ ] **AUTH-TPL-01**: Les e-mails Auth natifs (recovery, invite, confirmation, email_change) sont en français, brandés copper/navy, avec le footer légal LCEN
- [ ] **AUTH-TPL-02**: Le report manuel de ces templates et réglages vers le Dashboard Supabase de production est documenté dans une checklist versionnée
```
Cocher `[x]` les items réellement livrés à ce stade (AUTH-PWD-01, AUTH-PWD-02, AUTH-TPL-01,
AUTH-TPL-02 pour la partie documentaire). Laisser AUTH-PWD-03 en `[ ]` tant que le
Dashboard prod n'a pas été réglé (le local est fait, la prod non).
  </action>
  <verify>
    <automated>grep -q "AUTH-PWD-01" .planning/REQUIREMENTS.md && grep -q "Email Templates" supabase/templates/README.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/templates/README.md` existe et contient `Authentication → Email Templates`
    - Il contient les 4 noms de fichiers `recovery.html`, `invite.html`, `confirmation.html`, `email_change.html`
    - Il contient `xpwoczcbyamnjknloxgz` et `URL Configuration`
    - Il contient explicitement que la configuration ne s'applique qu'à l'instance locale
    - `grep -c "AUTH-PWD-0\|AUTH-TPL-0" .planning/REQUIREMENTS.md` retourne au moins 5
    - `grep -q "AUTH-PWD-03" .planning/REQUIREMENTS.md` sort en code 0
  </acceptance_criteria>
  <done>La configuration prod est une checklist versionnée, et les exigences AUTH-* existent dans REQUIREMENTS.md.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Vérification humaine du flux complet en local</name>
  <files>(aucun — vérification uniquement, aucun fichier modifié)</files>
  <read_first>
    - .planning/phases/06.4-mot-de-passe-oublie-templates-auth/06.4-PKCE-FINDINGS.md (verdicts A1/A3 mesurés en 06.4-02 — référence en cas d'écart aux étapes 5 ou 9)
    - supabase/templates/recovery.html (contenu attendu de l'e-mail, à comparer avec ce qu'affiche Inbucket)
  </read_first>
  <action>
Préparer l'environnement, puis rendre la main à l'utilisateur sans rien modifier :
`npx supabase stop && npx supabase start` (recharge `config.toml` et les templates), puis
`npm run dev` (un seul serveur sur `:3000` — faire `pkill -f "nuxt dev"` d'abord en cas de
doublon). Vérifier que `npx supabase status` liste Inbucket sur `http://127.0.0.1:54324`,
présenter la procédure ci-dessous et attendre le retour de l'utilisateur.
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:54324 | grep -qx 200 && curl -s -o /dev/null -w '%{http_code}' http://localhost:3000/pro/reinitialiser-mot-de-passe | grep -qx 200 && echo OK</automated>
  </verify>
  <done>L'utilisateur a répondu « validé » après avoir déroulé les 10 étapes, ou les écarts constatés sont consignés.</done>
  <what-built>
    Plan 01 : 4 templates Auth brandés (français, copper/navy, footer LCEN) déclarés dans
    `supabase/config.toml`, `minimum_password_length` passé à 8, `localhost:3000` ajouté aux
    redirections autorisées.
    Plan 02 : bouton « Mot de passe oublié ? » fonctionnel sur `/pro/claim` (message neutre,
    cooldown 60 s) + page `/pro/reinitialiser-mot-de-passe` (états waiting/ready/expired/success,
    `signOut()` après changement).
    Plan 03 : checklist de report vers le Dashboard Supabase prod.

    Ce contrôle est manuel parce qu'il exige un token GoTrue réellement signé : les tests e2e
    du repo injectent un JWT fabriqué et ne peuvent pas produire un vrai lien de récupération.
  </what-built>
  <how-to-verify>
    1. `npx supabase stop && npx supabase start` (recharge la config et les templates), puis
       `npm run dev` (un seul serveur sur `:3000`).
    2. Ouvrir `http://localhost:3000/pro/claim`, onglet « Se connecter », cliquer
       « Mot de passe oublié ? ». Saisir une adresse de compte de test existante, envoyer.
       → Attendu : message « Si un compte existe pour cette adresse… » et bouton grisé
       affichant un décompte « Réessayer dans N s ».
    3. Recommencer avec une adresse **qui n'existe pas** (ex. `inconnu@exemple.fr`).
       → Attendu : **exactement le même message**. Toute différence est un défaut bloquant
       (énumération de comptes).
    4. Ouvrir Inbucket : `http://127.0.0.1:54324`.
       → Attendu : e-mail « Réinitialisez votre mot de passe BÂTI-AXE », en français,
       bandeau orange « BÂTI-AXE », bouton orange « Choisir un nouveau mot de passe »,
       mention « valable une heure », footer légal L34-5 en bas.
    5. Cliquer le lien du bouton.
       → Attendu : arrivée sur `/pro/reinitialiser-mot-de-passe`, brève phase
       « Vérification du lien… » puis formulaire de nouveau mot de passe. **Pas** de
       redirection vers `/pro/claim`.
    6. Saisir `court` (5 caractères) → attendu : refus avec « entre 8 et 72 caractères ».
       Saisir deux valeurs différentes dans les deux champs → attendu : « ne correspondent pas ».
    7. Saisir un mot de passe valide identique dans les deux champs, valider.
       → Attendu : message de succès puis redirection vers `/pro/claim`.
    8. Se reconnecter avec le **nouveau** mot de passe → attendu : connexion réussie.
    9. Recliquer le **même** lien de l'e-mail (déjà consommé).
       → Attendu : écran « Ce lien n'est plus valide » avec lien de retour, aucun formulaire
       de saisie, aucune session ouverte.
    10. Si un écart est constaté à l'étape 5 ou 9, comparer avec les verdicts A1/A3 de
        `06.4-PKCE-FINDINGS.md` — c'est là que le format réel de l'URL a été mesuré.
  </how-to-verify>
  <acceptance_criteria>
    - Étapes 2 et 3 : messages strictement identiques pour une adresse existante et inexistante
    - Étape 4 : e-mail en français, brandé, avec footer L34-5 visible
    - Étape 5 : le formulaire s'affiche, aucune redirection vers `/pro/claim`
    - Étapes 6 : les deux refus de validation s'affichent
    - Étape 8 : reconnexion réussie avec le nouveau mot de passe
    - Étape 9 : écran « Ce lien n'est plus valide », aucun formulaire rendu
  </acceptance_criteria>
  <resume-signal>Répondre « validé » ou décrire l'étape qui a échoué et ce qui s'est affiché à la place</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Instance locale → instance de production | Ce qui est vérifié en local ne s'applique pas automatiquement à la prod |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.4-13 | Repudiation | Écart silencieux entre config locale et prod | mitigate | `supabase/templates/README.md` versionné + bloc `user_setup` de ce plan : la config prod devient une tâche traçable, pas un oubli |
| T-06.4-14 | Information Disclosure | Énumération de comptes | mitigate | Vérifiée **expérimentalement** aux étapes 2-3 du checkpoint (adresse existante vs inexistante), pas seulement par revue de code |
| T-06.4-15 | Elevation of Privilege | Mot de passe faible accepté en production | accept (temporairement) | `minimum_password_length = 8` posé en local ; la prod reste à 6 tant que la checklist Dashboard n'est pas exécutée — AUTH-PWD-03 laissée décochée pour rendre l'écart visible |
</threat_model>

<verification>
1. `npx vitest run && npx playwright test` — suite complète verte
2. Checkpoint humain validé (flux réel observé dans Inbucket)
3. `supabase/templates/README.md` et les entrées AUTH-* présents
</verification>

<success_criteria>
- Flux complet vérifié à la main sur l'instance locale, y compris le cas « lien déjà utilisé »
- Aucune différence de message entre une adresse existante et une adresse inconnue
- Configuration prod écrite et traçable
</success_criteria>

<output>
Créer `.planning/phases/06.4-mot-de-passe-oublie-templates-auth/06.4-03-SUMMARY.md`
</output>
