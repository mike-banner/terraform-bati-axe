---
phase: 06.4
slug: mot-de-passe-oublie-templates-auth
status: approved
nyquist_compliant: true
wave_0_complete: false
created: 2026-08-31
---

# Phase 06.4 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest 29.x (unitaire) + Playwright (e2e) |
| **Config file** | `vitest.config.ts` (alias `~` → `.`, environment node) / `tests/e2e/` (Playwright) |
| **Quick run command** | `npx vitest run tests/unit/<fichier>.test.ts` |
| **Full suite command** | `npx vitest run && npx playwright test` |
| **Estimated runtime** | ~60 secondes |

---

## Sampling Rate

- **After every task commit:** Run `npx vitest run` (tests unitaires touchés)
- **After every plan wave:** Run `npx vitest run && npx playwright test` (suite complète, incluant `admin-login.spec.ts` pour non-régression du pattern d'auth)
- **Before `/gsd-verify-work`:** Full suite must be green **+ procédure manuelle Inbucket exécutée au moins une fois** (le flux réel de recovery n'est pas entièrement automatisable dans ce projet — limite acceptée, pas une dette)
- **Max feedback latency:** 60 secondes

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 06.4-01-0X | 01 | 1 | Lien "Mot de passe oublié ?" + appel `resetPasswordForEmail` | Énumération d'utilisateurs | Réponse neutre native Supabase, ne jamais révéler si l'e-mail existe | manuel + smoke e2e | — | ❌ W0 | ⬜ pending |
| 06.4-01-0X | 01 | 1 | Page reset détecte `PASSWORD_RECOVERY` et affiche le formulaire | Interception/réutilisation lien | Token recovery à usage unique, `signOut()` après changement de mot de passe | manuel uniquement | Procédure Inbucket (`http://127.0.0.1:54324`) | ❌ pas de test auto prévu | ⬜ pending |
| 06.4-02-0X | 02 | 1-2 | Templates HTML contiennent la palette copper/navy + footer LCEN | — | Cohérence visuelle avec 06.3 | unit | `npx vitest run tests/unit/auth-templates.test.ts` | ❌ W0 — à créer | ⬜ pending |
| 06.4-01-0X | 01 | 1 | Lien expiré affiche un message clair | Abus/DoS via spam de reset | Rate limiting natif Supabase + cooldown UI | manuel | Générer un lien, le réutiliser deux fois, observer `route.query.error_code` | ❌ pas automatisable simplement | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/unit/auth-templates.test.ts` — vérifie la présence de la palette copper/navy et du footer LCEN exact dans les 4 fichiers `supabase/templates/*.html`
- Aucune installation de framework nécessaire (Vitest déjà en place)
- Pas de fixture partagée nécessaire pour ce périmètre

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Détection de session `PASSWORD_RECOVERY` sur `/pro/reinitialiser-mot-de-passe` | Flux self-service pro | Nécessite un vrai lien GoTrue signé (token réel), impossible à obtenir via le pattern de mock JWT utilisé par les tests e2e existants | `supabase start` → déclencher `resetPasswordForEmail` → lire Inbucket (`http://127.0.0.1:54324`) → cliquer le lien → vérifier la page |
| Lien de recovery expiré ou déjà utilisé | Gestion d'expiration | Nécessite d'attendre l'expiration réelle (`otp_expiry`=3600s) ou de rejouer un token consommé | Générer un lien, attendre ou le réutiliser deux fois, observer `route.query.error_code` et le message affiché |
| Validation A1 : flux PKCE réel (`?code=` vs hash fragment) | Fiabilité de la page de reset | Comportement exact non re-testé contre l'instance locale de ce projet dans cette session (RESEARCH.md, Assumption A1) | Inspecter un vrai lien de recovery généré via Inbucket avant d'écrire la logique finale de la page |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-08-31
