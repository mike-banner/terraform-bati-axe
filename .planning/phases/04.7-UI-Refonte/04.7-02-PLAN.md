---
phase: 04.7-UI-Refonte
plan: 02
type: execute
wave: 2
depends_on: ["04.7-01"]
files_modified:
  - app/pages/index.vue
  - app/components/BeforeAfterSlider.vue
autonomous: true
requirements: [UI-LANDING-01]
must_haves:
  truths:
    - "Le hero de la landing est une grille Bento 12 colonnes asymétrique (variante C), pas un split 2 colonnes"
    - "Le titre principal est massif (text-6xl→text-8xl, tracking-tight, font-black)"
    - "Les cartes bento ont rounded-3xl, p-8/p-10, hover:-translate-y et ombre dynamique"
    - "Le CTA principal est orange, rounded-full, hover:scale-105 avec shadow-safety/20"
    - "Aucune classe cyan/green/emerald dans index.vue"
  artifacts:
    - path: "app/pages/index.vue"
      provides: "Hero Bento Grid Variant C + sections landing re-thémées"
      contains: "md:grid-cols-12"
    - path: "app/components/BeforeAfterSlider.vue"
      provides: "Widget avant/après aux bordures/radius charte"
  key_links:
    - from: "app/pages/index.vue"
      to: "app/assets/css/tailwind.css"
      via: "classe .bento-card + reveal-item"
      pattern: "bento-card"
---

<objective>
Reconstruire la landing autour du hero Bento Grid Variant C (sketch 001) : grille 12 colonnes asymétrique, typographie massive DM Sans, cartes rounded-3xl, micro-interactions orange. Restyler les sections restantes et le BeforeAfterSlider.

Purpose: La landing est la vitrine — c'est la preuve visible du changement drastique.
Output: `index.vue` en Bento Grid, zéro cyan/vert.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04.7-UI-Refonte/04.7-UI-SPEC.md
@.planning/phases/04.7-UI-Refonte/04.7-PATTERNS.md
@.agent/skills/sketch-findings-bati-axe/references/landing-hero.md
@.agent/skills/sketch-findings-bati-axe/sources/001-landing-hero/index.html
</context>

<tasks>

<task type="auto">
  <name>Tâche 1 : Reconstruire le hero en Bento Grid Variant C</name>
  <read_first>
    - app/pages/index.vue (fichier modifié — hero split 2 col lignes ~16-76, CTA lignes 37-43/182-188, stagger reveal scoped lignes ~194-211)
    - .agent/skills/sketch-findings-bati-axe/sources/001-landing-hero/index.html (VARIANT C, lignes 133-206 — STRUCTURE de référence, à traduire en Nuxt/Tailwind, pas copier)
    - .agent/skills/sketch-findings-bati-axe/references/landing-hero.md
    - .planning/phases/04.7-UI-Refonte/04.7-UI-SPEC.md (sections 2, 4, 5)
  </read_first>
  <action>
    Remplacer le hero split 2 colonnes par une grille Bento Variant C : conteneur `max-w-7xl mx-auto px-6 pt-12 pb-24`, grille `grid grid-cols-1 md:grid-cols-12 gap-6`. Reproduire structurellement les 4 cartes du sketch (adaptées au contenu BÂTI-AXE réel, ne pas inventer de fausses métriques SaaS génériques ; réutiliser le copy/valeur existant de la landing) :
    - Carte intro `md:col-span-8 bg-white rounded-3xl p-10 border border-slate-200 shadow-sm` avec titre `text-5xl md:text-7xl font-black tracking-tight text-slate-900` (partie secondaire en `text-slate-400`), sous-texte `text-lg text-slate-500`, et CTA principal vers `/simulateur`.
    - Carte accent `md:col-span-4 bg-industrial text-white rounded-3xl p-8` (metric/valeur clé de la mise en relation).
    - Carte `md:col-span-4 bg-slate-900 text-white rounded-3xl p-8` (liste de bénéfices / catégories artisans).
    - Carte visuelle `md:col-span-8 ... rounded-3xl p-8 border border-slate-200`.
    Ajouter la classe `.bento-card` (définie en plan 01) sur chaque carte et `.reveal-item` avec le conteneur parent `.reveal` pour le stagger.
    CTA principal : `bg-safety text-white px-8 py-4 rounded-full font-bold text-lg hover:scale-105 transition-transform shadow-lg shadow-safety/20` ; CTA secondaire : `bg-white border border-slate-200 text-slate-700 rounded-full`. Le lien de destination (`/simulateur`) et toute logique NuxtLink existante restent inchangés.
    Ne modifier AUCUN texte légal, lien, ou logique `useAsyncData`/fetch éventuelle du script — restyle du template uniquement.
  </action>
  <verify>
    <automated>grep -q "md:grid-cols-12" app/pages/index.vue && grep -q "rounded-3xl" app/pages/index.vue && grep -q "shadow-safety/20" app/pages/index.vue && grep -cE "cyan-|green-|emerald-" app/pages/index.vue | grep -qx 0 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `index.vue` contient `md:grid-cols-12`, au moins 3 occurrences de `rounded-3xl`, `bento-card`, `hover:scale-105`, `shadow-safety/20`
    - `grep -cE "cyan-|green-|emerald-|#22C55E|#0891B2|#22D3EE|#ECFEFF|#164E63" app/pages/index.vue` renvoie `0`
    - Le titre principal utilise `text-6xl`, `text-7xl` ou `text-8xl` avec `font-black`/`font-bold` + `tracking-tight`
  </acceptance_criteria>
  <done>Hero Bento 12 colonnes en place, typo massive, CTA orange rounded-full.</done>
</task>

<task type="auto">
  <name>Tâche 2 : Restyler les sections restantes de la landing + BeforeAfterSlider</name>
  <read_first>
    - app/pages/index.vue (sections sous le hero — cards, CTA secondaires, footer)
    - app/components/BeforeAfterSlider.vue (bordures, radius, poignée)
    - .planning/phases/04.7-UI-Refonte/04.7-UI-SPEC.md (section 6, classes communes)
  </read_first>
  <action>
    Harmoniser toutes les sections restantes d'index.vue sur la charte : cartes `bg-white rounded-3xl p-8 border border-slate-200 shadow-sm` avec `.bento-card` au survol ; titres de section `text-slate-900 font-bold` échelle agrandie ; corps `text-slate-500` ; tout ancien `rounded-md`/`rounded-lg` décoratif → `rounded-2xl`/`rounded-3xl` ; tout bouton → `rounded-full`. Généraliser `.reveal-item` aux items de grille des sections.
    BeforeAfterSlider.vue : passer le conteneur et les images en `rounded-3xl overflow-hidden border border-slate-200` ; poignée de drag en `bg-safety` (#F97316) ou `bg-white border border-slate-200` ; aucune couleur cyan/vert. Ne PAS toucher à la logique de drag (`@mousemove`/`@touchmove`, refs de position).
  </action>
  <verify>
    <automated>grep -cE "cyan-|green-|emerald-|#22C55E|#0891B2|#22D3EE|#ECFEFF|#164E63" app/pages/index.vue app/components/BeforeAfterSlider.vue | grep -v ':0$' | grep -q . && echo FAIL || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "cyan-|green-|emerald-|#22C55E|#0891B2|#22D3EE|#ECFEFF|#164E63" app/pages/index.vue app/components/BeforeAfterSlider.vue` renvoie `0` pour les deux fichiers
    - `BeforeAfterSlider.vue` contient `rounded-3xl` et conserve ses handlers de drag existants (aucun handler supprimé)
  </acceptance_criteria>
  <done>Landing entièrement re-thémée, slider aux formes charte, zéro cyan/vert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Visiteur anonyme → page publique | contenu marketing, aucune donnée sensible rendue |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-047-02-A | Information Disclosure | index.vue (page publique) | accept | Aucune donnée prospect/pro sur la landing ; restyle template seul |
| T-047-02-B | Denial of Service (a11y) | animations reveal/bento | mitigate | prefers-reduced-motion géré globalement (plan 01) ; contraste slate-900 sur slate-50 ≥ 4.5:1 |
| T-047-02-C | Tampering | drag handlers BeforeAfterSlider | mitigate | Restyle visuel uniquement, handlers `@mousemove`/`@touchmove` inchangés (vérifié en acceptance) |
| T-047-SC | Tampering | npm installs | mitigate | Aucune dépendance ajoutée |
</threat_model>

<verification>
- `grep -cE "cyan-|green-|emerald-|#22C55E|#0891B2|#22D3EE|#ECFEFF|#164E63" app/pages/index.vue app/components/BeforeAfterSlider.vue` == 0
- `npm run build` compile
</verification>

<success_criteria>
Landing en Bento Grid Variant C, typo massive DM Sans, cartes rounded-3xl, CTA orange, slider re-stylé, zéro vert/cyan.
</success_criteria>

<output>
Créer `.planning/phases/04.7-UI-Refonte/04.7-02-SUMMARY.md` à la fin.
</output>
