---
phase: 04.7-UI-Refonte
plan: 05
subsystem: frontend-forms
tags: [ui, forms, bento, tailwind]
dependency-graph:
  requires: ["04.7-01"]
  provides: ["formulaires-charte-bento"]
  affects:
    - app/pages/pro/claim.vue
    - app/pages/espace/profil.vue
    - app/pages/simulateur.vue
    - app/components/UploadProgress.vue
tech-stack:
  added: []
  patterns: ["tokens sémantiques gris/orange + .bento-card hérités du plan 01"]
key-files:
  created: []
  modified:
    - app/pages/pro/claim.vue
    - app/pages/espace/profil.vue
    - app/pages/simulateur.vue
    - app/components/UploadProgress.vue
decisions:
  - "Plan finalisé en exécution inline par l'orchestrateur : l'agent worktree a été coupé (tempête réseau ECONNRESET + plafond de session) sans commit"
  - "Bleu résiduel du simulateur (box magic-link mode dév + CTA) converti en slate/orange charte, bien qu'il ne soit pas capté par le grep 'cyan/green/emerald' — cohérence palette"
  - "Les 4 fichiers avaient déjà zéro couleur interdite (héritage des tokens du plan 01) ; le travail a porté sur radius/typo/layout bento"
metrics:
  duration: "inline (post-coupure agents)"
  completed: "2026-07-04"
---

# Phase 04.7 Plan 05 : Formulaires (claim, profil, simulateur) Summary

Les trois formulaires longs et le composant UploadProgress re-thémés sur la charte bento : inputs `rounded-xl border-slate-200`, sections en cartes `rounded-3xl`, boutons de soumission orange `rounded-full`, barres de progression `bg-safety`, typographie de titres agrandie.

## Ce qui a été fait

**Tâche 1 — claim.vue + profil.vue** (commit `style(04.7-05): ...claim et profil...`)
- `claim.vue` : inputs `rounded-md` → `rounded-xl border-slate-200` (6×), boutons submit `bg-foreground rounded-md` → `bg-safety text-white rounded-full hover:scale-105 shadow-safety/20` (2×), bouton secondaire en `rounded-full`, cartes de sections upload en `bento-card rounded-3xl p-8` (2×), titre `text-4xl md:text-5xl`
- `profil.vue` : inputs `rounded-xl border-slate-200`, pills de catégories `rounded-xl`, sections (logo, bio, catégories, téléphone, zone) converties en cartes `bento-card rounded-3xl border-slate-200 shadow-sm p-8` (5×), boutons Annuler/Enregistrer/Logo en `rounded-full` (Enregistrer en `bg-safety`)

**Tâche 2 — simulateur.vue + UploadProgress.vue** (commit `style(04.7-05): ...simulateur et UploadProgress...`)
- `simulateur.vue` : conteneur du wizard en carte `rounded-3xl border-slate-200 shadow-sm p-8 md:p-10`, barre de progression `bg-slate-100` remplie `bg-safety`, tuiles de sélection `rounded-2xl` avec état sélectionné `border-safety bg-safety/10 text-slate-900`, boutons Suivant/Précédent en `bg-safety rounded-full`, titres d'étape `text-3xl md:text-4xl`, box magic-link et textes dév passés de bleu → slate/orange charte
- `UploadProgress.vue` : piste `bg-slate-100`, barre remplie `bg-safety`

Aucune logique métier touchée : validation Zod, sélection multi-catégories (`categories TEXT[]`), appels presigned R2, `POST /api/v1/projects`, `useRequireAuth` et handlers d'étapes tous préservés (restyle de classes uniquement).

## Vérification

- `grep -cE "cyan-|green-|emerald-|#22C55E|#0891B2|#22D3EE|#ECFEFF|#164E63"` sur les 4 fichiers → `0` chacun
- `grep -c "blue-" app/pages/simulateur.vue` → `0` (bleu résiduel éliminé)
- rounded-3xl : claim 2, profil 5, simulateur 1 ; barres `bg-safety` présentes
- 17 occurrences de logique métier préservées (saveProfile/handleLogoUpload/selectCategory/projects POST/useRequireAuth/presign)
- `npm run build` compile sans erreur (gate complet post-vague 2)

## Deviations from Plan

Finalisé en inline par l'orchestrateur après coupures répétées des agents worktree (réseau + quota). Conversion du bleu simulateur ajoutée au périmètre (cohérence charte, non exigée par le grep gate).

## Self-Check: PASSED

- FOUND: app/pages/pro/claim.vue (rounded-3xl ×2, boutons rounded-full)
- FOUND: app/pages/espace/profil.vue (cartes rounded-3xl ×5)
- FOUND: app/pages/simulateur.vue (carte wizard rounded-3xl, barre bg-safety)
- FOUND: app/components/UploadProgress.vue (barre bg-safety)
