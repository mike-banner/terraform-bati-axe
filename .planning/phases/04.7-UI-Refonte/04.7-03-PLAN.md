---
phase: 04.7-UI-Refonte
plan: 03
type: execute
wave: 2
depends_on: ["04.7-01"]
files_modified:
  - app/pages/app/dashboard.vue
  - app/pages/app/messages.vue
  - app/components/PremiumBadge.vue
  - app/components/IdentityBreadcrumbs.vue
  - app/components/ui/PremiumBadge.vue
  - app/components/ui/IdentityBreadcrumbs.vue
  - app/components/ui/BadgeDecennaleCertifiee.vue
  - app/components/ui/BadgeEntrepriseVerifiee.vue
autonomous: true
requirements: [UI-DASHBOARD-01]
must_haves:
  truths:
    - "dashboard.vue rend <UiPremiumBadge> et <UiIdentityBreadcrumbs> (versions charte), plus les doublons racine"
    - "Les composants racine PremiumBadge.vue et IdentityBreadcrumbs.vue sont supprimés"
    - "BadgeDecennaleCertifiee et BadgeEntrepriseVerifiee sont en gris+orange (plus de green/cyan)"
    - "Le dashboard pro est en charte bento (cartes rounded-3xl, typo massive, badges statut amber/slate)"
    - "Aucune classe cyan/green/emerald dans les 8 fichiers"
  artifacts:
    - path: "app/pages/app/dashboard.vue"
      provides: "Dashboard pro re-thémé pointant vers les badges ui/"
      contains: "UiPremiumBadge"
    - path: "app/components/ui/BadgeEntrepriseVerifiee.vue"
      provides: "Badge SIRET vérifié en gris+orange"
  key_links:
    - from: "app/pages/app/dashboard.vue"
      to: "app/components/ui/PremiumBadge.vue"
      via: "auto-import Nuxt préfixe Ui"
      pattern: "UiPremiumBadge"
---

<objective>
Corriger le bug de doublons morts (dashboard.vue rend les vieux badges cyan racine au lieu des versions ui/ conformes), supprimer les doublons racine, restyler les badges SIRET/décennale encore en vert/cyan, et appliquer la charte bento au dashboard pro et à la page messages.

Purpose: Le dashboard est la page pro quotidienne ; les badges de confiance doivent refléter la nouvelle charte.
Output: dashboard + messages + 6 composants badge/breadcrumb re-thémés, doublons supprimés.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04.7-UI-Refonte/04.7-UI-SPEC.md
@.planning/phases/04.7-UI-Refonte/04.7-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Tâche 1 : Repointer dashboard vers les badges ui/ et supprimer les doublons racine</name>
  <read_first>
    - app/pages/app/dashboard.vue (imports/usages badges lignes ~192 IdentityBreadcrumbs, ~202 PremiumBadge, badge amber lignes ~203-209)
    - app/components/PremiumBadge.vue (racine, à supprimer)
    - app/components/IdentityBreadcrumbs.vue (racine, à supprimer)
    - app/components/ui/PremiumBadge.vue (version charte à conserver — analog de référence)
    - app/components/ui/IdentityBreadcrumbs.vue (version charte à conserver)
    - .planning/phases/04.7-UI-Refonte/04.7-PATTERNS.md (finding n°2)
  </read_first>
  <action>
    Dans dashboard.vue, remplacer `<IdentityBreadcrumbs ...>` par `<UiIdentityBreadcrumbs ...>` et `<PremiumBadge ...>` par `<UiPremiumBadge ...>` (même props). Vérifier par grep qu'aucun autre fichier ne référence `<PremiumBadge`/`<IdentityBreadcrumbs` (hors préfixe `Ui`) : `grep -rn "PremiumBadge\|IdentityBreadcrumbs" app/ --include=*.vue`. Si dashboard est bien le seul consommateur (confirmé par PATTERNS), supprimer `app/components/PremiumBadge.vue` et `app/components/IdentityBreadcrumbs.vue`.
    Retoucher au besoin ui/PremiumBadge.vue et ui/IdentityBreadcrumbs.vue pour raccorder aux tokens (déjà en hex charte `#64748B`/`#F97316`/`#334155`/`#F8FAFC` — aucune couleur à changer, vérifier seulement l'absence de cyan/green ; ces fichiers restent la référence badge du projet).
    Badge de statut amber du dashboard (lignes ~203-209) : conserver amber (amber n'est pas interdit — sert d'état "attention/pending"), harmoniser en `rounded-full px-3 py-1.5`.
  </action>
  <verify>
    <automated>test ! -f app/components/PremiumBadge.vue && test ! -f app/components/IdentityBreadcrumbs.vue && grep -q "UiPremiumBadge" app/pages/app/dashboard.vue && grep -q "UiIdentityBreadcrumbs" app/pages/app/dashboard.vue && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `app/components/PremiumBadge.vue` et `app/components/IdentityBreadcrumbs.vue` n'existent plus (`test ! -f`)
    - `dashboard.vue` contient `UiPremiumBadge` et `UiIdentityBreadcrumbs`, plus aucune balise `<PremiumBadge`/`<IdentityBreadcrumbs` non préfixée
    - `grep -rn "<PremiumBadge\b\|<IdentityBreadcrumbs\b" app/ --include=*.vue` renvoie vide
  </acceptance_criteria>
  <done>Dashboard branché sur les badges ui/ conformes, doublons racine supprimés.</done>
</task>

<task type="auto">
  <name>Tâche 2 : Restyler BadgeDecennaleCertifiee + BadgeEntrepriseVerifiee en gris+orange</name>
  <read_first>
    - app/components/ui/BadgeDecennaleCertifiee.vue (lignes 2 et 7 : green-100/green-200/green-800/green-600)
    - app/components/ui/BadgeEntrepriseVerifiee.vue (lignes 2 et 7 : cyan-100/cyan-200/cyan-800/cyan-600)
    - app/components/ui/PremiumBadge.vue (analog de référence : schéma exact à répliquer)
  </read_first>
  <action>
    Répliquer le schéma de ui/PremiumBadge.vue sur les deux badges : conteneur `inline-flex items-center gap-1.5 px-3 py-1.5 rounded-full border border-[#64748B]/30 text-[#334155] bg-[#F8FAFC] text-xs font-semibold` ; icône SVG `text-[#F97316]`. Remplacer donc `bg-green-100/80 border-green-200 text-green-800` et `text-green-600` (décennale) et les `cyan-*` équivalents (entreprise) par ce schéma. Conserver le hover d'élévation `.premium-badge` (transition box-shadow + translateY, garde reduced-motion) si présent, sinon l'ajouter à l'identique de PremiumBadge.
    Ne PAS toucher aux props ni à la logique conditionnelle d'affichage (`v-if` sur `decennal_status==='valid'` / `siret_status==='active'`) — restyle du template/style uniquement.
  </action>
  <verify>
    <automated>grep -cE "cyan-|green-|emerald-|#22C55E|#0891B2|#22D3EE|#ECFEFF|#164E63" app/components/ui/BadgeDecennaleCertifiee.vue app/components/ui/BadgeEntrepriseVerifiee.vue | grep -v ':0$' | grep -q . && echo FAIL || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "cyan-|green-|emerald-|#22C55E|#0891B2|#22D3EE|#ECFEFF|#164E63"` sur chacun des deux fichiers renvoie `0`
    - Chaque badge contient `border-[#64748B]/30` et une icône `text-[#F97316]`
    - Les `v-if` de condition d'affichage sont conservés à l'identique
  </acceptance_criteria>
  <done>Badges SIRET et décennale en gris+orange, zéro vert/cyan.</done>
</task>

<task type="auto">
  <name>Tâche 3 : Appliquer la charte bento au dashboard pro et à app/messages.vue</name>
  <read_first>
    - app/pages/app/dashboard.vue (structure des cartes, sections upload docs, statut)
    - app/pages/app/messages.vue (page messages pro — densité couleur faible)
    - .planning/phases/04.7-UI-Refonte/04.7-UI-SPEC.md (section 6, classes communes)
  </read_first>
  <action>
    dashboard.vue : cartes/sections en `bg-white rounded-3xl p-8 border border-slate-200 shadow-sm` + `.bento-card` au survol ; titres `text-slate-900 font-bold` échelle agrandie ; corps `text-slate-500` ; boutons `rounded-full` (CTA principal `bg-safety text-white hover:scale-105 shadow-safety/20`, secondaires `bg-white border border-slate-200`) ; anciens `rounded-md/lg` → `rounded-2xl/3xl`. Les blocs consommant déjà les tokens sémantiques (`bg-cta`, `text-primary`) héritent automatiquement du re-thème du plan 01 — vérifier le rendu, ajuster le radius si en dur.
    app/messages.vue : même traitement charte (conteneur, cartes, boutons). Ne PAS toucher à la logique de chargement des messages ni aux appels API.
  </action>
  <verify>
    <automated>grep -cE "cyan-|green-|emerald-|#22C55E|#0891B2|#22D3EE|#ECFEFF|#164E63" app/pages/app/dashboard.vue app/pages/app/messages.vue | grep -v ':0$' | grep -q . && echo FAIL || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "..."` (motif complet) sur dashboard.vue et app/messages.vue renvoie `0` pour chacun
    - dashboard.vue contient au moins 2 occurrences de `rounded-3xl` et `bento-card`
    - Aucun appel API / `useAsyncData` / `useRequireAuth` supprimé (diff = template/classes uniquement dans le script)
  </acceptance_criteria>
  <done>Dashboard et messages pro en charte bento, zéro vert/cyan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pro authentifié → dashboard SSR | page protégée par useRequireAuth ; affiche statut de vérification |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-047-03-A | Information Disclosure | v-if des badges (siret_status/decennal_status) | mitigate | Restyle template seul ; conditions d'affichage conservées à l'identique (acceptance) — un badge ne doit pas s'afficher hors de sa condition |
| T-047-03-B | Elevation of Privilege | useRequireAuth sur dashboard | mitigate | Composable d'auth non touché ; interdiction d'introduire un watchEffect fragile (invariant CLAUDE.md) |
| T-047-03-C | Denial of Service (a11y) | hover badges/cartes | mitigate | prefers-reduced-motion géré (plan 01 + .premium-badge) |
| T-047-SC | Tampering | npm installs | mitigate | Aucune dépendance ajoutée |
</threat_model>

<verification>
- `grep -cE "cyan-|green-|emerald-|#22C55E|#0891B2|#22D3EE|#ECFEFF|#164E63" app/pages/app/dashboard.vue app/pages/app/messages.vue app/components/ui/Badge*.vue` == 0
- `test ! -f app/components/PremiumBadge.vue`
- `npm run build` compile
</verification>

<success_criteria>
Doublons morts supprimés, badges de confiance gris+orange, dashboard/messages pro en charte bento, zéro vert/cyan.
</success_criteria>

<output>
Créer `.planning/phases/04.7-UI-Refonte/04.7-03-SUMMARY.md` à la fin.
</output>
