---
phase: 04.7-UI-Refonte
plan: 04
type: execute
wave: 2
depends_on: ["04.7-01"]
files_modified:
  - app/pages/espace/leads/index.vue
  - app/pages/espace/leads/[id].vue
  - app/pages/espace/messages/index.vue
  - app/components/LeadAge.vue
  - app/components/LeadCountdown.vue
autonomous: true
requirements: [UI-DASHBOARD-01]
must_haves:
  truths:
    - "La grille de leads passe en cartes bento (1→2→3 col) rounded-3xl avec hover d'élévation"
    - "La page détail lead est en charte (sections rounded-3xl, typo massive, CTA orange)"
    - "LeadAge n'utilise plus de vert (ramp fresh=slate → mid=amber → old=red)"
    - "Aucune classe cyan/green/emerald dans les 5 fichiers"
  artifacts:
    - path: "app/pages/espace/leads/index.vue"
      provides: "Dashboard leads en grille bento"
      contains: "rounded-3xl"
    - path: "app/components/LeadAge.vue"
      provides: "Badge de fraîcheur sans vert"
  key_links:
    - from: "app/pages/espace/leads/index.vue"
      to: "app/components/LeadAge.vue"
      via: "rendu du badge de fraîcheur par carte"
      pattern: "LeadAge"
---

<objective>
Appliquer la charte bento au tableau de bord leads (grille responsive 1→2→3 colonnes), à la fiche lead détaillée et à la messagerie espace, et retirer le vert résiduel du badge de fraîcheur LeadAge (ramp sémantique slate/amber/red).

Purpose: Le dashboard leads est le cœur de valeur du pro Premium.
Output: 5 fichiers re-thémés, zéro vert/cyan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04.7-UI-Refonte/04.7-UI-SPEC.md
@.planning/phases/04.7-UI-Refonte/04.7-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Tâche 1 : Restyler la grille leads et la fiche détail en bento</name>
  <read_first>
    - app/pages/espace/leads/index.vue (grille de leads, cartes, filtres, widget marché local)
    - app/pages/espace/leads/[id].vue (fiche détail, sections coordonnées masquées/débloquées)
    - .planning/phases/04.7-UI-Refonte/04.7-UI-SPEC.md (sections 4, 5, 6)
    - CLAUDE.md (invariant masquage serveur ADR-004 — ne pas altérer les v-if de masquage)
  </read_first>
  <action>
    leads/index.vue : grille `grid grid-cols-1 md:grid-cols-2 xl:grid-cols-3 gap-6` ; chaque lead card `bg-white rounded-3xl p-8 border border-slate-200 shadow-sm` + classe `.bento-card` ; titres `text-slate-900 font-bold`, meta `text-slate-500` ; boutons de déblocage/CTA `rounded-full bg-safety text-white hover:scale-105 shadow-safety/20`. Widget "Marché local" et bannières paywall : cartes `rounded-3xl border border-slate-200`. Badges de qualif : amber pour manquant, slate/orange pour validé (jamais green).
    leads/[id].vue : sections en `rounded-3xl p-8 border border-slate-200` ; bloc coordonnées : restyle visuel UNIQUEMENT, ne jamais modifier les `v-if`/`v-else` qui distinguent état masqué (`***`) vs débloqué (invariant ADR-004 — le masquage est serveur, le template affiche ce que l'API renvoie). CTA d'abonnement `rounded-full` orange.
    Ne toucher à aucun appel API (`useAsyncData`/`$fetch`), à `useRequireAuth`, ni à la logique conditionnelle de masquage.
  </action>
  <verify>
    <automated>grep -cE "cyan-|green-|emerald-|#22C55E|#0891B2|#22D3EE|#ECFEFF|#164E63" app/pages/espace/leads/index.vue app/pages/espace/leads/[id].vue | grep -v ':0$' | grep -q . && echo FAIL || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "..."` (motif complet) sur les deux fichiers renvoie `0`
    - leads/index.vue contient `xl:grid-cols-3` et `rounded-3xl` et `bento-card`
    - Le nombre de blocs `v-if`/`v-else` dans leads/[id].vue est identique avant/après (masquage préservé) — vérifier `grep -c "v-if\|v-else" app/pages/espace/leads/[id].vue` inchangé
  </acceptance_criteria>
  <done>Grille leads et fiche détail en charte bento, masquage ADR-004 intact.</done>
</task>

<task type="auto">
  <name>Tâche 2 : Retirer le vert de LeadAge, restyler LeadCountdown et la messagerie espace</name>
  <read_first>
    - app/components/LeadAge.vue (ligne 42 : fresh = text-emerald-700 bg-emerald-50 border-emerald-200)
    - app/components/LeadCountdown.vue (compte à rebours déblocage 72h)
    - app/pages/espace/messages/index.vue (liste de messages)
    - .planning/phases/04.7-UI-Refonte/04.7-UI-SPEC.md
  </read_first>
  <action>
    LeadAge.vue : la ramp de fraîcheur (`fresh`/`mid`/`old`) ne doit plus contenir de green/emerald. Remplacer `fresh` `text-emerald-700 bg-emerald-50 border-emerald-200` par un ton neutre positif slate : `text-slate-700 bg-slate-100 border-slate-200`. Conserver `mid` (amber) et `old` (red) tels quels (amber/red autorisés). Passer le badge en `rounded-full`. La logique de calcul d'âge (`created_at`, seuils 24h/7j) reste inchangée.
    LeadCountdown.vue : restyle visuel (rounded-full, couleurs slate/orange), logique de timer inchangée.
    espace/messages/index.vue : cartes `rounded-3xl border border-slate-200`, boutons `rounded-full`, aucune couleur cyan/vert. Logique inchangée.
  </action>
  <verify>
    <automated>grep -cE "cyan-|green-|emerald-|#22C55E|#0891B2|#22D3EE|#ECFEFF|#164E63" app/components/LeadAge.vue app/components/LeadCountdown.vue app/pages/espace/messages/index.vue | grep -v ':0$' | grep -q . && echo FAIL || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "..."` (motif complet) sur les 3 fichiers renvoie `0`
    - LeadAge.vue conserve 3 crans de ramp (fresh/mid/old) ; `mid` contient toujours `amber`, `old` toujours `red`
    - LeadAge.vue et LeadCountdown.vue conservent leur logique de calcul (aucun computed/prop supprimé)
  </acceptance_criteria>
  <done>Badges de fraîcheur/compte à rebours et messagerie espace sans vert/cyan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pro authentifié → API leads (masquage serveur) | coordonnées prospect masquées côté serveur (ADR-004) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-047-04-A | Information Disclosure | v-if masquage dans leads/[id].vue | mitigate | Restyle visuel seul ; interdiction absolue de modifier les conditions distinguant `***` vs coordonnées ; comptage v-if/v-else vérifié inchangé |
| T-047-04-B | Elevation of Privilege | useRequireAuth sur pages /espace | mitigate | Composable d'auth non touché ; pas de watchEffect fragile introduit |
| T-047-04-C | Denial of Service (a11y) | hover cartes leads | mitigate | prefers-reduced-motion géré globalement (plan 01) |
| T-047-SC | Tampering | npm installs | mitigate | Aucune dépendance ajoutée |
</threat_model>

<verification>
- `grep -cE "cyan-|green-|emerald-|#22C55E|#0891B2|#22D3EE|#ECFEFF|#164E63" app/pages/espace/leads/*.vue app/pages/espace/messages/index.vue app/components/LeadAge.vue app/components/LeadCountdown.vue` == 0
- `npm run build` compile
</verification>

<success_criteria>
Dashboard leads en grille bento responsive, fiche détail charte (masquage intact), badges de fraîcheur/timer sans vert, messagerie espace re-thémée.
</success_criteria>

<output>
Créer `.planning/phases/04.7-UI-Refonte/04.7-04-SUMMARY.md` à la fin.
</output>
