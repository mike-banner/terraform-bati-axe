---
phase: 04.7-UI-Refonte
plan: 05
type: execute
wave: 2
depends_on: ["04.7-01"]
files_modified:
  - app/pages/pro/claim.vue
  - app/pages/espace/profil.vue
  - app/pages/simulateur.vue
  - app/components/UploadProgress.vue
autonomous: true
requirements: [UI-FORMS-01]
must_haves:
  truths:
    - "Les formulaires (claim, profil, simulateur) adoptent inputs/labels/sections charte"
    - "Les cartes de sections de formulaire sont rounded-3xl border-slate-200"
    - "Les boutons de soumission sont orange rounded-full"
    - "Aucune classe cyan/green/emerald dans les 4 fichiers"
  artifacts:
    - path: "app/pages/pro/claim.vue"
      provides: "Onboarding pro re-thémé (densité tokens la plus élevée)"
      contains: "rounded-3xl"
  key_links:
    - from: "app/pages/pro/claim.vue"
      to: "app/assets/css/tailwind.css"
      via: "tokens sémantiques + classe .input/.card re-thémées"
      pattern: "bg-cta|text-primary"
---

<objective>
Re-thémer les trois formulaires longs (claim onboarding pro, profil pro, simulateur multi-étapes) et le composant UploadProgress sur la charte : inputs cohérents, sections en cartes rounded-3xl, boutons orange rounded-full, échelle typographique agrandie. Ces pages ont la plus forte densité de tokens sémantiques — elles héritent de la couleur du plan 01 mais nécessitent l'édition radius/typo/layout.

Purpose: Formulaires = friction de conversion ; ils doivent être impeccables et cohérents.
Output: 4 fichiers re-thémés, zéro vert/cyan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04.7-UI-Refonte/04.7-UI-SPEC.md
@.planning/phases/04.7-UI-Refonte/04.7-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Tâche 1 : Re-thémer claim.vue et profil.vue (formulaires pro)</name>
  <read_first>
    - app/pages/pro/claim.vue (densité tokens 45 — sections upload, SIRET, catégories, consentements)
    - app/pages/espace/profil.vue (densité tokens 36 — édition infos publiques, catégories multi-select)
    - .planning/phases/04.7-UI-Refonte/04.7-UI-SPEC.md (sections 4, 6)
    - CLAUDE.md (sélection multi-catégories via checkboxes — marché dynamique ; upload R2 presigned — ne pas toucher)
  </read_first>
  <action>
    Les deux pages : chaque section de formulaire dans une carte `bg-white rounded-3xl p-8 md:p-10 border border-slate-200 shadow-sm` ; en-têtes de section `text-2xl md:text-3xl font-bold text-slate-900`, aides `text-slate-500` ; inputs harmonisés (bordure `border-slate-200`, focus `ring` orange via token, `rounded-xl`) — réutiliser la classe utilitaire `.input` (re-thémée en plan 01) ou classes Tailwind équivalentes ; checkboxes de catégories en pills `rounded-full border-slate-200` avec état actif `bg-safety/10 border-safety text-slate-900` ; boutons de soumission `bg-safety text-white rounded-full px-8 py-4 hover:scale-105 shadow-safety/20`, boutons secondaires `bg-white border border-slate-200 rounded-full`.
    Interdits : modifier la logique de sélection multi-catégories (`categories TEXT[]`), les appels presigned R2, la validation, ou `useRequireAuth`. Restyle template/classes uniquement.
  </action>
  <verify>
    <automated>grep -cE "cyan-|green-|emerald-|#22C55E|#0891B2|#22D3EE|#ECFEFF|#164E63" app/pages/pro/claim.vue app/pages/espace/profil.vue | grep -v ':0$' | grep -q . && echo FAIL || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "..."` (motif complet) sur claim.vue et profil.vue renvoie `0` pour chacun
    - Chaque fichier contient au moins 2 `rounded-3xl` et des boutons `rounded-full`
    - Aucune fonction de soumission / upload / `useRequireAuth` supprimée (diff script = néant ou cosmétique)
  </acceptance_criteria>
  <done>Formulaires pro claim + profil en charte, zéro vert/cyan.</done>
</task>

<task type="auto">
  <name>Tâche 2 : Re-thémer simulateur.vue (multi-étapes) et UploadProgress.vue</name>
  <read_first>
    - app/pages/simulateur.vue (densité tokens 24 — 6 étapes, sélection nature projet à icônes, barre de progression)
    - app/components/UploadProgress.vue (barre de progression upload)
    - .planning/phases/04.7-UI-Refonte/04.7-UI-SPEC.md
  </read_first>
  <action>
    simulateur.vue : conteneur d'étape en carte `bg-white rounded-3xl p-8 md:p-10 border border-slate-200 shadow-sm` ; titres d'étape `text-3xl md:text-4xl font-bold text-slate-900` ; barre de progression en `bg-slate-100` remplie `bg-safety` ; tuiles de sélection (nature projet) en `rounded-2xl border-slate-200`, état sélectionné `border-safety bg-safety/10` ; boutons Suivant/Précédent `rounded-full` (principal orange, retour `bg-white border-slate-200`). Conserver toute la logique d'étapes, validation Zod côté client, et l'appel `POST /api/v1/projects` inchangés.
    UploadProgress.vue : barre remplie en `bg-safety`, conteneur `bg-slate-100 rounded-full`, texte `text-slate-500`. Aucune couleur cyan/vert. Logique de progression inchangée.
  </action>
  <verify>
    <automated>grep -cE "cyan-|green-|emerald-|#22C55E|#0891B2|#22D3EE|#ECFEFF|#164E63" app/pages/simulateur.vue app/components/UploadProgress.vue | grep -v ':0$' | grep -q . && echo FAIL || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "..."` (motif complet) sur simulateur.vue et UploadProgress.vue renvoie `0` pour chacun
    - simulateur.vue contient `rounded-3xl` et la barre de progression utilise `bg-safety`
    - La logique des 6 étapes et l'appel projects POST sont conservés (aucun handler d'étape supprimé)
  </acceptance_criteria>
  <done>Simulateur et barre d'upload en charte, zéro vert/cyan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Particulier/Pro anonyme → soumission formulaire (Zod + API) | saisie utilisateur validée serveur |
| Pro → upload documents R2 (presigned) | documents sensibles isolés (ADR-003) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-047-05-A | Tampering | validation Zod / logique multi-catégories | mitigate | Restyle template seul ; validation et `categories TEXT[]` non touchées (invariant marché dynamique) |
| T-047-05-B | Information Disclosure | flux upload R2 presigned | mitigate | Aucune modification des appels presigned (ADR-003) ; restyle visuel de la barre uniquement |
| T-047-05-C | Denial of Service (a11y) | focus states inputs | mitigate | Focus ring orange conservé/renforcé ; contraste labels slate-900 sur blanc ≥ 4.5:1 |
| T-047-SC | Tampering | npm installs | mitigate | Aucune dépendance ajoutée |
</threat_model>

<verification>
- `grep -cE "cyan-|green-|emerald-|#22C55E|#0891B2|#22D3EE|#ECFEFF|#164E63" app/pages/pro/claim.vue app/pages/espace/profil.vue app/pages/simulateur.vue app/components/UploadProgress.vue` == 0
- `npm run build` compile
</verification>

<success_criteria>
Formulaires claim/profil/simulateur et UploadProgress en charte (inputs, sections rounded-3xl, boutons orange), logique métier intacte, zéro vert/cyan.
</success_criteria>

<output>
Créer `.planning/phases/04.7-UI-Refonte/04.7-05-SUMMARY.md` à la fin.
</output>
