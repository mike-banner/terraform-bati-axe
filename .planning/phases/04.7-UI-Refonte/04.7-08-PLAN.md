---
phase: 04.7
plan: 08
type: execute
wave: 4
depends_on: [04.7-07]
files_modified:
  - app/components/ui/IdentityBreadcrumbs.vue
  - app/components/ui/PremiumBadge.vue
  - app/pages/app/dashboard.vue
  - app/pages/pro/profile.vue
autonomous: false
requirements: [UI-PREMIUM-01]
user_setup: []

must_haves:
  truths:
    - "Les composants premium respectent la charte MASTER.md (Figtree, Noto Sans, Couleurs Cyan/Vert)"
    - "Le PremiumBadge utilise un effet CSS avancé (glow, shimmer ou glassmorphism)"
    - "Les micro-interactions n'altèrent pas le layout (pas de layout shift au survol)"
    - "Les animations respectent `prefers-reduced-motion`"
  artifacts: []
  key_links: []
---

<objective>
Implémenter les composants UI "Haut de Gamme" discutés avec l'utilisateur pour renforcer la confiance B2B et l'effet premium de la plateforme BÂTI-AXE.
Cela inclut : un fil d'Ariane identitaire (Breadcrumbs), un badge "Artisan Premium" animé (CSS/JS effects), et l'intégration de micro-interactions fluides sur les cartes de chantiers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@design-system/bati-axe/MASTER.md
@app/pages/app/dashboard.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Création du composant PremiumBadge</name>
  <files>
    - app/components/ui/PremiumBadge.vue
  </files>
  <action>
Créer un composant réutilisable `PremiumBadge.vue`.
- Design : Fond légèrement teinté (ex: vert d'eau ou cyan), texte en gras (Figtree).
- Effet JS/CSS : Ajouter une animation subtile (ex: un effet "shimmer" continu ou un léger "glow" au survol) pour marquer l'exclusivité.
- Accessibilité : S'assurer que le contraste reste WCAG AA et que l'animation se désactive si `prefers-reduced-motion` est actif.
  </action>
  <verify>
    <automated>
grep -q "PremiumBadge" app/components/ui/PremiumBadge.vue || echo "File not created"
    </automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Création du composant IdentityBreadcrumbs</name>
  <files>
    - app/components/ui/IdentityBreadcrumbs.vue
  </files>
  <action>
Créer un composant réutilisable `IdentityBreadcrumbs.vue`.
- Objectif : Offrir une navigation claire et rassurer l'artisan sur sa position (ex: `BÂTI-AXE > Espace Pro > Mes Chantiers`).
- Design : Utilisation de la police Noto Sans, texte grisé avec un chevron SVG entre les niveaux, dernier niveau mis en évidence (couleur Cyan primaire).
- Ajouter des liens fonctionnels `NuxtLink` si nécessaire.
  </action>
  <verify>
    <automated>
grep -q "IdentityBreadcrumbs" app/components/ui/IdentityBreadcrumbs.vue || echo "File not created"
    </automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Intégration et Micro-Interactions sur le Dashboard</name>
  <files>
    - app/pages/app/dashboard.vue
  </files>
  <action>
Injecter les nouveaux composants premium dans le Dashboard (`dashboard.vue`) et améliorer les interactions.
- Insérer le `IdentityBreadcrumbs` en haut de la page.
- Appliquer le `PremiumBadge` au statut de l'artisan (si `is_verified` ou Premium).
- Ajouter des micro-interactions JS/CSS sur les cartes de leads/chantiers :
  - Survol : Élévation douce (shadow-lg) et translation verticale (`-translate-y-1`) avec une transition fluide (`transition-all duration-300`).
  - Assurer la cohérence avec le style "Vibrant & Block-based" du MASTER.md.
  </action>
  <verify>
    <automated>
grep -q "PremiumBadge" app/pages/app/dashboard.vue || echo "Badge non intégré"
grep -q "IdentityBreadcrumbs" app/pages/app/dashboard.vue || echo "Breadcrumbs non intégrés"
    </automated>
  </verify>
  <done>
    - Composants premium codés et intégrés sans erreurs.
  </done>
</task>

<task type="checkpoint:human-verify">
  <what-built>
Les composants haut de gamme (Badges animés, Breadcrumbs) et les micro-interactions ont été ajoutés au Dashboard.
  </what-built>
  <how-to-verify>
1. Va sur la page `/app/dashboard` (ou `/espace/leads` selon le routage actuel) dans ton navigateur.
2. Vérifie la présence et le design du Fil d'Ariane en haut.
3. Vérifie l'animation et le rendu "Premium" du badge d'Artisan.
4. Passe la souris sur une carte pour valider la micro-interaction (élévation fluide, pas de bug visuel).
  </how-to-verify>
  <resume-signal>Type "approved" si le rendu Premium est validé !</resume-signal>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.7-08-1 | Accessibilité | Animations CSS/JS | mitigate | Utiliser la media query prefers-reduced-motion pour couper les animations si besoin. |
</threat_model>

<verification>
**Phase 4.7-08 Completion Verification:**
- [ ] PremiumBadge.vue créé avec effet haut de gamme (shimmer/glow).
- [ ] IdentityBreadcrumbs.vue créé avec clarté B2B.
- [ ] Composants intégrés dans le dashboard.
- [ ] Micro-interactions fluides implémentées sur les éléments interactifs.
</verification>

<success_criteria>
✅ Le design est enrichi avec des éléments premiums B2B.
✅ L'expérience utilisateur est plus fluide grâce aux micro-interactions JS/CSS.
✅ Le code reste propre et conforme à Nuxt 3 / Tailwind.
</success_criteria>

<output>
Create `.planning/phases/04.7-UI-Refonte/04.7-08-SUMMARY.md`
When done.
</output>
