---
phase: 04.7-UI-Refonte
plan: 08
type: execute
wave: 3
depends_on: ["04.7-02", "04.7-03", "04.7-04", "04.7-05", "04.7-06", "04.7-07"]
files_modified: []
autonomous: false
requirements: [UI-AUDIT-01]
must_haves:
  truths:
    - "grep transversal vert/cyan sur app/ retourne 0 usage décoratif"
    - "Aucune régression d'accessibilité (contraste AA, focus visibles, reduced-motion)"
    - "Aucun scroll horizontal aux breakpoints 375/768/1024/1440"
    - "L'utilisateur valide visuellement le changement drastique gris+orange"
  artifacts:
    - path: ".planning/phases/04.7-UI-Refonte/04.7-08-SUMMARY.md"
      provides: "Rapport d'audit final + validation humaine"
  key_links:
    - from: "audit grep gate"
      to: "toutes les pages/composants app/"
      via: "critère transversal zéro vert/cyan"
      pattern: "cyan-|green-|emerald-"
---

<objective>
Vérification transversale de fin de phase : gate zéro vert/cyan sur tout `app/`, audit d'accessibilité et responsive, puis checkpoint de validation visuelle humaine (le changement drastique doit être immédiatement visible).

Purpose: Garantir que le critère d'acceptation transversal est tenu et obtenir le feu vert utilisateur.
Output: Rapport d'audit + approbation humaine.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04.7-UI-Refonte/04.7-UI-SPEC.md
@.planning/phases/04.7-UI-Refonte/04.7-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Tâche 1 : Gate transversal zéro vert/cyan + build + audit a11y/responsive</name>
  <read_first>
    - .planning/phases/04.7-UI-Refonte/04.7-CONTEXT.md (critère transversal, interactions verrouillées)
    - .planning/phases/04.7-UI-Refonte/04.7-UI-SPEC.md (section 5 interactions, accessibilité)
  </read_first>
  <action>
    Exécuter le gate transversal : `grep -rnE "cyan-|green-|emerald-|#22C55E|#0891B2|#22D3EE|#ECFEFF|#164E63" app/`. Résultat attendu : AUCUN usage décoratif. Si une occurrence subsiste, la corriger sur place (couleur charte slate/orange/amber/red) ou documenter précisément pourquoi elle est sémantiquement irréductible (elle ne devrait pas l'être : amber/red/orange/slate couvrent tous les états).
    Lancer `npm run build` (ou `nuxt build`) — doit compiler sans erreur.
    Audit accessibilité manuel léger : vérifier que `prefers-reduced-motion` neutralise les hover/reveal (présent dans tailwind.css + composants badge) ; vérifier la présence de focus visibles (ring orange) sur inputs/boutons ; échantillonner le contraste des combos clés (slate-900 sur slate-50, slate-500 sur blanc, blanc sur safety #F97316) ≥ 4.5:1 pour le texte.
    Audit responsive : `npx playwright test` si des e2e existent (auth mockée via helper gotoLeads — voir CLAUDE.md) ; sinon vérifier via inspection que les grilles bento passent en 1 colonne < md et qu'aucune largeur fixe ne dépasse le viewport (pas de scroll horizontal) aux points 375/768/1024/1440.
    Consigner les résultats dans le SUMMARY.
  </action>
  <verify>
    <automated>grep -rcE "cyan-|green-|emerald-|#22C55E|#0891B2|#22D3EE|#ECFEFF|#164E63" app/ | grep -v ':0$' | grep -q . && echo FAIL || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rnE "cyan-|green-|emerald-|#22C55E|#0891B2|#22D3EE|#ECFEFF|#164E63" app/` ne renvoie aucune ligne
    - `npm run build` se termine sans erreur
    - Le SUMMARY documente : résultat du gate, statut build, combos de contraste échantillonnés, statut reduced-motion, statut responsive
  </acceptance_criteria>
  <done>Gate transversal vert (0 occurrence), build OK, audit a11y/responsive consigné.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Tâche 2 : Checkpoint — validation visuelle humaine du changement drastique</name>
  <action>Présenter la refonte à l'utilisateur pour validation visuelle (voir how-to-verify) et attendre le signal de reprise.</action>
  <what-built>
    Refonte UI drastique complète : palette gris industriel + orange sécurité, typographie massive DM Sans, hero Bento Grid Variant C, cartes rounded-3xl avec micro-interactions, sur l'ensemble des pages (landing, dashboard pro, leads, formulaires, premium, profil public, admin, légal) et composants (badges, breadcrumbs, sliders). Doublons morts supprimés. Zéro vert/cyan.
  </what-built>
  <how-to-verify>
    1. Lancer un seul serveur dev : `npm run dev` (si doublon `:3000`, `pkill -f "nuxt dev"` puis relancer).
    2. Ouvrir `http://localhost:3000/` — vérifier le hero Bento 12 colonnes, la typo massive, le fond slate-50, les CTA orange rounded-full. Le rendu doit être IMMÉDIATEMENT et visiblement différent de la v1 (plus aucun cyan/vert).
    3. Parcourir : `/simulateur`, `/pro/claim`, `/espace/profil`, `/espace/leads`, `/espace/premium`, un profil public `/pro/{dept}/{slug}-{id}`, `/admin`, une page `/legal/*`.
    4. Sur chaque page : confirmer cartes rounded-3xl, boutons orange, badges gris+orange, aucun résidu cyan/vert, hover d'élévation fluide.
    5. Tester `prefers-reduced-motion` (réglages OS) : les animations doivent se désactiver.
    6. Redimensionner (mobile/desktop) : aucun scroll horizontal.
  </how-to-verify>
  <acceptance_criteria>
    - L'utilisateur confirme que le changement est drastique et visiblement différent de la v1
    - Aucun résidu visuel cyan/vert signalé
    - Aucun défaut bloquant de layout/responsive signalé
  </acceptance_criteria>
  <resume-signal>Taper "approuvé" pour clôturer la phase 04.7, ou décrire les défauts à corriger.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Auditeur → application complète | vérification transversale, aucune donnée modifiée |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-047-08-A | Denial of Service (a11y) | animations globales | mitigate | Gate reduced-motion vérifié explicitement dans l'audit |
| T-047-08-B | Information Disclosure | rendus conditionnels (masquage/verified) | accept | Aucune modification de logique dans ce plan ; vérification visuelle seule |
| T-047-SC | Tampering | npm installs | mitigate | Aucune dépendance ajoutée |
</threat_model>

<verification>
- `grep -rnE "cyan-|green-|emerald-|#22C55E|#0891B2|#22D3EE|#ECFEFF|#164E63" app/` == vide
- `npm run build` compile
- Approbation humaine obtenue
</verification>

<success_criteria>
Critère transversal zéro vert/cyan tenu sur tout app/, accessibilité et responsive validés, changement drastique approuvé par l'utilisateur.
</success_criteria>

<output>
Créer `.planning/phases/04.7-UI-Refonte/04.7-08-SUMMARY.md` à la fin.
</output>
