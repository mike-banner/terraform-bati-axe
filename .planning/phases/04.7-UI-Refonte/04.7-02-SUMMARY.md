---
phase: 04.7-UI-Refonte
plan: 02
subsystem: frontend-landing
tags: [ui, bento-grid, tailwind, landing]
dependency-graph:
  requires: ["04.7-01"]
  provides: ["hero-bento-grid-variant-c"]
  affects: ["app/pages/index.vue", "app/components/BeforeAfterSlider.vue"]
tech-stack:
  added: []
  patterns: ["bento-card + reveal-item mutualisés depuis app/assets/css/tailwind.css (plan 01)"]
key-files:
  created: []
  modified:
    - app/pages/index.vue
    - app/components/BeforeAfterSlider.vue
decisions:
  - "Métrique carte accent = donnée réelle existante (2 min de mise en relation), pas de fausse métrique SaaS inventée"
  - "Suppression du <style scoped> reveal-item du hero au profit du .reveal-item/.bento-card mutualisé du plan 01 (évite la duplication d'animation)"
metrics:
  duration: "~35 min"
  completed: "2026-07-03"
---

# Phase 04.7 Plan 02 : Landing Bento Grid Variant C Summary

Hero de la landing reconstruit en grille Bento 12 colonnes asymétrique (variante C du sketch 001) avec typographie massive et cartes rounded-3xl ; sections restantes et BeforeAfterSlider re-thémés gris/orange, zéro cyan/vert.

## Ce qui a été fait

**Tâche 1 — Hero Bento Grid Variant C** (`app/pages/index.vue`)
- Remplacement du hero split 2 colonnes par `grid grid-cols-1 md:grid-cols-12 gap-6` dans `.reveal`
- Carte intro (`md:col-span-8`) : titre `text-5xl md:text-7xl font-black tracking-tight`, sous-texte `text-slate-500`, CTA principal orange `rounded-full shadow-safety/20 hover:scale-105` vers `/simulateur`, CTA secondaire vers `/pro/claim`
- Carte accent (`md:col-span-4 bg-industrial`) : métrique réelle « 2 min » (mise en relation), reprise du copy existant, pas de métrique inventée
- Carte bénéfices (`md:col-span-4 bg-slate-900`) : pills des critères de contrôle (décennale, Kbis, zone)
- Carte visuelle (`md:col-span-8`) : photo chantier existante (`heroPhoto`), badge « Pro vérifié à la main » en `bg-safety rounded-full`
- Suppression du `<style scoped>` dupliquant `.reveal-item` (déjà défini globalement en plan 01) au profit de la classe mutualisée

**Tâche 2 — Sections restantes + BeforeAfterSlider**
- Bandeau manifeste : `bg-slate-900`, titre `font-black tracking-tight`
- « Comment ça marche » : 3 cartes `bento-card rounded-3xl border-slate-200` avec `.reveal`/`.reveal-item`
- Section vérification : liste transformée en cartes `bento-card rounded-3xl`, puce `bg-safety`
- CTA final : bouton `rounded-full bg-safety shadow-safety/20 hover:scale-105`
- `BeforeAfterSlider.vue` : conteneur `rounded-3xl border border-slate-200` (au lieu de `rounded-xl`), poignée de drag `bg-safety` avec icône blanche (au lieu de `bg-white`/`text-neutral-700`) ; handlers `@mouseenter/@mousemove/@mouseleave/@touchmove` et refs de position (`containerRef`, `split`, `isHovering`) strictement inchangés

Aucune logique métier, texte légal, lien de destination ou script (`useAsyncData`/fetch) modifié — restyle de template uniquement, conformément à l'objectif du plan.

## Vérification

- `grep -cE "cyan-|green-|emerald-|#22C55E|#0891B2|#22D3EE|#ECFEFF|#164E63" app/pages/index.vue app/components/BeforeAfterSlider.vue` → `0` pour les deux fichiers
- `md:grid-cols-12`, `bento-card` (5 occurrences), `rounded-3xl` (4 occurrences dans index.vue), `hover:scale-105`, `shadow-safety/20` présents dans `index.vue`
- `BeforeAfterSlider.vue` contient `rounded-3xl` et conserve ses 4 handlers de drag
- `npm run build` compile sans erreur

## Deviations from Plan

None - plan exécuté tel quel.

## Self-Check: PASSED

- FOUND: app/pages/index.vue
- FOUND: app/components/BeforeAfterSlider.vue
- FOUND: commit e7c27eb (hero Bento Grid Variant C)
- FOUND: commit a64cf9e (sections restantes + slider)
