---
phase: 04.7-UI-Refonte
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/assets/css/tailwind.css
autonomous: true
requirements: [UI-MASTER-01]
must_haves:
  truths:
    - "Le fond de page est slate-50 (#F8FAFC) et non plus cyan clair"
    - "Les boutons primaires (token cta) sont orange sécurité (#F97316) et non plus vert"
    - "La police de titre est DM Sans (poids 900 dispo) et non plus Figtree"
    - "Une classe .bento-card fournit l'élévation au survol avec respect de prefers-reduced-motion"
    - "Aucune occurrence de cyan/green/emerald ni des hex cyan/vert dans tailwind.css"
  artifacts:
    - path: "app/assets/css/tailwind.css"
      provides: "Tokens de couleur gris industriel + orange, fonts DM Sans, utilitaires bento/reveal"
      contains: "--color-cta: #F97316"
  key_links:
    - from: "app/assets/css/tailwind.css"
      to: "toutes les pages consommant bg-cta/text-primary/bg-page/text-text"
      via: "cascade @theme inline + :root"
      pattern: "--color-(cta|primary|page|text)"
---

<objective>
Refondre la fondation CSS : remplacer intégralement la palette cyan/vert par le gris industriel (#64748B) + orange sécurité (#F97316), basculer la typographie sur DM Sans, et ajouter les utilitaires bento (.bento-card) + reveal stagger. Ce plan est le levier : la redéfinition des 6 tokens sémantiques recolore ~10 pages sans les toucher.

Purpose: Établir la source de vérité couleur/typo/interaction pour toute la vague 2.
Output: `app/assets/css/tailwind.css` re-thémé, grep vert/cyan = 0 sur ce fichier.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.7-UI-Refonte/04.7-UI-SPEC.md
@.planning/phases/04.7-UI-Refonte/04.7-PATTERNS.md
@.agent/skills/sketch-findings-bati-axe/sources/themes/default.css
</context>

<tasks>

<task type="auto">
  <name>Tâche 1 : Remplacer tous les tokens de couleur cyan/vert par gris industriel + orange</name>
  <read_first>
    - app/assets/css/tailwind.css (fichier modifié — lire en entier, tokens dupliqués dans @theme inline lignes 8-87, :root lignes 89-153, .dark lignes 155-195)
    - .planning/phases/04.7-UI-Refonte/04.7-UI-SPEC.md (section 3 palette)
    - .planning/phases/04.7-UI-Refonte/04.7-PATTERNS.md (finding n°1)
  </read_first>
  <action>
    Dans les trois blocs (`@theme inline`, `:root`, `.dark`), remplacer chaque valeur cyan/vert par la charte :
    `--color-primary` `#0891B2`→`#64748B` ; `--color-secondary` `#22D3EE`→`#94A3B8` ; `--color-cta` `#22C55E`→`#F97316` ; `--color-page` `#ECFEFF`→`#F8FAFC` ; `--color-text` `#164E63`→`#0F172A` ; `--color-foreground`/`--foreground`/`--card-foreground`/`--popover-foreground`/`--sidebar-foreground` `#164E63`→`#0F172A` ; `--color-ring`/`--ring` `#0891B220`→`#F9731633`.
    Ajouter deux tokens nommés dans `@theme inline` pour activer `bg-industrial`/`bg-safety`/`text-safety` en Tailwind v4 : `--color-industrial: #64748B;` et `--color-safety: #F97316;`.
    Neutraliser les couleurs shadcn dérivées : `--accent`/`--color-accent` `#22C55E`→`#F97316` ; charts (`--chart-1..5` et `--color-chart-1..5`) et sidebar (`--sidebar*`, `--color-sidebar*`) : remplacer toute valeur `#0891B2`/`#22C55E`/`#22D3EE`/`#ECFEFF`/`#164E63` par des valeurs slate/industrial/safety (`#64748B`, `#94A3B8`, `#F97316`, `#F8FAFC`, `#0F172A`, `#E2E8F0`). `--color-muted-foreground`/`--muted-foreground` restent `#64748B` (déjà conforme).
    Bloc `.dark` : `--color-primary`/`--primary` `#22D3EE`→`#94A3B8` ; `--color-secondary`/`--secondary` `#0891B2`→`#64748B` ; `--accent`/`--sidebar-accent`/chart cyan/vert → `#F97316`/slate. `--destructive` (`#DC2626`/`#EF4444`) et `--color-border-error` inchangés.
    Objectif dur : après édition, `grep -nE "cyan-|green-|emerald-|#22C55E|#0891B2|#22D3EE|#ECFEFF|#164E63" app/assets/css/tailwind.css` ne renvoie plus AUCUNE ligne (hors commentaires — nettoyer aussi les commentaires mentionnant "cyan"/"green"/"vert" pour éviter toute confusion en aval).
    Ne PAS toucher aux tokens de spacing, shadow, radius, border, disabled dans cette tâche.
  </action>
  <verify>
    <automated>grep -cE "cyan-|green-|emerald-|#22C55E|#0891B2|#22D3EE|#ECFEFF|#164E63" app/assets/css/tailwind.css | grep -qx 0 && grep -q -- "--color-cta: #F97316" app/assets/css/tailwind.css && grep -q -- "--color-safety: #F97316" app/assets/css/tailwind.css && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "cyan-|green-|emerald-|#22C55E|#0891B2|#22D3EE|#ECFEFF|#164E63" app/assets/css/tailwind.css` renvoie `0`
    - Le fichier contient littéralement `--color-cta: #F97316`, `--color-page: #F8FAFC`, `--color-industrial: #64748B`, `--color-safety: #F97316`
    - Les trois blocs (@theme inline, :root, .dark) restent structurellement présents (aucun bloc supprimé)
  </acceptance_criteria>
  <done>Palette re-thémée gris+orange, grep vert/cyan = 0 sur le fichier CSS.</done>
</task>

<task type="auto">
  <name>Tâche 2 : Basculer les fonts sur DM Sans et ajouter les utilitaires bento + reveal</name>
  <read_first>
    - app/assets/css/tailwind.css (lignes 4 import Google Fonts, 9-10 font tokens, @layer components 217-313)
    - .agent/skills/sketch-findings-bati-axe/sources/themes/default.css (keyframes slideUpFade, transitions)
    - .agent/skills/sketch-findings-bati-axe/references/landing-hero.md (pattern .bento-card)
    - .planning/phases/04.7-UI-Refonte/04.7-PATTERNS.md (bloc "Bento card hover", squelette .reveal-item)
  </read_first>
  <action>
    Remplacer l'import Google Fonts (ligne 4) par DM Sans uniquement : `@import url('https://fonts.googleapis.com/css2?family=DM+Sans:opsz,wght@9..40,400;9..40,500;9..40,700;9..40,900&display=swap');`. Régler `--font-sans: 'DM Sans', system-ui, sans-serif;` et `--font-heading: 'DM Sans', system-ui, sans-serif;` (ponytail : DM Sans pour titres ET corps — c'est la police réellement retenue par le sketch gagnant ; Satoshi n'est pas sur Google Fonts et son self-hosting est écarté, YAGNI).
    Dans `@layer components`, ajouter la classe `.bento-card` : `transition: transform 0.3s cubic-bezier(0.16,1,0.3,1), box-shadow 0.3s ease;` et `.bento-card:hover { transform: translateY(-5px); box-shadow: 0 20px 40px -15px rgba(0,0,0,0.1); }`.
    Ajouter les utilitaires de reveal stagger : keyframes `@keyframes reveal-up { from { opacity:0; transform: translateY(16px);} to { opacity:1; transform: translateY(0);} }`, une classe `.reveal-item { animation: reveal-up 0.6s cubic-bezier(0.16,1,0.3,1) both; }` et des délais nth-child (`.reveal .reveal-item:nth-child(n)` de 0.05s à 0.3s, 6 crans) — reprendre le squelette existant d'index.vue décrit dans PATTERNS.
    Ajouter un garde global : `@media (prefers-reduced-motion: reduce) { .bento-card:hover { transform: none; } .reveal-item { animation: none; opacity: 1; } }`.
    Mettre à jour `.btn-primary` (radius `--radius-md`→`9999px` pour `rounded-full`, ajouter `box-shadow: 0 8px 24px -8px rgba(249,115,22,0.35)` au hover) et `.card` (`border-radius: 1.5rem` = rounded-3xl) pour aligner les classes utilitaires legacy sur la charte bento — sans supprimer ces classes (usages secondaires).
  </action>
  <verify>
    <automated>grep -q "family=DM+Sans" app/assets/css/tailwind.css && grep -q "\.bento-card:hover" app/assets/css/tailwind.css && grep -q "prefers-reduced-motion" app/assets/css/tailwind.css && grep -q "reveal-up" app/assets/css/tailwind.css && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Figtree\|Noto Sans" app/assets/css/tailwind.css` renvoie `0`
    - Le fichier contient `family=DM+Sans`, `.bento-card:hover`, `@keyframes reveal-up`, un bloc `@media (prefers-reduced-motion: reduce)`
    - `--font-sans` et `--font-heading` référencent `'DM Sans'`
  </acceptance_criteria>
  <done>DM Sans active, .bento-card + reveal stagger disponibles, reduced-motion respecté.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Navigateur → CDN Google Fonts | ressource tierce chargée au runtime (pas de npm install) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-047-01 | Denial of Service | Import Google Fonts (DM Sans) | accept | Police déjà utilisée dans le sketch validé ; `display=swap` évite le FOIT ; fallback `system-ui` déclaré |
| T-047-02 | Information Disclosure | Aucun rendu conditionnel touché | accept | Ce plan ne modifie que des tokens CSS/typo ; aucun `v-if` ni logique de masquage (ADR-004) n'est altéré |
| T-047-SC | Tampering | npm/pip/cargo installs | mitigate | Aucune dépendance ajoutée (DM Sans via CDN, pas de package) — pas de gate de légitimité requis |
</threat_model>

<verification>
- `grep -cE "cyan-|green-|emerald-|#22C55E|#0891B2|#22D3EE|#ECFEFF|#164E63" app/assets/css/tailwind.css` == 0
- `npm run build` (ou `nuxt build`) compile sans erreur CSS
</verification>

<success_criteria>
Fondation CSS re-thémée : palette gris+orange, DM Sans, utilitaires bento/reveal, reduced-motion. Zéro vert/cyan résiduel dans le fichier CSS.
</success_criteria>

<output>
Créer `.planning/phases/04.7-UI-Refonte/04.7-01-SUMMARY.md` à la fin.
</output>
