---
phase: 04.7-UI-Refonte
plan: 07
type: execute
wave: 2
depends_on: ["04.7-01"]
files_modified:
  - app/pages/admin/index.vue
  - app/pages/legal/cgu.vue
  - app/pages/legal/confidentialite.vue
  - app/pages/legal/mentions-legales.vue
autonomous: true
requirements: [UI-ADMIN-01, UI-LEGAL-01]
must_haves:
  truths:
    - "La console admin est en charte (tables/cartes rounded-3xl, badges statut sans emerald)"
    - "Le bouton de validation admin (ex emerald-600) est orange safety"
    - "Les pages légales adoptent la typo/spacing charte (contenu texte, pas de bento)"
    - "Aucune classe cyan/green/emerald dans les 4 fichiers"
  artifacts:
    - path: "app/pages/admin/index.vue"
      provides: "Console admin re-thémée, actions orange"
      contains: "rounded-3xl"
  key_links:
    - from: "app/pages/admin/index.vue"
      to: "app/assets/css/tailwind.css"
      via: "tokens sémantiques re-thémés"
      pattern: "bg-cta|text-primary"
---

<objective>
Re-thémer la console admin (tables de pros/projets, badges de statut, boutons d'action) et les trois pages légales (typographie/espacement, contenu texte pur). Retirer l'emerald des badges de statut et boutons admin.

Purpose: Cohérence sur les pages back-office et légales (crédibilité B2B).
Output: 4 fichiers re-thémés, zéro vert/cyan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04.7-UI-Refonte/04.7-UI-SPEC.md
@.planning/phases/04.7-UI-Refonte/04.7-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Tâche 1 : Re-thémer la console admin (tables, badges statut, boutons)</name>
  <read_first>
    - app/pages/admin/index.vue (ligne 327 badge is_verified emerald/amber, ligne 358 bouton validation bg-emerald-600, ligne 511 badge projet qualified emerald/amber)
    - .planning/phases/04.7-UI-Refonte/04.7-UI-SPEC.md (sections 4, 6)
    - CLAUDE.md (accès admin via ADMIN_EMAILS ; validation docs — ne pas toucher la logique)
  </read_first>
  <action>
    Cartes/panneaux et tableaux dans conteneurs `bg-white rounded-3xl border border-slate-200 shadow-sm` ; en-têtes `text-slate-900 font-bold` échelle agrandie ; lignes de table `border-slate-100`.
    Badges de statut : remplacer les variantes emerald (lignes 327, 511) `border-emerald-400 text-emerald-700 bg-emerald-50` par un ton "validé" neutre `border-slate-300 text-slate-700 bg-slate-100` (ou accent orange) ; conserver `amber` pour l'état en attente.
    Bouton de validation (ligne 358) `bg-emerald-600 hover:bg-emerald-700` → `bg-safety hover:bg-orange-600` (ou `bg-[#F97316] hover:bg-[#EA6A0C]`), `rounded-full`. Boutons secondaires `bg-white border-slate-200 rounded-full`.
    Ne PAS toucher à la logique de validation/rejet, aux appels `admin/verify.post.ts` côté client, ni au check `ADMIN_EMAILS`. Restyle template/classes uniquement.
  </action>
  <verify>
    <automated>grep -cE "cyan-|green-|emerald-|#22C55E|#0891B2|#22D3EE|#ECFEFF|#164E63" app/pages/admin/index.vue | grep -qx 0 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "..."` (motif complet) sur admin/index.vue renvoie `0`
    - Le bouton de validation utilise `bg-safety` ou `bg-[#F97316]` (plus de `bg-emerald`)
    - Aucun handler de validation/rejet supprimé (diff script cosmétique ou néant)
  </acceptance_criteria>
  <done>Console admin en charte, actions orange, badges sans emerald.</done>
</task>

<task type="auto">
  <name>Tâche 2 : Re-thémer les trois pages légales (typo/spacing)</name>
  <read_first>
    - app/pages/legal/cgu.vue
    - app/pages/legal/confidentialite.vue
    - app/pages/legal/mentions-legales.vue
    - .planning/phases/04.7-UI-Refonte/04.7-UI-SPEC.md (section 2 typographie)
  </read_first>
  <action>
    Contenu texte pur (aucun bento/CTA) : conteneur lisible `max-w-3xl mx-auto px-6 py-16` ; titre de page `text-4xl md:text-5xl font-bold tracking-tight text-slate-900` ; titres de section `text-2xl font-bold text-slate-900 mt-10` ; corps `text-slate-600 leading-relaxed` ; liens `text-safety hover:underline`. Aucune couleur cyan/vert. Ne modifier AUCUN texte juridique (contenu légal immuable) — seulement les classes de style.
  </action>
  <verify>
    <automated>grep -cE "cyan-|green-|emerald-|#22C55E|#0891B2|#22D3EE|#ECFEFF|#164E63" app/pages/legal/cgu.vue app/pages/legal/confidentialite.vue app/pages/legal/mentions-legales.vue | grep -v ':0$' | grep -q . && echo FAIL || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "..."` (motif complet) sur les 3 pages légales renvoie `0` pour chacune
    - Chaque page a un titre `text-4xl` ou `text-5xl` `font-bold`
    - Le texte juridique (contenu) est inchangé (diff limité aux attributs `class`)
  </acceptance_criteria>
  <done>Pages légales en typo/spacing charte, contenu juridique intact, zéro vert/cyan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin authentifié (ADMIN_EMAILS) → console de validation | actions privilégiées sur documents pros |
| Visiteur anonyme → pages légales | contenu statique |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-047-07-A | Elevation of Privilege | logique de validation admin | mitigate | Restyle template seul ; check ADMIN_EMAILS et appels verify.post non touchés |
| T-047-07-B | Repudiation | contenu juridique légal | mitigate | Aucune modification du texte légal (seulement classes CSS) — conformité LCEN/RGPD préservée |
| T-047-07-C | Denial of Service (a11y) | contraste texte légal | mitigate | Corps `text-slate-600` sur blanc ≥ 4.5:1 ; liens orange contrastés |
| T-047-SC | Tampering | npm installs | mitigate | Aucune dépendance ajoutée |
</threat_model>

<verification>
- `grep -cE "cyan-|green-|emerald-|#22C55E|#0891B2|#22D3EE|#ECFEFF|#164E63" app/pages/admin/index.vue app/pages/legal/*.vue` == 0
- `npm run build` compile
</verification>

<success_criteria>
Console admin et pages légales en charte, actions orange, badges sans emerald, contenu juridique intact, zéro vert/cyan.
</success_criteria>

<output>
Créer `.planning/phases/04.7-UI-Refonte/04.7-07-SUMMARY.md` à la fin.
</output>
