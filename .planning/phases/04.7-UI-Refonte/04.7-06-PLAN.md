---
phase: 04.7-UI-Refonte
plan: 06
type: execute
wave: 2
depends_on: ["04.7-01"]
files_modified:
  - app/pages/espace/premium.vue
  - app/pages/pro/[dept]/[slug].vue
  - app/pages/mon-projet/[token].vue
autonomous: true
requirements: [UI-PREMIUM-01, UI-PROFILE-PUBLIC-01]
must_haves:
  truths:
    - "La page premium a un hero exclusivité + cartes de pricing bento rounded-3xl + CTA orange"
    - "Le profil public pro est en charte (badges de confiance gris+orange, sections rounded-3xl)"
    - "L'espace particulier tokenisé (mon-projet) est en charte"
    - "Le badge de statut du profil public n'utilise plus emerald (verified=slate/orange, pending=amber)"
    - "Aucune classe cyan/green/emerald dans les 3 fichiers"
  artifacts:
    - path: "app/pages/espace/premium.vue"
      provides: "Page pricing en cartes bento CTA orange"
      contains: "rounded-3xl"
    - path: "app/pages/pro/[dept]/[slug].vue"
      provides: "Profil public re-thémé, badge verified sans emerald"
  key_links:
    - from: "app/pages/pro/[dept]/[slug].vue"
      to: "app/components/ui/BadgeEntrepriseVerifiee.vue"
      via: "rendu conditionnel du badge SIRET (restylé en plan 03)"
      pattern: "BadgeEntrepriseVerifiee|is_verified"
---

<objective>
Re-thémer la page d'abonnement premium (hero exclusivité, pricing, ROI, FAQ), le profil public pro et l'espace particulier tokenisé sur la charte bento. Retirer l'emerald du badge de statut de vérification du profil public (verified → slate/orange, pending → amber).

Purpose: Ces pages sont la vitrine de conversion (premium) et de confiance (profil public).
Output: 3 fichiers re-thémés, zéro vert/cyan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04.7-UI-Refonte/04.7-UI-SPEC.md
@.planning/phases/04.7-UI-Refonte/04.7-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Tâche 1 : Re-thémer la page premium (hero, pricing, ROI, FAQ)</name>
  <read_first>
    - app/pages/espace/premium.vue (hero exclusivité, toggle mensuel/annuel, pricing cards, bloc ROI, FAQ)
    - .planning/phases/04.7-UI-Refonte/04.7-UI-SPEC.md (sections 4, 5, 6)
    - .agent/skills/sketch-findings-bati-axe/references/landing-hero.md (bento cards, CTA orange)
  </read_first>
  <action>
    Hero : titre massif `text-5xl md:text-7xl font-black tracking-tight text-slate-900` ; carte(s) de pricing en `bg-white rounded-3xl p-8 md:p-10 border border-slate-200 shadow-sm` + `.bento-card`, carte "recommandée" mise en avant par `border-safety` + badge orange (pas green) ; toggle mensuel/annuel en pills `rounded-full` avec actif `bg-safety text-white` ; CTA free trial / abonnement `bg-safety text-white rounded-full px-8 py-4 hover:scale-105 shadow-safety/20` ; bloc ROI chiffré en carte bento contrastée (`bg-industrial text-white` ou `bg-slate-900`) ; FAQ items en `rounded-2xl border-slate-200`.
    Ne PAS toucher à la logique Stripe (redirections checkout, toggle trial), ni à `useRequireAuth`. Restyle uniquement.
  </action>
  <verify>
    <automated>grep -cE "cyan-|green-|emerald-|#22C55E|#0891B2|#22D3EE|#ECFEFF|#164E63" app/pages/espace/premium.vue | grep -qx 0 && grep -q "rounded-3xl" app/pages/espace/premium.vue && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "..."` (motif complet) sur premium.vue renvoie `0`
    - premium.vue contient `rounded-3xl`, `shadow-safety/20`, et une carte pricing mise en avant via `border-safety`
    - Aucune redirection Stripe / handler de toggle supprimé
  </acceptance_criteria>
  <done>Page premium en charte bento, CTA orange, zéro vert/cyan.</done>
</task>

<task type="auto">
  <name>Tâche 2 : Re-thémer le profil public pro et l'espace particulier tokenisé</name>
  <read_first>
    - app/pages/pro/[dept]/[slug].vue (ligne 102 emerald status, ligne 153 emerald/amber conditionnel is_verified — profil public, badges de confiance)
    - app/pages/mon-projet/[token].vue (espace particulier tokenisé — statut projet, pros consultants)
    - .planning/phases/04.7-UI-Refonte/04.7-PATTERNS.md (badge pattern)
    - CLAUDE.md (profil non vérifié ≠ 404 : is_verified:false rendu, pas d'erreur)
  </read_first>
  <action>
    [slug].vue : sections en `rounded-3xl p-8 border border-slate-200 shadow-sm` ; en-tête profil massif `text-4xl md:text-5xl font-bold text-slate-900`. Remplacer le badge de statut emerald (lignes 102, 153) : état vérifié → `text-slate-700 bg-slate-100 border-slate-300` (ou l'accent orange `text-[#F97316] border-[#F97316]/30 bg-[#F97316]/5`), état non vérifié → conserver `amber` (`text-amber-700 bg-amber-50 border-amber-200`). Les composants BadgeEntrepriseVerifiee/BadgeDecennaleCertifiee (restylés en plan 03) sont rendus tels quels — ne pas les éditer ici. Conserver strictement le rendu `is_verified:false` (jamais de 404 sur profil existant — invariant CLAUDE.md).
    mon-projet/[token].vue : cartes de statut/pros en `rounded-3xl border-slate-200`, boutons `rounded-full`, échelle typo agrandie, zéro cyan/vert. Logique de résolution du token et affichage inchangés.
  </action>
  <verify>
    <automated>grep -cE "cyan-|green-|emerald-|#22C55E|#0891B2|#22D3EE|#ECFEFF|#164E63" app/pages/pro/[dept]/[slug].vue app/pages/mon-projet/[token].vue | grep -v ':0$' | grep -q . && echo FAIL || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "..."` (motif complet) sur [slug].vue et [token].vue renvoie `0` pour chacun
    - [slug].vue conserve la branche `is_verified === false` rendant le profil (grep `is_verified` toujours présent) — aucun `throw`/404 ajouté sur ce chemin
    - Les deux fichiers contiennent `rounded-3xl`
  </acceptance_criteria>
  <done>Profil public et espace particulier en charte, badge statut sans emerald, invariant 404 respecté.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Visiteur anonyme → profil public pro | données pro publiques (verified/non-verified) |
| Particulier → mon-projet/[token] | accès tokenisé sans compte (magic-link) |
| Pro authentifié → premium (Stripe) | flux d'abonnement |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-047-06-A | Information Disclosure | rendu is_verified sur profil public | mitigate | Restyle seul ; branche `is_verified:false` conservée (invariant ≠404) ; aucun champ sensible nouvellement exposé |
| T-047-06-B | Spoofing | badges de confiance (SIRET/décennale) | mitigate | Conditions d'affichage des badges inchangées ; restyle en plan 03, rendu conditionnel non altéré ici |
| T-047-06-C | Tampering | logique Stripe checkout | mitigate | Restyle template seul ; redirections/handlers non touchés |
| T-047-06-D | Denial of Service (a11y) | hover cartes pricing/profil | mitigate | prefers-reduced-motion géré (plan 01) |
| T-047-SC | Tampering | npm installs | mitigate | Aucune dépendance ajoutée |
</threat_model>

<verification>
- `grep -cE "cyan-|green-|emerald-|#22C55E|#0891B2|#22D3EE|#ECFEFF|#164E63" app/pages/espace/premium.vue app/pages/pro/[dept]/[slug].vue app/pages/mon-projet/[token].vue` == 0
- `npm run build` compile
</verification>

<success_criteria>
Premium, profil public et espace particulier en charte bento, badges de statut sans emerald, invariants (≠404, Stripe, badges conditionnels) intacts, zéro vert/cyan.
</success_criteria>

<output>
Créer `.planning/phases/04.7-UI-Refonte/04.7-06-SUMMARY.md` à la fin.
</output>
