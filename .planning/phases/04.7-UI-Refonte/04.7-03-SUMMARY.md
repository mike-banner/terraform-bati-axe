---
phase: 04.7-UI-Refonte
plan: 03
subsystem: frontend-dashboard-badges
tags: [ui, dashboard, badges, bento, tailwind]
dependency-graph:
  requires: ["04.7-01"]
  provides: ["dashboard-pro-bento", "badges-confiance-gris-orange", "doublons-morts-supprimes"]
  affects:
    - app/pages/app/dashboard.vue
    - app/pages/app/messages.vue
    - app/components/ui/BadgeDecennaleCertifiee.vue
    - app/components/ui/BadgeEntrepriseVerifiee.vue
tech-stack:
  added: []
  patterns: ["tokens sémantiques gris/orange + .bento-card hérités du plan 01"]
key-files:
  created: []
  modified:
    - app/pages/app/dashboard.vue
    - app/pages/app/messages.vue
    - app/components/ui/BadgeDecennaleCertifiee.vue
    - app/components/ui/BadgeEntrepriseVerifiee.vue
  deleted:
    - app/components/PremiumBadge.vue
    - app/components/IdentityBreadcrumbs.vue
decisions:
  - "Correctif de bug : dashboard.vue rendait les doublons racine cyan au lieu des versions ui/ conformes — repointé sur UiPremiumBadge/UiIdentityBreadcrumbs, doublons racine supprimés (seul consommateur confirmé par PATTERNS finding n°2)"
  - "Badge amber 'Vérification en cours' conservé : amber n'est pas interdit, il sert d'état attention/pending sémantique"
  - "Tâche 3 (bento dashboard/messages) terminée par l'orchestrateur en inline après coupure réseau des agents worktree ; tâches 1-2 déjà commitées par l'agent (ea69e54, 5973afc)"
metrics:
  duration: "interrompu (réseau) puis finalisé inline"
  completed: "2026-07-03"
---

# Phase 04.7 Plan 03 : Dashboard Pro & Badges de Confiance Summary

Dashboard pro repointé sur les badges `ui/` conformes (doublons racine morts supprimés), badges SIRET/décennale re-thémés gris+orange, et charte bento appliquée au dashboard et à la page messagerie.

## Ce qui a été fait

**Tâche 1 — Repoint badges + suppression doublons** (commit `ea69e54`)
- `dashboard.vue` : `<PremiumBadge>` → `<UiPremiumBadge>`, `<IdentityBreadcrumbs>` → `<UiIdentityBreadcrumbs>`
- Suppression de `app/components/PremiumBadge.vue` et `app/components/IdentityBreadcrumbs.vue` (doublons morts, seul consommateur = dashboard)

**Tâche 2 — Badges de confiance gris+orange** (commit `5973afc`)
- `ui/BadgeDecennaleCertifiee.vue` : `green-*` → schéma charte (`border-[#64748B]/30`, `bg-[#F8FAFC]`, icône `text-[#F97316]`)
- `ui/BadgeEntrepriseVerifiee.vue` : `cyan-*` → même schéma
- `v-if` de condition d'affichage (`decennal_status==='valid'` / `siret_status==='active'`) conservés à l'identique

**Tâche 3 — Charte bento dashboard + messagerie** (commit `style(04.7-03)`)
- `dashboard.vue` : cartes documents et checklist en `bento-card rounded-3xl` (bg-white, border-slate-200, shadow-sm), titre entreprise `text-4xl font-bold`, boutons d'envoi KBIS/décennale et CTA fallback en `rounded-full bg-safety hover:scale-105 shadow-safety/20`
- `messages.vue` : carte placeholder en `bento-card rounded-3xl`
- Aucun appel API / `useRequireAuth` / `loadProData` / `uploadDoc` supprimé — restyle de classes uniquement

## Vérification

- `grep -cE "cyan-|green-|emerald-|#22C55E|#0891B2|#22D3EE|#ECFEFF|#164E63"` sur dashboard.vue et messages.vue → `0` chacun
- `test ! -f app/components/PremiumBadge.vue` et `test ! -f app/components/IdentityBreadcrumbs.vue` → OK (supprimés)
- dashboard.vue : `rounded-3xl` ×2, `bento-card` ×2, `UiPremiumBadge` + `UiIdentityBreadcrumbs` présents
- 9 occurrences de logique métier préservées (useRequireAuth/loadProData/uploadDoc)
- `npm run build` compile (vérifié au gate post-vague)

## Deviations from Plan

Tâche 3 finalisée en exécution inline par l'orchestrateur : les agents worktree ont été coupés à répétition (tempête réseau ECONNRESET + plafond de session). Les tâches 1 et 2, déjà commitées par l'agent avant coupure, ont été mergées telles quelles.

## Self-Check: PASSED

- FOUND: app/pages/app/dashboard.vue (bento + rounded-3xl ×2)
- FOUND: app/pages/app/messages.vue (bento)
- NOT FOUND (voulu): app/components/PremiumBadge.vue, app/components/IdentityBreadcrumbs.vue
- FOUND: commits ea69e54 (repoint), 5973afc (badges), style(04.7-03) (bento)
